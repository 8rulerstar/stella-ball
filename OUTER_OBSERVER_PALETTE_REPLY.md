# 관측창 밖의 존재 시안 · 팔레트 회신

> 회신일: 2026-08-14
> 대상: 「관측창 밖의 존재 시안」 5턴 산출물 (`boss-art.js`, 인트로 5비트, 보스 5안)
> 결론: **형태·구조·페이즈 설계는 그대로 채택. 팔레트만 스펙 4절 기준으로 되돌려 주세요.**

## 채택한 것

- **보행 행성(3e) 확정** — 이동이 형태에 들어 있어 인트로 통과 연출과 맞는 유일한 안이라는 판단에 동의합니다.
- **페이즈 4단을 다리 손실로 표현** — 체력바를 보지 않아도 진행도가 읽힙니다. 전장 264px에서도 세 축(다리·기울기·동공)이 전부 실루엣에 남는 것을 실기로 확인했습니다.
- **4비트 → 5비트** — 「눈이 멈춘다」 1.4초의 근거가 타당해 그대로 넣었습니다.
- **절차적 픽셀, 래스터 반입 없음** — 프로젝트의 이연 아트 규칙과 정확히 맞습니다. `boss-art.js`를 표현 전용 계층에 그대로 얹었습니다.
- **인트로의 개체 = 보스** — 확정했습니다. 다만 이름은 끝까지 주지 않고 좌표(`관측되지 않은 점`, `∅`)만 씁니다.

## 되돌린 것 — 팔레트

시안은 남보라 6단 + 자홍 이음매입니다. `OUTER_OBSERVER_INTRO_SPEC.md` 4절은 **청록-잿빛 저대비**이고, 살구빛은 보상·CTA 전용이라 존재에 쓰지 않기로 되어 있습니다. 스펙을 유지하기로 결정해, 통합하면서 색만 프로젝트 토큰 기반으로 교체했습니다. **형태 코드는 한 줄도 건드리지 않았습니다.**

| 자리          | 시안 원본             | 현재 적용             | 근거                                |
| ------------- | --------------------- | --------------------- | ----------------------------------- |
| 본체 램프 6단 | `#07050e` … `#3d2d70` | `#070b0d` … `#3f6669` | `--night-0/1/2`, `--void`, `--line` |
| 이음매(P1→P4) | `#a83f88` → `#ef62a6` | `#3f6b6d` → `#74bfc0` | 부서질수록 차갑게 밝아진다          |
| 동공 림       | `#d276b4` / `#ff8fd0` | `#8ba39f` / `#a9c6c2` | `--mist`                            |
| 끊긴 단면     | `#ef62a6` / `#5a1f4c` | `#74bfc0` / `#1d3438` | `--sb-lumi`                         |
| 관측 눈금     | `#4a2a70` / `#8f4a9c` | `#2c4145` / `#4d7f80` | 계측기로 읽히게                     |

원본 값은 `prototypes/boss-art.js` 헤더 주석에 남겨 두었습니다.

## 확인이 필요한 지점

교체 과정에서 **명도 폭을 한 번 넓혔습니다.** 스펙의 「저대비」를 문자 그대로 적용하니 상위 4색이 전부 최암부라 전장 264px에서 실루엣이 배경에 묻혔습니다(히스토그램상 밝은 축 픽셀이 2% 미만). 색조는 그대로 두고 램프 상단 세 단만 올려, 실루엣이 읽히면서도 살구빛 별자리 노드가 여전히 화면에서 가장 밝은 상태를 유지하도록 했습니다.

- 저대비 원칙은 **여백의 배경 존재**를 위해 쓰인 규칙이었습니다. 이제 같은 몸이 **플레이어가 때리는 전장 보스**이기도 하므로, 두 용도의 대비 기준을 분리할지 판단이 필요합니다.
- 현재는 한 램프를 공유하고 인트로 쪽만 `opacity: 0.84`로 눌러 쓰고 있습니다.

## 요청

1. 위 청록-잿빛 매핑을 기준으로 **채도를 한 번 더 검토**해 주세요. 지금 값은 통합을 막지 않으려고 제가 잡은 임시값입니다.
2. 여백용과 전장용의 대비 기준을 **하나로 갈지 둘로 갈지** 의견 주세요.
3. 색·프레임 수는 아직 `ASSET_PLAN.md`에 확정값으로 기록하지 않았습니다. 회신 뒤에 기록합니다.

## 참고

- 미구현으로 남긴 것: 6절의 「5점 이상 별자리 현현 반응」, 「월드 첫 진입·첫 보스 처치 반응」, 8-1 클리어 엔딩 화면.
- 보류: 모바일에는 양옆 여백이 없어 인트로가 성립하지 않습니다. 첫 실행 서사가 플랫폼마다 갈리는 문제로, 아직 결정하지 않았습니다.
