# Stella Ball — UI 리디자인 인수인계 (Ink & Brass v2)

작성: 2026-08-12. 테마의 단일 기준 문서다.

## 적용 상태

- `prototypes/stella-ball-theme.css`: 게임 스타일시트 뒤에 로드되는 최종 테마 패스.
- `prototypes/prism-breakers.html`: 위 테마를 정식으로 로드한다.
- 전투 물리·밸런스·캔버스 판정색은 변경하지 않는다.
- 다운로드 패키지의 `prism-breakers-themed.html`은 원본과 구조가 달라 채택하지 않았다. 중복 실행본을 만들지 않는 것이 현재 모듈 구조에 안전하다.

## 방향

관측소 밤 세계관과 Galmuri11, 기존 픽셀 에셋을 유지한다. AI풍 네이비·네온 골드 대신 잉크빛 차콜 밤과 낡은 황동·양피지로 바꾸며, 골드는 화면의 주 행동·보상·별빛 사건에만 쓴다. 글로우와 채도는 한 단계 낮춘다.

## 팔레트

- `night-0 #0a0a11`, `night-1 #14141f`, `night-2 #1e1e2c`, `void #2c2740`
- 본문 `moon #c3c9d6`, 보조 `mist #9298ac`, 선 `line #494c63`
- 강조 `star #e6c684`, 황동 `gold #b08d55`
- CTA: `#d3b072 → #8f6537`, 테두리 `#d9bc82`, 글자 `#241d10`
- 하드 픽셀 섀도: `0 3px 0 #050508`

## 별지기 UI 색

캔버스의 `heroes[].col` 원색은 판정과 피드백을 위해 유지한다. UI 표면만 다음 톤 매칭색을 사용한다.

- 가온 `#d9b269`, 비연 `#cf7183`, 루미 `#74bfc0`, 하루 `#9cc878`
- 세라 `#a99bd6`, 태오 `#d99a66`, 닉스 `#8f7fd0`, 리아 `#bca6d8`

## 컴포넌트 규칙

- 아이콘은 단독 스프라이트 대신 어두운 플레이트 위에 두고 `saturate(.72) hue-rotate(-6deg) brightness(1.04)` 필터를 사용한다.
- 비활성·잠금 아이콘은 `grayscale(.75) brightness(.62)`를 쓴다.
- 선택된 로스터 카드는 황동 이중선과 미세한 인셋 글로우, 비선택 카드는 채도·밝기를 낮춘다.
- 파티 칩은 유닛 색 좌측 레일을 유지한다.
- HUD·허브·지도·결과·설정은 2단계 잉크 표면과 저채도 황동 테두리를 공유한다.

## 다음 단계

테마는 CSS로 가능한 범위를 적용했다. 로스터 상단 색 스트립, 전장 토큰의 스킬 뱃지·네임 필, 64×64 초상화와 쿨다운 아이콘은 `showRoster()` 및 캔버스 렌더 마크업 변경이 필요한 후속 작업이다. 구현 전 이 문서의 팔레트와 색 이원화 원칙을 유지한다.
