# DEVLOG — Prism Breakers

> 하루를 마칠 때 5~10분만 기록한다. 좋은 결과뿐 아니라 **막힌 지점, 버린 가설, 사람이 개입한 판단**을 남긴다.

## 작성 형식

```md
## YYYY-MM-DD — 짧은 제목

- Codex에게 시킨 일:
- 결과:
- 막힌 점 / 우회:
- 사람이 직접 결정·수정한 점:
- 증거: 커밋, 파일, 스크린샷, 봇 결과 링크
```

## 2026-08-18 — 조준 축 검증, 그리고 계기가 아니라 게임 쪽 문제

- 요청: 「이제 뭐 해야 할까」에 대해 밸런스를 제안했고, 사장님이 그 전에 조준 역전부터 규명하기로 정했다.
- 왜 먼저인가: 0-4절이 체력 재조정을 위해 목표 클리어율을 정해 달라고 남겨 뒀는데, 봇 클리어율을 사람 난이도로 옮기려면 「조준이 좋을수록 잘 깬다」가 성립해야 한다. 조준 사다리가 그 반대를 가리켰다.
- 결과: 세 가설(지형이 뾰족하다 / 정책각이 나쁘다 / 표본이 부족하다)을 세우고 갈랐다. **앞의 둘이 동시에 참이다.** 고정 최선각은 정책각의 2~6배 피해를 내고, 동시에 0.5° 이웃과의 피해 차이가 148~256이다(기준값 500 안팎). 발 수로 분해하니 요동은 둘째 발에서 약 15배로 터진다 — 둘째 발은 첫 발이 다시 배치한 판에 쏘기 때문이다. 측정은 `BOT_REPORT.md` 0-7절.
- 막힌 점 / 우회: 클리어율은 80~89%에서 포화돼 신호가 없었다. `bossHpOverride`로 피해 용량을 재고, 정책각이 잡음 분포의 몇 백분위에 앉는지로 바꾸니 형태가 나왔다. 지형을 직접 그리려고 하니스에 결정적 각도 손잡이 `aimOffset`과 `shots`를 추가했다. 두 손잡이 모두 기본값에서 무해함을 `run-bot.mjs` 재실행 후 `latest-report.json` 전체 대조로 확인했다(`generatedAt` 한 줄만 다름).
- 내가 새로 찾은 줄 알았던 것: σ=0에서 시드가 무력하다는 사실은 **이미 0-3절에 적혀 있었다.** 다만 `latest-report.json`은 그 버킷을 계속 `cases: 210`으로 내보내고 있었다(실제로 서로 다른 판은 35개). 문서에만 있고 데이터에는 없던 셈이라, 생성기가 `distinctRuns`를 함께 내도록 고쳤다.
- 뒤집힌 기록: `run-bot.mjs`에 「이 표가 단조 감소하면 계기가 살아 있는 것이다」라는 주석이 있었다. 시드를 16개로 올리자 단조가 깨졌다(`contact` 80 → 75.9 → 84.6 → 89.1). 원래의 매끄러운 곡선이 시드 2~6개짜리 표본의 착시였으므로, 그 주석을 건강 신호로 쓰지 말라는 내용으로 바꿨다.
- 사람의 판단: 다음 작업으로 조준 역전 규명을 선택.
- 남긴 결정 사항: 요동 폭(1-1의 p10~p90이 4.3배)을 좁힐 것인지가 목표 클리어율보다 먼저다. 값은 정하지 않았다.
- 증거: `BOT_REPORT.md` 0-7절, `bot/runtime-harness.mjs`(`aimOffset`·`shots`), `bot/run-bot.mjs`(`distinctRuns`), `npm run check` 통과.

## 2026-08-18 — 이번 주에 내가 낸 실수

- 왜 남기나: 이 파일의 서식은 "막힌 지점, 버린 가설"을 요구하는데, 그동안 그 칸에는 잘된 작업의 우회로만 적혀 있었다. 08-14~08-18에 내가 **틀린** 것들을 남긴다. 재발을 막는 규칙은 `prototypes/MAINTENANCE.md`의 "Traps this codebase has already fallen into"로 옮겨 적었다.

**프레임이 멈춘다는 걸 알면서 두 번 더 잊었다**

- 승리 판정이 벽시계에, 승리 연출이 프레임 시계에 있어 macOS E2E가 하루 종일 멈춘 건 08-14에 고쳤다(`game-session.js:588`, `afterFeedbackUpdate`). 그래 놓고 인트로 소품 복구를 `requestAnimationFrame`에 걸었다. 숨은 탭에서는 영영 실행되지 않는다. 같은 착각을 원인까지 파고든 직후에 되풀이했다. 동기 실행 + 강제 리플로로 바꿨다.
- 교습 안내는 등장 애니메이션에서 `opacity: 0 → 1`을 돌렸는데 `fill-mode`가 없었다. 실행 중인 애니메이션이 기본 규칙을 덮으므로 프레임이 한 번만 늦어도 0%에 못 박혀 안 보인다. 키프레임에서 opacity를 뺐다.

**서로 이어지지 않은 숫자 두 개 — 사장님이 잡아주셨다**

- 조향 교습 정지를 `ball.y < H - 380`, 즉 판 높이 기준 520px 고정으로 잡았다. 그런데 플레이어에게 가리킨 지정 구역은 275px이었다. 둘을 잇는 것이 아무것도 없어서 어떤 출력으로 쏘든 항로의 48.6~52.5%에서 멈췄다 — "노란 원 안에 들어가기 전에 멈춰버림"이라는 제보 그대로다. 보스 좌표만 바뀌어도 따로 놀 구조였다. 이제 항로 진행률(`TEACH_STEER_ROUTE_FRACTION = 0.62`)에 묶어 한쪽만 고치면 따라온다.
- 패링 정지도 처음엔 거리 기준이었다. 거리는 시간이 아니다. 느린 샷은 같은 거리를 오래 지나므로 도달 전에 창이 닫힐 수 있다. 접촉까지 남은 시간(`reach / closing < 0.18`, 공명 창 0.4초의 절반 이하)으로 바꿨다.

**가르치려고 만든 장치가 길을 막았다**

- 첫 판은 E2E를 교착시켰다. 패링 수업 문구가 이미 "미리 Space를 눌러라"라고 지시하는데, 내 정지가 그 **이미 누른** 입력을 다시 요구했다. 진행할 방법이 없었다. `onboarding.parryQueued`로 막았다.
- 정지가 입력으로만 풀리게 만들어 입력이 없으면 영구히 갇히는 상태도 만들었다. `TEACH_HOLD_GRACE_MS = 9000`으로 조용히 풀어 준다.

**소스를 문법 없이 편집했다**

- 미사용 코드를 지우려고 중괄호 깊이로 함수 범위를 셌다. `skillIcon`은 중괄호 없는 화살표 함수에 여러 줄 삼항이라 셀 중괄호가 없었고, 머리만 잘려 삼항 꼬리가 다음 함수에 붙어 빌드가 깨졌다. `npm run smoke`가 런타임 전체를 이어 붙여 파싱하는 단계에서 잡았다 — 그 게이트가 없었으면 브라우저를 열 때까지 몰랐다.
- 하니스에 정책을 추가하며 템플릿 리터럴 **안쪽** 주석에 백틱을 써서 `runtime-harness.mjs` 파싱을 깨뜨렸다.

**확인하지 않고 단정했다**

- `dataset.ooRestore`의 실제 속성은 `data-oo-restore`다. 카멜케이스 선택자는 아무것도 못 고르고 **조용히** 성공한다. 복구가 0건을 처리하는 동안 나는 됐다고 봤다. 재발 방지로 `outer-observer.js:667`에 주석을 남겼다.
- 보스 아트 팩을 받고 "어느 보스를 어느 세계에 둘지는 아트 결정"이라며 사장님께 넘겼다. 팩의 슬러그가 이미 `aries-*`, `taurus-*`처럼 세계 이름이었다. 물어볼 필요가 없는 걸 물어 왕복을 한 번 버렸다.
- 인트로 스펙의 엔딩 문장이 낡았다고 말했는데 윈도우 쪽에서 이미 갱신돼 있었다. 파일을 열기 전에 단정했다.
- 미참조 파일을 경로 문자열 grep으로 찾으려 한 시도는 결과가 전부 오탐이었다. 런타임이 이름을 조립해 부르는 파일은 그 방식으로 보이지 않는다.

- 사람이 잡아준 것: 보스-세계 배정 방향, 조향 정지 위치 제보, 우분투 CI 과다 조사 중단, 14시 세션 조기 종료.
- 증거: 08-14~08-18 커밋, `npm run smoke` 파싱 단계가 잡은 빌드 파손, `scripts/test-onboarding-e2e.mjs` 교착.

## 2026-08-16 — 문서 최신화와 워크트리 정리

- 사람이 시킨 일: 문서를 최신화하고 쓸모없는 파일을 정리한다.
- 결과: 08-15·08-16 DEVLOG 항목을 커밋 본문에서 복원하고(당시 55커밋에 기록이 0건이었다), `BOT_REPORT`·`PROGRESS_REPORT`·`CODEX_COLLABORATION`의 기준일과 내용을 현재에 맞췄다. 미포맷 상태로 커밋돼 있던 4개 파일을 정리해 `format:check`를 다시 통과시켰다. 인덱스에 빠져 있던 `AGENTS.md`·`CLAUDE.md`·`CROSS_PLATFORM.md`·`FIGURE_ART_SPEC_6_7.md`를 등재했다.
- **막힌 점 / 우회**: 「쓸모없는 파일」을 참조 횟수로 찾으려 했더니 오탐만 나왔다. `build_expanded_roster_animations.py`·`generate_guide_brand_art.py`는 코드에서 아무도 부르지 않지만 **살아 있는 에셋을 만드는 재생성 도구**이고(`luna-portrait.png`, `favicon-32.png` 등), `bot/prism-sim.mjs`도 참조 0건이지만 `PROJECT_CONTEXT`가 「이전 규칙용 근사 봇 결과는 보관만 한다」고 명시한 보존 대상이다. 정적 참조 계산으로는 이 둘을 구분할 수 없다.
- 실제로 나온 것은 **오래된 git 워크트리 두 개, 합계 59MB**였다. `.claude/worktrees/onboarding`(30MB)과 `vfx-polish`(29MB)가 각각 29008d1·7a697e8에 묶여 있었다 — 지금보다 57커밋 이전이다.
- **버릴 뻔한 것**: `vfx-polish`에 main에 한 번도 들어온 적 없는 `VFX_POLISH_SPEC.md`(24KB)가 있었다. 타이틀·소환·패링·별빛 노드 네 건의 요구서이고 §6에 발주자 결정까지 기록돼 있다. 워크트리를 그냥 지웠으면 통째로 사라졌다. main으로 옮기고, 작성 이후 55커밋이 지났으므로 네 건 중 무엇이 처리됐는지 다시 대조해야 한다는 주석을 달았다.
- **버린 가설**: `onboarding` 워크트리에 미커밋 변경 47줄이 있었다. 1단계 수업의 전제를 「거상에게 곧장 보내세요」에서 **「유성은 무기가 아니라 신호다」**로 바꾸는 안이었다 — 거상에 부딪혀도 아무 일이 없고, 잠든 별지기에게 보내 `Space`를 눌러야 공격이 나온다는 것을 첫 수업에서 가르친다. 기준 파일이 1007줄이던 시절의 것이라 지금 1476줄짜리에는 적용되지 않는다. 아이디어 자체는 현재 수업이 「직격! 잘했어요」로 직격을 칭찬하는 것과 방향이 다르므로 기록해 둔다.
- 브랜치 `worktree-onboarding`·`worktree-vfx-polish`는 남겼다. 디렉터리만 지웠으므로 커밋 이력은 그대로다.
- 증거: `npm run check` 통과, `npx prettier --check .` 통과, `git worktree list`에 main만 남음.

## 2026-08-16 — 디자인 세션 §2~§11 반입, 렉의 진짜 원인, 축에 잠긴 샷

- 사람이 시킨 일: 디자인 세션 요구서를 절 단위로 구현하고, 남은 렉 제보와 조작 이물감을 잡는다.
- 결과 (디자인 세션): 월드 팔레트와 아트를 실은 스테이지 지도(§2·§3), 타격의 크기와 방향(§4), 화자 넷을 채널 넷으로 가른 대사 체계(§5, `game-speech.js`), 거상의 포효와 밀어내기(§6-2), 반사판으로 읽히는 벽과 판으로 읽히는 껍질(§6-3·§6-4), 여백을 가로지르는 로켓·UFO·외계인(§7), 거상의 등장과 죽음(§8·§11), 다른 별과 구분되는 안내별(§9), 별 하나를 남기는 인트로(§10)를 반입했다. 각성 연출은 `game-awaken-fx.js`로 분리했다.
- **막힌 점 / 우회 (렉)**: 두 번의 성능 패스가 렉을 못 잡은 이유는 **측정 대상이 대리 지표였기 때문**이다. 이 프로젝트를 보는 미리보기 창은 `document.hidden`이 true라 `requestAnimationFrame`이 아예 안 돌고, 그래서 「돌고 있는 CSS 애니메이션 수」 같은 것을 대신 세고 있었다. `scripts/profile-frames.mjs`가 온보딩 E2E와 같은 방식으로 실제 Chromium을 CDP로 몰아 합성 구조를 재자 원인이 드러났다 — 온보딩 화면의 **합성 레이어 117개**, 그중 대부분이 `#dawn-sky`의 애니메이션 요소마다 하나씩 승격된 것이었다. 별 수를 줄여서는 절대 못 고치는 비용이다(레이어당 비용이지 별당 비용이 아니다). 별밭을 요소 160개에서 정적 캔버스 한 장으로 바꿔 **117 → 44 레이어**, `#dawn-sky` 자식 244 → 13이 됐다. 중간안 둘은 측정 후 버렸다(전체 화면 캔버스 8fps 재도색 9.4→12.3 Mpx, 다섯 장 분할 17.0 Mpx).
- 반짝임은 부분 갱신으로 되찾았다. 130개 중 26개만 자기 4×4 상자를 12fps로 지운다 — 더럽혀지는 면적이 수백 제곱픽셀이라 타일 한둘만 재업로드된다. 레이어 44개·10.76 Mpx로 정적판과 동일하다.
- **막힌 점 / 우회 (축 잠김)**: 수업의 패링 샷은 「수직에 가까운」 정도가 아니라 **정확히 수직**이었다. 발사석 (360, 748)과 미리내 (360, 405)가 같은 x이고 수업이 조준을 그 게이트에 고정하므로 `aim.x`가 정확히 0이고, 하류의 어떤 연산도 그걸 깨지 않는다(드래그는 스칼라 곱, 벽은 부호 반전, 전투 전체에서 `Math.random`은 크리티컬 판정 한 곳뿐). 그래서 매 시도가 바이트 단위로 동일했고 플레이어가 개입할 수 없는 13.7초가 생겼다. 벽 반사에 「궤적이 축에서 0.02 이내일 때만 2도 튼다」를 넣었다 — 한 번 틀면 비율이 0.035가 되어 같은 선에서 다시 발동하지 않는다. 부호는 무작위가 아니라 고정이다: 하니스 봇이 같은 런타임을 돌리며 동일 입력에 동일 출력을 전제하기 때문이다. 측정: 최대 |vx|/speed가 **0.0000 → 0.9314**, 캠페인 스윕 87.1% → 87.9%.
- **막힌 점 / 우회 (베이킹)**: `paintFeedbackAsset`의 `shadowBlur`를 구운 글로우로 바꾸는 시도를 두 번 하고 **둘 다 측정 후 기각**했다. 그림자와 이미지를 한 장으로 구우면 `screen`이 둘에 «각각» 적용돼야 하는데 미리 합성하면 다른 값이 된다(최대 17% 어두워짐). 그림자만 따로 구워 두 번 그리면 합성 순서는 맞지만 블러가 원본 크기로 구워진 뒤 확대돼 옅어진다(10% 어두워짐, 1.36배). 아트 요구서의 불만이 「이펙트가 약하게 읽힌다」인데 어두워지는 최적화는 가져갈 수 없다. 기각 이유를 호출부에 적어 다음 사람이 저녁을 다시 쓰지 않게 했다.
- 살아남은 것은 별빛 노드 후광이다. 같은 측정에서 밝기비 0.997~1.031로 **같은 빛**이고 노드 7개 기준 0.032ms → 0.019ms(1.7배)다. 다만 이 1.7배는 캔버스가 GPU 가속된 이 기계의 정직한 수치이고, 변경을 부른 수치는 따로 있다 — `shadowBlur`를 0으로 강제한 절제 실험에서 3패링 CPU 래스터 프레임이 **46.34ms → 7.77ms**였다. GPU에서는 싸고 Chromium이 소프트웨어로 떨어지면 잔혹한 비용이며, 그게 재현이 안 되는 렉 제보들이 말하는 상황이다.
- 그 외: 조준의 5도 스냅 제거, 샷에 위력 범위 부여, 정산을 대기열이 아니라 예산으로 전환, 인트로를 첫 실행 사건으로 만들고 시작 버튼이 건너뛰지 못하게 막음, 각성을 다시 움직임에 묶고 패링은 별자리에 남김.
- 소리: 50종 팩의 `ui-01`~`ui-05` 다섯이 반입 이후 한 번도 울린 적이 없었다. 메타 UI가 합성 사각파 3종만 쓰고 샘플 경로를 아예 안 탔기 때문이다. 놀고 있던 대체 테이크까지 실제 사건에 배정했고 **새 자산은 만들지 않았다**. 잦은 알림이 기관총이 되지 않도록 큐별 최소 간격과 「직전 소리로부터 조용했을 때만 낸다」 규칙을 뒀다.
- 증거: `npm run check` 통과, 캠페인 스윕 87.9%, `scripts/profile-frames.mjs`·`probe-settle-cost.mjs`·`probe-steer-lesson.mjs`·`probe-sky-guests.mjs`.

## 2026-08-15 — 8-1 엔딩, 수명주기 누수 아홉 건, 난이도 계측기

- 사람이 시킨 일: 디자인 세션의 여백 하늘 레이어를 반입하고, 남은 버그와 성능을 잡는다.
- 결과: `sky-ambience.js`로 여백 하늘 앰비언스를 넣고, **8-1에 엔딩 화면**을 붙였다. 최종 스테이지를 깨면 일반 승리 카드(메달·샷 수·「다른 파티로 더 짧게」)가 떴는데, 다음 스테이지가 있을 때 맞는 말이지 없을 때 할 말이 아니다. `PROJECT_CONTEXT`와 인트로 스펙 둘 다 이걸 미구현으로 적어두고 있었다. 스펙의 금지선은 지켰다 — 존재는 끝까지 이름이 없고 좌표만 남으며, 유일한 따뜻한 색은 되찾은 별자리가 이미 가진 살구빛이고 그걸 「돌아온 별의 수」에 쓴다. 존재 쪽은 청록-잿빛 저대비 그대로다.
- 막힌 점 / 우회: 그 엔딩의 첫 시도는 조용히 덮였다. `win()` 뒤에 도는 스토리 훅이 일반 「별 해방」 카드로 오버레이를 통째로 다시 써버렸다. **렌더된 제목을 되읽어 확인**해서 잡았지, 그렸다고 가정했으면 못 봤다.
- 수명주기 누수 아홉 건을 닫았다(화면을 도중에 떠날 때 다섯, 승리 컷신 중 발사 경로 차단 포함 넷). 수령 가능한 골드를 파괴하고 다시 지급하던 문제와 깨진 세이브 복구도 함께 처리했다.
- 별자리 정산의 두 결함: `figureFieldDamage`에 `battleComplete` 가드가 없어 승리 컷신 중이나 패배 후에도 거상에 피해를 주고 `scheduleWin`까지 부를 수 있었다. 그리고 `grantFigureBoon`이 다음 `afterShotStart`에 걸어두는데, 유성이 남은 정산에서는 `finalizeBilliardShot`이 `startShot`을 동기로 부르므로 그 훅이 이미 몇 초 전에 지나가 있었다 — 2번 샷에서 얻은 표식이 3번이 아니라 **4번 유성에 붙었다**. 이제 기다리는 유성이 있으면 바로 건네고, 다음 공이 정말 없는 마지막 샷 경로에서만 큐에 넣는다.
- **난이도 계측기**: 세 번째 피해 경로를 고치고 나니 캠페인이 89%가 되어 체력 곡선을 다시 깎아야 하는데, **무엇에 맞춰 깎을지 잴 방법이 없었다.** 피해는 보스 체력에서 잘리므로 봇이 깬 모든 스테이지가 「피해량 = 체력」을 보고하고 "이 파티가 얼마나 세게 칠 수 있었나"에는 답이 없다. `runCampaignStage`에 `bossHpOverride`를 넣어 999,999로 두고 5발을 다 쓰면 판의 **역량**이 나온다. 34스테이지에서 체력은 봇 역량 중앙값의 0.22~0.77배였다 — 필요한 것의 1.3~4.5배를 넣고 있다. 1.0을 넘는 곳은 8-1 하나뿐(2.43배)이라 캠페인이 평지 하나에 끝의 절벽 하나로 읽힌다.
- 구조 쪽이 더 쓸모 있다: 역량이 **7스테이지 주기로 반복**된다. `game-data.js`가 레이아웃을 `CAMPAIGN_LAYOUTS[campaignIndex % 7]`로 배정하는데 체력은 120에서 380으로 선형 상승하고, **두 곡선이 서로를 모른다.** 그래서 측정 난이도가 체력 순서를 안 따른다 — 7-1(350)과 5-5(320)가 7-7(380)보다 어렵고, 한 레이아웃을 공유하는 네 스테이지는 역량이 491로 같은데 체력만 265→380으로 오른다. **체력 값은 하나도 바꾸지 않았다**: 목표 난이도를 고르는 건 오너의 판단이고 이 작업은 그 판단을 위한 계측기다. 표와 결정 사항 셋(목표 클리어율, 7레이아웃 주기 유지 여부, 8-1의 2.43배 절벽 유지 여부)은 `BOT_REPORT` 0-4에 있다.
- 죽은 전역 둘(`barriers`·`seeds`)을 지웠다. 아무 데서도 쓰지 않는 상태를 초기화 목록에 넣는 건 죽은 코드를 길게 살려두는 것일 뿐이다.
- 증거: `npm run check` 통과, `BOT_REPORT` 0-4 표, 8-1 엔딩 실기 확인(카드 제목·∅ 좌표·강조색 `rgb(255,210,160)`).

## 2026-08-14 — 승리 판정 시계 이전, 하니스가 못 보던 절반, 성능 기준선

- 사람이 시킨 일: 14시까지 코드 정리·최적화·버그 수정·연출 강화·하니스 검증·검수·테스트 구동·문서 최신화를 계속 진행.
- **버그 1 (종일 미해결)**: macOS 헤드리스에서 `npm run test:onboarding`이 최종 전투에서 멈추던 원인을 잡았다. 계측해보니 승리 타이머가 27.6초에 등록되고 20초가 지나도 발화하지 않았고, 그 상태에서 심어 본 120ms 타임아웃도 1500ms 뒤까지 pending인 반면 `requestAnimationFrame`은 정상이었다(문서 visible·focus 상태). 즉 페이지의 `setTimeout`만 죽어 있었다. 근본 문제는 환경이 아니라 설계였다 — 승리 **연출**은 프레임 시계(`battle.victory.t`)로 흐르는데 **판정**만 벽시계 `setTimeout(2550)`이라 두 시계가 어긋날 수 있었고, 승리 컷 도중 일시정지해도 타이머는 계속 흘렀다. 판정을 `afterFeedbackUpdate` 훅으로 옮겨 같은 시계에 태웠다. 이 맥에서 처음으로 E2E가 통과했다(30.7초).
- **버그 2**: 인트로가 흩뜨린 여백 소품이 타이틀을 떠난 뒤에도 세션 내내 남아 허브·편성·전투가 있지도 않은 사건의 잔해를 이고 다녔다. `teardown()`에서 원복하게 했다. 두 함정을 밟았다 — `dataset.ooRestore`는 실제 속성이 `data-oo-restore`라 카멜케이스 셀렉터가 하나도 안 잡혔고, 원복 마지막 단계를 `requestAnimationFrame`에 미뤘더니 탭이 가려진 채 나가면 영영 돌아오지 않았다. 동기로 바꿨다.
- **하니스 (가장 큰 수확)**: `bot/latest-report.json`이 `d542727` 이후 갱신되지 않은 채였고, 재측정하니 클리어율이 34.4%→17.7%로 반토막이었다. 저체력대는 그대로고 고체력대만 0이 되는 모양이 이상해 파보니 **96건 전부에서 별자리 발동이 0회**였다 — 조준 정책이 한 샷에 접점 2개를 못 넘는데 발동 최소치는 3개다. 하니스가 이 게임의 간판 기믹을 한 번도 재지 않고 있었다. 발사 각도 96개를 훑어 별지기를 가장 많이 스치는 방향을 고르는 `chain` 정책을 넣자 별자리가 실제로 터졌다: `contact` 33.3% / `chain` **91.7%**. 고체력대 붕괴는 게임이 어려워진 게 아니라 피해 예산이 봇이 못 걷는 길로 옮겨간 것이었다.
- **연출**: 8-1은 형태가 진행도를 대신하는데(다리 손실·기울기·동공 확장) 타격감이 P1과 P4에서 같았다. `impact()`의 기존 세 값을 페이즈당 12%씩 키웠다(P4에서 +36%). 페이즈 없는 보스는 수치가 그대로라 34스테이지에 회귀가 없다.
- **검수**: 프로젝트 검수 스킬로 돌렸더니 FAIL. 지적 4건이 **전부 오늘 내가 만든 문서-코드 불일치**였다 — 보스 체력 상한 380 서술, "훈련장만 4인 파티", "7월드 34개", 그리고 가장 큰 것으로 **8-1이 `PROJECT_CONTEXT`에 한 줄도 없었다**(grep의 `8-1` 매치는 전부 날짜 오탐이었다). 전부 반영했다.
- **성능**: 최적화에 앞서 재봤다. 게임플레이 갱신 19µs, `draw()` 141µs(1-1)·100µs(8-1), 합쳐 프레임 예산의 약 1%다. **최적화할 문제가 없어서 하지 않았고**, 대신 기준선을 `PROJECT_CONTEXT`에 남겼다. 8-1이 일반 스테이지보다 싼 것은 절차적 보스를 페이즈마다 한 번만 굽고 blit하기 때문으로, 캐시 설계가 의도대로 동작한다는 증거다.
- 증거: `npm run check` 전 구간 통과(E2E 30.7초 포함), `bot/latest-report.json` 3정책 144건, 커밋 7건.

## 2026-08-14 — 관측창 밖의 존재를 최종 보스로 확정하고 구현

- 사람이 시킨 일: 다른 디자인 세션에서 온 「관측창 밖의 존재 시안」(5턴, `boss-art.js`·인트로 5비트·보스 5안·스크린샷 40여 장)을 적용.
- 사람이 직접 결정한 점: (1) 인트로의 개체와 최종 보스를 **같은 것으로 확정** — 스펙 3절의 "전장 바깥" 금지선을 1~7월드로 한정하고 8-1에서만 전장에 세운다. (2) 팔레트는 **스펙 4절 유지** — 시안의 남보라+자홍 대신 청록-잿빛을 쓰고 채도 재검토를 디자인 세션에 회신. (3) 범위는 전부 구현.
- 결과: `boss-art.js`를 표현 전용 계층에 얹고(형태 코드는 그대로, 색만 프로젝트 토큰으로 교체), 8번째 월드 `outside`와 스테이지 8-1(1600 HP, 4인 파티, `shape: []`)을 추가했다. 페이즈 4단은 기존 `stagePhases`에 `effect: "form"`을 넣어 **전투 룰을 바꾸지 않고** 형태만 바뀌게 했다. 인트로는 `outer-observer.js`로 분리해 `#dawn-sky` 자식 레이어에 5비트 8.0초로 넣었다.
- 막힌 점 / 우회: (1) `shape: []`가 `carveAstrolabe`의 `points[0]`에서 크래시해 빈 도형 가드를 넣었다. (2) `.title-mode canvas { display:none }` 때문에 인트로 캔버스가 통째로 사라져 data URL `<img>`로 바꿨다 — dawn.js 소품과 같은 방식이다. (3) 스펙의 "저대비"를 문자 그대로 적용하니 전장 264px에서 실루엣이 배경에 묻혀, 색조는 두고 램프 상단 세 단만 올렸다. 살구빛 별자리 노드가 여전히 화면에서 가장 밝다. (4) 보스 이름이 7곳에 하드코딩돼 있어 `bossDisplayName()` 하나로 모았다. 8-1만 좌표(`관측되지 않은 점`)를 쓰고 나머지 34스테이지는 그대로다.
- 추가 지시 (같은 날): 인트로에서 최종 보스를 더 강조하고, 메타적으로 게임 UI를 붙잡아 흔들라는 요구. 5비트 → **6비트 9.8초**로 늘려 「붙잡는다」를 넣었다. 사람이 두 가지를 골랐다 — 발톱 하나로 모서리를 짚고(몸통은 계속 `main` 뒤), 흔드는 것은 중앙 관측창뿐(여백 소품은 제자리). 이 연출은 스펙 2·3·5절의 금지 조항 셋과 충돌해 세 조항을 모두 개정했다.
- 이때 걸린 함정: `main`에 transform이 남으면 `position: fixed` 자손의 컨테이닝 블록이 되어 편성·소환·기록 전체화면 오버레이가 캔버스 폭에 갇힌다. 같은 사고가 `.stage`에서 한 번 있었다. 인라인 transform 대신 `fill-mode` 없는 키프레임을 쓰고, 연출 종료와 `teardown()` 양쪽에서 클래스를 벗긴다. 실기로 `.overlay.squad-scene`이 1440×820 전체화면으로 뜨는 것과 종료 후 `computed transform: none`을 확인했다.
- 미구현으로 남긴 것: 6절의 「5점 이상 별자리 현현 반응」과 「월드 첫 진입·첫 보스 처치 반응」, 8-1 클리어 엔딩 화면. 모바일에는 양옆 여백이 없어 인트로가 성립하지 않는 문제는 **보류**다.
- 증거: `npm run verify`·`npm run smoke`(스크립트 22개) 통과, 8-1 실기 진입으로 4인 파티·1600 HP·페이즈 1→4 전환과 라벨 확인, 인트로 5비트의 비트별 인라인 값과 정지 구도 스크린샷. **`npm run test:onboarding`은 이 맥에서 이번 변경과 무관하게 실패 중이며(2026-08-14 아침부터, CI Ubuntu/Windows는 통과) 회귀 검증에 쓰지 못했다.**

## 2026-08-13 — 타이틀 천구·별자리 하향

- Codex에게 시킨 일: 타이틀의 천구가 여전히 너무 위에 붙어 보이는 문제 수정.
- 결과: 로고·CTA 묶음은 원위치에 두고, 천구와 오리온을 담은 SVG만 `top: -4%`에서 `4%`로 내려 상단 베젤과 머리별 사이에 여백을 만들었다.
- 증거: 1280×720 실제 브라우저에서 천구·별자리 좌표와 화면 확인, 콘솔 오류 0건, `npm run check`.

## 2026-08-13 — 온보딩 6단계 브라우저 E2E 고정

- Codex에게 시킨 일: 다음 우선순위로 온보딩 전체 자동 플레이 테스트 구현.
- 결과: 새 Chromium 프로필에서 실제 마우스 드래그·이동 중 클릭·Space 키를 보내 1~6 카드를 완주한다. 직격, 발사당 조향 1회, 루나 지정 항로, 미리내 실제 접점, 안내별 4개를 포함한 정확한 5점 오망성, 스타터 3인 실전, 세 번째 슬롯과 무료 소환권 보상까지 단언한다.
- 막힌 점 / 우회: 최종 보스 120 HP를 매번 전부 깎으면 첫 세션 회귀가 밸런스·물리 시간에 종속된다. 파티와 120 HP 시작값을 먼저 검사한 뒤 격리된 브라우저 픽스처만 1 HP로 낮추고, 다음 실제 입력이 정상 피해·승리·보상 훅을 통과하게 했다. 테스트는 외부 패키지 없이 Node 기본 기능과 Chromium DevTools 프로토콜을 사용한다.
- 사람이 직접 결정·수정한 점: 없음. 운영 런타임과 전투 수치는 바꾸지 않았다.
- 증거: `npm run test:onboarding` 약 32초 통과, 카드 `1 / 6`~`6 / 6`, `pentagram`, `figurePoints: 5`, 슬롯 3, 무료 소환 1장; `npm run check`와 GitHub Windows/Ubuntu 검증 경로에 연결.

## 2026-08-13 — 대형 런타임의 안정 하위 소유자 분리

- Codex에게 시킨 일: 유지보수용 모듈화를 한 단계 더 진행.
- 결과: 메타 상태·경제를 `game-meta-state.js`, 전투 충돌·기믹·정산을 `game-combat-physics.js`, 패링 궤적·별자리 인식을 `game-figure-recognition.js`로 분리했다. 기존 화면·능력·연출 파일은 소비자 역할에 집중하며, 세 쌍의 로드 순서를 HTML·스모크·검증기·포터빌리티 검사·런타임 하니스에서 동일하게 고정했다.
- 막힌 점 / 우회: classic script는 파일을 나눠도 전역 렉시컬 스코프를 공유하므로 무작정 잘게 쪼개면 순서 의존성만 늘어난다. 변경 이유가 독립적인 세 경계만 추출하고, 선택 근거와 제약을 `prototypes/ADR-002-STABLE-SUBOWNERS.md`에 기록했다.
- 사람이 직접 결정·수정한 점: 없음. 저장 형식, 전투 규칙, 온보딩 연출, 에셋은 변경하지 않았다.
- 증거: `npm run smoke`의 20개 스크립트 순서·전역 재정의 0건, `npm run check`, 포맷·공백 검사.

## 2026-08-13 — 유지보수용 런타임 모듈 경계

- Codex에게 시킨 일: 전체 리팩터링에 이어 유지보수를 위한 모듈화도 시행.
- 결과: 직접 파일 실행을 유지하는 `StellaRuntime` 레지스트리를 추가하고 `combat`, `figure`, `onboarding`의 최소 공개 API를 동결했다. 전투 시작·종료, 피해, 벽 충돌, 패링, 별자리 정산, 승리, 피드백을 명명된 수명주기 훅으로 연결해 전역 재정의 이름을 37개에서 17개로 줄였다. 전투 결과 카드와 실패 화면은 온보딩에서 기본 세션 소유로 이동했다.
- 막힌 점 / 우회: 네이티브 ES 모듈 전환은 Windows 직접 실행과 무빌드 배포 계약을 깨뜨린다. 이번에는 classic script 호환 레지스트리와 점진적 이전을 선택하고 판단 근거를 `prototypes/ADR-001-RUNTIME-MODULES.md`에 남겼다.
- 사람이 직접 결정·수정한 점: 없음. 기존 사용자 변경과 에셋은 건드리지 않고 런타임 계약만 정리했다.
- 증거: 모듈 API 동결·중복 등록·잘못된 훅 이름·우선순위·레거시 재정의 허용 목록·마지막 유성 3분기를 검사하는 `npm run smoke`; 전체 검증은 인계 전에 실행.

## 2026-08-13 — 전역 재정의 제거와 훅 핫패스 최적화

- Codex에게 시킨 일: 최적화와 유지보수를 더 진행.
- 결과: 남아 있던 전역 함수 재정의 17개를 모두 제거했다. 물리·조준·피해는 `game-combat.js`, 피드백 갱신은 `game-feedback.js`, 프레임 렌더는 `game-meta.js`의 단일 함수가 소유한다. 보조탄·필드 효과는 전용 그리기 훅, 편성 후처리는 화면 훅, 절차 전장은 `render.installStageArena()` 전략으로 연결했다.
- 최적화: 런타임 훅은 호출할 때마다 콜백 배열을 복사하지 않는다. 등록·해제 때만 정렬된 불변 스냅샷을 만들므로 매 프레임 `afterDraw`·`afterFeedbackUpdate`·렌더 훅의 배열 할당이 사라졌다.
- 막힌 점 / 우회: classic script의 후방 함수 참조는 모든 스크립트 로드 후 호출되는 런타임 특성을 이용해 빈 전방 선언 없이 유지했다. 직접 실행 계약과 로드 순서는 바꾸지 않았다.
- 증거: `npm run smoke`의 `overriddenGlobals: 0`, 모듈·훅 계약 검사, 전체 자동검증과 브라우저 회귀검증.

## 2026-08-13 — 런타임 전수 리뷰와 전투 수명주기 정리

- Codex에게 시킨 일: 전체 코드를 세밀하게 읽고 최적화, 버그 수정, 유지보수 리팩터링과 코드 정리를 시행.
- 결과: 마지막 유성의 별자리 현현 전에 실패 화면이 뜨던 판정 경합을 연출 완료 후 단일 판정으로 바꾸고, 처치·유성 환급·최종 실패를 하니스 회귀 테스트로 고정했다. 메뉴 이탈과 일시정지 중 지연 공격·별자리 타이머가 진행되지 않게 했고, 온보딩 종료/취소 시 임시 배치와 잠금 UI를 원복한다. 훈련장 무제한 HUD 캐시, 전투 밖 `R` 단축키, 확인창 이벤트 복원, 소환 건너뛰기 중복 실행, 장시간 백그라운드 뒤 BGM 스케줄 폭주, 잘못 저장된 설정값과 로컬 서버의 잘못된 URL 디코딩도 보강했다.
- 최적화: 절차 전장 캔버스를 최근 4개 LRU로 제한해 캠페인 순회 시 약 90 MiB까지 커질 수 있던 캐시를 약 10 MiB 수준으로 제한했다. 동일 종류·크기의 픽셀 버튼 프레임을 공유하고, 전투 팝업 배열은 프레임마다 새 배열을 만들지 않고 제자리 정리한다.
- 막힌 점 / 우회: 전체 포맷 검사는 이번 변경과 무관한 기존 문서·생성 스크립트 11개도 지적했다. 생성 산출물 디렉터리와 사용자 파일은 제외하고 추적 중인 소스·스타일·명세만 기계적으로 정규화해 공식 포맷 기준선까지 복구했다.
- 증거: `npm run check`, `git diff --check`, JS·Python 구문 검사, 마지막 별자리 3분기 하니스, 로컬 브라우저의 타이틀·온보딩 이탈·훈련 HUD·단축키·이미지 로드 검증, 로컬 서버의 정상/잘못된 인코딩/경로 이탈 요청 검증.

## 2026-08-13 — 최신 전투 온보딩과 확정 오망성 쇼케이스

- Codex에게 시킨 일: 폐기된 정산 공격 규칙을 가르치던 1-1 수업을 현재 전투 시스템에 맞추고, 첫 관측에서 최고 별자리 연출을 반드시 보여 주기.
- 결과: 수업을 `드래그 발사 → 이동 중 좌·우 조향 합산 1회 → Space 패링 → 스타터 3인 실전`으로 다시 구성했다. 성공 판정은 실제 발사·조향·패링·별자리 정산 이벤트를 사용한다. 첫 패링 실습은 루나가 유성을 미리내 접점으로 보정하고, 발사 뒤 누른 Space를 그 접점까지 기억한다. 실제 접점 하나와 안내별 넷이 정확한 오망성을 만들어 도형 교정·현현·피해 연출 뒤에만 결과 카드가 돌아온다.
- 막힌 점 / 우회: 0.4초 패링 창과 자유 조준을 그대로 요구하면 처음 보는 플레이어가 대표 연출을 놓칠 수 있었다. 일반 전투 수치는 건드리지 않고 수업 단계에서만 항로와 Space 예약을 보조했으며, 공명을 성공하기 전에는 실전으로 건너뛰지 못하게 했다.
- 사람이 직접 결정·수정한 점: 7점 북두칠성보다 형태를 즉시 알아보고 연출 밀도가 높은 5점 오망성을 첫 클라이맥스로 사용한다. 안내별은 물리·충돌 판정이 없는 연출 보조이며 실제 패링 접점 하나는 반드시 필요하다.
- 증거: `prototypes/js/game-onboarding.js`, `prototypes/js/game-figure.js`, `prototypes/js/game-combat.js`, 로컬 브라우저 실입력 검증, `npm run check`; 커밋은 아직 만들지 않음.

## 2026-08-13 — 업적 칩의 출석 보상 오표기 수정

- Codex에게 시킨 일: 업적·관측 보상이 모두 없는 상태인데 허브의 `업적` 칩에 `수령 1`이 뜨는 원인을 확인하고 수정.
- 결과: 원인은 `claimCount()`가 업적 탭에서 받을 수 없는 프로필의 일일 출석 보상까지 합산한 것이었다. 이제 허브 업적 칩은 업적·관측 보상 항목만 세며, 출석은 프로필의 자체 수령 버튼에서만 표시한다.
- 막힌 점 / 우회: 출석은 실제로 받을 수 있는 보상이므로 `claimCount()` 전체를 0으로 고치는 대신, 칩의 이동 목적지(업적 탭)와 같은 범위만 집계하도록 분리했다.
- 증거: `prototypes/js/game-meta.js`, `bot/runtime-harness.mjs`의 `probeArchiveClaimCount()`. 빈 업적·관측 보상 상태에서 `attendanceReady: true`, `archiveClaims: 0`을 확인했고 `npm run check`, `git diff --check`를 통과했다.

## 2026-08-13 — 업적·관측 보상 개별 수령과 달성 토스트

- Codex에게 시킨 일: 업적 탭의 `모두 수령`을 없애고, 보상은 전부 항목별로 직접 받게 하며 새 업적 달성은 토스트로 알리기.
- 결과: 스테이지 클리어 골드도 합산 숫자가 아니라 `1-2 클리어 보상` 같은 개별 관측 보상 항목으로 쌓인다. 업적·클리어 보상 모두 각자의 `수령하기`만 남았다. 새 업적은 한 번만 토스트하며, 전투 밖에서는 토스트를 누르면 업적 탭을 연다. 전투 중 토스트는 전장 입력을 가로채지 않는다.
- 사람의 결정과의 경계: 기존 저장의 합산 대기 골드는 `이전 관측 보상` 한 항목으로 보존한다. 이미 달성되어 있던 업적은 업데이트 직후 새 달성처럼 토스트하지 않는다.
- 증거: 커밋 [`d542727`](https://github.com/8rulerstar/prism-breakers/commit/d542727), `prototypes/js/game-meta.js`, `prototypes/js/game-onboarding.js`, `prototypes/stella-ball-theme.css`, `bot/runtime-harness.mjs`의 개별 수령 probe. 두 클리어 보상 중 하나를 수령해도 다른 하나가 남고, 이전 저장의 `pendingGold` 60도 별도 항목으로 수령되는 것을 확인했다. `node bot/run-bot.mjs`와 `npm run check` 통과.

## 2026-08-13 — 양자리 관측 잔광 안내별

- Codex에게 시킨 일: 뉴비가 첫 별자리 연출을 놓치지 않도록, `1-1`을 제외한 양자리 `1-2`·`1-3`에 첫 성공 패링 뒤 안내별 둘을 제공하는 보조 기믹 적용.
- 결과: 첫 패링의 실제 접점은 그대로 별빛 노드가 되고, 진행 반대쪽으로 안내별 둘이 점등되어 즉시 3점 별자리를 만든다. 안내별은 충돌 판정·물리·추가 입력이 없으며, 그 샷이 별자리로 정산될 때만 전투당 1회 충전을 소진한다. 패링 실패는 충전을 소진하지 않는다.
- 사람의 결정과의 경계: 별자리를 자동으로 발동하거나 패링을 대체하지 않는다. `1-1`과 양자리 밖 캠페인, 훈련장에는 적용하지 않았다.
- 증거: 커밋 [`d542727`](https://github.com/8rulerstar/prism-breakers/commit/d542727), `prototypes/js/game-data.js`, `prototypes/js/game-session.js`, `prototypes/js/game-figure.js`, `bot/runtime-harness.mjs`. 하니스의 캠페인 probe가 `1-1: 0→0`, `1-2: 1→0`, `1-3: 1→0`, `2-1: 0→0` 충전 전후 상태를 확인했다. `node bot/run-bot.mjs`와 `npm run check` 통과.

## 2026-08-13 — 전투 시작 강화 선택 제거

- Codex에게 시킨 일: 편성을 마친 뒤 나오는 강화 카드 선택을 없애고 바로 전투로 진입하게 정리.
- 결과: `시작`은 중립 전투 상태를 초기화한 뒤 즉시 `setupBattle()`을 호출한다. 강화 후보·선택 화면·결과 화면의 선택 강화 표기와 전용 스타일을 제거했으며, 전투는 항상 기본 5발·기본 피해 규칙으로 시작한다.
- 사람의 결정과의 경계: 패링, 별자리, 별지기 고유 능력, 전투 중 피해 계산은 이번 제거에서 바꾸지 않았다.
- 증거: `prototypes/js/game-session.js`, `prototypes/js/game-data.js`, `prototypes/prism-breakers-foundation.css`; 자동 검증은 이 변경 뒤 다시 실행한다.

## 2026-08-13 — 발사당 1회 궤도 전환·현재 런타임 밸런스 하니스

- Codex에게 시킨 일: 유성이 계속 튀는 코어는 유지하되, 발사 후에도 판단할 조작을 한 번 주고, 기믹 없는 전장에서 2·3·4인과 보스 체력을 현재 코드로 측정.
- 결과: 이동 중 좌클릭은 진행 방향의 좌측, 우클릭은 우측으로 유성을 꺾으며 운동량을 더한다. 둘은 발사당 합산 한 번만 쓸 수 있고, 정지 중 좌클릭 드래그 발사는 그대로다. `bot/runtime-harness.mjs`는 별도 물리 근사 대신 현재 classic scripts와 고정 dt 전투 갱신을 VM에서 실행한다. 기본 96건(고정 2·3·4인, 체력 180·260·340·420, 직접/패링 정책, 시드 4개)은 클리어율 63.5%, 평균 잔여 체력 31.0, 평균 패링 1.9회를 냈다.
- 막힌 점 / 우회: 첫 방향 계산은 Canvas의 아래 방향 y축을 표준 좌표처럼 주석으로 설명해 좌·우를 바꿀 위험이 있었다. 오른쪽 진행 유성으로 좌클릭은 위쪽·우클릭은 아래쪽 속도를 얻는 probe를 추가하고, 같은 샷 두 번째 전환이 거부되는지도 함께 검사했다. 고정 파티에서는 2→3→4 결과가 단조롭지 않아 이 결과로 해금 순서나 체력을 확정하지 않는다.
- 사람이 직접 결정·수정한 점: 궤도 전환은 좌/우 각각 한 번이 아니라 **발사당 합산 한 번**으로 두고, 패링과는 별도 조작으로 유지한다. 수치·최적 타이밍은 실기 뒤에 정한다.
- 증거: `prototypes/js/game-combat.js`, `prototypes/js/game-core-render.js`, `bot/runtime-harness.mjs`, `bot/latest-report.json`, `BOT_REPORT.md` 0-1절; `npm run check`은 이 변경 뒤 다시 실행한다.

## 2026-08-13 — 7월드 무기믹 캠페인·왼쪽 순차 해금

- Codex에게 시킨 일: 별자리의 점 개수를 활용해 7개 월드까지 캠페인을 늘리고, 기믹 없는 보스 HP·배치 기준선으로 레벨을 설계하며, `1-1` 같은 스테이지 번호를 지도에서 가장 크게 보이게 변경.
- 결과: 양자리(3)·화살자리(4)·까마귀자리(4)·카시오페이아(5)·백조자리(5)·오리온자리(6)·북두칠성(7)의 총 34스테이지를 생성했다. 1-1의 120 HP 튜토리얼부터 7-7의 380 HP까지 월드 안에서 완만히 오른다. 모든 활성 캠페인 스테이지의 범퍼와 `gimmicks`는 비어 있으며, 시작 배치와 패링·조향 경로가 기본 난도를 맡는다. 월드 지도는 첫 노드가 왼쪽에 오고 클리어 순서대로 오른쪽으로 열린다.
- 막힌 점 / 우회: 기존 12개 캠페인에는 기믹 데이터가 직접 들어 있어 남겨두면 무기믹 기준선이 섞인다. 과거 레이아웃은 `LEGACY_GIMMICK_STAGES`로 보존하고, 실제 `stages` 배열은 새 34개 캠페인과 훈련장만 담도록 분리했다. 따라서 기존 세이브의 `progress.clears` 하나로도 새 순서에 그대로 대응한다.
- 사람이 직접 결정·수정한 점: 월드마다 스테이지 수를 같게 강제하지 않고 별자리 점 수 3·4·4·5·5·6·7을 따른다. 기믹을 먼저 늘리지 않고 HP·배치·조작의 기본 재미부터 검증한다.
- 증거: `prototypes/js/game-data.js`, `prototypes/js/game-session.js`, `prototypes/js/game-meta.js`, `prototypes/js/game-onboarding.js`, `prototypes/prism-breakers-polish.css`; 자동 검증은 이 변경 뒤 다시 실행한다.

## 2026-08-13 — 별지기 미경유 첫 직격 감쇠

- Codex에게 시킨 일: 발사하자마자 보스가 첫 충돌 대상이 되는 유성 직격을 특히 낮추고, 별지기를 경유하는 플레이를 자연스럽게 보상.
- 결과: 유성이 별지기를 한 번도 건드리지 않은 보스 직격은 0.6배, 그 샷의 첫 충돌이 보스면 추가 0.5배(기본 합산 0.3배)가 된다. 패링·각성·별자리·유닛 능력은 바꾸지 않았다. 같은 96건을 다시 실행한 결과, 260 HP의 직접 경로는 모든 고정 파티에서 실패하고 패링 경로는 모두 클리어했다.
- 막힌 점 / 우회: 단순히 첫 보스 충돌만 낮추면 유성-보스 왕복의 두 번째부터는 다시 원래 피해가 되어 우회 경로가 남는다. 그래서 별지기 미경유 상태 전체를 먼저 낮추고, 첫 충돌 보스에는 한 번 더 감쇠했다. 레일·범퍼·잔재·방벽·스테이지 벽도 첫 충돌 판정에 포함했다.
- 사람이 직접 결정·수정한 점: 보스 직격을 삭제하지 않고, 회복용·위험한 선택지로는 남긴다. 별지기 경유와 패링의 리턴을 높이는 방향을 우선한다.
- 증거: `prototypes/js/game-data.js`, `prototypes/js/game-combat.js`, `bot/latest-report.json`, `BOT_REPORT.md` 0-1절; 자동 검증은 이 변경 뒤 다시 실행한다.

## 2026-08-13 — 접점 기억 패링 적용

- Codex에게 시킨 일: 빠르게 튀는 유성에서도 패링의 손맛을 살리도록, 충돌 직전뿐 아니라 충돌 직후에도 `Space`로 공명할 수 있는 설계를 실제 전투에 적용.
- 결과: `Space`는 선입력 공명 대기를 열고, 일반 유성-별지기 충돌은 짧은 `Space · 공명` 접점 잔광을 남긴다. 잔광 중 늦게 누르면 저장한 접점·입사 방향을 사용해 물리를 되감지 않고 같은 공명 반동·각성·능력·별빛을 한 번만 적용한다. 빈 공간에서 끝난 입력은 모은 별빛을 보존하고 짧은 재사용만 막으며, 대기 직후 실제 접점을 놓친 경우만 별빛을 지운다. 성공 직후 유성의 최고 연쇄 속도도 읽기 쉬운 범위로 제한하고, 짧은 충돌 히트스톱을 더했다.
- 사람이 직접 결정·수정한 점: 추가 포인터 조작·미래 궤적 예고·도형 선언은 넣지 않는다. 혼전에서도 플레이어가 대상 선택을 하지 않고, 실제 접점 하나만 공명한다. 창·잔광·재사용 시간과 속도 상한은 실기 전 임시값이다.
- 증거: `prototypes/js/game-figure.js`, `prototypes/js/game-combat.js`, `npm run check`, 문법·포맷 검사, `git diff --check` (커밋 전).

## 2026-08-13 — 전투 패링 규칙 전면 통일

- Codex에게 시킨 일: 무한 훈련장과 일반 전투의 조작 차이를 없애고, 모든 전투를 패링·별빛·즉시 각성 기준으로 통일.
- 결과: 모든 스테이지에서 `Space` 패링 성공한 유성-별지기 접촉만 추가 운동량, 공명 각성, 접촉 능력, 별빛 노드를 만든다. 비패링 유성·별지기·보스·다른 별지기 충돌은 물리 반동만 남긴다. 샷 종료 정산 공격과 정산 배너는 전투 전체에서 제거됐고, 3~7점 별자리는 캠페인과 훈련장에서 동일하게 발동한다. 윤슬의 칼날·관통도 모든 전투에서 패링 성공 뒤에만 켜진다.
- 사람의 결정과의 경계: 무한 훈련장의 무한 체력·자동 유성 보충, 캠페인 보스 체력·스테이지 기믹·클리어 보상은 전장 성격으로 유지한다. 패링 창·실패 대기시간·피해 수치는 이번 통일에서 조정하지 않았다.
- 증거: `game-figure.js`, `game-combat.js`, `game-feedback.js`, `game-data.js`, `npm run check`, 변경 파일 포맷 검사, `git diff --check`.

## 2026-08-13 — 패링 별빛을 한 샷 정산으로 전환

- Codex에게 시킨 일: 별빛 패링의 짧은 사슬 제한을 없애고, 모은 별빛은 샷이 끝나면 사라지도록 정리.
- 결과: 성공 패링 노드는 한 샷에 최대 7개까지 남으며 시간 제한 없이 미리 보인다. 샷 종료 시 3~~7개만 현재 점수의 별자리로 한 번 발동하고, 0~~2개는 소멸한다. 7개도 즉시 발동하지 않는다. 실패는 그 샷에서 모은 노드를 전부 삭제하고 재사용 대기시간을 준다.
- 사람의 결정과의 경계: 별빛은 샷 사이에 이월하지 않으며, 패링 타이밍·실패 대기시간 수치는 아직 실기 조정 대상이다.
- 증거: `npm run check`, 변경 파일 포맷 검사, `git diff --check`.

## 2026-08-13 — 훈련장 패링 각성·정산형 능력 전환

- Codex에게 시킨 일: 6점 오리온자리·7점 북두칠성까지 별빛 상한을 열고, 훈련장에서는 샷 종료 정산 공격 대신 패링 성공 순간에 기존 정산형 능력을 발동하도록 전환.
- 결과: 별빛 상한은 7점이며 6점 오리온자리·7점 북두칠성이 분류·공통 포위 피해까지 발동한다. 훈련장 패링 성공은 별지기를 약한 공명 각성으로 깨우고, 샛별은 보스까지 검기를 발사한다. 검기는 가까울수록 강해지고 사거리 밖 실패는 없다. 미리내 저격과 모루 충격파도 같은 성공 충돌에서 즉시 실행한다. 윤슬도 패링 성공 뒤에만 회전 칼날과 보스·다른 별지기 관통을 얻고, 비패링 충돌에서는 일반 유닛으로만 반동한다. 훈련장 샷 종료의 일반 정산 공격과 정산 배너는 계속 나오지 않는다.
- 사람의 결정과의 경계: 적용 범위는 훈련장이다. 캠페인은 패링 입력이 도입되기 전까지 기존 샷 종료 정산을 유지한다. 가온의 피해·패링 창·재사용 대기시간 수치는 실기 조정 전 임시값이다.
- 증거: `game-figure.js`, `game-combat.js`, `game-feedback.js`, `game-data.js`; 자동 검증은 이 변경 뒤 실행한다.

## 2026-08-13 — 유성 저속 감속·조기 정산 롤백

- Codex에게 시킨 일: 직전 변경 뒤 유성이 충돌 후 너무 빨리 힘을 잃는 체감을 추적하고, 원인이 된 물리 변경만 되돌리기.
- 결과: 포인터 제거 커밋은 우클릭 보정과 달무리 전환 보너스만 제거했으며 기본 감쇠는 바꾸지 않았음을 확인했다. 실제 원인은 `0002530`의 저속 감쇠 `0.972`와 정산 문턱 `유성 150 / 별지기 110`이었다. 이를 기존 균일 감쇠 `0.9915`, 정산 문턱 `68 / 55`로 되돌렸다.
- 사람의 결정과의 경계: 훈련장 패링은 성공 충돌에만 추가 가속·각성·접촉 능력을 주는 새 규칙이라 이 롤백에서 건드리지 않았다. 패링 없이 부딪혔을 때의 기본 탄성 반동과 저속 관성만 복구한다.
- 증거: 커밋 `0002530`·`9326c55`의 물리 diff 대조, `npm run check`, 포맷 검사.

## 2026-08-13 — 훈련장 패링 별빛 사슬

- Codex에게 시킨 일: 정지 위치 기반 별자리와 계속 튀는 충돌 각성의 모순을 피하도록, 유성 패링 성공 접점을 고정 별빛 노드로 남기는 훈련장 실험을 구현.
- 결과: 무한 훈련장에서만 이동 중 `Space`로 짧은 패링 창을 연다. 그 창에 유성이 별지기와 닿으면 기존의 추가 가속·깨움·접촉 능력이 적용되고 접점에 별빛이 남는다. 평상시 충돌은 기본 탄성만 남는다. 3·4점은 다음 패링을 잠깐 기다렸다가 사슬이 끝나면 발동하고, 5점은 즉시 발동한다. 창을 놓치면 3점 미만의 별빛은 사라지고 사슬을 닫으며 잠시 재사용을 막는다. 별자리 연출은 접점 노드를 뼈대로 교정할 뿐, 유성·별지기의 물리는 멈추지 않는다.
- 막힌 점 / 우회: 기존 `wakeUnit()`은 속도·벽·유닛 간 충돌에도 자동으로 깨어나므로, 훈련장에서만 그 경로를 막아 비패링 탄성 반동이 각성으로 변하지 않게 했다. 별자리별 고유 효과는 이번 범위가 아니므로 기존 공통 포위 피해를 유지했다.
- 사람이 직접 결정·수정한 점: 우클릭과 이동 중 좌클릭은 복구하지 않고, 패링은 캠페인이 아닌 훈련장에서 먼저 검증한다. 3점은 낮은 피해로도 발동할 수 있게 두고 정밀 도형 일치도로 보상을 깎지 않는다.
- 증거: `npm run smoke`, `npm run check`, 변경 파일 포맷 검사, `git diff --check`.

## 2026-08-13 — 이동 중 포인터 개입 제거

- 작업 내용: 이후 유성 패링을 위한 입력 자리를 비우기 위해, 모든 전투에서 이동 중 좌클릭 90° 전환과 우클릭 미세 운동량 추가를 제거했다.
- 결과: 유성이 멈춰 있을 때의 좌클릭 드래그 발사는 유지한다. 이동 중 좌·우클릭은 더 이상 물리·각성·운동량에 영향을 주지 않는다. 달무리의 기존 클릭 전환은 새 충돌 발동 조건을 정하기 전까지 기본 충돌 반응만 남긴다.
- 사람이 직접 결정·수정한 점: 패링 자체와 수치 설계는 아직 적용하지 않는다.
- 증거: `npm run check`, 변경 파일 포맷 검사.

## 2026-08-13 — 훈련장 별자리 도형 기준선·검증 범위 정리

- 작업 내용: 현재 워킹 트리의 별자리 도형 연출, 실루엣 5종, 유성 감속 조정, Observatory Ground 전장 바닥 패스를 코드·에셋 매니페스트·운영 문서와 대조했다.
- 결과: 훈련장 전용 도형은 `game-figure.js`가 정산을 감싸서 처리한다. 유성과 실제로 움직인 별지기가 3점 이상이면 6개 도형 중 하나를 분류하고, 선 교정·실루엣 현현 뒤 기존 포위 피해를 적용한다. 2점은 선 포위 효과다. 실루엣은 양자리·화살자리·까마귀자리·카시오페이아·백조자리 5종이며 오망성은 기존 전용 연출을 쓴다.
- 막힌 점 / 우회: `npm run verify`가 HTML에서 실제로 불러오는 `game-figure.js`·`game-arena-carve.js`와 중첩 실루엣을 검사하지 않아, 문서에는 반입 완료인데 검증 리포트에서는 빠지는 불일치가 있었다. 런타임 파일·참조·매니페스트 파일 검사에 추가했다. 사양서의 총 재생시간(5초)과 좌우 반전 지원도 코드와 달랐다. 실제 값인 5.6초와 미구현 상태로 고쳤다.
- 사람이 직접 결정·수정한 점: 별자리 고유 능력, 구분 SFX, 좌우 반전, 캠페인 도입은 미정으로 보존했다. 현재 구현된 공통 포위 피해를 고유 능력처럼 홍보하지 않는다.
- 증거: `npm run check`, 변경 파일 대상 Prettier 검사, PNG 규격 384×384 확인, `git diff --check`.

## 2026-08-13 — 죽은 핀볼 레이어 제거, 한글 줄바꿈, 하니스로 잰 1샷 클리어와 별자리 발동률

- 작업 내용: "게임을 분석하고 평가해달라"는 요청에서 시작해, 문서 대조 → 코드 감사 → 실기 플레이 → 헤드리스 측정 순으로 진행했다. 그 결과를 바탕으로 두 가지를 고쳤고, 이어서 별자리 도형 인식기의 발동률을 스윕했다. 수치와 측정 조건 전문은 [BOT_REPORT.md](BOT_REPORT.md)에 있다.
- 결과 ① **발사석에서 위로 최대 파워 한 발이면 캠페인 11개 중 10개가 1샷 클리어된다.** 발사석 `x`가 360인데 12개 스테이지 전부 보스 `x`도 360이라 조준이 필요 없고, 보스 위 약 110px 복도의 왕복 시간이 `boss.hitCooldown` 0.22초보다 짧아 유성이 천장과 보스 사이에 갇혀 계속 때린다. 2-1만 페이즈 밀어내기 덕에 버텼다(108/260). 1-2에서 5회 반복 전부 260/260 동일. 설계한 루프(별지기를 굴려 깨우고 배율을 쌓는 것)를 전부 건너뛸 수 있다는 뜻이라, 체력만 올려서는 해결되지 않는다. **미해결로 남긴다.**
- 결과 ② **죽은 핀볼 레이어를 걷어냈다(약 1,100줄).** `game-core-physics.js`가 810줄에서 41줄이 됐다. 지운 것은 중력 720 솔버 2벌, 플리퍼 일체(`flipperPose`·`resolveSegment`·`bounceOffFlipper`·`drawFlipper`·`flipperKick`·`PHYSICS` 상수), 레거시 플런저 포인터 핸들러 3개, 그리고 지금 로스터에 없는 캐릭터들의 능력 20여 종이 담긴 옛 `hitGate` 본체다. 살아 있는 파일에 남아 있던 유령 이름 토스트도 전부 사라졌다. 오버라이드 대상(`hitGate`·`hitBumper`·`simulatePhysics`·`update`·`drawPinballTable`·`drawCombatControls`·`drawZoneRules`·`drawAimGuide`)은 빈 베이스 선언으로 남겼다 — 지우면 하위 패스의 대입이 암묵적 전역이 된다.
- 결과 ③ **한글이 어절 중간에서 끊기던 문제를 고쳤다.** 프로젝트 CSS 전체에 `word-break` 규칙이 한 줄도 없어서 "위상 공명 / 기", "사냥꾼 / 의 눈", "돌아갔습니 / 다."처럼 깨지고 있었다. `prism-breakers-foundation.css`의 `:root`에 `word-break: keep-all`과 `overflow-wrap: break-word`를 넣었다.
- 결과 ④ **별자리 인식기는 "잘 그린 것"과 "우연"을 가르지 못한다.** 훈련장에서 무작위 조준 795샷을 실제 물리로 굴린 결과, 어떤 판정값에서도 실제 발동률이 균일 무작위 산포의 오발동률과 같거나 오히려 낮았다(0.28에서 5점 57% 대 61%). 물리로 굴러 멈춘 배치가 난수와 통계적으로 구별되지 않는다. 부수적으로 인식기의 구조적 한계 둘을 확인했다 — 회전을 정규화하므로 **정사각형과 정십자는 거리가 정확히 `0.000`으로 같은 도형**이고, 연결 순서를 보지 않으므로 오각형과 오망성은 원리적으로 구분 불가다(후자는 코드 주석에 이미 기록돼 있었다). 그리고 도형의 **규칙성이 오발동률을 좌우한다** — 같은 판정값에서 오망성 단독 0.2%, 카시오페이아 4.6%, 남십자 13.1%, 양자리 49.8%였다.
- 막힌 점 / 우회: (1) 헤드리스 창은 `visibilityState`가 `hidden`이라 `requestAnimationFrame`이 스로틀돼 실시간 관찰이 불가능했다. 프레임 루프 대신 `update`/`updateSpecial`/`updateFeedback`을 고정 dt로 직접 호출했다. 대신 **체감 속도·연출 타이밍은 이번에 전혀 검증하지 못했다.** (2) 초기 스윕 수치가 실행마다 크게 흔들렸다(3점 오발동 31.6% ↔ 55%). 하니스가 쓰던 LCG 난수의 스트림 상관이 원인이라, `Math.random`으로 바꾸고 시드 3개 재현을 확인한 값만 남겼다. (3) 레거시 포인터 핸들러가 정말 죽었는지 확신이 없어 브라우저에서 직접 실험했다 — 타겟 단계에서도 capture 리스너가 등록 순서와 무관하게 먼저 실행되므로, 나중에 등록된 당구 핸들러가 항상 선점한다. 제거 후 드래그 발사 속도가 1725(= 750 + 1.0×975, 당구 공식)로 나오는 것으로 재확인했다.
- 사람이 직접 결정·수정한 점: 밸런스 수치 튜닝은 범위에서 제외(하니스 중단 지시), 핀볼 정리는 도달 불가 코드만이 아니라 라이브 함수 안의 죽은 분기까지 포함하는 전체 정리를 선택, 줄바꿈 규칙은 두 화면 국소가 아니라 전역 적용을 선택, 5점 후보에 오망성 포함을 지정. 그리고 **"조준으로 도형을 만드는 건 사실상 불가능하니 실제 결과를 우연과 동일하게 가정하고 판정값을 유하게 잡는다"**는 방향을 정했다.
- 남은 것 / 주의: 1샷 클리어는 고치지 않았다. 판정값 완화안은 아직 코드에 반영하지 않았다. 사람이 도형을 의도적으로 노렸을 때의 발동률은 측정하지 않았다. `prettier --write`를 편집한 파일 전체에 돌린 탓에 손대지 않은 줄 약 130줄이 함께 재정렬됐다 — HEAD가 원래 포맷 기준에 맞지 않는 상태였기 때문이며, 기능 영향은 없다.
- 증거: `npm run verify` / `npm run smoke` 통과. 브라우저에서 전투 1판 완주(콘솔 에러 없음, 결과 화면 정상), 강화 카드·결과 카드의 줄바꿈 개선을 스크린샷으로 확인. 수치 전문은 [BOT_REPORT.md](BOT_REPORT.md). 핀볼 정리 diff는 동시에 진행되던 다른 작업 세션의 커밋 [`4503853`](https://github.com/8rulerstar/prism-breakers/commit/4503853)에 함께 담겨 커밋됐다 — 같은 워킹 트리를 두 세션이 동시에 편집한 결과이며, 그 커밋의 메시지는 핀볼 정리를 언급하지 않는다.

## 2026-08-13 — 사장 코드 2000여 줄 제거, 도형 프로토타입 분리, 오버라이드 검사

- 작업 내용: 사용자가 "최적화·버그수정·모듈화·죽은 코드 정리" 항목을 목록으로 뽑아달라고 해서 조사 후 6단계로 나눴고, 1~5단계 실행 지시를 받았다.
- 결과 (플레이어가 보는 버그): 편성 자리 이름 **경유·공명·마무리 별지기**가 전부 존재하지 않는 발동 조건을 설명하고 있었다. `triggerZone`이 삭제되면서 `zone` 값을 읽는 코드가 하나도 남지 않았는데 UI만 계속 조건을 약속하고 있었다. 자리마다 실제로 다른 건 거상과의 거리뿐이라, 캠페인 자리 40개의 실측 분포(204~417)를 재서 300·390 두 문턱으로 **근접/중거리/원거리 자리**를 계산하게 바꿨다. 배율 팝업이 조준 안내 문구와 12px 차이로 겹치던 것도 48px 확보로 고쳤다.
- 결과 (사장 코드): 전역 함수를 나중 파일이 재정의하면서 **원본을 붙잡지 않으면 앞 정의가 통째로 죽는다.** `showMeta`는 5번 정의되고 마지막 하나만 살아 있었고, 죽은 사본 중 하나가 `#hubStageSelect` 버튼을 통째로 들고 있었다. 재할당 6개는 삭제, 선언 11개는 빈 스텁으로 축소해 494줄을 걷어냈고, 마지막 남은 `showMeta` 67줄도 이어서 제거했다. 큐트 토큰 렌더링 경로(`cuteUnitArt`가 빈 뒤 읽는 곳 4군데가 전부 발동 불가), `heroAnimArt` 항등 맵, `portraitScale`·`atlas` 탈출구도 함께 정리했다.
- 결과 (성능): 조준선이 매 프레임 `gates.map((g) => ({ ...g, type: "unit" }))`로 게이트를 통째 복제하고 있었다. 실제로 읽는 필드는 다섯 개뿐이고 `type`은 복제본에서 읽히지도 않는다. 라이브 게이트를 그냥 순회하도록 바꿔 호출당 0.86µs → 0.44µs.
- 결과 (CSS): 스타일만 있고 화면에 한 번도 안 붙는 클래스 57종, 1518줄을 제거했다(8102 → 6884). grep이 아니라 **전 화면을 돌며 DOM이 실제로 붙이는 클래스 241개를 수집**해 대조했다. 규칙은 모든 선택자의 모든 클래스가 죽었을 때만 지웠다.
- 결과 (모듈화·재발 방지): 도형 프로토타입 379줄을 `game-figure.js`로 분리했다. `settleParty`를 감싸므로 로드 순서는 game-combat 바로 뒤여야 한다. 그리고 **이번 달 버그 3건의 원인이던 패턴을 `npm run smoke`가 잡도록** 했다 — 나중 파일이 원본을 붙잡지 않고 덮어쓴 정의를 찾아 파일·줄과 함께 실패시킨다. 빈 몸통은 선언 자리이므로 예외다.
- 막힌 점 / 우회: 파일명 문자열 검색으로는 `characters/anim/*.png` 16개가 미참조로 보인다(경로를 문자열 조합으로 만든다). 고아 파일 정리 때 지울 뻔했다. CSS 정적 분석의 접두어 휴리스틱도 `roster-scene` 같은 살아있는 이름을 오탐해서, DOM 수집 방식으로 바꿨다.
- 사람이 직접 결정·수정한 점: 6개 항목 중 1~5번 실행 지시. 도형 프로토타입은 채택이 정해질 때까지 문서에 적지 않기로 했다.
- 증거: 각 단계마다 `npm run verify` / `npm run smoke` 통과와 라이브 런타임 확인. 오버라이드 검사는 일부러 고아 정의를 넣어 **실패하는 것까지** 확인한 뒤 원복했다. 현재 오버라이드되는 전역 35개는 전부 붙잡혔거나 빈 몸통이다.

## 2026-08-13 — 로스터 개명·신규 아트, 훈련장 개방과 4인 편성

- 작업 내용: 사용자가 받아온 신규 로스터 패치를 적용하고, 그전에 "훈련장에서 정산이 계속 뜨고 편성이 3명"이라는 지적을 추적했다.
- 결과: **무한 훈련장에 진입로가 아예 없었다.** `stageIndex`를 훈련장(12)으로 세팅하는 코드가 코드베이스에 하나도 없었다. 설정하는 곳은 온보딩(0)·별자리 지도·스테이지 선택 셋뿐이고 `campaignStages`가 훈련장을 걸러낸다. `R` 키는 훈련장 진입이 아니라 편성창을 여는 키라, 그동안 마지막에 고른 캠페인 스테이지로 들어가고 있었다. 허브 지도 아래 미션 바에 `무한 훈련장` 버튼을 넣어 열었다. `showStageSelect()`에도 같은 버튼을 뒀지만 그 화면은 허브에서 닿지 않는다 — `game-onboarding.js`가 `showMeta`를 감싸지 않고 통째로 교체해서 `#hubStageSelect`·`#metaLaunch`가 사라진 상태다. 훈련장은 파티 4명(스테이지에 네 번째 `slots`, `partySlotCount()`가 4 반환, `ZONE_RULES`에 네 번째 역할 `자유 별지기` 추가 — 로스터·HUD 요약·`startShot`이 전부 이 배열을 자리 인덱스로 읽어서 인덱스 3이 undefined였다), 거상은 판 정중앙, 기믹은 전부 제거했다.
- 로스터 패치: 내부 hero id는 그대로 두고 이름만 바꿨다. 샛별·미리내·별하·살별·윤슬·달무리·모루·그믐. 스프라이트 24종을 전부 교체하며 규격을 통일했다(idle 192×192 6프레임, 구르기·공격 각 4프레임). 살별의 320px 12프레임 시트와 윤슬의 256px 단일 프레임, 그리고 윤슬만 갖고 있던 `portraitScale` 예외가 사라졌다.
- 막힌 점 / 우회: 세 가지가 걸렸다. (1) 사용자가 실행하던 것이 `~/Documents/ChatGPT/오픈 AI 게임 헤커톤/`의 46커밋 뒤처진 별도 체크아웃이었다. `RUN_STELLA_BALL.command`가 두 군데 있었고 내용이 같았다. 확인 후 휴지통으로 옮겼다. (2) 기계적 개명이 한국어 조사를 깨뜨렸다. 조사는 앞 이름의 받침을 따르는데 8명 중 셋의 받침 계층이 바뀌었다(미리내 무받침, 살별·윤슬 받침). 코드 14건, 문서 22건을 고쳤고, ㄹ 받침은 "으로"가 아니라 "로"라는 예외도 따로 처리했다. (3) 파일명 검색으로는 `characters/anim/*.png` 16개가 미참조로 보인다 — `game-data.js`가 경로를 문자열 조합으로 만들기 때문이라, 고아 파일 정리 때 지울 뻔했다.
- 사람이 직접 결정·수정한 점: 훈련장 진입 버튼 위치(별자리 지도 하단), 훈련장에서 기믹 전부 제거, 과거 기록 문서의 구 이름도 전부 새 이름으로 소급 치환(`DOCUMENTATION_INDEX.md`의 "과거 시안 용어는 소급해 고치지 않는다" 규칙보다 읽는 사람의 혼선을 우선한 판단), 신규 아트는 프로젝트 오너 제작으로 표기.
- 증거: `npm run verify` / `npm run smoke` 통과. 라이브 런타임에서 확인 — 허브 버튼 → 편성 4자리 → 전투 게이트 4개, 거상 (360, 450), idle 8종·액션 16종 전부 선언 규격과 일치하며 404 없음, 콘솔 에러 없음. `cuteUnitArt`가 비면서 읽는 곳이 사라진 큐트 시트 4종을 삭제했고, `NEW_UNIT_SET_01.md`의 후보 3종과 `ATTRIBUTION.md`가 참조하는 토큰 1종은 남겼다.

## 2026-08-12 — 일시정지 버튼이 전투 밖에서도 남던 회귀 수정

- 작업 내용: 사용자가 "게임 중이 아닌데도 좌상단 일시정지 버튼이 계속 보인다"고 지적했다.
- 결과: 이번 세션 A단계에서 내가 넣은 CSS가 원인이었다. `.pause-button`은 원래 기본값이 `display: none`이고 `body.game-mode`일 때만 켜지는 구조였는데, 초승달 실루엣을 걷어내며 추가한 규칙이 뒤에서 `display: grid`를 무조건 덮어써 씬 게이트를 무력화했다. 중복 블록을 기본 규칙에 합치고 `display`는 씬 규칙에만 맡겼다. 같이 발견한 것 하나 — 760px 미디어쿼리가 버튼이 우상단이던 시절의 `right: 8px`를 들고 있었다. `left`가 함께 있어 동작에는 영향이 없었지만 의도와 어긋나 정리했다.
- 막힌 점 / 우회: 없음. 원인이 한 규칙이라 재현과 수정이 바로 됐다.
- 사람이 직접 결정·수정한 점: 사용자가 실제 플레이에서 잡아낸 회귀다.
- 증거: `npm run verify` / `npm run smoke` 통과. 씬별 `display` 확인 — 타이틀·허브·편성·상점·프로필·업적·도서관에서 모두 `none`, `game-mode` 전투에서만 `grid`, `onboarding-locked` 동안에는 다시 `none`

## 2026-08-12 — 별자리 월드 재편, 새 기믹 5종, 도서관, 출석, 별지기 도색 (B단계)

- 작업 내용: 12가지 지적 중 B단계 다섯 항목 — 별자리 월드 구조(3), 새 기믹 5종(2), 도서관 탭(7), 출석 보상(12), 상점 확장(10).
- 결과: **캠페인이 별자리 월드가 됐다.** 별자리 하나가 월드고 그 꼭짓점이 스테이지다. `북두칠성` 7개(두베·메라크·페크다·메그레즈·알리오트·미자르·알카이드)와 `카시오페이아` 5개(카프·셰다르·감마 카스·루크바·세긴), 총 12개 캠페인 스테이지다. 노드 위치는 `WORLDS[].shape`의 실제 별자리 도형에서 나오고, 허브 지도는 좌우 화살표로 월드를 넘긴다. 진행 판정은 여전히 `progress.clears` 하나뿐이라 기존 저장에 마이그레이션이 필요 없다. **새 기믹 5종**: 굳은 껍질(피해 n회 무시), 흐린 발판(지나가면 별자리 배율 −0.5), 도는 방벽(체력을 가지고 보스를 공전), 페이즈 밀어내기(체력이 꺾이면 별지기 셋과 유성을 네 모서리로), 페이즈 재수면(꺾일 때 다시 잠들어 2회 충돌해야 깨어남). 스테이지당 새 기믹 하나 원칙을 12개 전부에 적용했다. **도서관 탭**을 업적 좌측에 붙였다 — 별지기 8명 도감과 별자리 배율 조건표다. **출석 보상**은 하루 한 번, 연속 일수에 따라 60·70·80·90·110·140·220 골드이며 허브 수령 배지에 합산된다. **상점에 별지기 도색**을 추가했다(개당 320골드, 유닛별 5색). 유성 도색과 같은 hue-rotate 방식이라 새 시트가 없고, `heroes[].col`과 전장 판정색은 건드리지 않는다.
- 막힌 점 / 우회: (1) 껍질을 붙이려면 보스 피해가 한 곳을 지나야 하는데, `boss.hp`를 직접 깎는 코드가 **9곳**에 흩어져 있었다. 처음 찾은 6곳은 `game-combat.js`였고, 나머지가 `game-core-render.js`의 `resolveAssist`, `game-session.js`의 `areaAttack`, `game-core-physics.js`의 룬 범퍼였다. 전부 `applyBossHit()`로 모으고, 껍질에 막히면 각 호출부가 자기 팝업을 건너뛰게 했다. `damage()`는 세 파일에 선언이 있는데 `game-combat.js`가 마지막에 덮어쓰므로 앞의 둘은 죽은 코드였다. (2) 스테이지가 5개에서 13개로 늘자 `libraryArt.stages[stageIndex]`가 undefined를 반환해 전투 진입이 터졌다. 아트 표는 의도적으로 스테이지보다 작으므로 `stageArtFor()`가 폴백하도록 바꿨다. (3) 재수면 가드가 처음엔 3회 충돌을 요구했다 — 가드를 0으로 만든 그 충돌에서 반환해 버렸기 때문이다. 가드가 0이 되면 같은 충돌이 깨우도록 고쳤다. (4) 도서관 목록이 폭만 있고 높이가 0이었다. 원인은 내가 넣은 900px 미디어쿼리였고, 세로로 쌓일 때는 목록이 스크롤 박스이길 그만두게 했다.
- 사람이 직접 결정·수정한 점: 월드 구조와 기존 1-x·2-x를 북두칠성으로 이관(기존 유지 불필요), 상점 추가분을 별지기 색 스킨으로 하는 것은 사용자가 골랐다. 모루 스프라이트를 Warrior로 바꾸려던 계획은 "샛별·그믐이 이미 Warrior"라는 사용자 지적으로 취소했다 — 소스 팩에 클래스가 5종뿐이고 전부 배정돼 있어 C단계에서 다시 정한다.
- 남은 것: C단계(윤슬 roll/attack 시트, 모루 스프라이트 교체). 12개 스테이지의 난이도 곡선과 새 기믹 5종의 밸런스는 아직 측정하지 않았다 — 하니스가 현재 물리로 갱신된 뒤의 작업이다. `showStageSelect()` 전체 화면 지도는 월드 대응만 했고 실제 사용 여부는 확인하지 않았다.
- 증거: `npm run verify` / `npm run smoke` 통과. 기믹 5종 런타임 확인 — 1-5 껍질 3겹에서 피해 `[0,0,0,30]`·HP 감소 30, 1-6 발판 통과 시 배율 3.0→2.5, 1-7 방벽 2개 반경 136·체력 70, 2-1 체력 40% 타격 시 별지기 `(180,498)(540,498)(360,420)` → `(96,724)(624,724)(96,176)`, 2-2 가드 2에서 1회 충돌 후 미각성·2회에서 각성. 브라우저 확인 — 북두칠성 7노드가 국자 모양으로, 카시오페이아 5노드가 W로 배치되고 잠긴 노드는 점선, 도서관 8장 + 배율 5행, 출석 첫 수령 60골드·재수령 0, 별지기 도색 5행 25칩·샛별 홍옥 구매 후 `hue-rotate(312deg)`이고 미리내는 `none`

## 2026-08-12 — 일시정지 아이콘, 용어·토스트 큐, 칼날 지속, 온보딩 위치, 1-3 기믹 정리

- 작업 내용: 사용자가 12가지를 지적했고, 그중 코드만으로 끝나는 6가지를 A단계로 묶어 먼저 처리했다(일시정지 버튼 / RIPOSTE 용어 + 메시지 겹침 / 윤슬 칼날 / 온보딩 위치 / 1-3 기믹 과다 / 허브 칩 라벨).
- 결과: **일시정지 버튼**은 `stella-ball-dawn.js`의 `MAP`이 `.pause-button`에 `moon`(초승달) 실루엣을 입히고 있었다. `❚❚` 글리프가 초승달 위에 얹혀 신발처럼 보였다. 매핑을 빼고 36px 원형 컨트롤로 되돌렸다. **`RIPOSTE`를 `연타`로** 바꿨다(HUD 콤보 리드아웃, 캔버스 배너 2곳, 피격 팝업, 리듬 포병 안내, 업적 이름 `3연타`). **토스트가 큐를 갖는다** — 기존 `toast()`는 요소 하나의 `textContent`를 덮어써서, 한 충돌에 기믹 셋이 터지면 마지막 하나만 보이고 둘은 사라졌다. 이제 순서대로 재생되며, 뒤에 밀린 게 있으면 표시 시간을 2.2초에서 0.9초로 줄이고 `+N` 배지를 단다. 마지막 한 줄만 2.2초를 온전히 받는다. **윤슬의 칼날**은 속도 82 아래에서 사라지고 있었다. 임계를 10으로 내려 멈추기 직전까지 남고, 회전 상수항을 7.4에서 3.2로 낮춰 속도 비례가 눈에 보이게 했다(피해 판정 게이트 105는 그대로라 밸런스 불변). **온보딩 카드**는 좌하단 10px 글씨에서 화면 가운데 13px로 옮기고, 실습 단계에서는 전장 위가 아니라 창 상단 바로 빠지게 했다. 이 카드에 남아 있던 구 네이비색도 새벽 관측소 팔레트로 맞췄다. **1-3**은 범퍼 2 + 반사 벽 2 + 공허 잔재 2로 기믹 셋을 한꺼번에 담고 있었다. 반사 벽만 남겼다. **허브 칩**은 `업적 · 되찾은 별 0`에서 `업적 0 / 5`가 됐다.
- 막힌 점 / 우회: 첫 큐 구현은 먼저 뜬 메시지가 2.2초를 다 쓰고 대기 수도 안 보였다. `toast()`가 이미 표시 중일 때 현재 표시 시간을 잘라내고 배지를 다시 그리도록 고쳤다. `RIPOSTE`는 처음 찾은 4곳 외에 캔버스 그림자·본문 이중 `fillText`와 `game-feedback.js`의 피격 팝업에 3곳이 더 있었다.
- 사람이 직접 결정·수정한 점: 12개를 한 번에 하지 않고 A(코드)·B(새 시스템)·C(아트) 3단계로 나누는 것, 이번엔 A만 하는 것은 사용자 승인 아래 진행했다. 다음 단계 합의도 함께 받았다 — 캠페인은 **북두칠성을 첫 월드로 삼고 기존 1-x·2-x는 그 꼭짓점으로 이관(기존 유지 불필요)**, 상점 추가분은 **별지기 색 스킨**(소스 팩에 유닛별 5색이 이미 있어 새 아트가 거의 안 든다). 모루 스프라이트는 Warrior로 정했다가, 샛별·그믐이 이미 Warrior라는 사용자 지적으로 재검토 상태다 — 소스 팩에 Warrior·Lancer·Archer·Monk·Pawn 5종뿐이고 전부 배정돼 있어 C단계에서 다시 정한다.
- 남은 것: B단계(별자리 월드, 기믹 5종, 도서관 탭, 출석 보상, 상점 확장)와 C단계(윤슬 roll/attack, 모루 교체). 공허 잔재는 1-3에서 빠진 뒤 전용 스테이지가 생길 때까지 무한 훈련장에만 있다.
- 증거: `npm run verify` / `npm run smoke` 통과. 브라우저 확인 — 일시정지 버튼 `data-pbtn` 없음·36px 원형, 토스트 3연속 투입 시 `공명 범퍼(+2) → 반사 벽(+1) → 연타` 순서로 0.9/0.9/2.2초 재생, 같은 문구 연속 투입은 큐 0으로 억제, `updateBladeWheel` 속도별 강도 `20→0.214 / 82→0.303 / 800→1.0`(이전엔 82 미만 0), 1-3 진입 시 `walls 2 / adds 0 / bumpers 0`, 허브 칩 `업적 0 / 5`, 온보딩 카드 중앙 배치 확인

## 2026-08-12 — 편성·배치 한 화면 통합, 수령 표시, 상점주인 얼굴, 프로필 아이콘

- 작업 내용: 사용자가 실제 플레이에서 지적한 5가지 — 편성창 비율·설명 과다, 업적 수령 표시, 배치 유닛 크기, 상점주인 얼굴 누락, 프로필 아이콘 부재.
- 결과: **편성과 배치를 한 화면으로 합쳤다.** 위쪽은 실제 720×900 비율의 전장 미니맵이고 자리마다 별지기가 초상·이름·발동 역할과 함께 서 있으며, 아래 트레이에 보유 별지기가 전부 나열된다. 트레이에서 자리로 끌어 놓거나(HTML5 drag) 눌러서 지정하고, 배치된 별지기끼리는 자리를 맞바꾼다. 헤더 3줄 + 스테이지 그리드 + 파티 스트립 + 카드 그리드 + 우측 상세 패널이던 구성이 헤더 2줄 + 미니맵 + 상세 카드 1장 + 트레이로 줄었다. `deployed[i]`가 곧 배치이며 `selected`는 이를 미러링한다. 배치 마커는 46px에서 76px(초상 64px)로 키웠다. 업적은 5열 233px 그리드를 3열 304px로 넓혀 8~~9px 글씨를 11~~13px로 올렸고, 수령 가능한 카드에는 빨간 `수령` 배지가 맥동한다. `.claim-badge`를 황동에서 빨강으로 바꿔 허브 칩·업적 카드·우편함이 같은 기호를 쓴다. 상점주인 오르는 `☄` 글리프 대신 픽셀 킷의 `orr` 스프라이트를 쓴다. 프로필에 아이콘 선택창을 붙였다 — 별·달·해·하트 절차 실루엣, 달토끼·우주비행사·망원경·나침반·오르 스프라이트, 룬석 7종으로 16개 선택지이며 선택은 로컬에 저장되고 허브 아바타에도 반영된다.
- 막힌 점 / 우회: (1) **`showDeployment()`가 `for (let i = 0; i < 3; i++)`로 슬롯 3개를 가정하는데 3번째 자리 해금 전 파티는 2명이라, `deployed[2]`가 `undefined`인 채 `game-session.js:465`에서 터졌다.** 1-1 클리어 전에는 배치 화면 진입이 불가능했다는 뜻이다. 통합하면서 `slotCount` 기준으로 고쳤다. (2) 새 화면을 flex row로 짰더니 미니맵이 174×217로 줄었다. `height:100%`가 정의된 높이를 찾지 못해 flex 줄 높이가 상세 카드에서 나오는 순환이었고, `grid-template-rows: minmax(0,1fr)`로 바꿔 해결했다. (3) 검수 중 `getBoundingClientRect()`가 전부 0을 반환했는데, 헤드리스 창이 문서를 숨김으로 보고할 때의 값이었다. 치수 판단은 스크린샷으로 했다. (4) 새 픽셀 실루엣 코드 주석에 적은 `file://` 문자열이 `check-portability.mjs`의 절대경로 검사에 걸려 `npm run verify`가 실패했다. 문구를 바꿨다.
- 사람이 직접 결정·수정한 점: 다섯 항목 모두 사용자 지적이다. 편성과 배치를 두 단계로 두지 않고 한 화면으로 합치는 것, 수령 표시를 유성 도트가 아니라 빨간 배지로 하는 것은 사용자가 선택했다. 새 아이콘·초상은 만들지 않고 픽셀 킷이 절차 생성하는 실루엣과 기존 룬석만 쓴 것은 `ASSET_BACKLOG.md`의 지연 반입 규칙 때문이다. 하트·해 실루엣은 킷의 `shape()`에 판정식 두 개를 더한 것이라 파일이 늘지 않는다.
- 남은 것: 전장 미니맵의 자리 역할은 아직 한글 라벨이며 아이콘이 없다(백로그 `전장 자리 역할 아이콘`). 프로필 패널 아이콘·우편 봉투는 여전히 CSS 대체 표현이다. 온보딩 루나 대화 카드의 구 네이비색과 전투 캔버스 바닥 톤은 이전 항목 그대로 남아 있다.
- 증거: `npm run verify` / `npm run smoke` 통과, 브라우저 확인 — 2인·3인 파티 모두 편성 화면 정상 렌더, 트레이→자리 교체(`deployed` `["biyeon","gaon","ria"]` → 별하 투입 후 `["lumi","gaon","ria"]`)와 자리끼리 맞바꾸기 동작, 시작 → 강화 선택 → 전투 진입 시 `gates`가 `lumi@182,542 / gaon@538,542 / ria@360,470`으로 배치 순서 그대로 생성, 강화 화면 뒤로가기가 편성 화면으로 복귀, 아이콘 선택 후 `localStorage["stella-ball.profile-icon"]="heart"`와 허브 아바타 반영, 콘솔 에러 0건

## 2026-08-12 — 새벽 관측소(Dawn Observatory) UI 킷 반입

- 작업 내용: 외부에서 받은 `dawn-patch` 묶음(6파일)을 저장소에 반입하고, 검증 게이트와 문서 규칙을 새 로드 순서에 맞췄다.
- 결과: `stella-ball-theme.css`가 v2 "Ink & Brass"(네이비·황동)에서 v3 "Dawn Observatory"(청록-잿빛 밤 + 살구빛 여명)로 바뀌었다. diff 1200줄은 전부 색 값과 헤더 주석 2줄이며 셀렉터·구조 변경은 없다. 신규 3파일이 붙었다 — `stella-ball-pixel-ui.js`(캔버스로 픽셀 실루엣·스프라이트를 dataURL 생성, `window.StellaPixelUI` 노출), `stella-ball-dawn.js`(MutationObserver로 게임이 그린 버튼에 `data-pbtn` 자동 부여 + `#dawn-sky` 배경 데코), `stella-ball-dawn.css`(`[data-pbtn]` 표면과 데코 키프레임). 테마 기준 문서는 `UI_KIT_DAWN.md`가 되고 `UI_REDESIGN_HANDOFF.md`는 대체 표기만 남겼다.
- 막힌 점 / 우회: (1) `scripts/smoke-runtime.mjs`의 `expectedStyles`/`expectedScripts`가 정확한 순서 일치 검사라 파일만 넣으면 smoke가 즉시 실패했다. 새 스타일 1개와 스크립트 2개를 목록에 추가했다. (2) 검수 중 전투 캔버스가 완전히 검게 나와 반입 사고를 의심했으나, `git stash`로 되돌린 HEAD에서도 동일하게 검었고 `document.visibilityState === "hidden"`이었다. 헤드리스 브라우저 창이 페이지를 숨김으로 보고해 렌더 루프가 대기 상태로 있었을 뿐이다(`ARCHITECTURE.md`의 browser-hidden 규칙). `draw()`를 한 번 직접 호출하니 캔버스 표본의 100%가 채워졌고 보스·유성·회랑 타일이 정상이었다.
- 사람이 직접 결정·수정한 점: 반입 자체와 push는 사용자의 지시다. 문서까지 같은 커밋에 넣는 범위도 사용자가 선택했다. 패치가 `stella-ball-theme.css` 뒤에 `stella-ball-dawn.css`를 놓기 때문에 "테마가 마지막 스타일시트"라는 기존 규칙과 충돌했고, 마지막 자리를 dawn으로 넘기는 쪽으로 규칙을 고쳤다.
- 남은 것: 전투 캔버스 바닥 톤(`game-core-render.js`의 스테이지 아트 캐시 색)은 여전히 보라색으로, 주변 UI의 청록-살구와 어긋난다. 판정 가독성 검증 후 별도 패스로 둔다. 온보딩 루나 대화 카드도 구 네이비색이 남아 있다. `star` 실루엣은 킷에만 있고 자동 매핑에는 미사용이다.
- 증거: `npm run verify` / `npm run smoke` 통과(스타일시트 7, 런타임 스크립트 14), 로컬 서버 브라우저 확인 — 타이틀·허브·상점 화면에서 `[data-pbtn]` 요소 전부가 픽셀 실루엣을 받고 태그 누락 버튼 0개, 콘솔 에러 0건, 에셋 요청 404 0건

## 2026-08-12 — 업적창 스크롤·수령 손맛·타이틀 잔상 제거

- 작업 내용: 업적창에서 뒤로가기가 화면 밖으로 잘리고 스크롤도 없던 문제, 밋밋한 수령 반응, 타이틀 배경에 남아 있던 이전 브랜드 아트를 정리했다.
- 결과: 업적창을 다른 탭과 같이 전체 화면(`archive-scene`)으로 올리고, 업적 그리드만 스크롤 영역으로 두고 뒤로가기를 하단에 고정했다. 공용 `.system-panel`이 자동 좌우 여백으로 가운데 정렬돼 폭이 440px에 묶여 있던 것도 풀어 980px를 쓴다. 수령은 클래시 로얄 상자를 참고해 누를 때 눌리고(scale .94), 카드가 오버슈트로 튀며(`claimPop`), 금액이 위로 떠오르고 스파크 6개가 흩어진 뒤에야 화면이 갱신된다. 타이틀 배경의 `video-title-card.png`(구 Prism Breakers 키아트)를 관측소 그라디언트로 교체했다.
- 막힌 점 / 우회: 카드를 즉시 다시 그리면 애니메이션이 잘리고 숫자가 손가락 아래에서 바뀌어, `playClaimBurst()`가 620ms 뒤에 갱신 콜백을 부르도록 분리했다. 타이틀 잔상은 텍스트가 아니라 `.title-sequence:before`의 배경 이미지여서 DOM 텍스트 검색으로는 잡히지 않았고, 레이어 스택을 훑어 찾았다.
- 사람이 직접 결정·수정한 점: 세 항목 모두 사용자가 실제 플레이에서 지적했다. 수령 연출의 레퍼런스(클래시 로얄 상자 터치)도 사용자가 지정했다.
- 증거: `.overlay.archive-scene` 규칙, `playClaimBurst()`, `.title-sequence:before` 교체, 검증(뒤로가기 노출·그리드 스크롤 250/559·수령 시 카드 claiming + `+200` 버스트 + 스파크 6 + 잔액 0→200→400)

## 2026-08-12 — 프로필·랭킹·우편함, 1-3, 전체화면 메뉴, 에셋 장부 최신화

- 작업 내용: 소환/상점/프로필 탭 전체화면화, 타이틀 이탈 확인 팝업, 프로필(순위·우편함) 신설, 1-3 스테이지, 클리어 스테이지 점등, 스테이지명 잘림 해결, 그리고 이 모든 화면의 에셋 요구 기록.
- 결과: 소환·상점·프로필이 편성/배치와 같이 `fixed`로 창 전체를 쓴다. 하단 탭은 `프로필·소환·관측·상점·설정`이 되고, 타이틀 복귀는 프로필 안의 버튼 + 확인 팝업(`showConfirm`)으로 옮겼다. 프로필은 관측자 기록·순위표·우편함 3패널이며, 순위표에는 로컬 기록임을 명시했다. `1-3 침식의 계단`을 반사 벽 2개와 공허 잔재 2기로 열었다(기믹 모듈 재사용, 새 코드 없음). 클리어한 노드는 황동 테두리·별 점등·`★` 배지로 빛나고, 임무 바와 노드의 긴 이름은 `applyMarquee`로 흘러간다. 상점에는 도색장 오르와 보유 골드에 반응하는 대사를 붙였다.
- 막힌 점 / 우회: 임무 바의 기믹 표기가 범퍼 수만 세고 있어 1-3이 「공명 범퍼 × 2」로만 보였다. 벽·발판·잔재를 모두 나열하도록 고쳤다. 마퀴는 실제로 넘치는 요소에만 붙여야 짧은 이름이 흔들리지 않으므로, 렌더 후 `scrollWidth` 비교로 클래스를 붙인다.
- 사람이 직접 결정·수정한 점: 탭 5칸을 유지하기 위해 타이틀 탭을 빼고 프로필을 넣는 구성, Hive는 이번에 UI·로컬 기록까지만 만들고 실통신은 별도로 두는 범위는 사용자 승인 아래 진행했다.
- 증거: `showProfile`·`showConfirm`·`applyMarquees`·`isStageCleared`, `stages[2].gimmicks`(벽 2 / 잔재 2 런타임 생성 확인), `ASSET_BACKLOG.md`에 상점·소환·프로필·점등·1-3 기믹·사운드 6개 항목 추가

## 2026-08-12 — 골드를 자동 지급에서 업적 탭 수령으로 전환

- 작업 내용: RIPOSTE 업적을 참고해, 골드가 조용히 늘어나는 대신 업적 탭에 들어가 직접 수령하는 구조로 바꿨다.
- 결과: 스테이지 클리어 보상은 `관측 보상함`에 적립되고(`accrueGold`), 업적 5종은 각각 일회성 골드 보상(150·100·200·400·300)을 갖는다. 업적 화면 상단의 보상함 카드에서 「모두 수령」, 각 업적 카드에서 「수령하기」를 누르면 그때 잔액에 들어가고 `rewardToast`가 뜬다. 수령 완료는 `progress.claimedAchievements`에 남아 중복 수령이 불가능하다. 허브 상단 기록 칩에 수령 대기 수 배지를 붙여 들어갈 이유를 만든다.
- 막힌 점 / 우회: 업적 카드 마크업 교체가 한 번 누락돼 `pendingCard`가 정의되지 않은 채 참조되어 업적 화면이 통째로 열리지 않았다. 문자열 종료 따옴표가 `'`와 `"`로 어긋난 오타도 함께 있었다. 둘 다 고친 뒤 콜드 스타트로 재검증했다.
- 사람이 직접 결정·수정한 점: 수령형 전환은 사용자의 지시다. 클리어 골드를 없애지 않고 보상함 적립으로 돌린 것은, 업적 5종만으로는 500골드 도색을 살 반복 수입이 끊기기 때문이다.
- 증거: `accrueGold`·`claimAchievement`·`claimPendingGold`·`claimCount`(`game-meta.js`), 보상함 카드와 `.achievement-claim`, 검증(대기 300 → 수령 후 잔액 300·대기 0, RIPOSTE 수령 후 잔액 500·재수령 불가, 클리어 시 잔액 불변·대기 +100)

## 2026-08-12 — 편성 여백·상점 가격·업적 접근성 정리

- 작업 내용: 전체화면 전환 뒤 남은 편성창 여백, 가격이 버튼에만 있던 상점, 이름이 없어 찾기 어려운 업적 입구, 메인 CTA 문구를 손봤다.
- 결과: 편성 그리드가 남는 높이를 채우도록 `grid-auto-rows: minmax(64px, 1fr)`로 늘리고 상세 패널을 세로로 늘려 상단 정렬했다. 카드 아래 빈 공간이 492px 중 26px로 줄었다. 상점은 카드마다 `기본 지급 / 500 골드 / 구매 완료 · 500 골드` 가격 줄을 항상 보여 주고, 버튼은 「구매하기」로 행동만 말한다. 허브 상단 기록 칩은 「업적 · 되찾은 별」로 이름을 붙이고 hover를 넣었다. 타이틀과 허브의 시작 버튼을 모두 「게임 시작!」으로 통일했다.
- 막힌 점 / 우회: 가격 줄을 넣자 버튼과 「500 골드」가 두 번 보여 버튼 문구를 행동형으로 바꿨다. 편성의 「뒤로」가 허브가 아니라 별도 지도 화면으로 빠지고 있었는데, 허브가 이미 지도를 품고 있어 불필요한 우회라 허브 복귀로 바꿨다.
- 사람이 직접 결정·수정한 점: 편성창 깨짐·가격 미표시·업적 발견성·CTA 문구는 사용자가 실제 플레이에서 지적했다.
- 증거: `showShop`의 `.shop-price`, `.roster-shell` 그리드 규칙, `hubRecordChip`, 타이틀/허브 CTA 문구, 흐름 검증(칩→업적→허브, 편성→허브)

## 2026-08-12 — 각성 가독성·유성 도색 상점·전체화면 편성

- 작업 내용: 충돌로 깨어난 별지기가 눈에 안 띄는 문제, 좁은 편성/배치 화면, 허브 탭 구성, 외형 상점, 보상 수령 연출을 한 번에 정리.
- 결과: 깨어난 별지기는 링 폭발 + 「깨어남!」 팝업 + 샷이 끝날 때까지 남는 회전 점선 후광으로 표시한다(정산의 「각성」과 라벨을 분리). 샛별의 사거리 205는 상시 옅은 원으로 그린다. 편성·배치는 `position: fixed`로 창 전체를 쓰며, 배치 지도는 실제 전장과 같은 720:900 비율에 마커를 46px로 줄이고 「어디에 세울지」 설명을 붙였다. 허브 탭은 `타이틀·소환·관측·상점·설정`이 되고 업적은 상단 기록 칩으로 옮겼다. 편성 진입은 스테이지 시작 직후로 일원화해 허브의 3칸 슬롯을 제거했다. 관측소 상점에서 유성 도색 4종을 개당 500골드로 판매한다. 1-1 완료 보상은 골드가 아닌 무료 소환권 1장이며, 그 자리에서 바로 소환하도록 유도한다. 클리어 골드와 상점 구매는 RIPOSTE식 승격 연출을 참고한 `rewardToast` 수령 카드로 알린다.
- 막힌 점 / 우회: 도색은 새 에셋 없이 기존 프리즘 원석에 `hue-rotate`를 거는 팔레트 스왑인데, 처음 고른 각도가 이름과 색이 어긋났다(잔불이 파랗게 나옴). 캔버스에서 각도별 평균 RGB를 실제로 측정해 128/300/200으로 다시 잡았다. 편성 화면은 `.stage` 안쪽 오버레이라 좁은 컬럼에 갇혀 있었고, 조상 중 `transform`을 만드는 요소가 없음을 확인한 뒤 `fixed`로 뷰포트에 붙였다.
- 사람이 직접 결정·수정한 점: 상점 도입과 500골드 가격, 편성을 스테이지 진입 단계로 옮기는 구조, 첫 소환 무료는 사용자의 결정이다. 무료 소환은 골드 지급이 아닌 소환권으로 처리해 "1-1은 골드를 주지 않는다"는 기존 경제 규칙을 유지했다. `PROJECT_CONTEXT`의 원칙 5는 현금 결제 금지는 유지하되 획득 골드 기반 외형 상점을 허용하도록 갱신했다.
- 증거: 당시 `METEOR_SKINS`·`ECONOMY.skinCost`(`game-data.js`), `showShop`·`rewardToast`·`hasFreeSummon`(`game-meta.js`), `wakeUnit`·`SLASH_RANGE`(`game-combat.js`), 스킨별 캔버스 색상 측정(prism 165/148/224, ember 211/146/105, abyss 99/171/203, verdant 121/174/96), 콜드 스타트 6단계 → 실전 처치 → 무료 소환 1회 완주. 현재 샛별은 사거리 표시를 쓰지 않고 `GAON_CLOSE_RANGE`로 근거리 피해만 판정한다.

## 2026-08-12 — 메타 허브를 별자리 지도 + 하단 탭바로 재구성

- 작업 내용: 허브 하단 약 45%가 비어 있고 별자리 지도는 `스테이지 변경`을 눌러야 나오는 별도 화면이라 한 단계 깊었다. 지도를 허브 본문으로 올리고 메뉴를 하단 탭바로 내려 화면을 채운다.
- 결과: 허브가 `상단바 → 별자리 지도 → 임무 바 → 하단 탭바` 4단 세로 플렉스가 됐다. 지도가 남는 높이를 전부 차지한다(1440×900에서 525px). 별을 누르면 선택이 바뀌고 임무 바의 스테이지명·규칙·편성과 CTA가 함께 갱신되며, `1-1`을 고르면 CTA가 「수업 다시 보기」로 바뀐다. 탭바는 `편성·소환·관측·업적·설정` 5개이고 가운데 `관측`만 금색으로 띄웠다. 노드 목록은 `constellationMapStages()`로 뽑아 전체 지도 화면과 공유하므로 잠금 해제 시 한 곳만 고치면 된다.
- 막힌 점 / 우회: 탭 버튼이 공용 `.overlay button` 규칙에 덮여 금색 강조가 먹지 않아 탭바 팔레트를 `!important`로 다시 선언했다. 좁은 폭에서 `.constellation-node span`으로 설명줄을 숨기려 했더니 `.stage-star`와 `.stage-copy`도 같은 `span`이라 노드가 통째로 비었다. `.stage-copy > span`으로 좁혀 고쳤다. 좁은 화면은 노드를 별 배지 형태로 접고 지도가 남은 높이만 쓰도록 했다.
- 사람이 직접 결정·수정한 점: 클래시 로얄식 하단 탭바와 빈 공간을 별자리로 채운다는 구성은 사용자의 지시다. 이번 범위는 웹 화면 기준이며 모바일은 깨지지 않는 수준까지만 맞춘다.
- 증거: `showMeta()`(`game-onboarding.js`), `constellationMapStages()`(`game-meta.js`), `stella-ball-theme.css`의 HUB MAP MODE 절, 1440×900 레이아웃 측정(지도 525 / 임무 95 / 탭바 77, 오버플로 없음)과 탭·노드·CTA 동작 확인

## 2026-08-12 — 6단계 온보딩·실전 마무리·일시정지

- 작업 내용: 11단계 온보딩이 게임 이벤트로 카드를 자동 전환해 읽는 중에 넘어가는 문제를 고치고, 마지막을 직접 처치로 바꾸고, 전투 중 일시정지를 추가.
- 결과: 온보딩을 6장으로 압축하고 모든 전환을 버튼 입력으로 통일했다. 전투 이벤트(직격·미리내 접촉·윤슬 칼날)는 플래그만 기록하고, 결과 카드는 테이블 정지 후에만 돌아온다. 같은 목표를 두 번 놓치면 `괜찮아요, 다음으로`가 열린다. 마지막 6단계는 불멸 해제·체력 120·유성 무제한의 실전이며, 처치하면 업적 「첫 관측자의 증명」으로 직행한다. `ESC`/정지 버튼 일시정지에서 설정을 열면 허브가 아닌 정지 메뉴로 복귀한다.
- 막힌 점 / 우회: 6장 전환 후 실습 발사가 전부 막혔다. 구 11장 구조의 `dialogue === 0` 발사 가드가 남아 있었기 때문으로, 새 구조에서는 dialogue 0이 곧 첫 실습이다. 가드를 "카드가 화면에 있는 동안"으로 바꿔 해결했다. 같은 뿌리로 실습 조준 가이드도 dialogue 0에서 그려지지 않아 동일 기준으로 고쳤다. `무한 훈련장`은 HUD와 달리 5발 뒤 실패 화면이 떠서, 훈련·수업 전장의 자동 재장전을 발사 소진 지점에서 복구했다. 토스트와 정지 버튼이 우상단에서 겹쳐 버튼을 좌상단으로 옮겼다.
- 사람이 직접 결정·수정한 점: 6장 압축과 “마지막은 직접 잡는다”는 구성, 실패 시 2회 뒤 통과 허용, 저작권 표기 보류. 발사 불능은 실제 플레이에서 발견해 보고했다.
- 증거: `prototypes/js/game-onboarding.js`, `prototypes/js/game-combat.js`(발사 가드), `prototypes/js/game-session.js`(`showPauseMenu`), `RULES.tutorialCoreHp`, `prototypes/stella-ball-theme.css`의 PAUSE 절, 실제 포인터 이벤트 발사 검증(실습·실전·카드 차단)

## 2026-08-07 — 방향 전환과 발사형 코어 선택

- Codex에게 시킨 일: 행사 기준과 Hive·컴투스·OpenAI/Codex의 기대를 분석하고, 여러 조작 코어의 브라우저 프로토타입을 제작.
- 결과: 초기 Echo Run, 보드/블록형 시안, 그리고 발사 물리 기반의 Prism Breakers 프로토타입을 비교했다. 현재 `prototypes/prism-breakers.html`에는 6인 중 3인 파티 편성, 드래그 발사, 반사, 움직이는 보스 약점, 캐릭터별 물리 능력이 있다.
- 막힌 점 / 우회: 유저 사망 기록을 전 세계 난이도에 누적하는 구조는 유저가 많을수록 난해해지고, 감소하면 더 재미없어지는 하향 나선 위험이 있었다. 온라인 데이터를 코어에서 제거하고, 싱글 플레이를 완결한 뒤 기록·랭킹만 추가하는 구조로 전환했다.
- 사람이 직접 결정·수정한 점: 보드형 조작은 직관적이지 않다고 판단해 폐기. World Flipper에서 참조한 것은 핀볼 자체가 아니라 ‘즉시 이해되는 물리 장난감과 수집형 능력의 결합’이며, 최종적으로 당겨 쏘는 반사 유물탄으로 변형했다.
- 증거: [기준선 커밋 `76a8a3d`](https://github.com/8rulerstar/prism-breakers/commit/76a8a3da7f6c9f80d8edfb944e8f15804048c5fe), [자동 검증 리포트](https://github.com/8rulerstar/prism-breakers/actions/runs/31156765698), [첫 GitHub Pages 배포](https://github.com/8rulerstar/prism-breakers/actions/runs/31156820634), `GAME_DIRECTION.md`, `PROJECT_CONTEXT.md`, `CODEX_COLLABORATION.md`, `prototypes/prism-breakers.html`

## 다음 작업일에 기록할 것

- 제한 발사 수 / 보스 실드·페이즈 / 스킬 쿨다운을 어떤 순서로 넣었는가
- 캐릭터 능력이 단순 수치 상승이 되지 않도록 무엇을 제거하거나 바꿨는가
- 헤드리스 봇의 수치 결과와 그에 따른 사람의 밸런싱 판단

## 2026-08-07 — 픽셀 아트 자산 선별

- Codex에게 시킨 일: 제공된 에셋 팩과 기존 프로젝트를 조사해 화풍·프레임 규격·출처를 비교하고, Prism Breakers에 필요한 최소 에셋만 선별·정리.
- 결과: Tiny Swords 계열의 6인 파티 스프라이트, 트롤 보스, CC0 VFX, 가장자리 지형 장식을 `assets/`에 반입했다. 프리즘 탄·6종 룬·보스 약점은 게임 전용 픽셀 SVG로 새로 제작했고, `prism-breakers.html`의 파티 편성·전장·발사체·약점 렌더링에 연결했다.
- 막힌 점 / 우회: 서로 다른 16×16·100px·192px 팩을 동시에 쓰면 인상이 분산된다. 캐릭터와 보스는 Tiny Swords 계열로 통일하고, 기존 UI 팩은 이번 MVP에서 제외했다.
- 사람이 직접 결정·수정한 점: 아트 볼륨을 늘리기보다, 한 발의 궤적과 룬 능력을 명확히 읽히게 하는 데 필요한 에셋만 채택했다.
- 증거: `ASSET_PLAN.md`, `assets/ASSET_MANIFEST.json`, `assets/ATTRIBUTION.md`, `assets/original/`

## 2026-08-07 — 전투 수치와 강화 UI

- Codex에게 시킨 일: 현재 발사 데모를 제한 발사 수, 보스 방벽, 코어 노출, 강화 카드, 피해 피드백을 갖춘 짧은 보스전으로 확장하고 RuneCast의 UI 정보 계층을 웹에 맞게 재구성.
- 결과: 3발 안에 방벽을 깨고, 강화 하나를 선택한 뒤, 다시 3발로 코어를 공략하는 흐름을 구현했다. 피해는 기본 피해·룬 연계·표식·반사·강화가 합산되며, 게이지·부유 숫자·토스트로 결과를 전달한다.
- 막힌 점 / 우회: 첫 강화 선택 뒤 카드 오버레이가 전투 시작 화면에 남았다. 전투 단계 전환 함수에서 오버레이를 닫도록 수정하고, 실제 브라우저에서 강화 선택·발사·콘솔 오류 0을 재검증했다.
- 사람이 직접 결정·수정한 점: 강화는 수치가 많아지는 메타 시스템이 아니라 한 판의 반사 계획을 바꾸는 1회 선택으로 제한했다. RuneCast에서 가져온 것은 UI의 안전 영역·게이지·카드 계층이지, 원본 UI 아트가 아니다.
- 증거: `prototypes/prism-breakers.html`, [픽셀 아트 기준선 커밋](https://github.com/8rulerstar/prism-breakers/commit/d93304987676e8de065ac51ae52457e829cdebdc)

## 2026-08-07 — 파티 간 궤적 연계

- Codex에게 시킨 일: 캐릭터 능력을 단순 가속·재발사 수치가 아닌, 유물탄의 다음 행동을 바꾸는 연계 기믹으로 확장.
- 결과: 살별은 닿은 뒤 다음 파티 룬으로 공을 자동 전달하고, 달무리는 공이 멈출 때 줄로 연결해 원심 회전 뒤 재발사한다. 두 효과 모두 룬의 배치 순서와 다음 능력 발동을 설계하게 만든다.
- 막힌 점 / 우회: 기존 ‘가속’과 ‘되감기’는 결과가 숫자와 속도 변화로만 읽혀 수집형 파티 조합의 이유가 약했다. 전달선과 줄 효과를 Canvas에 직접 그려, 발동 원인과 다음 궤적이 화면에서 보이도록 전환했다.
- 사람이 직접 결정·수정한 점: 능력의 목표를 피해량 증가가 아니라 ‘다음 공의 상태를 바꾸는 규칙’으로 정했다. 살별은 파티 순서를, 달무리는 멈춘 공의 두 번째 기회를 담당한다.
- 증거: `prototypes/prism-breakers.html`의 `updateRelay`, `beginOrbit`, `updateOrbit`.

## 2026-08-07 — 스테이지별 고정 룬석 배치

- Codex에게 시킨 일: Sdorica Wonderland의 고정 배치 발상처럼, 스테이지마다 서로 다른 룬석 좌표를 두고 유닛을 배치하는 흐름으로 확장.
- 결과: 균열 회랑·침식의 계단·원심 정원 3개 스테이지를 만들었다. 각 스테이지는 룬석 세 곳과 보스 위치가 다르며, 파티 3인을 고른 뒤 룬석 사이에서 교체하고 전투를 시작한다.
- 막힌 점 / 우회: 기존에는 파티를 고르면 항상 같은 자리에 자동 배치돼, 유닛 조합만 바뀌고 전장 해석이 남지 않았다. 스테이지 선택 → 파티 선택 → 룬석 배치의 짧은 3단계로 분리했다.
- 사람이 직접 결정·수정한 점: 룬석 위치는 전투 중 이동시키지 않는다. 위치는 스테이지가 정하고, 플레이어는 어떤 유닛을 어느 위치와 연계 순서에 둘지만 결정한다.
- 증거: `prototypes/prism-breakers.html`의 `stages`, `showDeployment`; 브라우저에서 02 침식의 계단의 별하·샛별 교체와 전투 진입 확인.

## 2026-08-07 — 조건부 궤적 능력과 스프라이트 배치

- Codex에게 시킨 일: 세 번째 충돌에 100% 약점 유도로 전환하는 유닛과, 충돌 뒤 클릭 한 번으로 공을 우회전시키는 유닛을 추가하고 배치 화면의 카드도 실제 픽셀 스프라이트로 변경.
- 결과: 성위 관측자 그믐과 나침반 기사 리오를 추가했다. 그믐의 성위는 같은 공에서 3회 누적되고, 리오의 우회전은 한 발에 한 번만 사용할 수 있다. 배치 화면에서는 실제 캐릭터 스프라이트를 고정 룬석으로 드래그한다.
- 막힌 점 / 우회: 텍스트 카드 드래그는 전술 배치라는 느낌이 약했다. Tiny Swords의 기존 팔레트 변형 스프라이트 두 장만 추가해 화면 화풍을 유지하고, 마우스·터치 계열 포인터 조작 모두에서 교체되도록 보완했다.
- 사람이 직접 결정·수정한 점: 그믐은 세 번째 **같은 유닛 충돌**에서만 발동하게 해 자동 유도 남발을 막고, 리오는 재충돌해도 같은 발사 안에서는 우회전 사용 횟수를 되돌리지 않는다.
- 증거: `assets/characters/nyx-oracle-idle.png`, `assets/characters/rio-compass-idle.png`, `prototypes/prism-breakers.html`; 브라우저에서 리오 스프라이트를 1번 룬석으로 드래그해 교체 확인.

## 2026-08-07 — 충돌 보상과 능동 발사 타이밍

- Codex에게 시킨 일: 난이도를 피해 수치로만 낮추지 않고, 룬 충돌이 속도·운동량·입력 타이밍을 만들어 내도록 전투 피드백을 강화.
- 결과: 모든 룬 충돌에 속도 보존/가속과 광륜을 추가했다. 리오는 클릭 시 90° 우회전과 함께 운동량을 얻고, 달무리는 멈춘 공을 자동으로 내보내지 않고 최대 1.7초 동안 원심 회전시킨 뒤 클릭 순간의 충전량으로 발사한다.
- 막힌 점 / 우회: 자동 재발사는 능력이 일어났다는 사실은 보이지만 플레이어의 선택이 남지 않았다. ‘대기 상태 → 화면 위 안내 → 클릭 발사’로 바꿔 같은 능력이 타이밍 기술이 되게 했다.
- 사람이 직접 결정·수정한 점: 각 룬 충돌은 피해량보다 먼저 공을 계속 살아 있게 만드는 보상이어야 한다고 결정했다. 리오의 회전은 속도를 보존하는 대신, 의도적으로 추가 운동량을 줘 다음 선택지가 생기도록 했다.
- 증거: `prototypes/prism-breakers.html`의 `releaseOrbit`, `updateOrbit`, 리오의 `pointerdown` 명령; 브라우저에서 명중 후 클릭 충격 전환 확인.

## 2026-08-07 — 전방위 보스 피해와 약점 보상

- Codex에게 시킨 일: 약점만 맞혀야 피해가 들어가는 높은 조준 난도를 완화하고, 보스 몸체와 빛나는 약점의 보상을 분리.
- 결과: 보스 몸체 어디에 맞혀도 기본 피해가 들어가고, 이동하는 빛나는 약점은 1.8배 피해와 표식·치명 보정을 받는다. 몸체·약점 모두 피해 숫자와 토스트의 색·문구가 다르다.
- 막힌 점 / 우회: 약점만 유효하면 룬 연계를 만들었어도 마지막 몇 픽셀의 조준 실패가 한 발 전체를 무효화했다. 몸체 타격을 안전한 기본 보상으로 두고 약점을 고위험·고보상 목표로 전환했다.
- 사람이 직접 결정·수정한 점: 약점은 ‘유일한 정답’이 아니라, 잘 설계한 궤적을 크게 보상하는 목표가 되어야 한다고 판단했다.
- 증거: `prototypes/prism-breakers.html`의 `damage(weak=false)`; 브라우저에서 보스 몸체 직격 후 `몸체 타격 15 피해` 확인.

## 2026-08-07 — 충격감과 관대한 전투 루프

- Codex에게 시킨 일: 난도를 숫자만으로 낮추지 않고, 룬 충돌과 보스 명중이 시각·시간·운동량 측면에서 강하게 보상되도록 전투 감각을 재구성.
- 결과: 기본 발사를 단계당 4발로 늘리고 방벽/코어 수치를 낮췄다. 룬 충돌은 더 큰 속도 보존·가속을 주며, 보스 명중은 히트스톱·화면 흔들림·플래시·가능한 기기의 햅틱을 발생시킨다. HUD에 실시간 운동량도 표시한다.
- 막힌 점 / 우회: 피해 숫자만 커지면 성공·실패의 감각은 여전히 평평하다. 명중 직후 시간을 짧게 끊고 화면과 공의 속도를 함께 반응시키며, 몸체 타격도 안전한 진전으로 유지했다.
- 사람이 직접 결정·수정한 점: 정확한 약점 명중은 강한 보상으로 남기되, 한두 번의 실수로 판 전체가 무의미해지지 않게 했다. 현재 수치는 플레이 감각을 보기 위한 적극적 기준선이며, 이후 봇/플레이테스트 결과로 조정한다.
- 증거: `prototypes/prism-breakers.html`의 `impact`, `updateFeedback`, `momentumText`; 브라우저에서 몸체 명중 후 피해·운동량 표기·콘솔 오류 0 확인.

## 2026-08-07 — 당구식 이어쏘기와 연속 명중 콤보

- Codex에게 시킨 일: 매 발을 고정 시작점으로 되돌리는 구조를 당구처럼 멈춘 위치에서 이어 쏘는 구조로 바꾸고, RIPOSTE 레퍼런스를 단계형 연속 명중 피드백으로 변형.
- 결과: 다음 유물탄은 이전 공이 멈춘 좌표에서 출발한다. 보스 명중은 1.18초 동안 `COMBO xN`으로 누적되고, 3회부터 `RIPOSTE!` 표기·강화된 팝업·히트스톱·화면 충격으로 승격된다.
- 막힌 점 / 우회: 이전 구조는 매 발의 공간적 결과가 사라져 각도 선택이 분절됐다. 공의 최종 위치를 다음 샷의 시작 조건으로 보존해, 좋은 위치를 남기는 것이 다음 턴의 자산이 되도록 했다.
- 사람이 직접 결정·수정한 점: 콤보는 룬을 몇 개 통과했는지가 아니라 실제 보스에게 연속으로 닿았는지를 기준으로 한다. 그래서 수치 보상보다 ‘계속 압박하고 있다’는 감각을 전달한다.
- 증거: `prototypes/prism-breakers.html`의 `startShot`, `registerBossHit`, `drawCombo`; 브라우저에서 이어쏘기 위치와 `COMBO · 1 HIT` HUD 확인.

## 2026-08-07 — 룬 유닛의 직접 지원 공격

- Codex에게 시킨 일: 유물탄이 유닛을 통과했을 때 궤적 보정만 하지 않고, 해당 유닛이 보스에게 직접 공격하는 전투 행동을 추가.
- 결과: 각 룬 충돌은 유닛 색의 지원 투사체를 보스에게 발사한다. 지원 공격은 고유한 이름/색의 피해 팝업을 띄우고, 보스 콤보·히트스톱·화면 피드백에도 포함된다.
- 막힌 점 / 우회: 유닛 능력이 공의 상태를 바꾸는 효과에만 머물면, 캐릭터가 실제로 전투에 참여한다는 감각이 약했다. 유닛→보스 투사체를 짧게 보여준 뒤 직접 피해를 적용해 충돌 자체를 공격 미션으로 바꿨다.
- 사람이 직접 결정·수정한 점: 지원 공격은 유물탄 피해와 별개지만, 같은 보스를 연속 압박하는 행동이므로 콤보에는 포함한다.
- 증거: `prototypes/prism-breakers.html`의 `triggerAssist`, `resolveAssist`, `drawAssists`; 브라우저에서 `샛별 지원 명중 11` 및 `COMBO · 1 HIT` 확인.

## 2026-08-07 — 새 전투의 고정 발사 지점

- Codex에게 시킨 일: 당구식 이어쏘기는 유지하되, 새 스테이지 전투까지 이전 런의 공 위치가 이어지는 문제를 수정.
- 결과: 같은 전투의 다음 샷은 정지 위치에서 계속 시작하지만, 새 전투는 공·지원 투사체·콤보를 비우고 언제나 고정 출발점에서 시작한다.
- 막힌 점 / 우회: `startShot`이 공의 마지막 좌표를 읽는 구조라 전투 시작과 샷 전환을 구분하지 못했다. 새 전투 진입 직전에 공 상태를 명시적으로 초기화해 두 경우를 분리했다.
- 사람이 직접 결정·수정한 점: 공간적 결과를 누적하는 재미는 한 번의 전투 안으로 한정한다. 스테이지 재도전의 출발 조건은 항상 동일해야 한다.
- 증거: 발사 후 파티 편성 화면을 거쳐 새 전투를 시작했을 때, 브라우저에서 공이 좌하단의 고정 발사 지점으로 돌아오는 것과 콘솔 오류 0을 확인.

## 2026-08-07 — 방벽 단계 제거

- Codex에게 시킨 일: 방벽이 깨질 때 공과 전투가 멈추는 2단계 구조를 일단 제거.
- 결과: 보스는 체력 바 하나만 가지며, 몸체·약점·유닛 지원 공격 모두 즉시 그 체력에 피해를 준다. 방벽 파괴, 추가 강화 선택, 발사 횟수 재충전은 발생하지 않는다.
- 사람이 직접 결정·수정한 점: 현재 프로토타입에서는 충돌 궤적과 유닛 조합의 재미를 먼저 검증한다. 전투를 끊는 방어막 기믹은 나중에 필요성이 확인될 때만 별도 기믹으로 재도입한다.

## 2026-08-07 — 장기 전투 테스트 체력

- Codex에게 시킨 일: 세 스테이지 모두에서 유닛 능력과 궤적을 충분히 시험할 수 있게 보스 체력을 상향.
- 결과: 공통 보스 체력을 76에서 240으로 올렸다. 스테이지별 지형 차이는 유지하되, 어느 스테이지든 여러 발사와 연계를 관찰할 수 있다.

## 2026-08-07 — 물리 사건 기반 광역 시너지

- Codex에게 시킨 일: 공의 반사·표식·원심 발사를 파티의 직접 공격으로 바꾸고, 그 보상을 받을 다중 적 대상을 추가.
- 결과: 보스 주변에 재생성되는 공허 잔재 3체를 배치했다. 모루는 벽 반사마다 전 적 충격파, 미리내는 표식 약점 적중 시 폭발, 달무리는 원심 발사 시 전 적 원심 베기를 발동한다. 유물탄도 잔재에 직접 부딪혀 피해를 준다.
- 사람이 직접 결정·수정한 점: 광역 공격은 해당 캐릭터를 배치했을 때만 발동한다. 따라서 같은 벽 반사라도 모루를 데려왔는지에 따라 전술적 가치가 달라진다.

## 2026-08-07 — 가독성 중심 전투 UI

- Codex에게 시킨 일: 어두운 도트 전투의 분위기는 유지하면서 HUD·한글 설명·배치 화면·페이지 배경의 가독성을 높임.
- 결과: 시스템 한글 글꼴 우선순위를 적용하고, HUD를 더 불투명한 패널과 큰 정보 위계로 정리했다. 체력 바·도움말·토스트·파티 카드·룬석 배치도는 대비와 테두리를 강화했으며, 배경은 보라색 광원이 있는 어두운 그라데이션으로 조정했다.

## 2026-08-07 — 전투 에셋 통일

- Codex에게 시킨 일: 기존 픽셀 캐릭터와 코드로 그린 공허 잔재의 시각적 이질감을 줄일 전투 에셋을 제작·통합.
- 결과: 보라색 공허 잔재 픽셀 스프라이트를 제작하고, 크로마키 제거 뒤 `assets/enemies/void-wisp.png`으로 반입했다. 전투의 잔재 3체는 기존 원형 도형 대신 이 스프라이트를 사용한다.
- 사람이 직접 결정·수정한 점: 새 에셋은 캐릭터와 같은 제한 팔레트·하드 픽셀 윤곽을 지켜 전투의 역할 구분은 강화하되, 다른 화풍을 추가하지 않도록 했다.

## 2026-08-10 — 프리즘 에셋 라이브러리 확장

- Codex에게 시킨 일: 기존 공허 잔재와 같은 화풍으로 재사용 가능한 에셋 10개를 추가 제작.
- 결과: 적 3종, 전투 효과 3종, 전장 소품 4종을 투명 PNG로 제작해 `assets/library/`에 저장했다. 목록과 사용 기준은 `assets/ASSET_LIBRARY.md`에서 관리한다.
- 사람이 직접 결정·수정한 점: 현재 플레이가 복잡해지지 않도록 새 10개는 라이브러리로 보관한다. 다음 기믹·스테이지에 필요할 때만 선택적으로 전투에 반입한다.

## 2026-08-10 — 재사용 애셋 팩과 4프레임 애니메이션

- Codex에게 시킨 일: 프리즘 에셋을 다른 프로젝트에서도 쓰도록 별도 패키지로 보관하고, 각 에셋의 애니메이션 시트를 제작.
- 결과: `/Users/8rulerstar/Assets/Prism-Breakers-Pixel-Asset-Pack/`에 원본 11개와 애니메이션 11개를 저장했다. 모든 시트는 `256×256` 프레임 4개를 가로로 잇고, 적은 부유, 효과는 펄스, 소품은 발광으로 동작한다.
- 사람이 직접 결정·수정한 점: 화풍 일관성을 위해 새로운 프레임을 다시 생성하지 않고 원본 픽셀을 비파괴 변환했다. 패키지에 재생성 스크립트와 메타데이터도 함께 넣었다.

## 2026-08-10 — 유닛 상태 애니메이션 확장

- Codex에게 시킨 일: 재사용 패키지의 유닛 애니메이션을 단일 루프가 아니라 공격·idle·피격·이동 상태로 분리.
- 결과: 공허 유닛 4종 각각에 `idle`, `move`, `attack`, `hit` 4프레임 시트를 만들었다. 공격은 전방 돌진/섬광, 피격은 흔들림/화이트 플래시, 이동은 수평 드리프트, idle은 부유 움직임으로 구분한다.
- 사람이 직접 결정·수정한 점: 사망·스킬 시전은 현재 전투 규칙이 아직 정해지지 않아 보류했다. 상태 수를 늘리는 대신, 지금 필요한 네 상태의 구분을 먼저 명확하게 했다.

## 2026-08-10 — 제출용 Codex 에셋 제작 근거 정리

- Codex에게 시킨 일: 에셋 생성·투명 처리·재사용 패키지·상태 애니메이션 작업을 해커톤 제출에서 설명할 수 있도록 근거 문서화.
- 결과: `assets/ASSET_GENERATION_LOG.md`에 사람의 아트 방향 결정, Codex의 산출물, 재현 방법, 검증 흔적, 제출용 한 문장을 기록했다. `CODEX_COLLABORATION.md`의 실제 기능과 제출 문안에도 반영했다.
- 사람이 직접 결정·수정한 점: “Codex가 에셋을 만들었다”만 주장하지 않고, 어떤 문제를 해결했는지와 사람이 결정한 화풍·반입 기준을 함께 남겼다.

## 2026-08-10 — 런타임 애니메이션·헤드리스 봇·Hive 스파이크 경계

- Codex에게 시킨 일: 재사용 애셋의 상태 시트를 실제 전투에 반입하고, 조합 밸런스를 수치로 검증하며, Hive의 최소 연동 경계를 구현.
- 결과: 공허 잔재는 전투 중 4프레임 `idle` 시트로 부유하고, 직접 충돌·광역 피해 때 `hit` 시트로 전환한다. `bot/run-bot.mjs`는 DOM/canvas 없이 세 스테이지와 56개 3인 파티 조합을 4발 기준으로 실행해 168건의 JSON 리포트를 남겼다. 첫 실행의 균형 모델 결과는 클리어율 43.5%, 평균 잔여 체력 34.6, 평균 룬 충돌 4.9회다. `hive/`에는 브라우저 경계·실통신 전용 왕복 명령·비밀값 분리 규칙을 두었다.
- 막힌 점 / 우회: Hive Console AppID·데이터 스토어 키·배포된 서버 어댑터는 아직 제공되지 않아 실통신을 위조할 수 없었다. 대신 설정되지 않으면 성공으로 보이지 않는 클라이언트와 검증 명령을 만들고, `HIVE_SETUP.md`에 실제 완료 판정만 남겼다.
- 사람이 직접 결정·수정한 점: 봇 결과는 현재 재미의 판정이 아니라 다음 체력·능력 수치 조정의 출발점으로 쓰며, Hive는 Console 왕복이 확인되기 전까지 구현 완료로 표현하지 않는다.

## 2026-08-10 — 당구식 조준에서 세로 핀볼로 전환

- Codex에게 시킨 일: 정밀한 당구식 조준으로 시작하는 가로 전투를, 세로 핀볼 보드로 바꾸고 수집형 파티 능력이 더 먼저 보이게 재구성.
- 결과: 캔버스를 `720×900` 세로 웹 비율로 전환했다. 모든 룬볼은 하단 고정 플런저에서 위력만 정해 발사하고, 중력·상단/측면 반사·하단 드레인으로 흐른다. 각 스테이지에 룬 범퍼 군집을 배치했고, 범퍼는 속도·피해·콤보 피드백을 제공한다. 하단 좌·우 탭은 근처 룬볼을 되튀기는 플리퍼 충격이다. 고정 룬석 배치, 룬 충돌 시 직접 지원 공격, 약점 보상, 리오·달무리 같은 능동 개입은 유지했다.
- 막힌 점 / 우회: 단순히 세로 캔버스로 돌리면 기존 가로 좌표의 보스·룬·소품이 화면 밖으로 나갔다. 스테이지별 보스·룬·범퍼·소품 좌표를 모두 세로 보드 기준으로 다시 잡고, 하단 드레인은 반사 대신 다음 룬볼로 교체되도록 분리했다.
- 사람이 직접 결정·수정한 점: 핀볼의 자동 반사만으로 끝내지 않고, 플런저 위력과 하단 플리퍼 탭을 남겨 플레이어의 개입 지점을 만들었다. 따라서 조준 숙련보다 파티 조합·타이밍·콤보 설계가 중심이 된다.

## 2026-08-10 — 능력 조건의 핀볼 전환

- Codex에게 시킨 일: 가로 반사 전투에서 의미가 있던 정지·화면 클릭 조건을 세로 핀볼의 플리퍼·드레인 흐름에 맞춰 변환.
- 결과: 달무리는 다음 플리퍼 충격을 원심 회전으로 전환하고 탭 발사로 광역 베기를 낸다. 리오와 시아는 각각 다음 플리퍼를 우상단·좌상단 급선회로 바꾼다. 도라는 정지 대신 드레인 순간 관통 바늘을 발사한다. 기존 벽 반사·범퍼·유도·표식·릴레이 계열은 중력 핀볼에서도 그대로 보상으로 동작한다.
- 사람이 직접 결정·수정한 점: 핀볼의 모든 입력을 범용 클릭에 묶지 않고, 하단 위험 구간에서 하는 플리퍼 입력에 능력을 결합했다. 능력 발동이 우연한 클릭보다 보드 위치와 타이밍의 판단이 되도록 하기 위함이다.

## 2026-08-10 — 핀볼에서 이동 유닛 당구로 재전환

- Codex에게 시킨 일: 플리퍼 중심 핀볼에서 재미가 약해진다는 플레이 피드백을 반영해, 룬볼뿐 아니라 유닛도 실제 공처럼 굴러가게 바꾸고, 움직인 유닛이 멈춘 자리에서 공격하도록 재구성.
- 결과: 첫 룬볼은 하단 발사석에서 시작하고 이후 룬볼은 직전 정지 위치에서 이어 발사한다. 룬볼·유닛 충돌은 양쪽에 운동량을 주며, 유닛 이동 후 샛별의 근접 베기·미리내의 거리 저격·모루의 충격파 등 각성 공격이 발생한다. 별하의 분열체도 유닛과 물리 상호작용하고 능력을 발동한다.
- 막힌 점 / 우회: 고정 원형 유닛은 반사각을 예측하기 어렵고, 핀볼 플리퍼는 조작은 있어도 파티 배치의 전략적 의미를 약하게 만들었다. 유닛의 이동선까지 조준 가이드에 표시하고, 다음 샷의 시작 위치를 유지해 ‘좋은 배치를 남기는 것’을 전술로 만들었다.
- 사람이 직접 결정·수정한 점: 월드플리퍼식 핀볼 복제보다, “누구를 굴려 어디에 세울까”가 보이는 당구형 코어를 채택했다. 이 결정 없이 핀볼 시안으로 되돌리지 않는다.
- 증거: `prototypes/prism-breakers.html`의 `mobilePair`, `settleParty`, `billiardPredict`; `e0d2847`~`80b22e0`.

## 2026-08-10 — 전투 감각·보스·훈련장 정비

- Codex에게 시킨 일: 충돌이 속도를 빼앗아 루즈해지는 문제를 고치고, 7종 능력의 시각적 차이·보스 연출·장기 테스트 환경을 추가.
- 결과: 룬볼과 유닛 충돌 시 양쪽에 최소 운동량을 보장하고, 각 유닛에 검격·화살·분열·중계·전환·충격파·모사 이펙트를 연결했다. 보스는 공허 거상 idle/hit 스프라이트로 교체했고, 보스 처치에는 1.9초의 붕괴 연출을 넣었다. 기본 전투는 5발·보스 체력 260이며, 무한 훈련장은 체력 무한·5발 자동 보충으로 동작한다.
- 막힌 점 / 우회: 일회성 전투만으로는 분열체·배율·마찰의 변화를 관찰하기 어려웠다. 순위나 클리어 통계에 섞이지 않는 QA 전장을 별도 추가했다.
- 사람이 직접 결정·수정한 점: 훈련장은 제출 성과나 일반 스테이지가 아니라 밸런스 확인 도구로 제한한다. 실제 Hive 기록과도 분리한다.
- 증거: `prototypes/prism-breakers.html`의 `guaranteeMomentum`, `updateCloneBalls`, `scheduleWin`, `stages`의 `T`; `ddfade6`~`87c38d4`.

## 2026-08-11 — 현황 문서 동기화와 다음 세션 인수인계

- Codex에게 시킨 일: 실제 코드와 문서의 전투 코어·로스터·발사 수가 어긋난 부분을 정리하고, 보고와 다음 세션용 기준선을 남김.
- 결과: `PROJECT_CONTEXT.md`, `GAME_DIRECTION.md`, `CODEX_COLLABORATION.md`, `README.md`를 현재 당구형 7인/기본 5발 규칙으로 동기화하고 `PROGRESS_REPORT.md`를 추가했다.
- 막힌 점 / 우회: 이전 핀볼 문서에는 범퍼·플리퍼·28인 로스터·4발 등이 남아 있었다. 코드에서 현재 동작하는 규칙을 다시 대조했고, 스테이지 범퍼 좌표가 런타임에 연결되지 않는 점은 완료 기능으로 꾸미지 않고 다음 점검 항목으로 기록했다.
- 사람이 직접 결정·수정한 점: 다음 세션은 현재 당구형 코어를 기준으로 밸런스·범퍼 연결·현 규칙용 봇·Hive 실통신을 순서대로 진행한다.
- 증거: `PROGRESS_REPORT.md`, 이 문서, 문서 동기화 커밋.

## 2026-08-11 — 전투 손맛·피드백 패스

- Codex에게 시킨 일: 단순한 피해 숫자 증가가 아니라, 충돌·약점·각성·배율·처치가 각각 읽히도록 전투의 타격감과 반응 시간을 강화.
- 결과: 일반 명중과 약점/연계 명중에 서로 다른 히트스톱·화면 흔들림·플래시·사운드·링 이펙트를 연결했다. 유닛 충돌은 공명 링, 유닛 지원은 시간차 투사체와 각성 파동, 배율 상승은 카드 펄스와 별도 사운드로 분리했다. 처치 붕괴 연출은 1.9초에서 2.55초로 늘려 결과가 보이기 전에 화면이 넘어가지 않도록 했다.
- 사람이 직접 결정·수정한 점: 모든 충돌을 같은 과한 화면 흔들림으로 처리하지 않고, 일반 충돌은 짧게, 약점·RIPOSTE·각성처럼 전술적 가치가 높은 순간만 강한 반응과 읽을 시간을 준다.
- 증거: `prototypes/prism-breakers.html`의 `feedbackBeat`, `combatSfx`, `impact`, `queueUnitAssist`, `scheduleWin`.

## 2026-08-11 — 픽셀 이펙트 반입과 타이틀 콜드 오픈

- Codex에게 시킨 일: 전투 이펙트의 종류를 더 늘리고, 첫 화면부터 게임처럼 느껴지는 시작 연출을 구성.
- 결과: 이미 저장된 프리즘 직격·룬 충격파·프리즘 혜성·전기 고리 스프라이트를 전투에 반입했다. 약점·RIPOSTE·공명 충돌·배율·유닛 지원 투사체가 서로 다른 픽셀 실루엣으로 보이며, 지원 투사체는 충전 → 비행 → 착탄으로 읽힌다. 타이틀은 균열 개방, 보스 강하, 프리즘 충돌, 로고·대표 파티·시작 버튼의 순차 등장으로 변경했다.
- 사람이 직접 결정·수정한 점: 새 이미지를 늘리기보다 프로젝트 안의 재사용 가능 에셋을 우선 채택했다. 첫 진입에서도 플레이 시작을 과도하게 지연하지 않도록 버튼은 0.88초 뒤 바로 활성화한다.
- 증거: `prototypes/prism-breakers.html`의 `feedbackArt`, `paintFeedbackAsset`, `drawAssists`, `showTitle`.

## 2026-08-11 — 출시 후보 50×4 라이브러리

- Codex에게 시킨 일: 필요한 정적 에셋·애니메이션·시각 효과·SFX를 각 50종씩 만들고, 실제 코어에 필요한 것만 적용·검토.
- 결과: 정적 에셋 50종, 4프레임 애니메이션 50종, 픽셀 VFX 50종, 외부 샘플 없는 PCM WAV SFX 50종을 `PRODUCTION_50X4_MANIFEST.json`으로 고정했다. 시그니처 코어 브레이크 VFX는 ImageGen 원본을 투명 처리해 타이틀에 반입했고, 벽·유닛·약점·RIPOSTE·각성·배율 계열 대표 SFX 7개는 전투에 연결했다.
- 막힌 점 / 우회: 기존 후보 일부에 제거된 보스 방벽 표현이 남아 있었다. 50×4 출시 후보 선별에서는 이를 전부 제외하고, 현재 코어와 일치하는 약점·공명·각성·처치 표현만 남겼다. ImageGen의 크로마키 원본은 로컬 변환 후 알파 PNG로 검증했다.
- 사람이 직접 결정·수정한 점: 모든 후보를 한 화면에 쓰지 않고, VFX 4종과 SFX 7개만 런타임 기본값으로 반입한다. 나머지는 기믹·적 패턴이 확정될 때만 선택한다.
- 증거: `assets/PRODUCTION_50X4.md`, `assets/library/PRODUCTION_50X4_MANIFEST.json`, `scripts/build_50x4_manifest.mjs`, `scripts/generate_sfx_pack50.py`.

## 2026-08-11 — 「잊힌 별의 관측자」 세계관 레이어

- Codex에게 시킨 일: 물리·밸런스를 건드리지 않고 확정 세계관에 맞춰 모든 화면 문안, 첫 실행 프롤로그, 별자리/별 해방/밤하늘 진행도 연출을 연결.
- 결과: 룬볼을 유성, 유닛을 별지기로 통일하고, 첫 실행 시 스킵 가능한 4줄 프롤로그·정지 위치 금색 점선 별자리·약점 젬에서 떠오르는 별·클리어 수 최대 30개의 허브 밤하늘을 구현했다.
- 막힌 점 / 우회: 신규 에셋 제작은 범위 밖이었다. 기존 `video-title-card.png`와 `crit-star.png`만 재사용하고, 별자리 선은 Canvas 드로잉으로 처리했다.
- 사람이 직접 결정·수정한 점: 「잊힌 별의 관측자」 세계관은 채택하되 게임명은 보류하고, 표시명·워드마크는 Prism Breakers를 유지한다.

## 2026-08-11 — 관측소 비주얼 톤 통일

- Codex에게 시킨 일: 확정된 「잊힌 별의 관측자」 컨셉에 맞게 연출 에셋의 색감, UI, 전투판 분위기를 전면 정리.
- 결과: 청록·황동 중심의 혼합 톤을 심야 남색·공허 남보라·별빛 금색·달빛 청백색으로 통일했다. 메타 허브·타이틀·편성·스테이지 선택·전투 HUD를 밤하늘 관측소처럼 재구성하고, 배율·처치 연출은 불꽃 대신 별빛 확산과 `STAR RETURN!`으로 연결했다.
- 막힌 점 / 우회: 기존 에셋은 서로 다른 시기의 색보정을 가지고 있었다. 새 에셋을 추가하지 않고, CSS 오버레이와 Canvas 색상 레이어로 배경을 통일해 캐릭터별 고유 색은 식별성으로 보존했다.
- 사람이 직접 결정·수정한 점: 캐릭터 능력색은 전술 인지에 필요하므로 단일 팔레트로 탈색하지 않는다. 세계관 공통색은 배경·프레임·보상·중요 순간에만 사용한다.
- 증거: `prototypes/prism-breakers.html`의 Observatory art direction CSS, `drawStageFloor`, `drawArena`, `drawVictoryFx`, `earnBlaze`.

## 2026-08-11 — 게임명 확정: Stella Ball

- Codex에게 시킨 일: 확정한 게임명을 타이틀·워드마크·제출용 핵심 문서에 반영.
- 결과: 공식 표시명은 **Stella Ball**로 고정했다. 타이틀과 메타 UI는 새 별빛 워드마크 SVG를 사용하며, README·방향 문서·제출용 협업 문서도 같은 이름을 따른다.
- 사람이 직접 결정·수정한 점: 공개 저장소명과 URL 경로 `prism-breakers`는 이미 배포·검증 이력에 연결돼 있으므로, 게임의 표시명만 변경하고 기술 경로는 유지한다.
- 증거: `assets/original/stella-ball-wordmark.svg`, `prototypes/prism-breakers.html`, `GAME_DIRECTION.md`, `CODEX_COLLABORATION.md`.

## 2026-08-11 — Stella Ball 연출·피드백 정교화

- Codex에게 시킨 일: 이름 교체 뒤 약해진 타이틀 존재감과 전투·결과 화면의 피드백 계층을 화면별로 다시 점검하고 보강.
- 결과: 워드마크에 관측 궤도선·서브마크를 추가하고, 타이틀/허브/UI의 빛 흐름을 정리했다. 전투판에는 낮은 광도의 별먼지와 보스·발사 지점의 초점광을, 약점·RIPOSTE에는 별burst와 더 읽기 쉬운 라벨을, 승패 화면에는 별자리 행과 결과 카드 계층을 추가했다.
- 막힌 점 / 우회: 배경과 모든 이벤트를 동시에 밝히면 화면이 다시 난잡해진다. 상시 배경은 거의 보이지 않는 남청·달빛으로 제한하고, 높은 가치의 사건에만 금색·청백색 광도와 짧은 모션을 사용했다.
- 사람이 직접 결정·수정한 점: 파티별 고유 색과 능력 이펙트는 전술 식별용으로 유지한다. 공통 세계관 연출은 이 색을 덮지 않고, 보스 처치·별자리 완성·UI 프레임에만 더한다.
- 증거: `prototypes/prism-breakers.html`의 Premium feedback pass, `drawObservatoryAtmosphere`, `drawFeedbackBeats`, `outcome-constellation`; `assets/original/stella-ball-wordmark.svg`.

## 2026-08-11 — 별자리 캠페인 지도

- Codex에게 시킨 일: 카드 목록형 스테이지 선택을 별자리 위의 스테이지 버튼 형태로 바꾸고, 5장까지의 진행 구조를 먼저 보여 주기.
- 결과: 「별자리 관측도」에서 1~~3장은 현재 전투로 진입하고, 4~~5장은 잠긴 관측 지점으로 이어진다. 무한 훈련장은 캠페인과 분리한 하단 도구 진입으로 유지했다.
- 사람이 직접 결정·수정한 점: 아직 전투가 없는 4·5장을 플레이 가능한 것처럼 만들지 않는다. 이후 보스·지형·목표가 확정될 때 잠금만 해제한다.
- 증거: `prototypes/prism-breakers.html`의 `showStageSelect`, `constellation-map` CSS.

## 2026-08-11 — 1-1 실제 전장 온보딩

- Codex에게 시킨 일: 첫 실행의 프롤로그 다음에 메뉴가 아니라 실제 `1-1` 전장 튜토리얼로 바로 진입하게 만들고, 기존 전투 시안은 보류.
- 결과: 유성을 아래로 끌어 발사 → 미리내 충돌 → 정지 후 각성 공격을 단계 카드로 안내한다. 튜토리얼 보스는 불멸 상태라 실패·처치 없이 핵심 상호작용을 확인한 뒤 별자리 지도로 넘어간다.
- 사람이 직접 결정·수정한 점: 온보딩은 설명 카드 모음이 아니라 실제 물리 전장에서 한 번의 의미 있는 행동을 하게 한다. 1-2·1-3·2-1은 구현된 것처럼 노출하지 않고 잠금 처리한다.
- 증거: `prototypes/prism-breakers.html`의 `showOnboardingTutorial`, `renderOnboarding`, `STORY_INTRO_STORAGE`, `ONBOARDING_STORAGE`.

## 2026-08-11 — 1-1 관측 수업 확장과 파티 슬롯 해금

- Codex에게 시킨 일: 설명 카드 중심의 첫 튜토리얼을 실제 전장 변화와 도우미 대사 중심의 4단계 수업으로 교체하고, 다시보기와 성장 보상을 연결.
- 결과: 루나가 ① 유성 직격 ② 샛별을 굴린 뒤의 근접 각성 ③ 샛별·미리내 동시 각성과 별자리 배율 ④ 두 별지기 초기 배치를 순서대로 안내한다. 각 단계는 공·별지기 배치가 실제로 바뀌며, 빗맞혀도 중단되지 않도록 관측 유성은 무제한이다. 완료 시 업적 「첫 관측자의 증명」이 남고 세 번째 파티 슬롯이 해금된다. 허브와 별자리 지도에서 언제든 같은 수업을 다시 볼 수 있다.
- 막힌 점 / 우회: 일반 전투의 발사 제한과 1-1 수업의 반복 시도가 충돌했다. 튜토리얼 플래그에서만 HUD를 `관측 유성 · 무제한`으로 전환해 일반 스테이지 수치를 건드리지 않았다.
- 사람이 직접 결정·수정한 점: 첫 보상은 즉시 새 캐릭터 지급이 아니라 파티의 세 번째 자리 해금으로 둔다. 따라서 이후 로스터에서 실제 조합 선택이라는 다음 행동이 자연스럽게 열린다.
- 증거: `prototypes/prism-breakers.html`의 `onboardingLayouts`, `renderOnboarding`, `completeOnboarding`, `partySlotCount`, `achievementList`.

## 2026-08-11 — 초보 관측자용 세분화 안내

- Codex에게 시킨 일: 1-1 수업을 아기에게 가르치듯 더 천천히, 행동과 결과를 분리한 대사 흐름으로 세분화.
- 결과: 루나의 수업은 11개의 작은 안내가 됐다. 유성 찾기 → 아래로 끌기 → 비행 관찰 → 직격 확인 → 샛별의 정지 공격 → 미리내 추가 → 두 별지기 연결 → 별자리 배율의 의미 → 보상까지, 각 개념을 확인한 뒤 다음으로 넘어간다. 설명 전 발사는 막고, 빗나감·한 명만 각성 같은 중간 상태도 현재 해야 할 행동을 다시 말해 준다.
- 사람이 직접 결정·수정한 점: 처음부터 모든 조작을 열어 두지 않고, 루나의 확인 버튼 뒤에 해당 행동만 연다. 수업의 목적은 빠른 클리어가 아니라 처음 보는 사람도 충돌·정지·별자리 배율의 관계를 이해하는 것이다.
- 증거: `prototypes/prism-breakers.html`의 `lessons`, `continueOnboarding`, `setOnboardingDialogue`, `settleParty`.

## 2026-08-11 — 밸런스 하니스 재작성 인수인계

- 사람이 직접 확인한 문제: 현재 유닛 충돌의 운동량 보상, 별자리 배율, 각성 공격이 한 발 안에 중첩될 수 있어 정상 전투에서도 원샷 클리어 또는 지나치게 긴 체공이 생길 수 있다.
- Codex의 점검 결과: 기존 `bot/`은 4발·8인·이전 가로 반사 모델이라 현재 7인·5발·이동 유닛 전투의 수치를 판정하는 데 사용할 수 없다. 따라서 기존 `latest-report.json`은 보관만 하고 제출 수치로 사용하지 않는다.
- 다음 세션의 시작점: 현재 물리 기준 헤드리스 모델을 먼저 작성한 뒤, 보스 체력·마찰·유닛 충돌 최소/최대 운동량·별자리 배율·각성 피해를 함께 스윕한다. 목표는 클리어율 60~70%, 원샷은 희귀한 보상급 비율, 한 발의 체공이 과도하지 않은 상태다.
- 중단 지점: 하니스 재작성과 상수 스윕은 **아직 시작하지 않았다**. 이번 커밋에는 튜토리얼과 인수인계 문서만 포함한다.

## 2026-08-11 — 모듈화·성능·온보딩·1-2·메인 화면 마감

- Codex에게 시킨 일: 다른 세션과 Windows/macOS에서 쉽게 이어갈 수 있도록 런타임을 분리하고, 오래된 프로토타입을 정리하며, 충돌·정산 렉과 온보딩·메인 화면·필살기 연출을 전반적으로 개선한다.
- 결과: 단일 런타임을 책임별 CSS 5개와 classic JS 9개로 분리하고 `prototypes/ARCHITECTURE.md`에 로드 순서와 수정 위치를 고정했다. Windows 실행기, Node 정적 서버, 검증/스모크 npm 명령을 추가했다. 오래된 루트 게임 파일과 미사용 프로토타입을 제거했다.
- 결과: 최대 3개 90Hz 물리 서브스텝, 정적 전장 캐시, 60Hz 궤적 샘플링, 변화 기반 HUD, VFX 예산으로 충돌 스파이크를 낮췄다. 정산은 캐릭터별 확대·집중선·조준 표식·별자리 노드와 충전/해방/명중 SFX를 순차 재생하도록 바꿨다.
- 결과: 윤슬을 8번째 별지기로 추가했다. 윤슬은 이동 중 보스를 관통하고 속도 비례 회전 칼날 피해를 주며 별도 정산 공격이 없다. 1-1 온보딩은 미리내와 윤슬을 사용하고, 설명 모달이 중앙에 보일 때 입력을 잠근 뒤 `직접 해보기`에서만 조작을 연다.
- 결과: 메인 화면을 아군 미리내·윤슬·별하, 별자리 진행도, 현재 임무, 3단계 플레이 루프, 큰 CTA 중심으로 다시 구성했다. `1-2 균열 회랑`은 공명 범퍼만 추가한 첫 일반 전투로 열었다.
- 사람이 직접 결정·수정한 점: 한 스테이지에 잡몹·범퍼·롤오버를 한꺼번에 넣지 않고, 스테이지마다 새 기믹 하나만 추가한다. 후보 관측소 에셋과 SFX는 만들어 두되 화풍·가독성 검수 전에는 적용하지 않는다.
- 검증: 실제 브라우저에서 메인 CTA, 온보딩 재진입, 1-2 플레이, 충돌·정산·SFX를 확인했고 콘솔 오류가 없었다. 마감 커밋 전 `npm run verify`, `npm run smoke`, 포맷/portable 경로 검사를 다시 실행한다.
- 다음 시작점: macOS에서 `npm run verify`, `npm run smoke`, `npm run serve` 후 메인 CTA → 온보딩 입력 잠금 → 1-2 범퍼 → 정산 연출을 확인한다. 이후 우선순위는 현 물리 기준 봇 갱신과 1-3의 단일 신규 기믹 결정이다.

# 2026-08-12 — Ink & Brass UI 테마 정식 적용

- Codex에게 시킨 일: 다운로드한 UI 리디자인 패키지를 현재 `Projects/prism-breakers` 런타임에 안전하게 적용하고, 중복 실행본 없이 인수인계 문서로 보존.
- 결과: `stella-ball-theme.css`를 마지막 스타일시트로 연결해 HUD·허브·편성·지도·결과·설정 화면을 잉크빛 차콜과 황동 중심으로 통일했다. CSS만으로 가능한 범위를 적용해 물리·밸런스·캔버스 판정색은 유지했다.
- 막힌 점 / 우회: 내려받은 themed HTML은 이전의 단일 파일 구조 사본이라 현재 9개 JS/5개 CSS 모듈 구조에 그대로 쓰면 기능을 되돌릴 위험이 있었다. 테마 CSS만 정식 원본에 연결하고, UI 원칙은 `UI_REDESIGN_HANDOFF.md`에 남겼다.
- 사람이 직접 결정·수정한 점: 게임의 전술 구분에 필요한 캐릭터 원색은 캔버스에 유지하고, UI 표면만 저채도 매칭색으로 사용한다.
- 검증: `npm run verify`, `npm run smoke`, 스타일시트 순서 검사.

## 2026-08-12 — 전장을 비우는 튜토리얼 안내

- Codex에게 시킨 일: 중앙 안내 모달이 보스·별지기·유성을 가리는 문제를 고치고, 대화 배너와 조작 유도를 더 천천히 읽히게 구성.
- 결과: 루나의 설명은 전장 하단의 얇은 대화 배너로 옮겼다. 문장 단어가 순서대로 나타난 뒤 다음 버튼이 활성화되며, 실제 발사 단계에서는 배너가 완전히 사라진다. 그 대신 유성 출발점·목표 고리·당기는 방향의 손가락 포인터·점선 안내를 Canvas로 표시한다.
- 막힌 점 / 우회: 축소된 웹 전장에서는 좌하단 카드도 유성 출발점을 덮었다. 카드 위치만 옮기지 않고 하단 전체 폭의 짧은 대화 레인으로 바꿔 보스·중앙 유닛·조준 궤적을 남겼다.
- 사람이 직접 결정·수정한 점: 새 손가락 이미지 에셋은 만들지 않았다. 조작 안내는 일회성 Canvas 표현으로 충분하므로, 향후 일괄 아트 백로그에 불필요한 파일을 추가하지 않는다.
- 검증: 실제 브라우저에서 1-1 시작 배너와 “유성 발사해 보기” 뒤 전장 비움·목표선·손가락 유도 확인. `npm run verify`, `npm run smoke` 실행.

## 2026-08-12 — 최소 골드 보상 기반

- Codex에게 시킨 일: 재화 시스템을 과도하게 확장하지 않고, 스테이지 클리어 시 고정 골드가 쌓이는 최소 기반만 구현.
- 결과: 로컬 진행 데이터에 `gold` 필드를 추가했다. 일반 스테이지 클리어마다 100골드를 누적하고, 메타 상단과 결과 화면의 클리어 보상에서 확인할 수 있다.
- 막힌 점 / 우회: 첫 실행 수업·무한 훈련장을 같은 “클리어”로 취급하면 반복 보상이 생길 수 있다. 두 전장은 고정 보상에서 제외하고, 실제 일반 스테이지의 결과 확정 경로에서 한 번만 지급한다.
- 사람이 직접 결정·수정한 점: 골드는 현재 보상 감각과 향후 메타 확장 가능성만 보여 준다. 상점·가챠·구매형 강화는 이번 범위에 넣지 않는다.
- 검증: `npm run verify`, `npm run smoke`; 기존 저장 데이터에는 기본값 0을 병합한다.

## 2026-08-12 — 시작 3인과 별빛 소환

- Codex에게 시킨 일: 시작 별지기를 샛별·미리내·윤슬 3명으로 한정하고, 나머지 5명을 골드 기반 뽑기 탭에서 해금하도록 전환.
- 결과: 로컬 진행 데이터에 보유 별지기 목록을 저장한다. 로스터에는 보유한 캐릭터만 표시되며, 일반 스테이지 클리어로 얻는 100골드로 `별빛 소환`을 실행하면 미보유 별지기 한 명이 중복 없이 확정 해금된다.
- 막힌 점 / 우회: 확률표·중복 보상·천장·유료 재화까지 넣으면 MVP 경제가 불필요하게 커진다. 현재 풀에서 이미 보유한 캐릭터를 빼고 무작위 선택해, 한 번의 소환이 언제나 새 전술 선택지를 열도록 제한했다.
- 사람이 직접 결정·수정한 점: 시작 3인은 근접 정산(샛별)·원거리 정산(미리내)·이동형 지속 공격(윤슬)의 서로 다른 타이밍을 바로 보여 준다. 전용 뽑기 아트/SFX는 만들지 않고 기존 스프라이트와 CSS 소환 연출로만 먼저 검증한다.
- 증거: `ASSET_BACKLOG.md`의 별빛 소환·골드 자산 후보, `STARTER_HERO_IDS`, `GACHA_HERO_IDS`, `showGacha()`.

## 2026-08-12 — 전투 카드 중심 메타 허브

- Codex에게 시킨 일: 화면을 비우게 만들던 Hive 기록 인터페이스를 메타에서 제거하고, 모바일 전략 게임의 정보 위계를 참고해 다음 전투가 선명한 허브로 재구성.
- 결과: 상단 관측자 정보 아래에 스테이지 배경·상징·지형·유성 수·현재 기믹·관측 시작 CTA를 담은 하나의 메인 관측 카드를 배치했다. 이어서 전투 편성과 튜토리얼·업적·설정 3개 보조 메뉴만 남겨 화면 밀도를 정리했다.
- 막힌 점 / 우회: 이전 허브는 스테이지·파티·4개 빠른 메뉴·Hive 기록 문장이 같은 무게로 놓여 시작 행동이 약했다. 기록 안내는 백엔드 연동의 상태를 설명할 뿐 다음 행동에는 도움이 되지 않아, 전투 카드 안의 실제 전장 정보로 교체했다.
- 사람이 직접 결정·수정한 점: Hive 연동 자체는 제거하지 않고, 플레이 중 보이는 메타 UI에서만 전면 노출을 중단한다. 황동 CTA는 `관측 시작` 한 곳에만 사용하고 스테이지 변경은 보조 행동으로 둔다.
- 검증: `npm run verify`, `npm run smoke`, 런타임 소스에서 기존 `Hive 기록 인터페이스` 문구 0건 확인.

## 2026-08-12 — 클리어 후 별자리 지도 경유

- Codex에게 시킨 일: 스테이지 클리어 직후 다음 관측이 바로 캐릭터 편성으로 넘어가는 흐름을 정리.
- 결과: `다음 관측`은 별자리 지도(스테이지 선택)로 이동한다. 지도에서 다음 목적지를 고른 뒤에만 편성 화면이 열린다.
- 판단: 다음 별을 먼저 보여 주어 메타 진행과 밤하늘 세계관을 자연스럽게 연결한다. 패배 후 재관측은 빠른 재편성을 위해 기존 흐름을 유지한다.
- 검증: 결과 CTA의 연결 대상과 `showStageSelect()` 진입 경로를 확인하고 자동 검증을 실행한다.

## 2026-08-12 — 윤슬 스프라이트 시각 크기 정규화

- 요청: 윤슬만 다른 별지기보다 작아 보이는 문제를 수정.
- 결과: 충돌 반경과 물리값은 건드리지 않고, 원본 PNG의 넓은 투명 여백을 보정하는 전장 렌더 배율과 UI 초상화 배율을 각각 올렸다.
- 판단: 별도 비트맵 복사본을 만들지 않아 에셋 관리가 늘어나지 않으며, 편성·배치·전투에서 같은 시각적 체급으로 보인다.

## 2026-08-12 — 편성 슬롯 초상화 보강

- 요청: `선택 1·2·3` 슬롯이 이름만 보여 어떤 별지기를 골랐는지 직관적이지 않은 문제를 개선.
- 결과: 선택된 슬롯에는 각 별지기의 실제 스프라이트를 표시하고, 비어 있는 슬롯만 기존 룬석 표식과 번호를 유지한다.
- 판단: 별지기 고르기·배치 전에 같은 캐릭터를 눈으로 재확인할 수 있어, 별도 설명 텍스트를 늘리지 않아도 된다.

## 2026-08-12 — 런타임 성능·유지보수 1차 정리

- Codex에게 시킨 일: 기능을 추가하지 않고, 화면 전환·로컬 저장·공용 UI·렌더 루프의 경계를 정리해 성능과 수정 안전성을 높이기.
- 결과: `game-platform.js`에 안전한 저장소와 활성 화면 상태를, `game-ui.js`에 공용 초상화·화면 전환을 분리했다. 전투 루프는 실제 게임 화면일 때만 시뮬레이션·Canvas 렌더를 수행하며, 숨겨진 탭에서는 무거운 작업을 건너뛴다. 런타임 훅은 중복 등록 방지·해제·안전한 순회를 지원한다.
- 막힌 점 / 우회: 전체 ES 모듈 전환은 순차 로드와 전역 확장 코드를 한 번에 깨뜨릴 위험이 크다. 이번에는 직접 실행 가능한 classic-script 구조를 보존하며 공용 경계부터 추출했고, 남은 래퍼는 신규 추가 금지·별도 이관 대상으로 문서화했다.
- 검증: `npm run smoke`, `npm run verify`, 변경 파일 대상 Prettier 검사, `git diff --check`.

## 2026-08-12 — 스테이지 기믹 기반 3종

- Codex에게 시킨 일: 이후 스테이지에 재사용할 반사 벽·가속 발판·잡몹을 먼저 데이터 기반 기능으로 준비.
- 결과: 각 스테이지의 `gimmicks` 선언에서 벽·발판·잡몹을 생성한다. 벽은 유성·분열 유성·굴러가는 별지기 모두를 반사하고, 발판은 통과한 물체에 한 번씩 운동량을 준다. 잡몹은 기존 체력·직격·광역 공격 경로를 그대로 사용한다. 세 기믹은 무한 훈련장에만 함께 넣었다.
- 막힌 점 / 우회: 신규 아트 없이도 기능 검증이 먼저 가능해야 했다. 프로젝트의 에셋 규칙을 따라 기존 Canvas 도형과 필드 이펙트만으로 시각 구분을 만들고, 전용 에셋은 필요해질 때 백로그로 넘긴다.
- 사람의 범위 결정: 캠페인 `1-2`에는 범퍼만 유지하고, 이후 스테이지에 기믹을 한 개씩만 도입한다.

## 2026-08-12 — 전체 런타임 최적화 2차

- Codex에게 시킨 일: 전투 감각을 바꾸지 않고 HUD·SFX·이펙트 배열·조준 계산의 반복 비용을 전체 점검.
- 결과: HUD는 실제 값이 바뀔 때만 DOM을 갱신하며, 충돌 때마다 발사 아이콘·파티 요약을 다시 만들지 않는다. SFX Audio 풀은 해당 소리가 처음 필요할 때만 생성한다. 짧은 전투 이펙트 배열은 새 배열을 만들지 않고 제자리 정리하며, 조준 보정·가장 가까운 별지기 탐색도 임시 배열/정렬 없이 수행한다.
- 판단: 정적 전장 캐시와 비전투 화면 루프 중단 위에, 프레임 중 GC와 DOM 재배치를 줄이는 것이 저사양 웹 브라우저에서 가장 안전한 개선이다. 물리 상수·타격 수치·연출 시간은 변경하지 않았다.
- 검증: `npm run smoke`, `npm run verify`, 변경 JS 포맷 검사, `git diff --check`.

## 2026-08-12 — 문서 기준선 정리

- Codex에게 시킨 일: 최근 온보딩·메타·기믹·런타임 최적화 이후 문서가 서로 다른 상태를 가리키지 않도록 정리.
- 결과: `DOCUMENTATION_INDEX.md`에 문서별 역할·읽는 순서·현재 사실 기준선·갱신 규칙을 만들고, README·진행 보고·제출 원본의 날짜·모듈 수·용어·기믹 상태를 현재 코드와 맞췄다.
- 막힌 점 / 우회: 과거 시안을 설명하는 DEVLOG에는 이전 용어가 남아 있지만 이는 당시 결정의 증거다. 현재 상태 문서에서만 최신 용어와 수치를 단일 기준으로 유지한다.
- 사람의 판단: Hive 실통신과 현 물리 기준 봇은 준비 상태를 완료 성과로 올리지 않는다.

## 2026-08-12 — 빠른 인수인계 검사 명령

- 결과: `npm run check` 하나로 정적 증거 검증과 브라우저 런타임 계약 검사를 차례로 실행하게 하고, 실행·유지보수 안내에 같은 명령을 연결했다.
