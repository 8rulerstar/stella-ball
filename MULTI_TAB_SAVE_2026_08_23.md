# 탭 두 개가 서로의 진행을 지운다 — 출시 전 결정 요청

2026-08-23 출시 준비 스윕에서 확정한 데이터 손실이다. **저장 구조를
바꾸는 결정이라 코드로 밀지 않았다.** 관례대로 실측 근거와 선택지를 적는다.

## 1. 증상 — 실측

`saveProgress()`는 자기 탭의 메모리 객체를 통째로 쓴다:

```js
function saveProgress() {
  appStorage.writeRecord(PROGRESS_STORAGE, progress); // game-meta-state.js:170
}
```

합치기(merge)가 없고, 다른 탭이 썼다는 사실을 알 길도 없다 —
`addEventListener("storage")`가 저장소 전체에 **0곳**이다. 마지막에 쓴
탭이 이긴다.

진짜 탭 둘로 재현(iframe 아님, CDP로 새 탭을 열어 별도 연결로 몰았다):

| 순서 | 한 일                           | 디스크의 골드 |
| ---- | ------------------------------- | ------------- |
| 1    | 탭A가 1000골드 저장             | 1000          |
| 2    | 탭B를 연다(1000을 읽음)         | 1000          |
| 3    | 탭A가 500을 더 벌어 저장        | **1500**      |
| 4    | 탭B가 아무거나 저장(클리어 1회) | **1000**      |

**탭A가 번 500골드가 사라진다.** 탭B는 자기가 무엇을 지웠는지 모르고,
탭A도 화면에는 1500이 그대로 떠 있어 새로고침 전까지 아무도 모른다.

`saveProgress()` 호출 지점이 **17곳**이다(클리어 보상, 소환, 상점 구매,
업적 수령, 설정 변경 등) — 즉 탭B에서 «아무 일이나» 하면 발동한다.

## 2. 플레이어가 실제로 닿는가

닿는다. 꾸며낸 상태가 필요 없다:

- 브라우저의 「이전 세션 복원」이 탭을 두 개 띄운다.
- 탭을 복제한다(Ctrl+Shift+K / 우클릭 → 복제).
- 이미 열어 둔 채로 북마크·바로가기를 다시 누른다.
- 두 창에서 각각 켠다.

## 3. 선택지

### A. 쓰기 직전에 합친다 — 권고

`saveProgress()`가 쓰기 전에 디스크를 다시 읽어 필드별로 합친다.
합치는 규칙은 필드 성격이 정한다:

- **누적값**(gold·clears·freeSummons·pendingGold): 디스크 값을 기준으로
  «이 탭이 만든 증분»을 얹는다. 마지막 저장 이후의 델타를 들고 있어야
  하므로 그 자리에 마지막 스냅샷이 필요하다.
- **최고 기록**(bestShots는 작을수록 좋고, bestCombo·bestTime은 각각
  방향이 있다): 양쪽 중 더 좋은 쪽.
- **집합**(unlocked·업적): 합집합.
- **설정**(언어·음량): 마지막에 쓴 쪽. 사람이 방금 만진 값이다.

> **주의 — 「최솟값/최댓값만 취하는」 얕은 합치기는 가짜 수리다.**
> 검증 과정에서 확인했다: gold를 `Math.max(디스크, 메모리)`로 합치면
> 위 표의 4단계에서 max(1500, 1000) = 1500이라 통과하는 것처럼 보이지만,
> 탭B가 500을 «쓴» 경우(구매)에는 그 지출이 사라져 골드가 늘어난다.
> 누적값은 반드시 델타로 다뤄야 한다.

### B. 한 탭만 허용한다

`storage` 이벤트나 `BroadcastChannel`로 다른 탭의 존재를 감지해, 나중에
연 탭을 읽기 전용으로 두고 「다른 탭에서 실행 중입니다」를 띄운다.
구현이 A보다 훨씬 작고 데이터 규칙을 건드리지 않는다. 대신 두 탭을
일부러 쓰는 사람에게는 기능 제한이다.

### C. 알리기만 한다

`storage` 이벤트를 듣고 다른 탭이 썼을 때 「다른 탭에서 진행이
바뀌었습니다 · 새로고침」을 띄운다. 손실을 막지는 못하고 **눈치채게만**
한다. A·B를 못 할 때의 최소 조치다.

## 4. 어느 쪽이든 함께 볼 것

- 저장 실패가 조용하다. `appStorage.writeRecord`의 `catch {}`가 비어
  있어(`game-platform.js:20`) 저장이 막힌 브라우저 설정(사이트 데이터
  차단, 시크릿 창의 일부 구성)에서는 플레이어가 계속 벌고 화면은 계속
  보상을 보여 주다가 새로고침 한 번에 전부 사라진다. **경고 한 줄이라도
  띄우려면 조심할 것** — `game-platform.js`는 체인의 첫 파일이라 `toast()`가
  아직 없다. 플래그만 세우고 UI가 선 뒤에 읽어야 한다.
- 게이트는 어느 선택지든 막지 않는다. `saveProgress`는
  `verify-evidence.mjs`의 표식이 아니고, 새 함수 이름만 저장소에서
  유일하면 `smoke-runtime.mjs`의 중복 정의 금지도 통과한다.
- 봇 리포트는 저장을 타지 않으므로 불변이다.

## 5. 결정해 주실 것

1. **A·B·C 중 무엇으로 갈 것인가.** (권고 A, 시간이 없으면 B)
2. A로 간다면 **필드별 합치기 규칙**을 위 §3-A 초안대로 확정할 것인가.
3. §4의 저장 실패 알림을 이번에 함께 넣을 것인가.
