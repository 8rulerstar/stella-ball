# Stella Ball — 디자인·에셋 제작 백로그

> 기능 구현 중 발견한 시각 자산 요구를 잃지 않고 모으는 단일 장부다.
> **이 문서는 제작 목록이지, 즉시 제작 지시가 아니다.** 새 아트·애니메이션·SFX는 사람이 일괄 제작 패스를 승인한 뒤에만 반입한다.

## 작업 규칙

1. 기능 작업에서 새 시각 자산이 필요해 보이면, 먼저 이 문서에 항목을 추가한다. 새 PNG·GIF·WAV를 임의로 만들거나 외부 에셋을 바로 반입하지 않는다.
2. 현재 파일·CSS 도형·기존 라이브러리로 플레이 가능성을 유지할 수 있으면 임시 표현을 사용하고, 최종 자산 요구는 여기에 남긴다.
3. 항목에는 **무엇을**, **어디에서**, **어떤 플레이 피드백을 위해** 쓰는지 적는다. 단순히 “멋진 이펙트”처럼 목적이 없는 항목은 등록하지 않는다.
4. 프레임 수·해상도·팔레트·출처는 알 때만 적고, 모르는 값은 추정하지 않는다. `미정`으로 둔 뒤 일괄 아트 패스에서 결정한다.
5. `제작 준비`가 된 항목만 다음 에셋 작업의 입력이다. 제작 후에는 원본·라이선스·경로를 `ASSET_PLAN.md`와 `assets/ASSET_MANIFEST.json`에 기록하고, 이 장부는 `반입 완료`로 옮긴다.
6. 게임 규칙·수치가 바뀌어 필요가 사라지면 삭제하지 말고 `보류/폐기`와 이유를 남긴다. 나중에 같은 아이디어를 되살릴 근거가 된다.

## 상태와 우선순위

| 상태        | 뜻                                              |
| ----------- | ----------------------------------------------- |
| `후보`      | 기능 구현 중 발견. 아직 제작하지 않는다.        |
| `명세 필요` | 역할은 확정됐지만 형태·규격·출처가 부족하다.    |
| `제작 준비` | 일괄 아트 패스에 넣어도 되는 명세다.            |
| `반입 완료` | 파일·출처·실제 사용 경로가 확정됐다.            |
| `보류/폐기` | 현재 범위에서는 만들지 않는다. 이유를 보존한다. |

- `P0`: 없으면 조작·피드백·가독성이 깨진다. 다만 즉시 제작하지 말고 최소 임시 표현을 쓴다.
- `P1`: 전투나 화면의 핵심 정체성을 강화한다. 다음 일괄 패스 후보다.
- `P2`: 품질을 높이지만 현재 플레이에는 영향이 없다.

## 항목 작성 양식

```md
### [상태] P? — 자산 이름

- 화면 / 트리거:
- 플레이 목적:
- 필요한 형태: (정적 / 애니메이션 / UI 아이콘 / SFX / 배경)
- 희망 규격·프레임: (미정 가능)
- 화풍·색 규칙:
- 기존 후보·재사용 경로:
- 코드 연결 지점:
- 결정·검수 메모:
```

## 현재 후보

### [명세 필요] P1 — 보스별 발동 SFX 구분

- 화면 / 트리거: 보스 10종의 attack·death 프레임.
- 플레이 목적: 실루엣과 색은 구분되지만 소리가 공용 큐라, 어떤 보스의 어떤 상태인지 귀로 구분되지 않는다.
- 필요한 형태: 보스 계층 4종(void·teal·apricot·pale)별 attack 상승음과 death 붕괴음. WAV 0.6초 이하.
- 기존 후보·재사용 경로: `scripts/generate_sfx_parry.py`가 같은 규격(22050Hz 모노 16비트)으로 절차 생성하므로 거기에 계층 4종을 더하면 된다. 외부 샘플·라이선스 없음.
- 코드 연결 지점: `game-combat-physics.js`의 보스 피해·처치 경로, `game-feedback.js`의 `sampleSfxCue`.
- 결정·검수 메모: 아트는 반입됐으므로 소리만 남았다. attack·death 상태 자체가 아직 재생되지 않으므로(§보스 팩 규약), 상태 재생이 붙는 시점과 함께 본다.

### [후보] P2 — 보스 등장 컷인

- 화면 / 트리거: 각 월드 첫 진입 시 보스 등장.
- 플레이 목적: 월드가 바뀌었다는 것을 전투 시작 전에 알린다.
- 필요한 형태: 정지 컷인 1장 또는 3프레임 등장 연출.
- 기존 후보·재사용 경로: 이번 팩의 `boss10/<slug>.png` 정지 프레임 확대.
- 코드 연결 지점: 미정 (`game-session.js`의 전투 진입).
- 결정·검수 메모: 캠페인 구성이 확정된 뒤 판단한다.

### [반입 완료] P1 — 모루 오크 리스타일 본체

- 화면 / 트리거: 전투 유닛 토큰·구르기·정산 공격, 편성 트레이와 상세 카드의 모루 전신.
- 플레이 목적: 유일하게 잡부(Pawn) 실루엣이던 모루를 기사·사수 옆에서도 구분되는 브루트 실루엣으로 교체한다. Warrior 전환안은 샛별·그믐과 겹쳐 폐기됐고, 사람이 Tiny RPG Orc 리스타일 안을 승인했다 (2026-08-12).
- 필요한 형태: 애니메이션 (idle 6프레임, roll 4, attack 4) + 정지 토큰 1.
- 규격: 192px 셀. 원본은 100px 셀 초소형 스프라이트라 4배 무손실 확대 후 고정 윈도우로 발 기준선 y=136 정렬.
- 화풍·색 규칙: 고블린 녹색 피부 → 황동 브론즈 램프, 갑주·도끼는 강철 유지, 참격 이펙트는 잔불 크림. Ink & Brass 안에서 모루 고유색(#ffac67)은 링·이름에만.
- 반입 경로: `characters/taeo-orc-idle.png`, `characters/anim/taeo-roll.png`·`taeo-attack.png`, `characters/cute/taeo-orc-token.png`. 재생성은 `scripts/generate_taeo_orc_restyle.py`.
- 코드 연결 지점: `heroes.taeo`(frames 6)·`combatUnitSize.taeo`(118). `cuteUnitArt`와 `heroAnimArt`는 2026-08-13에 제거됐다.
- 결정·검수 메모: 구 Pawn 시트(`taeo-miner-idle.png`)와 구 큐트 토큰은 롤백 대비로 보존. 출처는 `ATTRIBUTION.md`의 Tiny RPG 항목.
- **종료 (2026-08-13)**: 신규 로스터 패치가 이 자리의 별지기를 모루로 바꾸면서 `taeo-orc-idle.png`·`anim/taeo-roll.png`·`anim/taeo-attack.png`를 전부 덮어썼다. 이 항목의 오크 리스타일은 더 이상 화면에 없고, `scripts/generate_taeo_orc_restyle.py`를 돌리면 새 아트가 지워진다. Tiny RPG 출처가 남은 파일은 `characters/cute/taeo-orc-token.png` 하나뿐이다.

### [명세 필요] P1 — 편성 트레이 카드 전술 정보 레이어

- 화면 / 트리거: 편성 화면 하단 트레이에서 별지기를 비교하고 자리로 끌어 놓을 때.
- 플레이 목적: 초상화만 보고도 역할·능력 유형·배치 여부를 빠르게 구분한다.
- 필요한 형태: UI 아이콘, 48×48 초상화 크롭, 능력 유형 배지.
- 희망 규격·프레임: 트레이 카드의 초상 표시 크기는 48×48, 전장 마커는 64×64. 배지 규격은 미정.
- 화풍·색 규칙: 새벽 관측소의 어두운 플레이트, 별지기 고유색은 테두리·작은 배지에만 사용.
- 기존 후보·재사용 경로: 현재 캐릭터 idle 스프라이트를 `setPortrait()`로 크롭해 쓴다. 팔레트는 `UI_KIT_DAWN.md`.
- 코드 연결 지점: `prototypes/js/game-session.js`의 `showRoster()`, `.squad-unit`·`.slot-card`.
- 결정·검수 메모: 2026-08-12에 편성과 배치가 한 화면으로 합쳐지면서 대상이 카드 그리드에서 트레이+전장 마커로 바뀌었다. 이전 로스터 카드 그리드 기준의 64×64 요구는 폐기한다.

### [후보] P2 — 전장 자리 역할 아이콘

- 화면 / 트리거: 편성 화면 전장 미니맵의 자리 1~3, 그리고 상세 카드의 역할 줄.
- 플레이 목적: 자리마다 거상과의 거리가 다르다(근접 / 중거리 / 원거리)는 것을 글자를 읽지 않고 구분한다.
- 필요한 형태: UI 아이콘 3종(근접 / 중거리 / 원거리).
- 희망 규격·프레임: 16×16 또는 20×20. 확정 아님.
- 화풍·색 규칙: 새벽 관측소. 자리 번호 배지의 살구색과 겹치지 않게 청록 계열로 둔다.
- 기존 후보·재사용 경로: 현재는 `slotRole()`이 돌려주는 한글 라벨을 마커 아래 텍스트로만 표시한다.
- 코드 연결 지점: `SLOT_BANDS`·`slotRole()`(`game-data.js`), `showRoster()`의 `.slot-zone`·`.squad-detail-zone`.
- 결정·검수 메모: 라벨 텍스트만으로 충분한지 실제 플레이에서 먼저 확인한다.

### [명세 필요] P1 — 전장 별지기 스킬 식별 배지

- 화면 / 트리거: 전투 중 유닛이 멈추거나 각성 공격을 준비·발동할 때.
- 플레이 목적: 어떤 별지기의 어떤 능력이 방금 전투 결과를 만들었는지 즉시 읽힌다.
- 필요한 형태: 전장 토큰용 작은 스킬 배지와 짧은 충전/발동 애니메이션.
- 희망 규격·프레임: 미정.
- 화풍·색 규칙: 캔버스 판정용 원색을 유지하되, UI 프레임은 저채도 황동/달빛 톤.
- 기존 후보·재사용 경로: `assets/library/restyle/fx/`의 유닛별 VFX. 새 배지는 이 효과와 실루엣이 겹치지 않아야 한다.
- 코드 연결 지점: `prototypes/js/game-core-render.js`, `prototypes/js/game-feedback.js`.
- 결정·검수 메모: 능력 규칙이 추가·변경될 때 한꺼번에 아이콘 언어를 설계한다.

### [후보] P2 — 루나 관측 보조 제스처 세트

- 화면 / 트리거: 1-1 온보딩 대사와 튜토리얼 다시보기.
- 플레이 목적: 긴 안내 문장을 보조하고, 지금 조작할 대상에 시선을 모은다.
- 필요한 형태: NPC 정적 초상화 또는 짧은 제스처 애니메이션.
- 희망 규격·프레임: 후보 팩의 규격 검수 후 결정.
- 화풍·색 규칙: 밤하늘 관측소 톤, 현재 픽셀 아트와 같은 축소 가독성.
- 기존 후보·재사용 경로: `assets/candidates/observatory-pack-01/`의 루나 후보.
- 코드 연결 지점: `prototypes/js/game-onboarding.js`.
- 결정·검수 메모: 후보 파일은 현재 런타임에 미반입. 실제 튜토리얼 폭에서 가장자리·읽기성을 먼저 검수한다.

### [일부 반입] P2 — 스테이지별 관측 지점 아트 키트

**2026-08-13 보스가 월드 구분을 맡는다.** 월드 보스 7종이 반입돼 월드를 구분하는 1차 수단이 됐다. **지도 상징과 카드 배경 타일은 미제작**이므로 항목을 남긴다.

- 화면 / 트리거: 별자리 캠페인 지도와 스테이지 선택 카드.
- 플레이 목적: 1-2 이후 새 스테이지의 기믹·분위기를 전투 시작 전에 구분한다.
- 필요한 형태: 지도 상징, 카드 배경 타일, 소품 레이어.
- 희망 규격·프레임: 미정.
- 화풍·색 규칙: 배경은 저채도 잉크 밤, 기믹은 한 스테이지당 한 색 포인트만 사용.
- 기존 후보·재사용 경로: `assets/library/props/`, `assets/terrain/` 및 스테이지 아트 라이브러리.
- 코드 연결 지점: `prototypes/js/game-data.js`의 `libraryArt.stages`, `showStageSelect()`.
- 결정·검수 메모: 1-3의 실제 전투 기믹이 결정된 뒤 그 기믹 하나에 맞춰 제작한다.

### [명세 필요] P1 — 별빛 소환(뽑기) 연출 세트

- 화면 / 트리거: 메타 허브의 `별빛 소환` 탭에서 골드로 아직 만나지 못한 별지기를 해금할 때.
- 플레이 목적: 새 별지기를 얻은 순간을 일반 메뉴 클릭과 구분하고, 다음 스테이지에서 써 보고 싶은 기대를 만든다.
- 필요한 형태: 소환 원형 배경, 별빛 카드 전환 애니메이션, 희귀도/해금 프레임, 짧은 상승·확정 SFX.
- 희망 규격·프레임: 카드/배경 규격과 프레임 수 미정. 웹 가로 화면과 축소 화면에서 모두 읽히는 크기로 일괄 결정한다.
- 화풍·색 규칙: Ink & Brass의 잉크 밤 표면 위에 달빛 청백·별빛 황동만 사용한다. 과도한 네온·무지개 색은 금지하고, 별지기 고유색은 해금된 캐릭터 카드에만 쓴다.
- 기존 후보·재사용 경로: 현재 캐릭터 idle 스프라이트, `assets/library/restyle/fx/crit-star.png`, 기존 절차 합성 효과음. 현재 구현은 이 파일을 새로 반입하지 않고 CSS 빛/회전으로 대체한다.
- 코드 연결 지점: `prototypes/js/game-meta.js`의 `showGacha()`와 `prototypes/stella-ball-theme.css`의 `.gacha-*`.
- 결정·검수 메모: 이번 기준은 100골드로 미보유 별지기 1명을 확정 해금하며, 중복/확률/천장/유료 재화는 범위 밖이다. 실제 전용 에셋 제작은 일괄 아트 패스에서만 한다.

### [명세 필요] P2 — 골드 메타 아이콘·획득 파편

- 화면 / 트리거: 일반 스테이지 클리어 보상과 메타 상단의 보유 골드 표시.
- 플레이 목적: 단순 숫자가 아니라 다음 별빛 소환에 쓸 수 있는 보상이라는 것을 빠르게 읽힌다.
- 필요한 형태: 작은 골드 코인 아이콘, 결과 화면의 획득 파편/짧은 반짝임 애니메이션, 선택적 SFX.
- 희망 규격·프레임: 미정.
- 화풍·색 규칙: 황동 외곽·달빛 하이라이트의 단순 픽셀 코인. 배경의 별빛 보상과 혼동되지 않도록 별 모양 대신 원형 코인 실루엣을 쓴다.
- 기존 후보·재사용 경로: 현재 구현은 CSS 원형 코인으로 대체한다.
- 코드 연결 지점: `prototypes/stella-ball-theme.css`의 `.hub-gold`, `.result-gold`.
- 결정·검수 메모: 소비처와 최종 경제 규칙이 확정된 뒤 소환 세트와 같은 팔레트로 한 번에 제작한다.

### [명세 필요] P1 — 관측소 상점과 도색장 오르

- 화면 / 트리거: 하단 탭 `상점`. 유성 도색을 고르고 구매하는 화면 전체.
- 플레이 목적: 골드가 쓰이는 곳을 하나의 장소로 기억시키고, 상인의 반응으로 "다음에 뭘 살지"를 생각하게 만든다.
- 필요한 형태: 도색장 오르의 초상(idle 1종, 말하는 상태 1종), 상점 카운터/작업대 배경 1장, 도색 카드 프레임, 구매 확정 SFX 1종.
- 희망 규격·프레임: 초상 64×64 픽셀 기준으로 검토 중. 배경 규격 미정.
- 화풍·색 규칙: Ink & Brass 잉크 밤 + 황동. 상인은 관측소 소속이므로 별지기 고유색을 쓰지 않고 황동·달빛만 사용한다.
- 기존 후보·재사용 경로: 2026-08-12부터 새벽 관측소 픽셀 킷이 절차 생성하는 `orr` 스프라이트(14×11 도트, 후드 흉상)를 원형 배지 안에 넣어 대체한다. 파일이 아니라 `StellaPixelUI.sprite('orr')`의 dataURL이므로 에셋 반입은 아니다. 대사는 `SHOP_KEEPER_LINES`의 텍스트만 사용한다.
- 코드 연결 지점: `game-meta.js`의 `showShop()`·`shopKeeperLine()`·`pixelSpriteMarkup()`, `stella-ball-pixel-ui.js`의 `MAPS.orr`, `stella-ball-theme.css`의 `.shop-keeper`·`.shop-card`·`.shop-orb`.
- 결정·검수 메모: 도색은 외형 전용이며 새 스프라이트 없이 기존 `prism-orb.svg`의 `hue-rotate` 팔레트 스왑으로 만든다. 전용 유성 아트는 4종이 확정된 뒤 일괄 제작한다.

### [명세 필요] P1 — 소환 연출 확장(전체 화면 기준)

- 화면 / 트리거: `소환` 탭이 창 전체를 쓰도록 바뀐 뒤의 빈 무대 영역.
- 플레이 목적: 확정 해금이라 긴장은 없지만, "새 별이 도착한다"는 도착감을 준다.
- 필요한 형태: 소환진 배경 1장, 별빛 수렴 4~6프레임, 카드 개봉 전환, 해금 확정 SFX.
- 희망 규격·프레임: 무대 영역이 화면 높이의 절반 이상을 차지하므로 기존 후보보다 큰 규격이 필요하다. 정확한 값 미정.
- 화풍·색 규칙: 위 소환 세트 항목과 동일.
- 기존 후보·재사용 경로: 현재는 CSS 궤도 링과 `✦` 글리프로 대체한다.
- 코드 연결 지점: `showGacha()`, `.gacha-ritual`·`.gacha-orbit`·`.gacha-reveal`.
- 결정·검수 메모: 위 `별빛 소환(뽑기) 연출 세트`와 같은 패스에서 함께 만든다. 전체 화면 전환으로 요구 규격이 커진 것이 이 항목의 존재 이유다.

### [명세 필요] P1 — 프로필·랭킹·우편함 아이콘 세트

- 화면 / 트리거: 하단 탭 `프로필`의 세 패널(관측자 기록 / 순위 / 우편함).
- 플레이 목적: 세 기능이 한 화면에 있어도 서로 다른 역할로 읽히게 한다.
- 필요한 형태: 관측자 엠블럼, 순위 표 헤더 장식, 우편 봉투 아이콘(읽음/안 읽음 2종), 우편 수령 SFX, 그리고 프로필 아이콘 선택지 확장분.
- 희망 규격·프레임: 패널 아이콘 24×24 검토 중, 확정 아님. 프로필 아이콘은 표시 크기 44×44.
- 화풍·색 규칙: 새벽 관측소. 안 읽은 우편과 수령 대기만 강조색을 쓰고 나머지는 무채색으로 둔다.
- 기존 후보·재사용 경로: 2026-08-12부터 프로필 아이콘은 선택창을 갖는다. 선택지는 픽셀 킷이 절차 생성하는 별·달·해·하트 실루엣과 달토끼·우주비행사·망원경·나침반·오르 스프라이트, 그리고 기존 룬석 7종이다. 전부 dataURL이라 새 파일은 없다. 패널 아이콘과 우편 봉투는 여전히 텍스트 배지와 CSS 카드로 대체 중이다.
- 코드 연결 지점: `showProfile()`·`profileIconOptions()`·`showProfileIconPicker()`, `stella-ball-pixel-ui.js`의 `shape('heart'|'sun')`과 `icon()`, `.profile-avatar`·`.icon-picker`·`.profile-panel`·`.rank-table`·`.mail-item`·`.claim-badge`.
- 결정·검수 메모: 순위표는 현재 로컬 기록 1행이다. Hive 실통신이 붙기 전에는 전용 아트를 만들지 않는다. 프로필 아이콘은 절차 생성분으로 충분한지 먼저 쓰고, 부족하면 일괄 패스에서 전용 세트를 만든다.

### [명세 필요] P2 — 클리어 스테이지 점등과 별자리 진행 연출

- 화면 / 트리거: 허브 별자리 지도에서 이미 클리어한 노드.
- 플레이 목적: 진행도를 숫자가 아니라 밤하늘의 밝기로 체감시킨다.
- 필요한 형태: 점등한 별 스프라이트(2~3단계 밝기), 노드 사이 연결선이 이어지는 짧은 애니메이션, 신규 해금 시 1회 재생되는 점등 SFX.
- 희망 규격·프레임: 미정.
- 화풍·색 규칙: 황동·달빛만. 클리어와 미클리어의 차이는 채도가 아니라 밝기와 글로우로 표현한다.
- 기존 후보·재사용 경로: 현재는 CSS `box-shadow` 펄스와 `★` 배지로 대체한다.
- 코드 연결 지점: `isStageCleared()`, `.constellation-node.cleared`, `.stage-cleared-mark`.
- 결정·검수 메모: 스테이지가 5개를 넘어가면 지도 레이아웃부터 재설계해야 하므로 그 전에는 제작하지 않는다.

### [일부 반입] P2 — 1-3 침식의 계단 전용 기믹 아트

**2026-08-13 파수만 반입.** 파수 본체는 보스 팩의 `erosion-warden`으로 들어왔다(idle/hit/attack/death 4상태 + 약점 젬 2개소). **벽 타일과 잔재는 여전히 미제작**이라 항목을 남겨 둔다 — 남은 범위는 반사 벽 2개의 타일뿐이다.

- 화면 / 트리거: `1-3` 전장의 반사 벽 2개와 공허 잔재 2기.
- 플레이 목적: 벽은 "튕겨 나간다", 잔재는 "먼저 치운다"로 서로 다른 행동을 유도한다.
- 필요한 형태: 반사 벽 타일(idle/충돌 2상태), 잔재 처치 파편, 벽 충돌 SFX.
- 희망 규격·프레임: 벽은 현재 118×18 좌표 기준. 아트 규격 미정.
- 화풍·색 규칙: 벽은 관측소 구조물이므로 황동, 잔재는 공허 계열 보라를 유지한다.
- 기존 후보·재사용 경로: 잔재는 `assets/enemies/void-wisp-*.png`를 이미 사용한다. 벽은 현재 CSS/캔버스 도형이다.
- 코드 연결 지점: `game-data.js`의 `stages[2].gimmicks`, `setupStageGimmicks()`.
- 결정·검수 메모: 1-3 밸런스가 봇 하니스로 검증된 뒤에 제작한다.

### [후보] P2 — 각성·수령 피드백 사운드 보강

- 화면 / 트리거: 별지기가 충돌로 깨어나는 순간, 업적/우편 보상 수령, 도색 구매.
- 플레이 목적: 시각 연출만 있고 소리가 공용 큐를 재사용 중이라, 세 사건이 귀로 구분되지 않는다.
- 필요한 형태: 각성 전용 짧은 상승음, 보상 수령 확정음, 구매 확정음.
- 희망 규격·프레임: WAV, 0.4초 이하.
- 화풍·색 규칙: 해당 없음. 기존 50종 SFX 팩의 음색 범위를 벗어나지 않는다.
- 기존 후보·재사용 경로: 현재 각성은 `awaken`, 수령·구매는 `unlock` 큐를 공유한다.
- 코드 연결 지점: `wakeUnit()`, `rewardToast()`, `claimAchievement()`, `claimMail()`, `buySkin()`.
- 결정·검수 메모: 소리 추가는 화면 작업이 끝난 뒤 한 번에 진행한다.

### [반입 완료] P1 — 별자리별 발동 연출과 도형 실루엣

**2026-08-13 반입.** 3~~5점 실루엣 5종(`assets/library/constellations/`)과 궤적·교정·현현·발동 연출을 별자리 런타임에 붙였다. 현재 정의·인식은 `game-figure-recognition.js`, 능력·연출은 `game-figure.js`가 소유한다. 성공한 `Space` 패링의 유성-별지기 접점이 고정 별빛 노드가 되며, 3–7개 노드가 별자리를 만든다. 규격·좌표 규약은 [ASSET_PLAN.md](ASSET_PLAN.md)의 「별자리 실루엣 규약」, 3~~5점 사양서는 [FIGURE_ART_SPEC.md](FIGURE_ART_SPEC.md), 6·7점은 [FIGURE_ART_SPEC_6_7.md](FIGURE_ART_SPEC_6_7.md)다. 오망성은 기존 전용 연출을 쓰므로 실루엣을 만들지 않았다. 아래는 최초 접수 기록이다.

**2026-08-13 6·7점 반입.** 오리온자리(6점)·북두칠성(7점) 실루엣 2종을 `assets/library/constellations/`에 넣고 `game-figure-recognition.js`의 `art`를 연결했다. 재생성은 `scripts/generate_constellation_art_6_7.mjs`. 미정이던 세 항목을 확정했다 — 오리온은 전신, 사이프는 반투명 암시, 북두칠성은 국자만. 반입 시점에는 `maxNodes`가 5라 볼 수 없었으나, 같은 날 패링 통합이 7로 올리면서 **일곱 계층 전부 실제로 발동한다.**

**2026-08-13 첫 고유 능력.** 화살자리가 `piercingShot`으로 갈라져 나왔다 — `axis: [3, 0]`(자루 끝→촉)을 따라 표 끝까지 직선을 쏘므로, 인식된 **방향이 결과를 가르는 첫 사례**다(촉이 보스를 향하면 56, 반대면 0). **남은 것: 나머지 7종의 고유 능력과 구분 SFX**(아직 `encloseDamage` 공용). 관통선은 전용 아트 없이 `figureFx` 위 코드 드로잉으로만 낸다.

- 화면 / 트리거: 모든 전투에서 `Space` 패링에 성공한 유성-별지기 접점이 한 샷에 3개 이상 모인 뒤 샷이 끝날 때. 양자리(3점), 화살자리·까마귀자리(4점), 카시오페이아·백조자리·오망성(5점), 오리온자리(6점), 북두칠성(7점) 중 하나가 반드시 분류된다. 0~2점은 다음 샷으로 넘기지 않고 소멸한다.
- 플레이 목적: 여덟 별자리는 각자의 간선과 이름표·실루엣(오망성·6·7점 제외)으로 구분되지만, 능력 효과는 아직 같다. 고유 능력을 결정한 뒤에도 플레이어가 무엇이 터졌는지 글자를 읽지 않고 알 수 있어야 한다.
- 필요한 형태: 별자리별 고유 능력에 연결할 발동 SFX 구분. 추가 버스트는 고유 능력 설계가 정해진 뒤에만 결정한다.
- 희망 규격·프레임: SFX 규격은 미정. 오망성만 현재 전용 연출(별 채우기·광선·맥동)을 갖고 있어 기준 삼을 수 있다.
- 화풍·색 규칙: 기존 프리즘 픽셀 FX 범위. 오망성만 따뜻한 `#ffd2a0` 계열을 쓰고 나머지는 차가운 `#9adfc9` 계열로 묶여 있다. 희귀도에 따라 색 계층을 나눠야 한다.
- 기존 후보·재사용 경로: `assets/library/fx/`의 `prism-impact` · `rune-shockwave` · `core-break-signature-512`. 현재는 공용 `areaBursts`와 `unlock` SFX만 재사용 중이다.
- 코드 연결 지점: `prototypes/js/game-figure-recognition.js`의 `FIGURE_SHAPES`(별자리 정의), `prototypes/js/game-figure.js`의 `FIGURE_ABILITIES`(능력 실행)와 `drawFigure` 훅의 이름표 블록.
- 결정·검수 메모: 선행 과제였던 도형별 연결 순서는 완료됐다. `FIGURE_SHAPES[].edges`가 실제 간선을, `figureFit()`이 실루엣과 선을 같은 변환으로 배치한다. 백조자리도 중심 별을 통과하는 십자 간선을 쓴다. 다음 자산 작업은 고유 능력이 확정된 뒤의 SFX만 대상으로 한다.

## 일괄 제작 패스 체크리스트

- [ ] `제작 준비` 항목만 선별했고, 플레이 목적이 겹치는 항목을 합쳤다.
- [ ] 각 항목의 실제 표시 크기와 필요한 상태(idle / hit 등)를 확정했다.
- [ ] 기존 `assets/library/` 재사용 가능 여부와 라이선스를 확인했다.
- [ ] 새 파일의 출처·생성 방법·라이선스를 `ASSET_PLAN.md`와 매니페스트에 기록할 준비가 됐다.
- [ ] 임시 표현을 새 파일로 바꾼 뒤, `npm run verify`, `npm run smoke`와 실제 화면 가독성 검수를 진행한다.
