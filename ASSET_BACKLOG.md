# Stella Ball — 디자인·에셋 제작 백로그

> 기능 구현 중 발견한 시각 자산 요구를 잃지 않고 모으는 단일 장부다.
> **이 문서는 제작 목록이지, 즉시 제작 지시가 아니다.** 새 아트·애니메이션·SFX는 사람이 일괄 제작 패스를 승인한 뒤에만 반입한다.

## 작업 규칙

1. 기능 작업에서 새 시각 자산이 필요해 보이면, 먼저 이 문서에 항목을 추가한다. 새 PNG·GIF·WAV를 임의로 만들거나 외부 에셋을 바로 반입하지 않는다.
2. 현재 파일·CSS 도형·기존 라이브러리로 플레이 가능성을 유지할 수 있으면 임시 표현을 사용하고, 최종 자산 요구는 여기에 남긴다.
3. 항목에는 **무엇을**, **어디에서**, **어떤 플레이 피드백을 위해** 쓰는지 적는다. 단순히 “멋진 이펙트”처럼 목적이 없는 항목은 등록하지 않는다.
4. 프레임 수·해상도·팔레트·출처는 알 때만 적고, 모르는 값은 추정하지 않는다. `미정`으로 둔 뒤 일괄 아트 패스에서 결정한다.
5. `제작 준비`가 된 항목만 다음 에셋 작업의 입력이다. 제작 후에는 원본·라이선스·경로를 `ASSET_PLAN.md`와 `assets/ASSET_MANIFEST.json`에 기록하고, 이 장부는 `반입 완료`로 옮긴다.
6. 게임 규칙·수치가 바뀌어 필요가 사라지면 삭제하지 말고 `보류/폐기`와 이유를 남긴다. 나중에 같은 아이디어를 되살릴 근거가 된다.

## 상태와 우선순위

| 상태 | 뜻 |
| --- | --- |
| `후보` | 기능 구현 중 발견. 아직 제작하지 않는다. |
| `명세 필요` | 역할은 확정됐지만 형태·규격·출처가 부족하다. |
| `제작 준비` | 일괄 아트 패스에 넣어도 되는 명세다. |
| `반입 완료` | 파일·출처·실제 사용 경로가 확정됐다. |
| `보류/폐기` | 현재 범위에서는 만들지 않는다. 이유를 보존한다. |

- `P0`: 없으면 조작·피드백·가독성이 깨진다. 다만 즉시 제작하지 말고 최소 임시 표현을 쓴다.
- `P1`: 전투나 화면의 핵심 정체성을 강화한다. 다음 일괄 패스 후보다.
- `P2`: 품질을 높이지만 현재 플레이에는 영향이 없다.

## 항목 작성 양식

```md
### [상태] P? — 자산 이름

- 화면 / 트리거:
- 플레이 목적:
- 필요한 형태: (정적 / 애니메이션 / UI 아이콘 / SFX / 배경)
- 희망 규격·프레임: (미정 가능)
- 화풍·색 규칙:
- 기존 후보·재사용 경로:
- 코드 연결 지점:
- 결정·검수 메모:
```

## 현재 후보

### [명세 필요] P1 — 로스터 카드 전술 정보 레이어

- 화면 / 트리거: 별지기 편성 화면에서 캐릭터를 비교·선택할 때.
- 플레이 목적: 초상화만 보고도 역할·능력 유형·선택 상태를 빠르게 구분한다.
- 필요한 형태: UI 아이콘, 64×64 초상화 크롭, 능력 유형 배지.
- 희망 규격·프레임: 64×64 초상화, 배지 규격은 미정.
- 화풍·색 규칙: Ink & Brass의 어두운 플레이트, 별지기 고유색은 좌측 레일·작은 배지에만 사용.
- 기존 후보·재사용 경로: 현재 캐릭터 idle 스프라이트. 상세 레시피는 `UI_REDESIGN_HANDOFF.md`의 다음 단계.
- 코드 연결 지점: `prototypes/js/game-meta.js`의 `showRoster()`.
- 결정·검수 메모: 실제 카드 정보 구조가 확정된 뒤 크롭과 배지를 한 번에 제작한다.

### [명세 필요] P1 — 전장 별지기 스킬 식별 배지

- 화면 / 트리거: 전투 중 유닛이 멈추거나 각성 공격을 준비·발동할 때.
- 플레이 목적: 어떤 별지기의 어떤 능력이 방금 전투 결과를 만들었는지 즉시 읽힌다.
- 필요한 형태: 전장 토큰용 작은 스킬 배지와 짧은 충전/발동 애니메이션.
- 희망 규격·프레임: 미정.
- 화풍·색 규칙: 캔버스 판정용 원색을 유지하되, UI 프레임은 저채도 황동/달빛 톤.
- 기존 후보·재사용 경로: `assets/library/restyle/fx/`의 유닛별 VFX. 새 배지는 이 효과와 실루엣이 겹치지 않아야 한다.
- 코드 연결 지점: `prototypes/js/game-core-render.js`, `prototypes/js/game-feedback.js`.
- 결정·검수 메모: 능력 규칙이 추가·변경될 때 한꺼번에 아이콘 언어를 설계한다.

### [후보] P2 — 루나 관측 보조 제스처 세트

- 화면 / 트리거: 1-1 온보딩 대사와 튜토리얼 다시보기.
- 플레이 목적: 긴 안내 문장을 보조하고, 지금 조작할 대상에 시선을 모은다.
- 필요한 형태: NPC 정적 초상화 또는 짧은 제스처 애니메이션.
- 희망 규격·프레임: 후보 팩의 규격 검수 후 결정.
- 화풍·색 규칙: 밤하늘 관측소 톤, 현재 픽셀 아트와 같은 축소 가독성.
- 기존 후보·재사용 경로: `assets/candidates/observatory-pack-01/`의 루나 후보.
- 코드 연결 지점: `prototypes/js/game-onboarding.js`.
- 결정·검수 메모: 후보 파일은 현재 런타임에 미반입. 실제 튜토리얼 폭에서 가장자리·읽기성을 먼저 검수한다.

### [후보] P2 — 스테이지별 관측 지점 아트 키트

- 화면 / 트리거: 별자리 캠페인 지도와 스테이지 선택 카드.
- 플레이 목적: 1-2 이후 새 스테이지의 기믹·분위기를 전투 시작 전에 구분한다.
- 필요한 형태: 지도 상징, 카드 배경 타일, 소품 레이어.
- 희망 규격·프레임: 미정.
- 화풍·색 규칙: 배경은 저채도 잉크 밤, 기믹은 한 스테이지당 한 색 포인트만 사용.
- 기존 후보·재사용 경로: `assets/library/props/`, `assets/terrain/` 및 스테이지 아트 라이브러리.
- 코드 연결 지점: `prototypes/js/game-data.js`의 `libraryArt.stages`, `showStageSelect()`.
- 결정·검수 메모: 1-3의 실제 전투 기믹이 결정된 뒤 그 기믹 하나에 맞춰 제작한다.

## 일괄 제작 패스 체크리스트

- [ ] `제작 준비` 항목만 선별했고, 플레이 목적이 겹치는 항목을 합쳤다.
- [ ] 각 항목의 실제 표시 크기와 필요한 상태(idle / hit 등)를 확정했다.
- [ ] 기존 `assets/library/` 재사용 가능 여부와 라이선스를 확인했다.
- [ ] 새 파일의 출처·생성 방법·라이선스를 `ASSET_PLAN.md`와 매니페스트에 기록할 준비가 됐다.
- [ ] 임시 표현을 새 파일로 바꾼 뒤, `npm run verify`, `npm run smoke`와 실제 화면 가독성 검수를 진행한다.
