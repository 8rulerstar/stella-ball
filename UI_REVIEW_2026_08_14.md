# 화면·연출 전수 검토 — 디자인 세션 요청서

> 검토일: 2026-08-14
> 대상: 타이틀, 허브·캠페인 지도, 편성, 소환, 상점, 기록·업적, 전투
> 방법: 데스크톱 1280×720 / 1440×820 실기, DOM·계산 스타일 실측
> 상태: **검토 결과와 요청. 여기서 새 래스터·SFX를 만들지 않는다.** 실제 반입은 [ASSET_BACKLOG.md](ASSET_BACKLOG.md)의 승인 절차를 따른다.

## 0. 읽는 법

각 항목은 **측정값 또는 화면 근거**를 달았다. 근거 없이 “좋게 만들자”류는 넣지 않았다. 우선순위는 다음 기준이다.

| 등급 | 뜻                                                           |
| ---- | ------------------------------------------------------------ |
| A    | 화풍의 근간이 깨지는 문제. 다른 작업보다 먼저 판단해야 한다. |
| B    | 특정 화면의 완성도 문제. 해당 화면을 손댈 때 같이 처리한다.  |
| C    | 있으면 좋은 개선. 여유가 있을 때.                            |

---

## 1. [A] 픽셀 아트가 거의 전부 비정수 배율로 확대·축소되고 있다

이 게임의 화풍은 픽셀 아트인데, **화면에 나오는 픽셀의 크기가 자리마다 다르다.** 정수 배율(2×, 3×, 4×)이 아니면 어떤 픽셀은 1칸, 어떤 픽셀은 2칸으로 그려져 격자가 흔들린다. `image-rendering: pixelated`는 이 어긋남을 부드럽게 감춰주지 않고 그대로 드러낸다.

### 실측 (1280×720, devicePixelRatio 2)

| 대상              | 원본 해상도 | 표시 크기 | 배율      |
| ----------------- | ----------- | --------- | --------- |
| **전장 캔버스**   | 720×900     | 496×620   | **0.689** |
| 달                | 44×44       | 150×150   | 3.41      |
| 고리 행성         | 48×26       | 160×87    | 3.33      |
| 우주비행사        | 52×52       | 101×101   | 1.93      |
| 배경 소품(캔버스) | 26×26       | 72×72     | 2.77      |
| 나침반            | 44×36       | 48×39     | 1.09      |
| 달토끼            | 80×60       | 68×51     | 0.85      |
| 편성 상세 초상    | 46×46       | 58×58     | 1.26      |
| 소환 후보 초상    | 46(높이)    | 34        | 0.74      |

**가장 큰 것은 전장 캔버스다.** 유닛·보스·지형·이펙트 등 전투의 모든 픽셀이 이 한 번의 0.689배 축소를 통과한다. 축소는 확대보다 나쁘다 — 픽셀이 뭉개지는 게 아니라 **행과 열이 통째로 버려진다.** 1px 외곽선이 구간마다 사라지고, 유닛 실루엣의 가장자리가 프레임마다 달라진다.

### 판단이 필요한 것

- 전장을 **정수 배율로만** 표시할 것인가? 그러면 뷰포트에 따라 여백이 생기지만 픽셀 격자가 고정된다.
- 아니면 픽셀 격자를 포기하고 부드러운 축소(`image-rendering: auto`)로 갈 것인가? 화풍이 바뀐다.
- 여백 소품(달·행성·비행사)은 **원본 해상도를 배율에 맞춰 다시 뽑을 것인가?** 예컨대 달을 44×44로 그려 3.41배 늘리는 대신, 50×50으로 그려 3배로 쓰면 격자가 맞는다.

이 셋 중 무엇을 선택하느냐에 따라 이후 모든 아트 규격이 달라지므로, **다른 항목보다 먼저 답이 필요하다.**

---

## 2. 화면별

### 2-1. [B] 편성 — 화면의 60%가 빈 격자다

- 배치 그리드가 352×440인데 그 안에 유닛 마커 2개와 보스 ◆ 하나뿐이다. 나머지는 균일한 점 격자다.
- 스테이지마다 지형·기믹이 다른데 **격자는 모든 스테이지에서 동일하다.** 편성 단계에서 “이 전장이 어떤 곳인지”를 읽을 수 없다.
- 상세 카드(262×217)에 비해 초상(58×58)이 작아 카드 상단이 비어 보인다.
- 정보가 네 귀퉁이로 흩어져 있다 — 제목 좌상단, 도움말 우상단, 트레이 좌하단, 버튼 하단 중앙, 상세 카드 우측.

**요청**: 배치 그리드에 스테이지 정체성을 넣는 안. 지형 실루엣, 기믹 위치 표시, 또는 전장 미리보기 축소판. 그리고 상세 카드의 정보 위계 재배치.

### 2-2. [B] 소환 — 연출 공간이 비어 있다

- 소환 원형이 화면 세로의 절반 이상을 차지하는데 내용물은 ✦ 하나와 별 3개다.
- 후보 카드 5장의 초상이 **0.74배로 축소**돼 누가 누군지 구분되지 않는다.
- 하단 CTA가 화면 폭 전체를 차지하는데 그 안의 글자는 작다.

**요청**: 이미 백로그에 「별빛 소환 연출 세트」와 「소환 연출 확장」이 있다. 이 검토에서 더할 것은 **후보 카드의 초상 규격**(축소하지 말 것)과 **원형 영역의 빈 공간 처리**다.

### 2-3. [B] 상점 — 카드가 잘리고 상태 표시가 버튼과 어긋난다

- 별지기 도색 섹션이 화면 하단에서 잘린다. 스크롤은 되지만 잘리는 위치에 아무 신호가 없다.
- 유성 도색 4장 중 장착 중인 카드만 버튼 대신 「장착 중」 텍스트라, 카드 높이와 정렬이 나머지 셋과 다르다.
- 상단 NPC 초상이 원형 크롭인데 스프라이트가 원 경계에서 잘린다.

**요청**: 구매 가능 / 보유 / 장착 중 **세 상태의 카드 하단 표현을 같은 높이로 통일**하는 안. 스크롤 경계 신호.

### 2-4. [B] 기록·업적 — 배지가 카드 밖으로 나가고 통계가 비어 있다

- 「수령」 배지가 카드 상단 경계 밖으로 걸쳐 다음 줄 카드와 겹친다.
- 업적 아이콘이 전부 같은 육각형 자리표시자다.
- 통계 3칸 중 두 칸이 `—`와 `0 HIT`로 비어 있다. 40스테이지를 클리어한 계정인데도 그렇다.
- 하단 「뒤로」 버튼이 스크롤 콘텐츠 위에 떠 있어 마지막 카드를 가린다.

**요청**: 배지의 위치 규약(카드 안쪽 / 모서리 걸침 중 택일), 업적 등급별 아이콘 세트, 통계가 빈 상태의 표현.

### 2-5. [C] 허브·캠페인 지도 — 월드 이동 화살표가 작다

- `◀` `▶` 버튼이 34×34다. 이 화면에서 가장 자주 누르는 컨트롤인데 가장 작다.
- 「최근 기록」이 `—`로 비어 있다.

### 2-6. [B] 타이틀 — 인트로 소품이 다른 화면까지 흐트러진 채 남는다

인트로 비트 3에서 우주비행사·달토끼·망원경·나침반이 흩어지고 **의도적으로 제자리에 돌아오지 않는다.** 그런데 이게 타이틀뿐 아니라 **허브·편성·상점·전투까지 세션 내내 유지된다.**

- 실측: 편성 화면에서 망원경이 `rotate(74deg) translate(10px, 16px)` 상태로 남아 있었다.
- 명세 5절의 의도는 “타이틀에 머무는 동안 방금 무슨 일이 있었는지가 남는다”였다.

**판단 필요**: 타이틀을 벗어날 때 소품을 원복할 것인가, 아니면 세션 내내 유지하는 게 맞는가? 후자라면 명세 문구를 그에 맞게 고쳐야 한다. (구현: `prototypes/outer-observer.js`의 `startleProps`)

---

## 3. 애니메이션·효과

### 3-1. [B] 상태 전이에 연출이 없다

화면 전환이 전부 즉시 교체다. 타이틀 → 허브, 허브 → 편성, 편성 → 전투 사이에 어떤 전환도 없어서, 같은 페이지 안에서 움직인다는 감각 대신 화면이 갈아끼워진다는 인상이 남는다.

**요청**: 관측소라는 설정에 맞는 전환 어휘 한 벌. 예컨대 관측창의 조리개, 성도의 이동, 눈금의 회전 중 하나로 통일.

### 3-2. [C] 빈 상태에 연출이 없다

「쌓인 보상 없음」, 「최근 기록 —」, 「최고 콤보 0 HIT」가 전부 텍스트 한 줄이다. 빈 상태가 화면의 상당 면적을 차지하는데 아무 표현이 없다.

### 3-3. [A/B 경계] 모션 감소 대응이 파일마다 다르다

`stella-ball-dawn.css`의 `prefers-reduced-motion` 규칙 선택자가 `#dawn-sky *`라 **그 안의 자식만** 멈춘다. `main` 쪽 애니메이션과 `#dawn-sky` 형제 요소는 덮이지 않아, 새 연출을 넣을 때마다 개별 가드를 따로 써야 한다(인트로의 `main.oo-grabbed`가 그 사례다).

**요청**: 프로젝트 전체에 적용되는 모션 감소 규약 한 벌. 어떤 클래스·어떤 계층까지 자동으로 멈추는지 정하면 이후 연출이 규약만 따르면 된다.

---

## 4. 이미 백로그에 있는 것 (중복 제기하지 않음)

아래는 [ASSET_BACKLOG.md](ASSET_BACKLOG.md)에 이미 등록돼 있다. 이 검토는 **규격·수치만 보강**한다.

| 백로그 항목                          | 이 검토가 더하는 것                     |
| ------------------------------------ | --------------------------------------- |
| 편성 트레이 카드 전술 정보 레이어    | 상세 초상 1.26배 문제, 정보 위계 흩어짐 |
| 별빛 소환 연출 세트 / 소환 연출 확장 | 후보 초상 0.74배 축소, 원형 영역 공백   |
| 프로필·랭킹·우편함 아이콘 세트       | 업적 아이콘이 전부 동일 자리표시자      |
| 관측소 상점과 도색장                 | 세 상태 카드 높이 불일치, 스크롤 경계   |
| 전장 별지기 스킬 식별 배지           | —                                       |
| 클리어 스테이지 점등·별자리 진행     | 지도 화살표 크기                        |

---

## 5. 디자인 세션에 요청하는 산출물

**1번(픽셀 배율)의 답이 먼저다.** 나머지는 그 답에 따라 규격이 달라진다. 각 항목에 대응하는 기존 게임의 해법은 7절에 정리했다.

1. **픽셀 격자 정책 한 장** — 전장·UI·배경 소품 각각을 어떤 배율로 표시할지, 그에 맞는 원본 해상도 표.
2. 편성 배치 그리드 시안 — 스테이지 정체성이 읽히는 안.
3. 화면 전환 어휘 시안 — 3-1의 전환 하나를 정해 4개 화면 전이에 적용한 스토리보드.
4. 카드 상태 규약 — 구매 가능 / 보유 / 장착 중 / 수령 대기의 배지·버튼 위치와 높이.
5. 빈 상태 표현 — 보상 없음, 기록 없음, 콤보 없음의 공통 형태.

색·프레임 수·원본 규격은 **제안값으로만** 적는다. `ASSET_PLAN.md`·`assets/ASSET_MANIFEST.json` 기록은 실제 반입 승인 뒤다.

---

## 6. 이번 검토가 보지 않은 것

정직하게 남긴다. 아래는 확인하지 않았으므로 “문제 없음”이 아니라 **“미확인”**이다.

- **온보딩 6카드** — 이 검토에서 실기로 훑지 않았다. 브라우저 E2E가 기능은 통과시키지만 구성·연출 품질은 별개다.
- **설정, 프로필, 도서관, 무한 훈련장** — 화면을 열지 않았다.
- **모바일·태블릿 비율** — 데스크톱만 봤다. 양옆 여백이 없는 비율에서의 레이아웃은 전혀 확인하지 않았다.
- **소리** — SFX·BGM의 균형과 타이밍은 이 검토의 범위 밖이다.
- **애니메이션의 실제 움직임** — 미리보기 창이 가려지면 CSS 트랜지션과 `requestAnimationFrame`이 멈춰, 정지 구도와 값만 확인했다. 유닛 구르기·패링 연출·별자리 현현이 흐르는 모습은 사람이 직접 봐야 한다.

---

## 7. 기존 게임 레퍼런스 대조

아래는 **널리 알려진 설계 패턴**을 우리 문제에 대응시킨 것이다. 남의 에셋이나 화면을 가져오자는 뜻이 아니라, 같은 문제를 이미 푼 방식이 있으니 바퀴를 다시 만들지 말자는 뜻이다.

### 7-0. 먼저 — 이미 잘 하고 있는 것

레퍼런스를 보기 전에 현 상태를 정확히 적는다. **전투 타격 피드백은 이미 상위 수준이다.**

`game-feedback.js`의 `impact()`는 콤보 수에 따라 세 값을 동시에 키운다.

| 값          | 범위                                     |
| ----------- | ---------------------------------------- |
| 히트스톱    | 0.028s (접촉) ~ 0.075s (약점·3연타 이상) |
| 화면 흔들림 | 5.5 ~ 10, 콤보 배수 최대 1.9배 곱        |
| 플래시      | 0.58 ~ 1.0, 같은 배수                    |

접촉 / 마무리 / 강타 / 기본 네 프로파일이 분리돼 있고, `hit` · `weak` · `riposte` 세 종류가 소리와 색까지 다르다. **이 부분은 강화 대상이 아니라 유지 대상이다.** 아래 제안은 여기에 더할 것만 적는다.

### 7-1. [A] 픽셀 격자 — 이건 업계에 정답이 있다

정수 배율 문제는 픽셀 아트 게임이 전부 겪었고, 사실상 두 갈래로 정리돼 있다.

| 방식                                   | 대표 사례                           | 이 게임에 적용하면                                                                |
| -------------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------- |
| **고정 저해상도 버퍼 + 정수 업스케일** | Celeste, Downwell, Shovel Knight    | 전장을 720×900 그대로 두되 **표시는 ×1, ×2만** 허용하고 남는 공간은 여백으로 둔다 |
| **픽셀 레이어와 UI 레이어 분리**       | Octopath Traveler 계열의 HD-2D 구성 | 전장 캔버스만 픽셀 규칙을 지키고, HUD·메뉴는 지금처럼 해상도 자유로운 CSS로 둔다  |

두 방식은 배타적이지 않다. **이 게임에 가장 잘 맞는 건 둘의 조합이다** — 전장 캔버스는 정수 배율만, 메뉴 UI는 지금처럼. 그러면 1번 항목의 표에서 문제가 되는 건 전장과 여백 소품뿐으로 줄어든다.

여백 소품(달 3.41배, 행성 3.33배, 비행사 1.93배)은 **원본을 배율에 맞춰 다시 뽑는 쪽**이 맞다. 달을 44×44로 그려 3.41배 늘리는 대신 50×50으로 그려 정확히 3배로 쓰면, 그림을 다시 그리지 않고 캔버스 크기만 바꿔도 격자가 맞는다. `stella-ball-dawn.js`가 절차적으로 그리므로 **아트 재작업 없이 수치만 바꾸면 되는 항목이다.**

### 7-2. [B] 편성 — 「배치 화면」이 아니라 「전장 자체」여야 한다

현재 편성 화면의 배치 그리드는 모든 스테이지에서 똑같은 점 격자다. 이 문제를 가장 깔끔하게 푼 계열은 **전투 전 배치와 실제 전장을 같은 화면으로 합친 전술 게임들**이다(Into the Breach 계열, 파이어 엠블렘의 출격 전 지도 확인).

공통 원리 두 가지:

1. **배치하는 판이 곧 싸울 판이다.** 축소된 추상 격자가 아니라 실제 지형·기믹·적 위치가 그대로 보인다.
2. **적의 다음 행동이 미리 보인다.** 배치가 도박이 아니라 판단이 된다.

이 게임에 옮기면 — 배치 그리드에 해당 스테이지의 **절차 생성 전장을 그대로 축소해 깔고**(이미 `game-arena-carve.js`가 그 이미지를 만든다), 기믹(범퍼·가속 발판·벽)의 실제 위치를 표시한다. 새 아트가 아니라 **이미 있는 것을 한 화면 앞으로 당기는 일**이다.

### 7-3. [B] 화면 전환 — 전환 자체가 정체성이 되는 사례

전환 연출을 정체성으로 쓴 대표는 페르소나 5 계열이다. 배울 점은 화려함이 아니라 **일관성**이다: 하나의 전환 어휘를 정하고 모든 전이에 쓴다.

이 게임은 소재가 이미 정해져 있다 — **관측소**다. 전환 후보:

- **조리개**: 관측창이 조였다 열리며 다음 화면이 드러난다. 전투 진입에 가장 잘 맞는다.
- **성도 이동**: 지도의 한 점에서 다음 점으로 시야가 미끄러진다. 허브↔편성에 맞는다.
- **눈금 회전**: 타이틀의 `title-dome-ticks`와 같은 어휘. 메뉴 간 이동에 맞는다.

**셋 다 만들지 말고 하나만 정해서 전부에 쓰는 것**을 권한다. 전환이 화면마다 다르면 일관성이 오히려 깨진다.

### 7-4. [B] 소환 — 길게 끄는 것이 아니라 「직전 0.5초」가 핵심

가챠 연출의 통념은 "길고 화려하게"지만, 최근 로그라이트의 보상 개봉 연출(Balatro의 부스터 팩 등)이 보여준 건 반대다.

- 연출은 **짧다**. 대신 결과 직전에 **색·소리로 등급 신호**를 한 번 준다.
- **언제나 건너뛸 수 있다.** 반복 플레이를 전제하기 때문이다.
- 결과는 **한 화면에 다 보인다.** 스크롤이나 다음 페이지가 없다.

현재 소환 화면은 원형이 세로 절반을 차지하는데 그 안이 비어 있다. **공간을 채우기보다 연출을 짧게 하고 원형을 줄이는 쪽**이 이 원리에 맞는다.

### 7-5. [C] 캠페인 지도 — 다만 이건 UI가 아니라 게임 설계 문제다

현재 캠페인은 7월드 34스테이지의 **선형** 진행이다(+ 8-1). 로그라이트 지도의 표준은 분기 선택이고, 그게 주는 것은 "내가 고른 경로"라는 소유감이다.

**그러나 이건 디자인 세션이 결정할 사안이 아니다.** 분기를 넣으면 난이도 곡선·보상 설계·해금 규칙이 전부 바뀐다. 이 문서는 **선택지가 있다는 것만 기록**하고, 판단은 사람에게 남긴다.

지금 범위에서 UI로 할 수 있는 것은 따로 있다 — 현재 지도는 클리어한 별과 안 한 별의 차이가 약하다. **지나온 경로가 별자리로 이어져 남는 표현**이면 선형이어도 진행감이 생긴다. (이미 백로그의 「클리어 스테이지 점등과 별자리 진행 연출」 항목이다.)

### 7-6. [B] 패링 — 이미 있는 피드백을 어디까지 더 밀 것인가

7-0에 적었듯 타격 피드백 자체는 충분하다. 액션 게임의 패링 연출(세키로 계열)과 대조했을 때 **비어 있는 축은 두 개**다.

1. **카메라 스케일이 없다.** 흔들림·플래시·히트스톱은 있는데 화면이 순간적으로 밀려들어오는 punch-in이 없다. 다른 셋과 달리 이건 아직 안 쓴 축이다.
2. **보스 상태에 따른 차등이 없다.** 8-1 보스는 페이즈 4단으로 형태가 변하는데, 같은 패링이 P1과 P4에서 똑같이 느껴진다. 페이즈가 오를수록 피드백을 키우면 형태 변화와 손맛이 같은 방향을 가리킨다.

### 7-7. 우리만 가진 것 — 베끼면 안 되는 부분

레퍼런스를 대조할 때 같이 적어야 공정하다. 이 게임의 조합은 어디에도 없다.

- **당구 물리 + 패링 타이밍 + 별자리 그리기**가 한 샷 안에서 이어진다.
- 그 결과물인 **별자리가 곧 보상이자 도감**이다.
- 관측창 안팎이라는 **화면 구조 자체가 서사**다(§1, 8-1 보스).

위 레퍼런스는 **개별 문제의 해법**으로만 쓰고, 이 세 축을 다른 게임의 구조에 맞추는 방향은 피한다.

### 7-8. 정리 — 무엇부터

| 순서 | 항목                                | 근거                                                              |
| ---- | ----------------------------------- | ----------------------------------------------------------------- |
| 1    | 픽셀 격자 정책 (7-1)                | 다른 모든 아트 규격이 여기 종속된다                               |
| 2    | 편성 = 전장 (7-2)                   | 새 아트 없이 있는 것을 당겨오는 일이라 비용 대비 효과가 가장 크다 |
| 3    | 화면 전환 어휘 하나 (7-3)           | 전 화면에 걸치므로 늦게 넣을수록 손댈 곳이 늘어난다               |
| 4    | 카드 상태·빈 상태 규약 (§2-3, §2-4) | 상점·기록·소환이 같은 규약을 공유한다                             |
| 5    | 패링 punch-in과 페이즈 차등 (7-6)   | 이미 있는 시스템의 확장이라 위험이 낮다                           |
