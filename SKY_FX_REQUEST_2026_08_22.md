# 여백 하늘 — 연출이 약속만 하고 하지 않는 것들

> 작성일 2026-08-22 · 밤샘 자율 세션의 실측 보고
>
> **이 문서는 요청서다. 값을 정하지 않았고, 결정 네 가지를 요청한다.**
> 코드는 건드리지 않았다 — 연출의 «무엇을 보여줄 것인가»는 오너 결정이다.
> 같은 밤에 잡은 **오류**(전투 중 유성·손님, 인트로 레이어 잔존)는 이미
> 고쳐서 올렸다(`8e65d5a`). 여기 남은 것은 고치려면 «무엇이 옳은가»를
> 먼저 정해야 하는 것들이다.

## 1. 확정된 것 — 하늘 밴드가 화면 폭 변화를 따라가지 못한다

여백 소품(별자리 걸이·눈금 고리)의 자리는 `layoutBands()`가 `main`의
가로폭에서 계산하는데, 이 함수는 **`resize`에서만** 다시 돈다. 그런데
`main`의 폭은 장면마다 바뀌고 그때는 `resize`가 나지 않는다.

| 장면   | `main` 폭 | 한쪽 여백 (1400px 창) |
| ------ | --------- | --------------------- |
| 타이틀 | 920       | 240                   |
| 허브   | 604       | 398                   |
| 전투   | 960       | 209                   |

`build()`가 타이틀에서 한 번 재므로, **허브에서는 소품이 화면 가장자리
쪽으로 몰리고**(밴드가 실제 여백보다 좁다) **전투에서는 밴드가 여백보다
넓어** 소품이 판 쪽으로 밀려 들어온다. 창 크기를 한 번 바꾸면 그때야
맞는다.

`span < 120 → opacity:0` 숨김 판정도 같은 낡은 값으로 이뤄진다.

## 2. 확정된 것 — 별을 흩는 반응이 흩을 별을 못 찾는다

`reactFigure()`(별자리 현현)와 `reactFrameAnomaly()`(역행 눈금)는 배경
레이어에서 **1px짜리 별 `div`**를 찾아 움직인다. 그런데 이 파일은 별밭을
캔버스 한 장으로 옮기며 그 `div`들을 **의도적으로 없앴다**(파일 안 주석에
근거가 남아 있다).

실측: L0의 `div`는 4개, 그중 `width === "1px"`인 것은 **0개**.

그래서 「별이 존재의 윤곽을 피해 흐른다」는 비트는 **아무 일도 하지
않는다.** 두 번째 반응은 더 나쁘다 — 후보가 없으면 «네 번째 자식»을
집는 대체 경로가 있어서, 마침 살아 있던 구름이나 유성 껍데기의 크기와
색을 2px 점으로 덮어쓰고 «복원»한다.

## 3. 확인이 필요한 것 — 망원경이 무엇을 관측하는가

망원경을 끌어다 놓으면 여백의 천체를 «관측»하는데, 판정 후보에
`#dawn-sky > canvas`(전체 화면 별 캔버스)가 섞여 있다. 문서 순서상 이
캔버스가 앞이라, **크기를 가지면 언제나 먼저 잡힌다** — 달·붉은 행성·
고리 행성은 영영 못 찾고 판정은 늘 「이름 없는 천체」가 된다.

**다만 이 세션에서는 재현하지 못했다.** 헤드리스 창에서 별 캔버스의
`getBoundingClientRect()`가 0×0으로 나와 `!b.width` 가드에 걸러졌다.
실기(가속 켜진 창)에서 한 번 끌어 보면 바로 갈린다.

## 4. 확인이 필요한 것 — 세로 화면에서 «관측창 밖»이 성립하는가

인트로의 존재는 몸통을 **화면 높이**로 키우고(`150vh`) 이동은 **화면
너비**로 잰다. 가로 화면에서는 여백을 지나가지만, 세로에서는 몸통이
화면보다 넓어져 「관측창 밖을 지나간다」가 「화면을 덮는 벽」이 된다.
모바일 레이아웃에는 애초에 지나갈 여백이 없다.

이것은 [OUTER_OBSERVER_INTRO_SPEC.md](OUTER_OBSERVER_INTRO_SPEC.md)가
「모바일에는 양옆 여백이 없어 인트로가 성립하지 않는다」고 **이미 보류로
적어 둔 것**과 같은 문제다. 그 보류를 지금 풀지 여부가 결정이다.

## 5. 결정 요청

1. **밴드를 언제 다시 재는가.** `setScene`마다 다시 잴 것인가(간단하지만
   장면 전환마다 레이아웃 읽기가 는다), 아니면 밴드를 폭이 아니라
   `main` 요소 자체에 붙여 CSS로 따라가게 할 것인가.
2. **별 흩기 비트를 되살릴 것인가, 접을 것인가.** 되살리려면 캔버스
   별밭에 «피해 흐르는» 그리기를 넣어야 하고(비용이 생긴다), 접으면
   그 문장을 스펙에서 빼야 한다. 지금처럼 «있다고 적혀 있고 아무 일도
   안 하는» 상태만은 아니어야 한다.
3. **망원경이 무엇을 집어야 하는가** — 실기 확인 뒤, 후보에서 전체 화면
   캔버스를 뺄지(천체만 남김) 아니면 「이름 없는 천체」를 정식 결과로
   인정할지.
4. **세로 화면 인트로의 보류를 풀 것인가.** 몸통을 너비 기준으로 바꿀지,
   세로에서는 인트로를 다른 연출로 대체할지, 계속 보류할지.

## 6. 재현

```bash
node scripts/probe-sky-guests.mjs      # 여백 소품 배치
node scripts/probe-session-churn.mjs   # 오래 켜 두었을 때의 누적
```

1·2절의 수치는 위 프로브가 아니라 이 문서를 쓰며 만든 일회성 계측이다 —
장면별 `main` 폭과 L0의 `div` 구성은 콘솔에서 두 줄로 다시 잴 수 있다.
