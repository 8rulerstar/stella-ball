# 접근성 — 지금 되는 것과, 결정이 필요한 하나

> 작성일 2026-08-22 · 밤샘 자율 세션의 실측 보고
>
> 접근성 축은 이 프로젝트에서 한 번도 재지 않았다. 실기 계측으로 현재
> 상태를 적고, 코드로 밀 수 있는 것 하나는 이미 고쳤으며(캔버스 정체),
> 조작 규칙에 닿는 하나는 **결정을 요청**한다.

## 1. 이미 되는 것 (실측)

- **모션 축소를 존중한다.** `prefers-reduced-motion: reduce`를 켜면
  실행 중인 CSS 애니메이션이 **0**이 된다(스타일시트 다섯 곳에 규칙이
  있고 스크립트도 `RM` 분기를 쓴다).
- **메타 화면은 탭으로 돈다.** 타이틀에서 `관측 시작 → 1분 튜토리얼 →
인트로 다시 보기` 순환이 정상이고, 허브·상점·소환·프로필의 조작은
  전부 실제 `<button>`이라 키보드로 닿는다.
- **상태는 소리로 읽힌다.** `#tip`이 `aria-live="polite"`이고 전투의
  `msg`가 그리로 흘러, 발사·정산·다음 샷 안내가 스크린리더에 중계된다.

## 2. 이번에 고친 것

전투 캔버스에 `role="img"`와 설명을 붙였다. 지금까지는 **이름 없는
그림**이라 스크린리더가 「여기에 무엇이 있는지」조차 말하지 못했다.
상태 중계는 위의 `#tip`이 이미 하므로, 캔버스는 「무엇을 보고 있는가」만
말하고 나머지는 그쪽에 맡긴다.

## 3. 결정이 필요한 것 — 전투를 키보드로 할 수 있는가

**지금은 할 수 없다.** 실측:

| 화면                         | 탭으로 닿는 조작       |
| ---------------------------- | ---------------------- |
| 타이틀·허브·상점·소환·프로필 | 전부                   |
| **전투**                     | **일시정지 버튼 하나** |

노드 조준은 좌클릭으로 노드를 찍고 빈 곳을 눌러 반대편을 고른다 —
**전부 포인터 전용**이다. `Space`는 발사만 하므로, 키보드만 쓰는
플레이어는 판을 볼 수는 있어도 한 발도 쏠 수 없다.

이것은 버그가 아니라 **조작 규칙의 공백**이라 코드로 밀지 않았다.

### 후보 (값을 정하지 않는다)

- **화살표로 노드 순회 + Enter로 찍기.** 지금 호버 미리보기가 이미
  「다음 하나를 가정해 그린다」를 하고 있어, 그 자리에 키보드 커서를
  얹는 것이 가장 작은 변경이다. 반대편 토글에는 키 하나가 더 필요하다.
- **숫자 키로 노드 지정.** 노드에 이미 순서 배지(1·2·3)를 그리므로
  번호와 손이 맞는다. 다만 노드가 9개를 넘으면 규칙이 깨진다.
- **하지 않는다.** 포인터 전용임을 명시하고, 대신 메타 화면과 수업만
  키보드로 완주할 수 있게 보장한다.

### 함께 정해야 하는 것

키보드 조준을 넣으면 **온보딩도 그것을 가르쳐야 한다.** 지금 1-1 수업은
암전과 손가락으로 «마우스로 찍는 손»을 보여 준다 — 키보드 경로가 생기면
그 수업이 두 갈래가 된다.

## 4. 재현

```bash
node scripts/probe-session-churn.mjs   # 이 문서의 계측은 일회성이다
```

탭 순회와 모션 축소는 CDP로 `Input.dispatchKeyEvent`(Tab)와
`Emulation.setEmulatedMedia`를 쓰면 두 줄로 다시 잰다.
