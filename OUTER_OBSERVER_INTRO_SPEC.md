# 관측창 밖의 존재 · 인트로 컷신 디자인 요구서

> 기준일: 2026-08-14  
> 대상: 데스크톱 웹의 중앙 세로 게임 영역 양옆 여백, 타이틀 진입 전 컷신, 별자리 고점의 배경 반응  
> 접수 경로: [ASSET_BACKLOG.md](ASSET_BACKLOG.md)의 「관측창 밖 존재·인트로 컷신」 항목  
> 상태: **디자인 명세. 실제 래스터·SFX 반입과 게임 규칙 변경은 이 문서의 범위 밖이다.**

이 문서는 다른 디자인 세션이 화면과 연출을 설계할 수 있도록 현재 구현·확정 제약·미정 서사를 한 곳에 모은 요구서다. 새 파일을 만들거나 즉시 코드에 적용하라는 지시가 아니다.

## 1. 한 줄 요약

중앙의 세로 게임 화면은 안전한 **관측창**, 데스크톱 양옆 여백은 관측창 바깥의 **심연**이다. 타이틀 시작 전과 드문 고점에서만 거대한 미지의 존재가 그 심연을 지나가며, 플레이어가 별자리를 만들수록 “관측자는 혼자가 아니다”라는 불안을 남긴다.

목표는 모바일 중심 레이아웃의 양옆 여백을 장식으로 숨기는 것이 아니라, **게임 밖에 더 큰 세계가 있다는 서사 장치**로 쓰는 것이다.

## 2. 현재 구현 사실과 변경 금지선

| 항목      | 현재 사실                                                                                                                                                              | 이번 요구서의 제약                                                                                                                                                                 |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 중앙 게임 | `main`이 데스크톱 중앙의 세로 게임 UI·전장이고 `z-index: 1`이다. 인트로 6비트에서만 발톱 하나가 `z-index: 2`로 그 앞에 온다.                                           | **2026-08-14 개정**: 몸통·글리치·실루엣은 여전히 중앙 전장·HUD·버튼 위에 올라오지 않는다. 예외는 타이틀 인트로 비트 5의 발톱 하나뿐이고, 전투 중에는 어떤 부위도 앞에 오지 않는다. |
| 양옆 배경 | `#dawn-sky`가 `main` 뒤(`z-index: 0`)에 달·행성·성운·오로라·별·소품을 렌더한다. 여백 소품 일부는 `pointer-events: auto`로 클릭을 받는다(`stella-ball-dawn.js`의 유성). | 새 연출은 이 배경의 **서사 레이어**다. 기존 장식 전부를 제거하거나 가리는 방향은 아니다. 클릭 가능한 기존 소품 위를 덮어 그 인터랙션을 죽이지 않는다.                              |
| 입력      | 유성 드래그·이동 중 조향·`Space` 패링은 중앙 전투의 핵심이다.                                                                                                          | 존재는 클릭 대상·충돌 대상·판정 대상이 아니며 패링, 조준, 버튼을 가로채지 않는다.                                                                                                  |
| 화풍      | 현행 기준은 새벽 관측소의 픽셀 아트, 청록-잿빛 밤과 살구빛 여명이다.                                                                                                   | 매끈한 3D, 실사 질감, 네온 사이버 글리치, 피·고어는 사용하지 않는다.                                                                                                               |
| 접근성    | `prefers-reduced-motion: reduce` 규칙의 선택자가 `#dawn-sky *`라서 **자식 요소만** 멈춘다(`stella-ball-dawn.css`).                                                     | 새 연출도 같은 설정에서 정지한 한 장의 배경 또는 미표시가 되어야 한다. 존재 레이어는 `#dawn-sky`의 자식으로 넣는다. 형제로 둘 경우 같은 변경에서 모션 감소 선택자를 확장한다.      |
| 게임 규칙 | 별자리·패링·보스 피해는 이미 전투 룰로 동작한다.                                                                                                                       | 컷신과 배경 반응은 피해·운동량·노드·보상·스테이지 잠금에 영향을 주지 않는다.                                                                                                       |

## 3. 서사 방향 — 확정된 것과 미정인 것

### 확정 방향

- 플레이어는 마지막 천문대의 계승자 **관측자**다. 유성의 궤적을 설계하고 별지기를 다시 점화한다.
- 밤하늘은 기억의 지도이며, 잊힌 빛이 사라진 자리에 공허와 공허 거상이 생긴다. 이 기존 설정은 유지한다.
- 존재는 1~7월드 내내 플레이어의 전장 **바깥**에 있다. 그 34스테이지 동안은 침공하는 적·보스·기믹으로 만들지 않는다.
- **2026-08-14 확정**: 인트로에서 창밖을 지나가는 개체와 최종 스테이지 8-1의 보스는 **같은 것**이다(§10-1 답). 여백의 몸과 전장의 몸을 `boss-art.js` 한 파일이 그리므로 형태가 100% 일치하고, 보스전 첫 대면이 설명 없이 연결된다. 정체·최종 반전은 여전히 미정이다.
- 메타 감각은 UI를 망가뜨리는 장난이 아니라, “플레이어가 관측하는 동안 반대로 관측당한다”는 감각에만 사용한다.
- **2026-08-14 개정**: 타이틀 인트로에 한해 존재가 관측창을 **직접 붙잡는다**. 관측당하는 데서 그치지 않고 저쪽이 이쪽 액자에 손을 댈 수 있다는 것까지 보여준다. 단 한 번, 타이틀에서만이고, 흔들리는 것은 관측창(`main`)뿐이다 — 여백의 달·소품은 제자리에 남아 「잡힌 것은 화면 전체가 아니라 이 창」이 읽히게 한다. UI는 부서지지 않고 값을 잃지도 않으며, CTA는 흔들리는 동안에도 계속 눌린다.

### 디자인 세션이 따라야 할 권장 해석

존재의 임시 호칭은 **외부의 관측자**다. 그것은 별자리 패턴과 관측창을 알아보지만, 첫 인트로에서는 공격·대사·정체 공개를 하지 않는다. 보이는 것은 거대한 일부뿐이다. 눈, 관절, 지느러미 같은 단일 단서도 별빛·천문 장치·성운과 헷갈릴 정도로 절제한다.

별자리는 “하늘을 예쁘게 잇는 그림”을 넘어, 현실을 잠깐 올바른 형태로 **교정하는 관측 패턴**으로 읽힌다. 다만 별자리가 봉인인지, 존재가 적인지, 플레이어가 시스템 오류인지 같은 최종 반전은 아직 **미정**이다. 아트·컷신은 어느 한 답을 확정해 보이면 안 된다.

## 4. 시각 언어

### 존재의 형태

- 전신, 이름표, 체력바, 정면 얼굴, 명확한 촉수 실루엣을 금지한다.
- 검은 덩어리보다 **별이 비어 버린 음영**, 여러 겹의 관측 고리, 지나가며 굽는 성운, 너무 큰 마디의 일부로 크기를 느끼게 한다.
- 중앙 관측창의 직선 프레임을 지나갈 때만 크기가 드러난다. 존재가 작게 보이는 것이 아니라 프레임보다 훨씬 커서 일부만 보인다는 인상이어야 한다.
- 윤곽은 차가운 청록·회색의 저대비, 별자리·보상·CTA만 살구빛으로 둔다. 존재에 보상색을 쓰지 않는다.
- 글리치는 RGB 분리·스캔라인·에러 코드가 아니라, 별점의 위치가 한 프레임 어긋나거나 관측 눈금이 반대 방향으로 흐르는 식의 **천문학적 오류**로 표현한다.

### 중앙과 여백의 관계

```text
심연 여백             관측창(게임 UI · 전장)             심연 여백
별이 빠진 윤곽   │   선명한 입력·보상·패링 정보   │   지나가는 거대 마디
느린 배경 반응   │   절대 가리지 않음              │   성운의 굴절
```

중앙의 밝은 UI는 안전한 현실, 양옆의 낮은 대비는 관측할 수 없는 외부라는 대비를 유지한다. 존재가 등장해도 중앙 텍스트·조준선·보스 체력보다 밝아져서는 안 된다.

## 5. 인트로 컷신 — 첫 실행과 타이틀 진입

### 목적

게임을 누르기 전에 “유성으로 별지기를 굴리는 전투”의 맥락을 길게 설명하지 않는다. 대신 **관측창이 열리고, 바깥의 무언가가 먼저 플레이어를 발견했다**는 한 장면만 남긴다. 첫 조작까지의 시간을 늘리는 장편 오프닝이 아니다.

### 비트 시트

> **2026-08-14 갱신**: 4비트에서 **6비트 9.8초**로 늘렸다. 통과만 하면 “큰 게 지나갔다”로 끝나므로, 통과 도중 한 번 멈춰 동공이 수축하는 1.4초를 넣어야 “관측당했다”가 된다. 비트 5(붙잡는다)는 사람이 2026-08-14에 추가 지시한 것이다. 구현은 `prototypes/outer-observer.js`.

| 순서           | 화면                                                                                                                                       | 감정/정보                                    | 조작 규칙                                            |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------- | ---------------------------------------------------- |
| 1. 정적 하늘   | 기존 달·별·성운이 있는 양옆 여백. 중앙은 아직 어둡거나 타이틀 별자리만 희미하다.                                                           | 관측소의 평온.                               | 즉시 건너뛰기 가능.                                  |
| 2. 관측 이상   | 양옆 별 몇 개가 꺼지지 않고 **제자리에서 잘못된 간격으로 고정**된다. 성운의 흐름이 아주 느리게 휜다.                                       | “하늘의 순서가 틀렸다.”                      | 입력을 막지 않는다.                                  |
| 3. 외부 통과   | 존재의 일부가 한쪽 여백에서 중앙 프레임 뒤를 지나 반대편으로 빠져나간다. 중앙 UI는 가리지 않고, 프레임 가장자리의 눈금·별만 짧게 어긋난다. | 존재의 크기와 ‘바깥에서 본다’는 감각.        | 점프 스케어와 강제 대기 금지. 흔들림은 비트 5에서만. |
| 4. 눈이 멈춘다 | 통과 도중 눈이 오른쪽 여백에서 멈추고 동공이 한 번 수축한다. 대사도 이름도 없다. 5.6–7.0s.                                                 | 관측당하는 순간.                             | 입력을 막지 않는다.                                  |
| 5. 붙잡는다    | 발톱 하나가 앞으로 올라와 관측창 오른쪽 모서리를 짚고, 관측창만 1.6초 흔들린다. 여백 소품은 제자리다. 7.0–8.6s.                            | 저쪽이 이 액자에 손을 댈 수 있다.            | 흔들리는 동안에도 CTA는 눌린다.                      |
| 6. 관측창 점등 | 발톱이 놓고 존재가 빠져나가며 잔상만 남는다. 워드마크·`게임 시작!` CTA가 선명해진다. 소품은 제자리로 돌아오지 않는다. 8.2–9.8s.            | 플레이어가 안전한 창 안에서 관측을 시작한다. | CTA는 연출 완료를 기다리지 않고 접근 가능해야 한다.  |

### 문장과 사운드

- 필수 대사는 없다. 대사가 있다면 루나의 한 문장 이하로 제한한다: `“이번에는 저쪽에서 먼저 우리를 보고 있어요.”`
- 존재의 이름·적대 선언·세계의 진실은 여기서 말하지 않는다.
- 전용 저음·왜곡 SFX 필요 여부와 길이는 **미정**이다. 기존 배경음·별빛 SFX만으로 읽히면 새 SFX를 만들지 않는다.

### 길이와 재생 정책

- 첫 진입은 짧은 자동 연출이며, 정확한 시간은 브라우저 실기 뒤 확정한다.
- `게임 시작!`은 첫 프레임부터 또는 매우 짧은 지연 뒤 눌릴 수 있어야 한다. 연출을 보지 않아도 온보딩과 전투에 도달해야 한다.
- 같은 브라우저 세션에서는 타이틀 재진입 때 약식 배경 반응만 쓴다. 매번 전체 통과 연출을 반복하지 않는다.
- 튜토리얼 다시 보기와 일반 캠페인 시작은 이 인트로를 재생하지 않는다.

## 6. 플레이 중 배경 반응

존재는 상시 장식이 아니라, 플레이어가 만든 별자리와 연결된 **드문 증인**이다. 전투 가독성을 위해 아래 3종을 넘기지 않는다.

| 트리거                         | 양옆 반응                                                                      | 중앙 전장 규칙                                                        |
| ------------------------------ | ------------------------------------------------------------------------------ | --------------------------------------------------------------------- |
| 첫 실행의 인트로               | 존재 일부의 1회 통과                                                           | 타이틀 UI 뒤에서만. 전장 없음.                                        |
| 5점 이상 별자리의 현현 완료    | 양옆 별이 잠깐 존재의 윤곽을 피해서 흐르거나, 멀리 눈금형 고리가 1회 수축한다. | 패링 노드·실루엣·보스 체력 표시보다 낮은 대비. 다음 입력을 막지 않음. |
| 월드 첫 진입 또는 첫 보스 처치 | 존재가 아닌 관측창 프레임의 작은 이상(별 하나 역행, 성운 굴절)만 남긴다.       | 결과 화면·스테이지 선택을 덮지 않음.                                  |

다음은 하지 않는다.

- 모든 패링·모든 별자리·매 전투에 존재를 보이기
- 보스 체력 구간에서 화면을 가리는 컷인
- 양옆 연출을 따라가게 만들어 중앙의 유성·패링 타이밍을 놓치게 하기
- 존재를 클릭 보상, 숨은 퀘스트, 피해 대상, 공포 벌칙으로 만들기

## 7. 디자인 산출물 요청

디자인 세션은 아래를 **시안/명세**로만 낸다. 래스터 에셋 또는 SFX 반입은 별도 승인이 필요하다.

1. 데스크톱 와이드 화면 한 장: 중앙 관측창과 양옆 심연의 기본 대비가 보이는 타이틀 상태.
2. 인트로 4비트의 스토리보드: 2절의 정적 하늘 → 관측 이상 → 외부 통과 → CTA 점등.
3. 5점 이상 별자리 반응 한 장: 중앙 전투는 선명하고 양옆 존재 반응은 낮은 대비인 상태.
4. `prefers-reduced-motion` 대체안 한 장: 움직임 없이도 존재의 불안을 전달하는 정적 구성.
5. 존재 형태의 금지/허용 시트: 전신·촉수 클리셰와 구분되는 부분 실루엣 3안 이상.

색·투명도·프레임 수·아트 원본 규격·SFX 길이는 아직 **미정**이다. 디자인 세션은 제안값과 이유를 적되, 확정값처럼 에셋 계획에 기록하지 않는다.

## 8. 기술 연결 후보 — 구현 승인 뒤에만 사용

| 기능             | 현재 연결점                                                                                                                                        | 구현 시 제약                                                                                                                                                                                                                                                                                                                                      |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 타이틀 인트로    | `prototypes/js/game-session.js`의 `renderTitlePresentation()`                                                                                      | 타이틀 UI 한 곳만 확장한다. 새 전역 함수 재정의 금지.                                                                                                                                                                                                                                                                                             |
| 양옆 심연 레이어 | `prototypes/stella-ball-dawn.js`의 `#dawn-sky`, `prototypes/stella-ball-dawn.css`                                                                  | `main` 뒤, `pointer-events: none`인 전용 존재 레이어를 둔다. 기존 배경 소품의 클릭 동작을 바꾸지 않는다.                                                                                                                                                                                                                                          |
| 별자리 고점 반응 | `game-figure.js`의 `figureFx`. 도형은 `figureFx.shape.id`, 점 개수는 `figureFx.drawn.length`, 현현 완료는 `figureFx.t >= FIGURE_REVEAL_AT` 이후다. | 실제 패링·별자리 판정은 읽기만 하며, 노드·피해·타이머를 변경하지 않는다. **`afterFigureShot` 훅으로는 5점 이상을 판정할 수 없다**: 성공 경로에서 `clearFigureShot()`이 훅보다 먼저 실행돼 payload의 `state.nodes`가 이미 비어 있고, 훅 발화 시점도 현현 완료가 아니라 패링 샷 종료다. 5점 이상 조건은 `figureFx`를 프레임 단위로 관찰해 확정한다. |
| 결과/월드 반응   | `afterBattleWin`, 스테이지 선택 화면                                                                                                               | UI의 포커스·버튼·결과 카드와 별도 레이어로 둔다.                                                                                                                                                                                                                                                                                                  |
| 모션 감소        | `stella-ball-dawn.css`의 기존 `prefers-reduced-motion` 규칙. 선택자가 `#dawn-sky *`라서 자식 요소에만 적용된다.                                    | 존재 통과·글리치·깜빡임을 중지하고 정적 저대비 배경만 남긴다. 존재 레이어를 `#dawn-sky` 자식으로 넣지 않으면 이 규칙이 걸리지 않으므로, 형제로 둘 때는 선택자 확장을 같은 변경에 포함한다.                                                                                                                                                        |

새 연출이 독립 스크립트로 분리될 만큼 커지면, `game-bootstrap.js` 뒤의 **표현 전용 스크립트**로만 추가한다. 그 경우 `prism-breakers.html`, `prototypes/ARCHITECTURE.md`, `scripts/smoke-runtime.mjs`의 고정 로드 순서를 같은 변경에서 갱신한다.

## 9. 검수 기준

- 3초 정지 화면만 봐도 중앙은 게임 가능한 관측창, 양옆은 그 바깥이라는 층위가 읽힌다.
- 존재를 전신으로 보지 않아도 크기·이질감·관측당하는 감각이 전달된다.
- 중앙의 유성, 패링 `Space` 안내, 별빛 노드, 보스 체력, CTA는 어느 프레임에서도 가려지지 않는다.
- 타이틀 CTA는 연출을 보지 않아도 누를 수 있고, 재진입마다 긴 컷신을 강제하지 않는다.
- 모션 감소 설정에서도 화면이 깨지거나 빈 검은 영역만 남지 않는다.
- 화풍은 새벽 관측소 픽셀 규칙을 지키며, “일반적인 보라 촉수 코스믹 호러”나 사이버 글리치로 읽히지 않는다.
- 에셋 반입 전에는 코드/CSS의 임시 표현 또는 시안만 사용한다. 실제 파일은 `ASSET_PLAN.md`·`assets/ASSET_MANIFEST.json`·출처 기록을 거친다.

## 10. 최종 확인이 필요한 질문

### 답이 나온 것 (2026-08-14)

1. **외부의 관측자는 끝까지 배경인가, 보스인가** → **보스로 확정.** 인트로의 개체와 8-1의 보스는 같은 몸이고 같은 코드가 그린다. 다만 **이름은 끝까지 주지 않는다** — 모든 화면이 별 이름 `관측되지 않은 점`과 바이어 기호 `∅`만 쓴다. 정체와 최종 반전은 열어 둔 채다.
2. **첫 실행에만인가, 매 진입마다인가** → **첫 실행은 6비트 전체, 재진입은 약식.** 2026-08-16(`0dbca4e`)에 표식이 `sessionStorage`에서 **`localStorage` 우선**으로 옮겨졌다 — 세션 저장소는 탭을 새로 열 때마다 비므로 그것만으로는 「첫 실행 한정」이 성립하지 않았다. 지금은 둘 중 하나라도 있으면 약식이다.
3. **팔레트** → **스펙 4절 유지.** 시안의 남보라+자홍 대신 프로젝트의 청록-잿빛 램프를 쓴다. 채도 재조정은 디자인 세션에 회신했다([OUTER_OBSERVER_PALETTE_REPLY.md](OUTER_OBSERVER_PALETTE_REPLY.md)).

### 아직 미정

2. 별자리를 “공허를 막는 봉인”으로 확정할 것인가, 현재처럼 관측·기억의 교정으로 열어 둘 것인가?
3. 인트로의 루나 한 문장을 넣을 것인가, 무대사로 갈 것인가? — 현재 구현은 **무대사**다.
4. **모바일에 인트로가 없다.** 연출의 무대가 데스크톱 양옆 여백이라, 여백이 없는 모바일에서는 6비트가 성립하지 않는다. 첫 실행 서사가 플랫폼마다 갈리는 문제이며 **보류 중**이다.

정체를 단정하는 아트·텍스트는 2번이 답을 얻기 전에는 만들지 않는다.

## 11. 구현 상태 (2026-08-14)

이 문서는 더 이상 요구서만이 아니다. 아래는 실제로 들어간 것이다.

| 항목           | 파일                                                                        | 메모                                                                                                                                                                                                                                                                                                                                     |
| -------------- | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 존재·보스 아트 | `prototypes/boss-art.js` (`window.StellaBossArt`)                           | 절차적 픽셀. 래스터 반입 없음. 페이즈 4단과 인트로용 동공 배율을 인자로 받는다.                                                                                                                                                                                                                                                          |
| 인트로 6비트   | `prototypes/outer-observer.js`                                              | `#dawn-sky` 자식 레이어. 런타임 훅을 걸지 않고 `.title-sequence`가 **보이는지**를 본다 — 존재만 검사하던 초기 판정은 튜토리얼이 시작해도 타이틀 마크업이 `display:none`으로 남아 있어 레이어가 세션 내내 합성됐다(`97ca72f`). 2026-08-14 디자인 세션 v2로 교체돼 20fps 스텝 보행·잔상 3겹·별을 먹는 그림자·관절로 묶인 발톱 리그를 쓴다. |
| 관측창 붙잡기  | `boss-art.js`의 `drawClaw` + `stella-ball-dawn.css`의 `ooFrameGrab`         | 발톱은 `z-index: 2`의 body 직계라 `#dawn-sky *` 모션 감소가 안 걸린다. 모션 감소에서는 비트 5를 통째로 뺀다. `main`의 transform은 `fill-mode` 없는 키프레임으로만 걸고, 연출 종료와 `teardown()` 양쪽에서 클래스를 벗긴다 — 남으면 `position: fixed` 전체화면 오버레이가 캔버스 폭에 갇힌다.                                             |
| 소품 반응      | `prototypes/stella-ball-dawn.js`의 `data-dawn-prop`                         | 우주비행사·달토끼·망원경·나침반. 비트 3에서 한 번 흩어지고 되돌아오지 않는다.                                                                                                                                                                                                                                                            |
| 8-1 스테이지   | `prototypes/js/game-data.js`의 `WORLDS` / `CAMPAIGN_WORLD_PLANS` 8번째 항목 | 1600 HP, 4인 파티, 별자리 없음(`shape: []`). 기존 순차 해금이 그대로 7-7 클리어를 조건으로 만든다.                                                                                                                                                                                                                                       |
| 페이즈 4단     | `gimmicks.phases`의 `effect: "form"`                                        | `runStagePhase`가 모르는 값이라 전투에 효과가 없다. 형태만 바뀐다.                                                                                                                                                                                                                                                                       |
| 전장 렌더      | `prototypes/js/game-core-render.js`의 `outsideBossFrame()`                  | 페이즈마다 264px로 한 번 굽고 이후 blit. 프레임당 `drawImage` 한 번.                                                                                                                                                                                                                                                                     |
| 이름 없음 유지 | `prototypes/js/game-data.js`의 `bossDisplayName()`                          | 8-1만 좌표를 쓰고 나머지 34스테이지는 그대로 `공허 거상`.                                                                                                                                                                                                                                                                                |

미구현: 6절의 「5점 이상 별자리 현현 반응」과 「월드 첫 진입·첫 보스 처치 반응」. 8-1 클리어 엔딩 화면은 2026-08-15에 구현했다.
