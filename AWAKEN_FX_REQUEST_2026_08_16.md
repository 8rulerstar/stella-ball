# 각성 연출 — 디자인 세션 요청서

> 작성일: 2026-08-16
> 대상: 별지기의 «각성» — 깨어나는 순간과, 멈춘 자리에서 나가는 정산 공격
> 상태: **반입 완료** (2026-08-16). 세션 결과물은 `prototypes/js/game-awaken-fx.js` 한 파일이고, 실측 전후는 [11절](#11-반입-결과)에 있다.
> 규칙: [ART_DIRECTION_REQUEST_2026_08_16.md](ART_DIRECTION_REQUEST_2026_08_16.md)와 같다 — **측정값이나 화면 근거가 없는 항목은 넣지 않는다.** 등급도 같다(A: 화풍·구조의 근간, B: 완성도, C: 여유가 있을 때). 여기서 새 래스터·SFX를 만들지 않는다. 반입은 [ASSET_BACKLOG.md](ASSET_BACKLOG.md)의 승인 절차를 따른다.

## 1. 요구 (원문)

|   # | 날짜       | 원문                                                       |
| --: | ---------- | ---------------------------------------------------------- |
|   1 | 2026-08-16 | "각성 연출 디자인 세션에 멋있고 손맛느껴지게 요청문서써줘" |

## 2. 왜 지금인가 — 각성이 방금 게임의 중심이 됐다

이 요청은 규칙 변경 바로 다음에 왔다. 같은 날([4fd2f4e](https://github.com/8rulerstar/prism-breakers/commit/4fd2f4e)) 각성을 «패링 전용»에서 «움직임»으로 되돌렸다.

|                           | 되돌리기 전 | 되돌린 뒤 |
| ------------------------- | ----------: | --------: |
| 한 샷의 각성 횟수(무패링) |         0회 |  최대 3회 |
| 패링 없는 판의 피해 용량  |         124 |       364 |
| 패링 없는 판의 클리어율   |        6.3% |     72.9% |

**즉 각성은 어쩌다 보는 것에서 «매 샷 보는 것»이 됐다.** 오너가 곧바로 연출을 요구한 이유가 이것이다 — 이제 이 연출이 게임에서 가장 자주 보이는 사건인데, 아래 4절이 보여 주듯 화면은 그 사건에 거의 반응하지 않는다.

같은 변경이 대기 시간도 만들었다. 실측(`scripts/probe-settle-cost.mjs`, 2-2, 3인)에서 유성이 멈춘 뒤 판이 플레이어에게 돌아오기까지 **0.00초 → 3.26초**가 됐고, 오너의 「렉 걸린다」는 제보가 이것이었다. 배분을 예산제로 바꿔 **1.68초**로 줄였다. **그 1.68초가 이 세션에 주는 시간 예산이다**(7절).

## 3. 이 문서가 다루는 것과 다루지 않는 것

다루는 것은 둘이다.

- **깨어나는 순간** — 유성이 별지기를 밀어 실제로 굴러가기 시작하는 그 프레임
- **정산 각성** — 굴러가던 별지기가 멈추고 자기 공격을 내보내는 구간

다루지 않는 것: 별자리 현현(별개 계통, [ART_DIRECTION_REQUEST §4-3](ART_DIRECTION_REQUEST_2026_08_16.md)), 보스 기믹 연출(같은 문서 §6), 타격 반응 전반의 강도(같은 문서 §4). 이 문서는 그 위에 얹히는 것이 아니라 **그 사이에 빠져 있는 한 칸**을 다룬다.

---

## 4. 지금 화면에서 각성이 무엇인가 (실측)

### 4-1. [A] 깨어나는 순간, 화면은 아무 반응도 하지 않는다

저장소의 모든 타격 반응은 `impact()` 한 함수를 지난다 — 흔들림·히트스톱·플래시가 전부 여기서 나온다. 호출 지점을 전수 조사했다.

| 사건                          | `impact()` 호출 | 흔들림 | 히트스톱 | 밀림 |
| ----------------------------- | :-------------: | -----: | -------: | ---: |
| 유성이 범퍼·발판에 닿음       |        O        |    4px |  0.028초 |    0 |
| **패링** 성공 접점            |        O        |    4px |  0.028초 |    0 |
| 유성이 보스를 때림            |        O        |    8px |  0.034초 |  6px |
| 연타 3+ · 약점 강타           |        O        |   24px |  0.075초 | 20px |
| 정산 공격이 **보스에 도달**   |        O        |   34px |  0.046초 | 28px |
| **유성이 별지기를 밀어 깨움** |      **X**      |  **0** |    **0** |    0 |

**게임에서 가장 자주 일어나는 사건이 유일하게 화면을 건드리지 않는 사건이다.** 범퍼에 스치는 것도 4px 흔들리는데, 별지기를 깨우는 것은 0px다.

> 이 눈금은 [281f6f9](https://github.com/8rulerstar/prism-breakers/commit/281f6f9)에서 이미 한 번 벌린 값이다(옛 5.5/5.5/10/8.5 → 4/8/24/34, 밀림·기울기·잔상 추가). 즉 **강약의 폭 문제는 이미 해결됐고 각성만 그 눈금 밖에 남아 있었다.**

깨어남이 실제로 내는 것은 넷뿐이다.

| 요소         | 값                                   | 지속   |
| ------------ | ------------------------------------ | ------ |
| 확산 링      | 반지름 68px (판 가로의 9.4%)         | 0.44초 |
| 회전 점선 링 | 반지름 45~58px, 점선 [6,7], 선 2~4px | 상시   |
| 글자 팝업    | `{이름} 깨어남!`                     | 기본값 |
| 소리         | `awaken` 0.78                        | —      |

색은 별지기 고유색 하나(`g.col`)이고, 크기 변화는 `wakeFlash` 0.62가 감쇠하는 것 하나다. **밀려난 방향을 말하는 요소가 없다** — 유성이 어느 쪽에서 와서 어느 쪽으로 밀었는지가 그림에 없다.

### 4-2. 정산 각성은 반대로 «많다»

정산 초점(`drawFinisherFocus`)이 한 프레임에 그리는 것: 상하 레터박스 띠, 별자리 호 6점, 보스 쪽 조준 레티클, 속도선 5줄, 방사선 18줄, 초상, 순번 숫자. 그 위에 별지기별 능력 이펙트가 얹힌다.

**그런데 이 화려함이 «각성»이 아니라 «발사»에 붙어 있다.** 초점이 잡히는 시점은 별지기가 멈춘 뒤이고, `impact()`가 울리는 시점은 그 공격이 **보스에 닿을 때**다. 깨어남 → 굴러감 → 멈춤 → 발사 → 명중의 다섯 박자 중 **화면이 반응하는 것은 마지막 하나뿐이다.**

### 4-3. [B] 「각성」이라는 말이 세 가지를 가리킨다

같은 판에서 동시에 쓰이는 어휘다.

| 화면에 뜨는 말       | 실제로 가리키는 것             | 어디서              |
| -------------------- | ------------------------------ | ------------------- |
| `{이름} 깨어남!`     | 밀려서 굴러가기 시작한 순간    | `wakeUnit`          |
| `{이름} 공명 각성`   | 패링으로 깨어난 순간           | `wakeUnit` (subtle) |
| `{이름} 각성`        | 멈춘 뒤 나가는 정산 공격       | 정산 피니셔 라벨    |
| `전원 공명 +3.0`     | 파티 전원을 패링으로 이은 배율 | 별자리 배율         |
| `STELLAR SETTLEMENT` | 정산 구간 전체                 | 정산 배너           |

플레이어가 「각성」을 볼 때 그것이 시작인지 끝인지 구분할 근거가 글자 안에 없다.

---

## 5. 손맛이 없는 이유 — 세 가지 진단

### 5-1. [A] 원인과 결과가 화면에서 이어지지 않는다

손맛은 **내가 한 일이 화면을 밀었다**는 느낌이다. 지금 인과의 사슬은 이렇게 끊겨 있다.

```
플레이어가 조준하고 쏜다  →  유성이 별지기를 때린다  →  [화면 반응 0]
                          →  별지기가 굴러간다      →  [상시 점선 링]
                          →  멈춘다                 →  [화려한 초점 연출]
                          →  보스에 닿는다          →  [34px 흔들림]
```

플레이어가 «한 일»(때린 것)과 화면이 «반응한 곳»(보스) 사이에 다섯 박자와 1.7초가 있다. 그 사이를 잇는 것이 없다.

### 5-2. [A] 무게가 없다 — 모든 각성이 같은 크기다

각성 피해는 이동거리·충돌수·벽 반사로 계산된다(`14 + min(22, travel/28) + collisions×3 + wallHits×4`). 즉 **잘 굴린 각성과 살짝 밀린 각성은 피해가 최대 3배 넘게 차이 난다.** 그런데 화면에 나오는 링·팝업·소리는 둘이 완전히 같다.

`wakeFlash`는 0.62 고정이고, 확산 링 반지름도 `g.r + 34` 고정이다. **충돌 세기를 읽는 값이 연출에 하나도 들어가지 않는다.**

### 5-3. [B] 굴러가는 동안이 비어 있다

깨어남(0.44초)과 정산(1.68초) 사이에 별지기가 실제로 굴러가는 구간이 있다. 실측에서 이 구간은 샷의 대부분이다 — 3인 각성 샷의 비행 시간 4.28초 중 대부분에서 별지기가 움직이고 있다. 이 구간의 연출은 **회전하는 점선 링 하나**뿐이고, 그마저 속도와 무관하게 항상 같다.

예외가 하나 있다 — 윤슬의 회전 칼날은 속도에 비례해 세진다(`0.2 + (speed - 10) / 700`). **속도를 읽는 연출이 로스터에 이미 한 명 있다는 뜻이고, 그것이 어떤 모양이어야 하는지의 선례다.**

---

## 6. 세션에 필요한 결정

- **깨어나는 순간에 화면을 얼마나 밀 것인가.** 지금 0px다. 범퍼가 4px, 보통 타격이 8px, 강타가 24px, 정산 명중이 34px인 눈금 위에서 어디에 놓을 것인가. 그리고 **방향이 있는 반응을 쓸 것인가** — 저장소에 `screenPushX/Y`·`screenTilt`·`screenGhost`가 이미 있고 보스 포효만 쓰고 있다.
- **세기를 무엇으로 읽을 것인가.** 충돌 속도인가, 그 샷의 누적 충돌 수인가, 예상 피해인가. 5-2의 3배 폭을 그림으로 옮기려면 축을 하나 골라야 한다.
- **굴러가는 동안 무엇을 보여 줄 것인가.** 윤슬의 칼날처럼 속도에 비례하는 것을 전원에게 줄 것인가, 아니면 각성한 별지기에게만 붙는 별도의 어휘를 둘 것인가.
- **다섯 박자를 몇 개로 줄일 것인가.** 깨어남·굴러감·멈춤·발사·명중을 전부 연출할 필요는 없다. 어느 둘에 힘을 몰 것인지가 이 요청의 핵심이다.
- **어휘 정리(4-3).** 「깨어남」과 「각성」을 시작·끝으로 나눌 것인가, 하나로 합칠 것인가.
- **인원수를 연출로 말할 것인가.** 3명이 동시에 각성한 샷과 1명만 각성한 샷은 지금 같은 모양으로 흐른다.

---

## 7. 시간 예산 — 이 요청의 가장 단단한 제약

**정산 전체가 쓸 수 있는 시간은 1.68초다.** 늘릴 수 없다. 이 숫자가 어디서 왔는지가 중요하다.

원래 배분은 「한 명이 1.62초를 온전히 갖는다」였고, 각성이 0~1회이던 시절에는 맞았다. 각성이 매 샷 3회가 되자 같은 배분이 그대로 **3명 4.84초 · 4명 6.46초**가 됐다. 실측 3.26초, 다섯 발이면 16초를 구경만 한다. 오너가 「렉 걸린다」고 한 것이 이것이다.

지금은 예산제다(`game-feedback.js`의 `SETTLE_LEAD`/`SETTLE_BEAT`/`SETTLE_BUDGET`).

| 상수            |         값 | 뜻                                     |
| --------------- | ---------: | -------------------------------------- |
| `SETTLE_LEAD`   |     0.34초 | 유성이 멈추고 첫 타가 시작하기까지     |
| `SETTLE_BEAT`   |     0.92초 | 한 명분 연출의 길이                    |
| `SETTLE_BUDGET` |     1.30초 | 첫 타 시작부터 마지막 타 종료까지 상한 |
| **합계**        | **1.64초** | 인원과 무관하게 일정                   |

인원이 늘면 간격이 좁아진다 — 2명 0.38초, 3명 0.19초, 4명 0.13초 간격. **즉 여러 명의 각성은 행렬이 아니라 «일제 사격»이 된다.** 이 겹침을 어떻게 보이게 할 것인가가 세션의 자리다.

별자리가 함께 뜬 샷은 현현(1.12초)이 끝난 뒤에 정산이 시작한다. 그 경우의 총 대기는 약 2.8초다.

## 8. 성능 예산 — 여기는 여유가 있다

**연출을 두껍게 하는 쪽으로는 캔버스에 여유가 충분하다.** 실측으로 확인했다(`scripts/probe-settle-cost.mjs`, 실제 창, 2-2, 3인 각성).

| 구간                           | `draw()` p50 | `draw()` p95 |
| ------------------------------ | -----------: | -----------: |
| 대기(발사 전)                  |        0.5ms |        0.9ms |
| 유성 비행 중                   |        0.5ms |        0.8ms |
| 정산 중                        |        0.5ms |        0.8ms |
| **정산 초점이 떠 있는 프레임** |    **0.5ms** |    **0.8ms** |

**정산 초점의 화려한 오버레이가 떠 있어도 `draw()`가 대기 상태와 같다.** 같은 구간의 CPU 프로파일은 70.9%가 idle이고, 게임 함수 중 1%를 넘는 것이 하나도 없다. 프레임 예산 16.7ms 대비 3%다.

DOM 쪽도 같은 샷에서 셌다 — 정산 구간의 `toast` 2회, `sync` 0회, 팝업 2회. 여기도 문제가 아니다.

**정리: 「렉」의 원인은 그리는 값이 아니라 기다리는 시간이었다.** 그래서 이 요청서의 제약은 성능이 아니라 7절의 1.68초다. 그 안에서라면 캔버스에 얼마든지 두껍게 그려도 된다.

다만 [ART_DIRECTION_REQUEST §4-7](ART_DIRECTION_REQUEST_2026_08_16.md)의 한 줄은 그대로 유효하다 — **캔버스에 그리는 방향으로 두껍게, DOM 요소를 늘리는 방향으로는 얇게.**

## 9. 이미 있는 것 (다시 만들지 말 것)

세션이 쓸 수 있는, 이미 저장소에 있는 재료다.

| 재료                                      | 어디에                                       | 지금 쓰는 곳                 |
| ----------------------------------------- | -------------------------------------------- | ---------------------------- |
| 방향 있는 화면 반응 4종                   | `screenPushX/Y`, `screenTilt`, `screenGhost` | 보스 포효만                  |
| 별지기 각성 공격 스프라이트 (8종 4프레임) | `assets/characters/*-attack.png`             | 정산 초점의 `playUnitAttack` |
| 능력별 이펙트 시트                        | `abilityFxSheets`                            | `emitAbilityFx`              |
| 32px 초상 (idle 시트 첫 프레임 크롭)      | `game-speech.js`의 `speechFace`              | 말풍선                       |
| 별지기별 고유색                           | `heroes[*].col`                              | 거의 모든 연출               |
| 속도 비례 연출의 선례                     | 윤슬의 `bladeStrength`                       | 윤슬 전용                    |

---

## 10. 한 줄 요약

각성은 방금 게임에서 가장 자주 보는 사건이 됐는데, **그 사건이 일어나는 순간 화면은 0px 움직이고**, 화면이 반응하는 것은 1.7초 뒤 보스 쪽에서다. 세기를 읽는 값도 연출에 하나도 들어가 있지 않다. 시간은 1.68초로 고정이고 그리기 예산은 남아돈다 — **짧은 시간 안에서, 원인이 있는 자리에, 세기가 보이게.**

---

## 11. 반입 결과 (2026-08-16)

세션이 낸 것은 파일 하나다 — `prototypes/js/game-awaken-fx.js`. 기존 파일은 고치지 않고 `wakeUnit`을 감싸고 `drawFinisherFocus`를 갈아 끼운다. 되돌리려면 `prism-breakers.html`의 `<script>` 한 줄과 `smoke-runtime.mjs`의 목록 한 줄만 지우면 된다.

### 반입 전 확인

남이 쓴 파일이라 전제부터 전수 확인했다. 이 파일이 이름으로 부르는 전역 15개(`drawFinisherFocus`·`impactStop`·`screenShake`·`screenPushX/Y`·`screenTilt`·`screenGhost`·`areaBursts`·`popups`·`assistShots`·`finisherFocus`·`drawFinisherMotif`·`playUnitAttack`·`reducedMotionPreferred` 등)와 훅 4종(`afterShotStart`·`afterFeedbackUpdate`·`afterArenaDraw`·`afterDraw`)이 모두 실재하고 이름이 정확했다. 원본 `drawFinisherFocus`는 순수 그리기 함수라 갈아 끼워도 잃는 상태가 없다.

**한 가지는 세션 쪽이 맞고 이 문서가 틀렸다** — 4-1의 눈금이다. 초안이 옛 값(5.5/5.5/10/8.5)을 인용했는데 실제로는 [281f6f9](https://github.com/8rulerstar/prism-breakers/commit/281f6f9)에서 이미 4/8/24/34로 벌어져 있었다. 세션의 계측(7~20px)은 그 실제 눈금 위에서 잡힌 값이라 그대로 유효하다. 위 표는 고쳤다.

### 반입 후 실측

같은 프로브, 같은 판(2-2, 3인, 전원 각성).

| 항목                         |     반입 전 |     반입 후 |
| ---------------------------- | ----------: | ----------: |
| 깨어날 때 화면 흔들림        |     **0px** |  **19.1px** |
| 깨어날 때 밀림               |           0 |       6.2px |
| 깨어날 때 기울기             |           0 |      0.0092 |
| 깨어날 때 잔상               |           0 |       0.227 |
| 깨어날 때 히트스톱           |           0 |     0.052초 |
| 정산 총 대기                 |      1.68초 |      1.66초 |
| `draw()` 초점 프레임 p50/p95 | 0.5 / 0.8ms | 0.6 / 0.9ms |
| 정산 구간 33ms 초과 프레임   |         2~3 |       **0** |

**시간 예산은 지켜졌고 그리기 비용은 0.1ms 늘었다.** 33ms 초과 프레임이 오히려 사라진 것은 초점 오버레이가 컷인(별지기를 `W*0.27, H*0.77`로 옮겨 그리는 큰 합성)에서 제자리 빔으로 바뀐 몫으로 보인다.

`npm run check` 통과. 온보딩 E2E도 통과하며 조향 단언이 살아 있다. 콘솔 오류는 헤드리스에서 사용자 제스처 없이 `navigator.vibrate`를 부를 때 크롬이 내는 경고뿐이고, 기존 `impact()`도 같은 호출을 한다.

### 남은 것

- **`screenPushX/Y`는 최댓값이 아니라 대입이다.** 한 프레임에 둘 이상이 동시에 깨어나면 마지막(가장 약한) 각성의 밀림이 앞의 것을 덮는다. `SOFTEN`이 2·3번째를 0.78·0.6배로 줄여 놓았는데 대입이라 그 약한 값이 이긴다. 실제 플레이에서는 접촉이 프레임마다 흩어져 거의 드러나지 않고, 기존 `impact()`도 같은 방식이라 집안 규칙과는 일치한다. 고칠지는 다음 조정에서 정한다.
- **봇 하니스는 이 파일을 로드하지 않는다**(`game-speech.js`도 마찬가지다). 연출 전용이라 피해·클리어율에는 영향이 없지만, 히트스톱이 브라우저에서는 시뮬레이션을 실제로 멈추므로 «벽시계 기준 샷 길이»는 둘이 다르다.
- 정산 빔이 화면에 띄우는 숫자는 정산 피해식의 기본값(`settleWeight`)이라, 별자리 배율이 곱해진 최종 피해와는 다르다.
