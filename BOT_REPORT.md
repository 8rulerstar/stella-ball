# BOT REPORT — 헤드리스 하니스 측정 기록

> 기준일: 2026-08-18 (계기 교체 2026-08-15, 0절 참조)
>
> 브라우저에 로드된 실제 런타임 함수(`update` · `updateSpecial` · `updateFeedback` · `figureMatch`)를 고정 dt로 직접 구동해 얻은 수치다. 물리를 따로 재구현한 `bot/prism-sim.mjs`와는 다른 경로이며, 그쪽 수치와 섞지 않는다.
>
> 측정 방법과 한계는 각 절의 **측정 조건**과 **한계**를 반드시 함께 읽는다.
>
> **어느 절을 읽어야 하는가**: 난이도를 알고 싶으면 [0-3절](#0-3-계기-교체--무기믹-기준선에서-실제-캠페인으로-2026-08-15)만 본다. 0-1 ~ 0-1-c는 무기믹 기준선의 이력이며, 그 스윕은 지금 포화돼 난이도 신호가 아니다. **밸런스 값을 정하려는 경우 [0-7절](#0-7-조준은-첫-발까지만-읽힌다-2026-08-18)을 0-4절보다 먼저 읽는다** — 조준 축이 어디까지 유효한지가 거기서 정해진다.

## 0. 하니스 구성

- 헤드리스 창에서는 `document.visibilityState`가 `hidden`이라 `requestAnimationFrame`이 스로틀된다. 프레임 루프에 의존하지 않고 `update(1/60)` → `updateSpecial(1/60)` → `updateFeedback(1/60)`를 동기 루프로 직접 호출했다.
- 이 방식은 **판정 결과**(클리어 여부, 피해량, 도형 인식)는 실런타임과 동일하지만, **체감 속도·연출 타이밍·프레임 안정성은 측정하지 않는다.** 그 항목은 실기 플레이로만 확인 가능하다.
- 정산 슬로모션(`0.46 → 0.62 → 0.82`) 시간 배율은 적용하지 않았다. 판정 결과에는 영향이 없고 경과 시간에만 영향을 준다.

## 0-1. 현재 런타임 기믹 없는 기준선 (2026-08-13)

`bot/runtime-harness.mjs`는 별도 물리 근사가 아니라 현재 ordered classic scripts를 VM에서 순서대로 읽고, 1/60초 고정 dt로 전투 갱신 함수를 호출한다. 이번 기준선은 전용 빈 전장, 강화 없음, 5발, 보스 체력 180·260·340·420, 고정 파티 2명(샛별·미리내) / 3명(+윤슬) / 4명(+별하), 시드 4개, 보스 직행(`direct`)과 접점 패링 우선(`contact`)의 96건이다. 임의 시점의 좌·우 궤도 전환은 기본 점수에 넣지 않았다.

- 별지기 미경유 보스 직격은 0.6배, 그 샷의 첫 충돌이 보스인 직격은 추가 0.5배(기본 합산 0.3배)로 낮췄다. 패링·각성·별자리·유닛 능력에는 적용하지 않는다.
- 변경 뒤 전체 클리어율은 34.4%, 평균 잔여 체력은 107.8, 평균 패링은 1.9회였다. 원본은 `bot/latest-report.json`에 남긴다.
- `direct`는 260에서 모든 고정 파티가 실패하고, `contact`는 260에서 모든 고정 파티가 4/4 클리어한다. 기믹 없는 260은 이제 “별지기 경유·패링을 해야 안정적으로 이기는 초반 기준선” 역할을 한다.
- 2인 고정 파티는 `contact`에서 420까지 4/4 클리어하지만, 3인은 340에서 33 체력을 남기고 0/4, 4인은 340에서 38 체력을 남기고 0/4였다. 인원을 늘린 결과가 단조롭게 강해지지 않았다.
- 이는 인원 수 자체의 강약이 아니라 추가 유닛의 능력·시작 슬롯·충돌 경로가 함께 바뀐 결과다. 이 96건만으로 “2인/3인/4인이 더 세다” 또는 순차 해금 단계를 확정해서는 안 된다.
- 다음 스윕은 같은 파티 크기의 조합·슬롯·발사 경로를 넓혀 중앙값과 하위 분위수를 비교한다. 그 뒤에 2→3→4 해금과 무기믹 보스 체력대를 정한다.

좌·우 궤도 전환도 하니스의 `probeSteerDirection()`으로 별도 확인했다. 오른쪽으로 진행하는 유성은 좌클릭 후 위쪽(진행 좌측), 우클릭 후 아래쪽(진행 우측) 속도를 얻으며, 같은 샷에서 두 번째 전환은 거부된다. 이는 입력 방향·1회 제한의 코드 판정 검증이고, 손맛과 최적 전환 시점은 브라우저 실기 범위다.

## 0-1-a. 재측정 (2026-08-14) — 이 스윕은 더 이상 밸런스 신호가 아니다

0-1의 96건을 같은 조건으로 다시 돌렸다. **위 0-1의 수치는 그날의 기록으로 두고, 아래가 현재 값이다.**

| 지표         | 2026-08-13 | 2026-08-14 |
| ------------ | ---------: | ---------: |
| 클리어율     |      34.4% |  **17.7%** |
| 평균 잔여 HP |      107.8 |      134.8 |
| 평균 패링    |        1.9 |        2.0 |

보스 체력대별로 보면 무너진 자리가 분명하다.

| 보스 HP | 2026-08-13 | 2026-08-14 |
| ------- | ---------: | ---------: |
| 180     |      13/24 |      13/24 |
| 260     |      12/24 |   **4/24** |
| 340     |       4/24 |   **0/24** |
| 420     |       4/24 |   **0/24** |

**저체력대는 그대로고 고체력대만 0이 됐다.** 패링 횟수는 1.9→2.0으로 사실상 같으므로, 파장 자체가 줄어든 것이 아니라 **한 판에 넣을 수 있는 총 피해가 줄었다.**

### 원인 — 봇이 별자리를 아예 못 쓴다

96건 전체를 집계하면 이렇다.

- **별자리 발동: 0회** (96건 합계)
- **한 샷 최대 노드: 2개** — 발동 최소치는 `FIGURE_PARRY.minNodes = 3`
- 안내별 보유 케이스: 0건 (빈 전장 시나리오에는 관측 잔광이 없다)

즉 이 스윕이 재는 것은 **당구 접촉 피해와 유닛 지원뿐**이고, 이 게임의 간판 기믹인 별자리는 한 번도 타지 않는다. 그 사이 게임이 별자리 쪽으로 피해 예산을 옮겼다면, 고체력대에서만 격차가 누적돼 무너지는 지금 모양이 정확히 나온다.

**따라서 17.7%를 “게임이 어려워졌다”로 읽으면 안 된다.** 이 수치는 “별자리를 못 쓰는 플레이어의 상한”이다.

### 격리 검증

같은 날 승리 판정을 벽시계 `setTimeout`에서 프레임 시계로 옮겼다(`scheduleWin` → `afterFeedbackUpdate`). 그 변경 **직전 커밋의 `game-session.js`로 되돌려 다시 돌린 결과도 17.7%로 동일**했다. 이번 하락은 그 변경과 무관하며, `d542727` 이후 누적된 변경의 결과다.

### 대응 — `chain` 정책 추가 (같은 날)

봇이 접점 3개를 잇도록 조준 정책 `chain`을 넣었다. 물리를 다시 풀지 않고, 발사 각도 96개를 훑어 **직선 경로가 별지기를 가장 많이 스치는 방향**을 고른다. 벽 반사 뒤 경로는 세지 않으므로 상한이 아니라 하한이다.

스윕을 3정책 144건으로 넓힌 결과다.

| 정책      | 케이스 | 클리어 |  클리어율 | 별자리 발동 |
| --------- | -----: | -----: | --------: | ----------: |
| `direct`  |     48 |      1 |      2.1% |           0 |
| `contact` |     48 |     16 |     33.3% |           0 |
| `chain`   |     48 |     44 | **91.7%** |      **44** |

`direct` 1건 + `contact` 16건 = 17건으로, 위 96건 재측정치(17.7%)와 정확히 일치한다. 즉 정책 두 개는 그대로고 **`chain`만 새로 열린 경로**다.

**별자리를 쓰느냐 마느냐가 클리어율을 33%에서 92%로 바꾼다.** 고체력대 붕괴의 정체가 이것이었다 — 게임이 어려워진 게 아니라, 측정이 게임의 절반만 보고 있었다.

세부에서 눈에 띄는 것:

- `chain` 4인 / 420 HP는 별자리를 8회나 발동하고 평균 패링 9.0인데도 0/4다. 인원이 늘어도 단조롭게 강해지지 않는다는 0-1의 관찰이 별자리를 쓰는 조건에서도 유지된다.
- `chain` 2인 / 420은 4/4다. 2인 파티가 고체력대에서 오히려 안정적인 이유는 아직 설명되지 않았다.

### 인용 규칙

`bot/latest-report.json`의 `summary.clearRate`는 세 정책을 뭉친 값(현재 47.9%)이라 **그대로 인용하면 안 된다.** 별자리를 쓰는 판과 못 쓰는 판이 섞여 있다. 리포트의 `summary.byPolicy`를 정책별로 인용한다.

### 남은 것

`d542727`과 지금 사이 어느 변경이 `contact`의 고체력대를 무너뜨렸는지는 아직 이분 탐색하지 않았다. 다만 `chain`이 같은 체력대를 클리어하므로, 그 변경은 **총 피해량을 줄인 게 아니라 별자리 경로로 옮긴 것**일 가능성이 높다.

## 0-1-b. 하니스가 매 판 마지막 발을 버리고 있었다 (2026-08-14 재측정)

위 표를 포함해 **이 문서의 모든 스윕 수치는 5발이 아니라 4발로 측정된 것**이었다.

`__botRun`의 루프 조건이 `shot < config.shots`였다. 마지막 발을 쏘면 그 안에서 `shot`이 5가 되고, 유성이 날아가기도 전에 다음 회차 조건이 거짓이 되어 루프가 끝난다. 5번째 발은 발사 직후 한 프레임만 돌고 버려졌다 — 피해도 별자리도 정산되지 않고, `shotDuration`에도 기록되지 않는다. 35스테이지를 기믹째로 훑을 때 `shotsUsed: 5`인데 기록된 체공 시간이 4개뿐인 런이 140건 중 64건 나와서 드러났다.

발사 조건을 루프 본문으로 옮겨 **마지막 유성이 멈춘 뒤에** 끝나게 고쳤다. 같은 스윕 144건을 다시 돌린 결과다.

| 정책      | 케이스 | 4발(종전) | 5발(수정) |  별자리 |
| --------- | -----: | --------: | --------: | ------: |
| `direct`  |     48 |      2.1% |      2.1% |       0 |
| `contact` |     48 |     33.3% |     41.7% |       0 |
| `chain`   |     48 |     91.7% |  **100%** | 44 → 44 |

평균 잔여 체력 90.6 → 77.8, 평균 패링 2.8 → 3.0, 뭉친 클리어율 42.4% → 47.9%다.

읽을 때 주의할 것 둘.

- **`chain`이 48/48이 되었다.** 별자리를 쓰는 경로는 이 기준선에서 더 이상 실패하지 않으므로, 이 스윕은 `chain`의 난이도를 구분하지 못한다. 고체력대를 다시 신호로 쓰려면 체력대를 올리거나 기믹을 넣어야 한다.
- 0-1·0-1-a의 서술(`chain` 4인/420이 0/4 등)은 4발 기준이라 **지금 리포트와 대조하면 어긋난다.** 그 절들은 당시 측정 기록으로 남겨 두었고, 현재 수치는 이 절과 `bot/latest-report.json`이 기준이다.

## 0-1-c. 지원 사격 피해가 한 번도 들어간 적이 없었다 (2026-08-15 재측정)

위 수치들도 절반의 게임으로 잰 것이었다. `queueUnitAssist`가 큐에 넣는 지원 사격(검기·저격·각성 피니셔)은 `delay`가 배정된 뒤 `resolveAssist`에서 피해가 정산되는데, **`delay`를 배정하는 `afterUnitAssistQueued` 훅이 `detonateShockwave` 한 곳에서만 발화되고 있었다.** 나머지 경로의 어시스트는 delay가 `undefined`인 채 큐에 들어가 정리 루프의 두 분기(`> 0`도 `<= 0`도) 모두에서 거짓이 되어 — 영원히 제거되지 않고, 피해도 팝업도 임팩트도 없이, 빔 장식만 보스에 박제됐다. 각성 피니셔가 불멸이 되면 `finisherFocus`가 풀리지 않아 **첫 각성 정산 뒤 전투 전체가 0.82배속으로 굳는** 부작용도 함께였다.

훅 발화를 `queueUnitAssist` 안으로 옮기고 delay를 숫자로 초기화한 뒤 같은 144건을 다시 돌린 결과다.

| 정책      | 어시스트 미적용(종전) | 어시스트 정상(수정) |
| --------- | --------------------: | ------------------: |
| `direct`  |                  2.1% |                2.1% |
| `contact` |                 41.7% |           **91.7%** |
| `chain`   |                  100% |                100% |

`contact`가 두 배가 됐다 — **패링 뒤의 지원 사격이 이 게임 피해 예산의 절반**이었다는 뜻이다. `chain`의 별자리 발동은 44→4로 급감했는데, 회귀가 아니라 평균 1.08발(종전 2.31발)에 전투가 끝나 별자리 정산까지 갈 일이 없어진 결과다(노드 3개 도달 자체는 48건 중 32건 유지).

**이 기준선은 이제 포화됐다.** 무기믹 180~420에서 `contact`·`chain` 모두 사실상 만점이라 난이도 신호가 없다. 다음 스윕 전에 보스 체력대 상향 또는 기믹 도입이 선행돼야 한다 — 이는 밸런스 판단이라 측정만 남긴다.

## 0-3. 계기 교체 — 무기믹 기준선에서 실제 캠페인으로 (2026-08-15)

0-1-c에서 지원 사격이 살아나며 무기믹 기준선이 포화됐다(`contact` 91.7% / `chain` 100%). 그런데 같은 날 **실제 35스테이지를 그대로 돌려 보니 신호가 살아 있었다** — 포화된 것은 게임이 아니라 계기였다. 기준선은 전용 빈 전장에 체력 네 단만 쓰는 인공 시나리오이고, 실제 스테이지는 저마다 다른 체력·배치·기믹을 가진다.

그래서 난이도 계기를 캠페인 스윕으로 옮겼다. `bot/run-bot.mjs`는 이제 둘을 함께 낸다.

| 스윕       | 역할                  | 상태                                                                |
| ---------- | --------------------- | ------------------------------------------------------------------- |
| `plain`    | 전투 규칙 회귀 기준선 | **포화 — 난이도 신호 아님** (`summary.plain.saturated`가 자동 판정) |
| `campaign` | **난이도 계기**       | 35스테이지 실측                                                     |

### 시드 대신 조준 오차를 축으로 쓴다

기존 스윕의 시드 축은 사실상 죽어 있었다. 런타임에서 `Math.random()`이 닿는 곳은 크리티컬 확률 하나뿐이라, 35스테이지 중 33개가 4개 시드에서 **완전히 같은 결과**였다.

그 자리를 **조준 오차**로 바꿨다. 발사 각도에 매 발 시드된 난수로 라디안 오차를 더한다 — 서투른 플레이어를 모사하는 축이고, 시드마다 다른 손떨림이 되므로 시드가 비로소 표본이 된다.

| 등급      | σ (rad) | 뜻        |
| --------- | ------: | --------- |
| `precise` |       0 | 오차 없음 |
| `steady`  |    0.04 | 숙련      |
| `loose`   |    0.10 | 보통      |
| `wild`    |    0.22 | 서투름    |

### 결과 (35스테이지 × 2정책 × 4등급 × 6시드 = 1,680건)

| 정책      | precise | steady | loose |      wild |
| --------- | ------: | -----: | ----: | --------: |
| `contact` |   82.9% |  68.6% |   80% |     77.6% |
| `chain`   |   85.7% |  79.5% | 73.3% | **61.9%** |

**`chain`이 단조 감소한다 — 계기가 살아 있다는 뜻이다.** 전체 클리어율은 76.2%.

### 읽을 때의 한계 둘 (반드시 함께 읽는다)

- **`precise`는 표본이 아니다.** σ=0이면 6개 시드가 전부 같은 한 줄을 재생한다. 「통하는 경로가 하나라도 있는가」를 묻는 결정론적 프로브 1회지, 6회 시행의 비율이 아니다. 아래 스테이지 표에서 `precise` 0%가 여럿 보이는 것은 그 스테이지가 불가능해서가 아니라 **봇이 고른 그 한 줄이 실패**해서다.
- **`contact`는 단조가 아니다**(82.9 → 68.6 → 80 → 77.6). 오차가 크면 첫 별지기를 지나쳐 더 멀리 굴러 더 많은 접점을 스치는 일이 생기기 때문이다. 난이도 곡선은 `chain`으로 읽는다.

### 가장 어려운 여덟 스테이지

`chain` 곡선은 precise / steady / loose / wild 순이다.

| 스테이지 | 보스 HP |   전체 | `chain` 곡선             |
| -------- | ------: | -----: | ------------------------ |
| `8-1`    |    1600 | **0%** | 0 / 0 / 0 / 0            |
| `7-1`    |     350 |  47.9% | 0 / 83.3 / 50 / 50       |
| `5-5`    |     320 |    50% | 0 / 83.3 / 50 / 66.7     |
| `4-3`    |     275 |  54.2% | 0 / 83.3 / 50 / 66.7     |
| `2-4`    |     220 |  60.4% | 0 / 100 / 66.7 / 66.7    |
| `7-6`    |     375 |  62.5% | 100 / 66.7 / 66.7 / 33.3 |
| `7-2`    |     355 |  64.6% | 100 / 66.7 / 66.7 / 66.7 |
| `6-5`    |     345 |  66.7% | 100 / 83.3 / 83.3 / 33.3 |

전 등급 전승은 `1-3`·`2-1` 둘뿐이다.

**8-1의 0%는 「불가능」이 아니다.** 정책·조준·시드를 넓혀 80런을 돌려 피해 분포를 따로 냈다.

| 8-1 봇 피해 (80런) |       값 | 1600 대비 |
| ------------------ | -------: | --------: |
| 최고               | **1328** |   **83%** |
| 상위 10 평균       |      943 |       59% |
| 중앙값             |      365 |       23% |

최고 기록은 `chain` · `wild` · 패링 11회 · 최대 노드 6에서 나왔다. 즉 **봇의 좋은 판은 이미 8할을 깎는다** — 남은 17%는 사람이 봇보다 잘하면 닿는 거리다. 0%인 이유는 상한이 아니라 중앙값이 낮아서(365)이고, 그 편차의 정체는 한 발에 별지기 셋을 잇는 긴 비행이 우연에 맡겨져 있다는 것이다. 봇에 별자리 반복 정책이 생기면 이 판정은 다시 해야 한다.

### 남은 것

- `contact`의 비단조를 설명하려면 접점 수와 이동 거리를 케이스별로 남겨야 한다. 지금 리포트에는 `shotsUsed`만 있다.
- 봇이 별자리를 의도적으로 반복 발동시키는 정책이 없다. 8-1처럼 체력이 큰 판은 그 정책 없이는 영원히 0%다.

## 0-4. 체력 곡선과 실제 피해 용량이 무관하다 (2026-08-15)

세 번째로 끊겨 있던 피해 경로(정산 각성 피니셔)를 이은 뒤 캠페인 클리어율이 76.2% → 89%로 올랐고 `chain`의 조준 곡선이 다시 평평해졌다(85.7/86.2/85.2/88.1). **게임의 체력 곡선은 피해의 상당 부분이 실제로는 들어가지 않던 동안 맞춰진 것이다.** 다시 자를 근거를 만들기 위해 「그 배치에서 5발이 낼 수 있는 피해 용량」을 쟀다.

### 측정 방법

보스 체력을 999,999로 덮어쓰고(하니스의 `bossHpOverride`) 5발을 끝까지 굴려 **깎아낸 총량**을 센다. 실제 체력으로 재면 피해가 체력에서 잘려 클리어한 판은 전부 「피해 = 체력」으로 보이기 때문에, 용량은 이 방법으로만 나온다. `chain` 정책, 조준 3등급(steady/loose/wild) × 시드 4개 = 스테이지당 12런.

### 결과

| 스테이지 | 현재 HP | 피해 용량 p10 / 중앙 / p90 | 현재HP ÷ 중앙 |
| -------- | ------: | -------------------------- | ------------: |
| `1-1`    |     120 | 237 / 541 / 1029           |      **0.22** |
| `2-4`    |     220 | 267 / 689 / 804            |          0.32 |
| `4-2`    |     265 | 326 / 491 / 955            |          0.54 |
| `5-4`    |     315 | 326 / 491 / 955            |          0.64 |
| `6-6`    |     350 | 326 / 491 / 955            |          0.71 |
| `7-7`    |     380 | 326 / 491 / 955            |      **0.77** |
| `8-1`    |    1600 | 390 / 659 / 812            |      **2.43** |

**34개 캠페인 스테이지 전부에서 체력이 봇 중앙 피해 용량의 0.22~0.77배다.** 즉 봇은 필요한 것의 1.3~4.5배를 때리고 있다. 유일한 예외가 8-1(2.43배)이고, 그래서 난이도가 34개 평지와 절벽 하나로 갈린다.

### 구조 — 난이도를 정하는 것은 체력이 아니라 배치다

용량 수치가 7개 주기로 반복된다. 원인은 `game-data.js:1204`의 `CAMPAIGN_LAYOUTS[campaignIndex % CAMPAIGN_LAYOUTS.length]`로, **배치는 7종이 순환하고 체력만 120 → 380으로 선형 증가**한다. 두 곡선은 서로를 모른다.

그래서 측정된 난이도 순서가 체력 순서와 어긋난다 — 최난은 `7-1`(350)·`5-5`(320)·`4-3`(275)이고 `7-7`(380)은 그보다 쉽다. 같은 배치를 쓰는 `4-2`·`5-4`·`6-6`·`7-7`은 용량이 491로 같은데 체력만 265 → 380으로 오르므로, **체력 상승분이 곧 난이도 상승분인 유일한 축**이 되어 버렸다.

### 결정이 필요한 것 (측정만 남기고 값은 정하지 않는다)

> **먼저 읽을 것**: [0-7절](#0-7-조준은-첫-발까지만-읽힌다-2026-08-18)에서 이 표의 중앙값이 요동치는 분포의 중앙값임을 보였다. 목표 클리어율을 정하기 전에 그 폭을 좁힐 것인지가 먼저 결정돼야 한다.

1. **목표 클리어율**을 정해야 값이 나온다. 체력을 p10 용량에 맞추면 나쁜 판만 실패하고, 중앙에 맞추면 반반이 된다. 봇은 사람보다 조준이 나쁘고 일관성은 높으므로 그대로 옮길 수는 없다.
2. **배치 7종의 순환을 유지할 것인가.** 유지한다면 체력은 「배치별 용량 × 목표 배수」로 잡아야 하고, 지금처럼 배치와 무관하게 선형 증가시키면 난이도가 계속 톱니처럼 흔들린다.
3. **8-1의 2.43배를 유지할 것인가.** 봇 최고 기록은 1328/1600(83%)이라 도달 불가능한 수치는 아니지만, 34개가 0.22~0.77배인 곡선 끝에 붙은 절벽이다.

## 0-11. 한 샷 안의 예측성 — 접점 되감기 (2026-08-18)

오너 제보: 「너무 튀는 게 예측이 안 되잖아. 당구처럼 예측 가능하게 하면서 손맛도 없애지 않는 방법 있어?」 0-7절은 **둘째 발**의 요동을 쟀다. 이건 **한 샷 안**의 문제라 계기가 없었다.

### 아니었던 것

- **첫 접촉은 이미 정확하다.** 마우스 1px(0.11°)에 진행 방향이 0.1~0.7° 바뀐다. 증폭이 없다.
- **별지기는 서 있다.** 접촉 826회 중 83.5%에서 별지기 속도가 0이었다(`probeLegibility`). 당구가 예측 가능한 전제 — 맞는 공이 정지 — 는 이미 만족하고 있었다.

### 원인은 「겹친 자리에서 잡는 법선」이었다

한 프레임을 최대 3조각으로만 쪼개는데(`slices`) 실제로는 2조각이라, 발사 직후 유성은 한 조각에 14px 움직인다. 반지름이 13이므로 접촉이 감지될 때 이미 접촉 거리의 40%를 파고든 상태고, 그 자리의 법선은 실제로 닿은 순간과 다르다.

상대 속도를 따라 되감아 `|p + vrel·t| = reach`인 t를 풀고 그 접점의 법선을 쓴다. 위치는 건드리지 않는다.

| 접촉별 방향차(마우스 1px, 중앙, 도) | 1회 | 2회 | 3회 |  4회 |      5회 |      6회 | 샷당 접촉 |
| ----------------------------------- | --: | --: | --: | ---: | -------: | -------: | --------: |
| 되감기 없음(예전)                   | 0.1 | 0.4 | 2.3 |  9.6 |     26.4 |     62.7 |         4 |
| **되감기**                          | 0.1 | 0.3 | 1.7 | 10.9 | **19.1** | **20.7** |         4 |

꼬리가 잡힌다(6접촉 62.7° → 20.7°). 캠페인 클리어율 83 → 83.2%로 **밸런스는 움직이지 않는다.** 무기믹 기준선은 77.8 → 61.1%로 내려갔는데(특히 `contact` 100 → 41.7%), 이 버킷은 0-1절에서 이미 포화 표본으로 기록돼 있고 시드가 무력한 결정론 48건이다. 난이도 신호로 읽지 않는다.

### 표본 16개로 반대 결론을 냈다 — 기록해 둔다

처음에는 **별지기 질량을 5배로** 올리는 안을 재고 채택했다. 근거는 「3접촉에서 방향차가 57.6°로 터지고 질량 5에서 1.9°로 평평해진다」였다. 실제로 그렇게 커밋했다(`37b4df8`).

그 측정은 **8스테이지 × 2정책 = 16표본을 각 스테이지의 정책각 «한 점»에서** 뽑은 것이었다. 각도를 ±40°까지 5° 간격으로 훑어 238표본으로 다시 재자 결론이 뒤집혔다.

| 접촉별 방향차(중앙, 도) | 1회 | 2회 | 3회 |  4회 |  5회 |  6회 | 샷당 접촉 |
| ----------------------- | --: | --: | --: | ---: | ---: | ---: | --------: |
| 질량 5 + 되감기         | 0.1 | 1.1 | 2.1 | 22.7 | 65.3 | 98.5 |         5 |
| **질량 1 + 되감기**     | 0.1 | 0.3 | 1.7 | 10.9 | 19.1 | 20.7 |         4 |

**모든 접촉에서 등질량이 더 예측 가능하다.** 57.6°는 운 나쁜 각 몇 개였다. 질량은 1로 되돌렸고 `HERO_MASS` 상수와 그 근거표는 주석으로 남겼다.

등질량은 그 자체로 당구의 규칙이다 — 정통으로 맞은 흰 공이 서는 것은 버그가 아니라 스턴샷이다. 이 게임에 없는 것은 질량차가 아니라 회전이다. 오너 판단: 「구름은 2D에서 이상하다, 목표는 당구 유사성이 아니라 예측성」이므로 회전은 넣지 않았다.

### 계수기 버그 셋

같은 측정에서 계수 오류를 세 번 냈다. 남겨 둔다.

1. `afterMobilePairCollision` 훅으로 별지기의 «충돌 전» 속도를 읽으려 했다. 그 훅은 임펄스가 끝난 뒤에 불린다 — 모든 별지기가 1080(정산 상한)으로 보였다. `mobilePair`를 감싸 해결.
2. 래퍼가 `mobilePair`의 7번째 인자(`massB`)를 버려서, 질량을 바꿔도 리포트가 안 움직였다.
3. 샷당 접촉 수를 마지막 프레임에서 읽었다. 유성이 멈추면 `simulatePhysics`가 `endShot → startShot`을 불러 `ball.bounces`를 다음 샷 기준으로 리셋하므로, 접촉 7회 한 샷이 2로 돌아왔다 — 그 값이 경로의 마지막 칸에도 들어간다. 최댓값으로 잡아야 한다. 이 버그 때문에 「샷의 99%가 3접촉 안에서 끝난다」는 틀린 중간 결론을 냈다(실제 중앙값은 4~5회).

### 계기

`probeTrajectory`(프레임별 위치·속도·누적 접촉), `probeLegibility`(충돌 전 별지기 속도·정면도), `probeGuideTruth`(조준 가이드가 그린 선 vs 실제 진로). `heroMass`는 프로브 전용 계기로 남아 있고 1에서 무해하다.

## 0-10. 조준 보정을 껐다 (2026-08-18)

오너 질문: 「보정을 왜 넣는 거야? 없애면 안 돼?」 0-8절에서 이 시스템의 버그를 고쳤을 뿐 존재 이유는 확인한 적이 없었다.

### 근거가 없었다

`git log -S"billiardAim"`으로 도입 시점을 찾으면 최초 수직 슬라이스(`cbd13fe`)다. 그 뒤 어떤 문서에도 왜 필요한지가 적혀 있지 않다. 문서에 등장하는 것은 0-8절의 버그와 「기본 스윕은 이 경로를 안 탄다」는 계측 주의사항뿐이다.

### 목적을 달성하지 못했다

`aimPath`로 켜고 끈 비교 (8스테이지 × 12시드).

| 보정 | 클리어율 | 별자리/판 | 띠(p90/p10) | 중앙 피해 |
| ---- | -------: | --------: | ----------: | --------: |
| 켬   | **87.5** |      0.16 |        2.75 |       587 |
| 끔   | **87.5** |      0.22 |        3.48 |       555 |

클리어율이 소수점까지 같다. 조준 안정성도 같다 — 같은 별지기를 첫 접촉으로 유지하는 최장 각도 폭이 **21도 대 20도**, 접촉에 성공한 각도가 **65/93 대 66/93**이다. 「조준을 도와준다」는 목적이 수치로 나타나지 않는다.

### 해로운 쪽은 측정된다

- **감도를 자리마다 다섯 배까지 왜곡한다.** 0-8절 측정: 목표 중앙에서 손 움직임의 0.42배, 원뿔 가장자리에서 2.16배. 같은 1px에 조준이 다르게 반응한다.
- **결과를 서로 비슷하게 만든다.** 띠가 켜면 2.75, 끄면 3.48이다. 잘 쏜 판과 못 쏜 판을 가깝게 당기므로, 실력을 드러내려는 방향과 정반대다.
- 0-8절에서 고친 「별지기를 갈아탈 때 1px에 2~4도 튀는」 불연속도 이 시스템의 것이었다.

### 처리

`AIM_ASSIST_PULL`을 0으로 두어 껐다. 상수 하나를 0.58로 되돌리면 복구된다. 수업이 조준을 강제하는 경로(`resolveBilliardAim` 훅)는 그대로다. 확인: `aimPath`를 켠 결과와 끈 결과가 39/39 지점에서 완전히 동일하다.

### 한계

**봇은 사람처럼 조준하지 않는다.** 각도를 계산해 정하지 마우스를 끌지 않으므로, 「손이 떨리는 것을 봐주는」 보정의 원래 목적은 이 계측으로 검증되지 않는다. 다만 감도 왜곡(0.42~2.16배)은 봇이 아니라 사람 입력 경로에 대한 측정이다.

## 0-8. 조준 보정이 별지기를 갈아탈 때 4도 튀었다 (2026-08-18)

오너 제보: 「조금만 움직여도 각도가 휙휙 움직이는데 좀 더 당구처럼 정교하게는 안 되나?」 0-7절의 요동(각도가 달라지면 *결과*가 달라진다)과 다른 문제다. 이쪽은 *조준선 자체*가 튀는 것이고, 물리가 아니라 입력이다.

### 측정

`probeAimTransfer`(2026-08-15에 만들어 두고 게이트에 물리지 않은 프로브)로 마우스 가로 위치를 1px씩 쓸며 실제 `cuePull` + `billiardAim`이 내놓는 발사각을 기록했다. 당김 40·80·160px, 6개 스테이지.

| 항목                         |                                   값 |
| ---------------------------- | -----------------------------------: |
| 마우스 1px당 조준 변화(중앙) |                      0.065 ~ 0.155도 |
| 같은 항목의 최소~최대        |            0.029 ~ 0.369도 (3~5.6배) |
| 1px에 각도가 튀는 지점       | 6개 중 4개 스테이지, 최대 **4.12도** |

튀는 자리마다 보정 상태(`assisted`)가 앞뒤로 모두 `true`였다. 즉 **원뿔에 들고 나는 경계가 아니라, 보정이 다른 별지기로 갈아타는 경계**다. 별지기가 하나뿐인 1-1에는 튀는 자리가 없다.

`billiardAim`은 원뿔(0.15rad) 안에서 **가장 가까운 별지기 하나**를 골라 그쪽으로 각을 0.58만큼 당긴다. 겨누는 각을 쓸다가 그 「가장 가까운」이 바뀌면 `delta`가 통째로 다른 별지기 것으로 갈아타며 부호까지 뒤집힌다. 코드에 남아 있던 이전 수정은 원뿔의 **바깥** 경계만 데워 없앤 것이라 이 자리에는 닿지 않았다.

### 수정과 결과

하나를 고르지 않고, 원뿔 안의 모든 별지기가 각자의 falloff만큼 당기는 값을 **더한다**. 각 항이 경계에서 0으로 수렴하므로 별지기가 들어오고 나가는 순간에도 합이 이어진다. 별지기가 하나뿐인 구간의 결과는 예전과 완전히 같다.

| 스테이지   |             최대 점프 (전 → 후) |              역주행 (전 → 후) |
| ---------- | ------------------------------: | ----------------------------: |
| 9·19·24·29 | 2.12~~3.97도 → **0.30~~0.31도** |                   없음 → 없음 |
| 0·14       |                 0.31도 → 0.48도 | 없음 → 24px, 최대 **0.024도** |

원래 멀쩡했던 두 스테이지에 아주 작은 역주행이 생겼다. 한 픽셀에 0.024도는 정상 이동량(약 0.12도)의 5분의 1이고 없앤 4도보다 160배 작아 그대로 뒀다. 도달 각도 폭은 전 스테이지에서 변하지 않았다.

### 밸런스 영향 없음

기본 스윕은 `billiardAim`을 거치지 않으므로(0절의 `aimPath` 주석) `bot/latest-report.json`은 한 자리도 움직이지 않는다. **그것은 「안 변했다」가 아니라 「측정되지 않았다」이므로**, `aimPath: true`로 사람과 같은 경로를 태워 840런을 다시 돌렸다.

| 지표      | 전 → 후         |
| --------- | --------------- |
| 클리어율  | 82.38% → 82.02% |
| 별자리    | 270 → 251       |
| 평균 피해 | 282.9 → 282.5   |

n=840에서 클리어율의 표준오차가 약 1.3pp이므로 전부 잡음 범위다. 감각만 바뀌고 난이도는 그대로다.

### 남은 것 (이번에 손대지 않음)

- **감도가 자리마다 3~5.6배 다르다.** 보정이 목표 중앙에서는 손보다 느리게, 원뿔 가장자리에서는 빠르게 돈다. 오너가 이번 범위에서 제외했다.
- **정밀도가 위력에 묶여 있다.** 당김 40px에서 1px당 0.14도, 160px에서 0.067도다. 세게 치려면 많이 당겨야 하므로 **약한 샷일수록 조준이 거칠어진다.** 진짜 당구와 가장 다른 지점이다. 역시 이번 범위 밖.

## 0-9. 운의 띠를 좁히는 레버 실험 (2026-08-18)

오너 결정: 「띠를 좁히자.」 0-7절이 5발 합계의 p10~p90이 4.3배로 벌어진다고 보인 뒤의 판단이다. **규칙을 직감으로 고르지 않기 위해 게임 코드를 고치기 전에 후보를 하니스에서 쟀다.**

### 실험대

`probeBandLever`가 `applyBossHit`(보스 피해가 전부 지나는 단일 통로)을 감싸 두 가지를 넣는다. 기본값에서는 완전히 무해하며, 추가 뒤 `bot/latest-report.json`이 한 자리도 움직이지 않는 것으로 확인했다.

- **cap C** — 단일 타격의 피해를 C로 자른다 (p90을 내린다)
- **floor F** — 한 발이 F보다 적게 넣었으면 차이를 채운다 (p10을 올린다)
- **접촉 조건** — 켜면 별지기를 스쳤거나 보스에 닿은 발만 채운다

비율 p90/p10은 배수에 대해 불변이므로, 배수를 올리거나 내려서는 띠가 움직이지 않는다. 가산(바닥)이나 상한(천장)만이 비율을 바꾼다.

### 측정 (8스테이지 × 32시드, `bossHpOverride: 999999`)

| 설정                         |       띠 | p10 | 중앙 |  p90 | 최대 | 중앙 변화 |
| ---------------------------- | -------: | --: | ---: | ---: | ---: | --------: |
| 기준                         |     3.85 | 262 |  578 |  981 | 1367 |        0% |
| cap 60                       |     2.79 | 216 |  380 |  589 |  786 |      -34% |
| cap 90                       |     3.03 | 240 |  464 |  708 |  936 |      -20% |
| cap 140                      |     3.21 | 260 |  528 |  814 | 1057 |       -9% |
| floor 70                     |     2.57 | 406 |  655 | 1029 | 1422 |      +13% |
| floor 100                    |     2.15 | 505 |  709 | 1075 | 1459 |      +23% |
| floor 70 (접촉시)            |     2.84 | 363 |  633 | 1024 | 1412 |      +10% |
| floor 100 (접촉시)           |     2.46 | 431 |  682 | 1058 | 1442 |      +18% |
| **floor 100 접촉 + cap 140** | **2.11** | 431 |  632 |  906 | 1122 |       +9% |

### 읽는 법

- **상한 단독은 실패다.** cap 60은 천장만 자르는 것이 아니라 p10까지 262 → 216으로 끌어내린다. 잘 친 판을 벌하면서 띠는 2.79까지밖에 못 좁히고, 중앙 피해를 34% 잃는다.
- **바닥이 원하는 모양을 만든다.** floor 100(접촉)은 p10을 262 → 431로 올리는데 p90은 981 → 1058로 거의 그대로다. 운 나쁜 판만 구제하고 잘 친 판은 건드리지 않는다.
- **둘을 함께 쓰면 상한이 비로소 쓸모를 얻는다.** 바닥이 받쳐 주므로 cap 140이 p10을 내리지 못하고 천장만 깎는다 — 띠 2.11(기준 대비 45% 감소), 중앙 변화 +9%.
- 중앙 변화가 작다는 것은 **보스 체력을 다시 깎을 일이 적다**는 뜻이다. floor 100 단독은 +18%라 34개 스테이지의 체력이 전부 어긋난다.

### 이 절에서 내가 틀렸던 것

접촉 조건을 처음 쟀을 때 띠가 기준선과 **바이트 단위로 동일**하게 나왔다. 「저조한 발은 전부 헛발이다」로 읽힐 수 있는 결과였는데, 사실은 계측 결함이었다 — `ball.starkeeperTouched`가 정산 중(`game-combat.js:1452`)에 false로 돌아가므로 `afterShotEnd`에서 읽으면 언제나 「안 닿았다」다. 접촉을 프레임마다 걸어 두도록 고친 뒤의 감사 결과는 정반대다: 샷의 82%가 접촉하고, **피해 100 미만인 발의 78%도 접촉했다.** 저조한 꼬리는 헛발이 아니라 「연결은 됐는데 운이 나빴던 발」이다.

### 남은 결정과 한계

- **천장을 자를 것인가**가 유일하게 남은 갈림길이다. 자르면 띠 2.11, 안 자르면 2.46이고 천장 1442가 유지된다. 조준이 결과를 잘 못 정하는 게임에서 천장은 드물게나마 실력이 드러나는 자리라, 이것은 수치가 아니라 설계 판단이다.
- **전부 봇 기준이다.** 봇은 매 발 정책으로 재조준하고 사람은 바뀐 판을 보고 다시 계획한다. 봇의 띠를 좁히는 것이 사람의 띠를 좁히는 것과 같다는 보장은 없다.
- 아직 게임 코드는 한 줄도 바뀌지 않았다. 이 절은 계측만 남긴다.

## 0-7. 조준은 첫 발까지만 읽힌다 (2026-08-18)

0-4절이 체력 재조정을 위해 **목표 클리어율**을 정해 달라고 남겨 뒀다. 그런데 봇 클리어율을 사람 난이도로 옮기려면 「조준이 좋을수록 잘 깬다」가 성립해야 한다. 조준 사다리가 그 반대를 가리키고 있어서, 값을 정하기 전에 축부터 검증했다.

### 이미 적혀 있던 사실이 데이터에는 반영돼 있지 않았다

`precise`가 표본이 아니라는 것은 [0-3절](#0-3-계기-교체--무기믹-기준선에서-실제-캠페인으로-2026-08-15)에 이미 적혀 있다. 다시 확인했다 — `Math.random`은 하니스 전체에서 한 곳, `aimSigma`가 곱해지는 자리에서만 쓰이므로 σ=0이면 시드가 무력하다. 같은 스테이지를 시드 11과 47로 돌리면 결과 객체가 완전히 같고(5/5 동일), 나머지 세 등급은 전부 다르다(0/5 동일).

문제는 **`bot/latest-report.json`이 그 버킷을 여전히 `cases: 210`으로 내보내고 있었다는 것**이다. 실제로 서로 다른 판은 35개다. 문서를 읽지 않고 JSON만 보면 신뢰구간이 6배 좁아 보인다. 이번에 생성기가 `distinctRuns`를 함께 내도록 고쳤다.

### 시드를 16개로 올리면 단조가 깨진다

35스테이지 × 16시드(`precise`는 결정적이므로 35런).

| 정책      | precise (n=35) | steady | loose | wild | 별자리/런 (precise → wild) |
| --------- | -------------: | -----: | ----: | ---: | -------------------------- |
| `contact` |             80 |   75.9 |  84.6 | 89.1 | 0.057 → 0.332              |
| `chain`   |             80 |   84.3 |  81.8 | 84.6 | 0.171 → 0.275              |

기록돼 있던 매끄러운 단조(81 → 84.8 → 85.2 → 88.1)는 시드 2개짜리 표본의 착시였다. 실제로는 `contact`가 먼저 75.9로 내려간다. 그리고 **`chain`에서는 조준 효과가 사실상 없다.**

### 피해 용량으로 바꾸면 형태가 보인다

클리어율은 80~89%에서 포화된다. `bossHpOverride: 999999`로 5발이 깎아낸 총량을 재고(24시드), 정책이 실제로 고른 각의 피해가 잡음 분포의 몇 백분위에 앉는지 봤다.

| 정책/조준        | 정확각 피해(중앙) | 잡음 피해(중앙) | 정확각의 백분위 |
| ---------------- | ----------------: | --------------: | --------------: |
| `contact`/steady |               496 |             486 |             54% |
| `contact`/loose  |               496 |             557 |             54% |
| `contact`/wild   |               496 |             561 |         **33%** |
| `chain`/steady   |               522 |             572 |             58% |
| `chain`/loose    |               522 |             564 |             50% |
| `chain`/wild     |               522 |             544 |         **42%** |

작은 흔들림(σ=0.04)에서는 정확각이 54~58백분위로 **정상**이다. 무너지는 것은 σ=0.22(약 12.6°)에서뿐이다.

### 지형도 — 봉우리는 반도 단위로 서 있다

측정을 위해 하니스에 `aimOffset`을 추가했다. 잡음이 아니라 **결정적인 각도 이동**이며, 0이면 완전히 무해하다(아래 재현 확인 참조). 전 방위 3° 간격 + 관심 구간 ±25°를 0.5° 간격으로 훑었다. 8스테이지.

| 항목                              |        범위 |
| --------------------------------- | ----------: |
| 정책이 고른 각의 피해 (오프셋 0)  |    262~1299 |
| 고정 오프셋 중 최선의 피해        |   1384~2089 |
| 이득                              | ×1.56~×6.13 |
| **0.5° 이웃과의 피해 차이(중앙)** |     148~256 |
| 무작위 잡음(wild) 피해 중앙       |     427~737 |

두 가지가 동시에 참이다. **정책각은 최적에서 한참 멀고**(고정 최선이 2~~6배), **동시에 지형이 반도 단위로 요동친다**(기준값 500 안팎에서 이웃 간 차이 150~~250). 무작위 잡음은 고정 최선(1384~2089)에 한참 못 미치므로, 잡음이 봉우리를 _찾는_ 것이 아니다 — 요동치는 판에서 5번 뽑는 것이 1번 뽑는 것보다 나을 뿐이다.

### 요동은 둘째 발에서 터진다

같은 스윕을 발 수만 바꿔 다시 돌렸다(0.25° 간격, 4스테이지).

| 발 수 | 피해 중앙 | 0.25° 이웃과의 차이 | 차이/중앙 |
| ----: | --------: | ------------------: | --------: |
|     1 |        32 |             **4~8** | 0.13~0.25 |
|     2 |       126 |          **81~100** | 0.62~0.79 |
|     5 |       511 |             207~244 | 0.43~0.47 |

첫 발에서 둘째 발로 넘어가며 절대 변동이 **약 15배**로 뛴다. 둘째 발은 첫 발이 다시 배치해 놓은 판에 쏘는 것이라, 0.25°가 판 전체를 바꾼다.

### 결론

- **한 발 안에서 조준은 읽힌다.** 0.25°가 피해를 13~25% 바꾸는 정도이고, 이것은 정상적인 조작 감각이다.
- **5발 합계는 계획할 수 없다.** 둘째 발부터 결과가 첫 발의 우연에 지배된다.
- 그래서 「조준이 나쁠수록 잘 깬다」가 관측된 것이다. 원인은 게임이 정밀함을 벌해서가 아니라, **합계가 요동치므로 표본을 여럿 뽑는 쪽이 이기기 때문**이다.
- **0-4절의 피해 용량 중앙값은 요동치는 분포의 중앙값이다.** 1-1의 p10~~p90이 237~~1029(4.3배)인 것이 그 폭이다. 어떤 체력을 고르든 그 값은 실력이 아니라 운이 지배하는 띠 안에 놓인다. 목표 클리어율을 정하기 전에 이 띠를 좁힐 것인지가 먼저 결정돼야 한다.

### 한계

- 지형도는 8스테이지, 발 수 분해는 4스테이지다. 전 스테이지가 아니다.
- **봇은 매 발 정책으로 다시 조준하고, 사람은 바뀐 판을 보고 다시 계획한다.** 이 측정이 말하는 것은 「5발을 미리 계획할 수 없다」이지 「둘째 발을 칠 수 없다」가 아니다. 사람에게 각 발은 첫 발만큼 읽힐 수 있다.
- 1발 측정에서 스테이지 29만 비율 0.89로 홀로 튄다. 원인은 확인하지 않았다.
- 당구형 물리에서 다중 충돌 뒤의 궤적이 초기 조건에 민감한 것 자체는 장르의 성질이다. 여기서 문제로 보는 것은 민감성의 존재가 아니라, **보상(피해·별자리)이 그 민감한 꼬리에 얹혀 있다는 것**이다.

### 재현 확인

`aimOffset`·`shots` 손잡이를 추가한 뒤 `node bot/run-bot.mjs`를 다시 돌려 `bot/latest-report.json`과 대조했다. `generatedAt` 한 줄을 빼고 **완전히 동일**하다. 기존 측정치는 영향을 받지 않는다.

## 0-6. 리포트 재생성 (2026-08-16)

`bot/latest-report.json`이 `4fd2f4e` 시점에 멈춰 있어 그 뒤 여섯 커밋의 변화를 담고 있지 않았다. 같은 코드로 두 번 돌려 **완전히 동일한 결과**가 나오는 것을 먼저 확인하고(하니스는 결정적이다) 재생성했다.

상위 수치는 거의 움직이지 않았다.

| 구간     |  이전 |  현재 |
| -------- | ----: | ----: |
| plain    | 80.6% | 77.8% |
| campaign | 82.8% | 83.0% |

움직인 것은 **별자리 발동이 어느 정책에서 나오느냐**다.

| 버킷                             |  별자리 이전 → 현재 |
| -------------------------------- | ------------------: |
| `plain.byPolicy.contact`         |          12 → **0** |
| `campaign.byAim.contact.precise` |         42 → **12** |
| `campaign.byAim.chain.*`         | 44→45, 55→51, 66→69 |

`contact`는 연쇄를 겨냥하지 않는 정책이라 별자리를 **우연히** 터뜨리고 있었고, 그 우연이 사라졌다. 접점을 여러 별지기에 걸치도록 각도를 고르는 `chain`은 그대로다. 클리어율이 거의 안 변한 것도 같은 이야기다 — `contact`의 별자리는 피해 예산에서 차지하는 비중이 작았다.

방향은 `4fd2f4e`(각성을 움직임에 돌려주고 패링은 별자리에 남김)와 `dac3850`(정산은 대기열이 아니라 예산)의 의도와 맞는다. **다만 이 두 커밋 중 어느 쪽이 얼마를 옮겼는지는 이분 탐색하지 않았다.**

## 0-5. 각성을 패링에서 떼어내기 전후 (2026-08-16)

오너 판단: 「지금 게임이 너무 밋밋한데 예전 각성 되돌리자. 패링은 그냥 별자리 완성용으로 두고.」 되돌리기 전에 «밋밋하다»가 수치로 무엇이었는지 먼저 쟀다.

### 측정 방법

`runCampaignStage()`, 고정 3인(샛별·미리내·윤슬), `aimSigma: 0.05`, 시드 4개, 12스테이지. 두 정책을 나란히 돌린다 — **`direct`는 `Space`를 한 번도 누르지 않는다**. 즉 「패링을 못 하거나 안 하는 손」이 그대로 그 정책이다. 용량은 0-4절과 같은 `bossHpOverride: 999999`, 클리어율은 실제 체력이다.

### 결과 — 패링이 보너스가 아니라 관문이었다

|                     | 되돌리기 전 | 되돌린 뒤 |         |
| ------------------- | ----------: | --------: | ------- |
| `direct` 용량 중앙  |     **124** |   **364** | ×2.9    |
| `direct` 클리어     |    **6.3%** | **72.9%** | +66.6%p |
| `contact` 용량 중앙 |         972 |       666 | ×0.69   |
| `contact` 클리어    |       93.8% |     91.7% | −2.1%p  |
| 패링 프리미엄       |   **7.8배** | **1.8배** |         |

되돌리기 전, 패링하지 않은 판의 피해 용량 중앙값은 124였다. 스테이지 체력은 1-1의 120에서 시작하므로 **패링을 안 하면 1-1 말고는 어떤 판도 넘길 수 없었다** — 12스테이지 중 1-3부터는 전부 0%다. 파티 셋이 판을 가로질러도 정산이 0이었기 때문인데, 그 판에서 실제로 일어난 일은 「유성이 별지기를 밀었다」뿐이고 그 밀림은 아무 값도 만들지 않았다.

되돌린 뒤 `direct`는 364/72.9%가 되어 판이 살아 있고, 스테이지가 올라가며 클리어율이 100%(1-1~2-4) → 25%(7-7)로 «내려간다» — 체력 곡선이 처음으로 물린다. 전에는 1-3부터 끝까지 평평한 0%였다.

`contact`의 용량이 972 → 666으로 내려간 것은 너프가 아니라 **이중 지급을 없앤 결과**다. 2954f79가 검기·저격·충격파를 정산에서 패링 접점으로 «옮겼는데», 정산 분기는 그대로 남아 있어 한 번의 패링이 같은 능력을 두 번 값을 치르고 있었다. 클리어율은 93.8% → 91.7%로 거의 그대로다 — 잘하는 손의 «결과»는 유지되고 천장만 내려왔다.

### 별자리와 각성이 서로를 지우던 문제

`beforePartySettle`에서 별자리가 발동하면 `handled`가 서고, 그 한 줄이 `settleParty`의 각성 분기를 통째로 껐다. 각성이 패링 전용이던 동안에는 「패링의 두 결과 중 하나만 준다」였지만, 각성이 움직임으로 돌아온 지금은 **접점 셋을 모은 샷이 그 대가로 파티의 정산 공격을 잃는** 구조가 된다. 잘한 플레이가 손해를 본다.

`handled`를 세우지 않고 순서로 풀었다. 실측(2-2, 브라우저 실기):

| 같은 샷           | 각성 | 별자리 | 총 피해 |
| ----------------- | ---- | ------ | ------: |
| 패링 없음         | 샛별 | 없음   |  **19** |
| 패링 1 + 안내별 4 | 샛별 | 오망성 | **108** |

두 번째 샷의 피니셔 지연은 1.88초로 잡혔다 — 별자리 현현(1.12초) + 0.24 + 기본 0.52. 현현이 끝난 뒤 정산이 이어지므로 겹쳐서 둘 다 안 읽히는 일이 없다.

### 표준 스윕(`node bot/run-bot.mjs`)도 같은 방향을 가리킨다

`bot/latest-report.json`의 전후 비교다. 위 표와 달리 이쪽은 기본 스윕 구성(무기믹 전장 96건 + 캠페인 35스테이지 × 2정책 × 4등급)이다.

| 지표                    |    전 |        후 |
| ----------------------- | ----: | --------: |
| 무기믹 `direct` 클리어  |  2.1% | **41.7%** |
| 무기믹 `contact` 클리어 |  100% |      100% |
| 무기믹 `chain` 클리어   |  100% |      100% |
| 무기믹 전체             | 67.4% |     80.6% |
| 캠페인 전체             |   89% | **82.8%** |

패링하는 두 정책은 100%를 유지하고 패링하지 않는 정책만 2.1% → 41.7%로 올라간다. 캠페인 89% → 82.8%는 이중 지급이 사라진 몫이며, PROGRESS_REPORT가 적어 둔 목표(60~70%)보다는 여전히 높다.

### 남은 것

- **8-1(1600 HP) 미측정.** 12스테이지 표본에 넣지 않았다. 표준 스윕에서는 2.1%로 나오는데, 0-4절의 2.43배 절벽이 이 변경으로 어떻게 움직였는지는 따로 재야 한다.
- 0-4절의 체력 재단 근거(용량 중앙 491 등)는 이 변경 이전 수치다. 체력 곡선을 다시 자를 때는 0-4가 아니라 이 절의 용량을 쓴다.

## 0-2. 7월드 캠페인 초벌 실행 (2026-08-13)

활성 캠페인은 양자리 3·화살자리 4·까마귀자리 4·카시오페이아 5·백조자리 5·오리온자리 6·북두칠성 7, 총 34스테이지다. HP는 1-1의 120에서 7-7의 380까지 오른다. 양자리 `1-2`·`1-3`은 관측 잔광 1회를 가지며, 첫 성공 패링에 안내별 둘을 더하고 별자리 정산 때만 소진한다. 나머지 활성 스테이지의 `bumpers`와 `gimmicks`는 비어 있다.

- `runCampaignStage()`로 고정 3인(샛별·미리내·윤슬), 접점 패링 정책, 시드 11을 한 번 통과시킨 결과는 **19/34 클리어(55.9%)**였다.
- 이 결과는 출시 난이도 판정이 아니다. 봇은 별지기를 순서대로 향하는 단순 정책이며, 각 스테이지의 시작 배치·보스 위치가 경로 품질에 미치는 영향을 드러내는 초벌 확인이다.
- 따라서 다음 작업은 체력만 재조정하는 것이 아니라, 월드마다 여러 파티·시드·조향 시점을 스윕해 “같은 HP에서 배치가 불공정하게 어려운 곳”과 “높은 HP여도 경로가 너무 쉬운 곳”을 분리하는 일이다.

## 1. 발사석 직상 1발로 캠페인이 뚫린다

### 결과

발사석에서 **위쪽으로 최대 파워 한 발**만 쏘면 캠페인 11개 스테이지(1-1 수업 제외) 중 **10개가 1샷 클리어**된다.

| 스테이지 | 1-2 | 1-3 | 1-4 | 1-5 | 1-6 | 1-7 | 2-1   | 2-2 | 2-3 | 2-4 | 2-5 |
| -------- | --- | --- | --- | --- | --- | --- | ----- | --- | --- | --- | --- |
| 1샷 처치 | ○   | ○   | ○   | ○   | ○   | ○   | **×** | ○   | ○   | ○   | ○   |

2-1만 버티며 260 중 108만 깎였다. 페이즈 밀어내기가 유성을 보스에게서 떼어내기 때문이다. 즉 **설계로 막은 것이 아니라 기믹 하나가 우연히 막고 있다.**

1-2에서 같은 입력을 5회 반복한 결과는 5회 모두 260/260으로 동일했다. 재현성 있는 구조적 현상이며 확률 문제가 아니다.

### 원인

세 값이 겹친다.

1. `startShot()`의 발사석은 `x = W/2 = 360`이고, **캠페인 12개 스테이지 전부 보스 `x`가 360**이다. 조준이 필요 없다.
2. 보스 `y`는 190~214, 충돌 반경 66, 천장은 `y = 13`이다. 보스 위쪽에 약 110px 복도가 생긴다.
3. `boss.hitCooldown`은 0.22초인데 그 복도 왕복 시간이 더 짧다. 유성이 천장과 보스 사이에 갇혀 쿨다운마다 계속 타격한다.

보스 체력만 올리면 왕복 횟수만 늘어난다. 발사석·보스 x 분산, 보스 위 공간 제거, 별지기를 거치지 않은 직격의 배율 상한 중에서 골라야 한다.

### 측정 조건

정리 전 HEAD, 파티 샛별·미리내·모루, 강화 미선택, 보스 체력 260.

## 2. 별자리 도형 인식기

`resolveFigure()`는 깨어난 별지기와 유성을 꼭짓점으로 삼아 점 구름을 만들고, `figureMatch()`로 템플릿과의 거리를 잰다. 거리가 `FIGURE.reject` 이하이면 발동한다.

### 2-1. 인식기의 구조적 제약

- **연결 순서를 보지 않는다.** 점 위치만 비교하므로 오각형과 오망성은 원리적으로 구분이 불가능하다. 코드 주석에도 이미 기록돼 있다.
- **회전을 정규화한다.** 그래서 45° 돌리면 겹치는 도형은 같은 도형이다. 실측한 **정사각형 ↔ 정십자 거리는 정확히 `0.000`**이었다. 둘을 함께 넣으면 정십자 정답률이 19%로 붕괴한다.
- **좌우 반전은 시도하지 않는다.** 뒤집힌 W는 W와 매칭되지 않는다.
- 결론: **정규 도형은 점 개수당 사실상 하나뿐이고, 두 번째 템플릿부터는 비대칭이어야 한다.**

### 2-2. 도형의 규칙성이 오발동률을 결정한다

균일 무작위로 점을 흩뿌렸을 때 발동하는 비율이다. 각 도형 **단독**, `gate 0.19`, N=4000.

| 도형              | 점  | 무작위 오발동 |
| ----------------- | --- | ------------- |
| 오망성 (정오각형) | 5   | **0.2%**      |
| 카시오페이아      | 5   | 4.6%          |
| 백조자리 (북십자) | 5   | 5.2%          |
| 남십자자리        | 4   | 13.1%         |
| 오리온 삼태성     | 3   | 18.0%         |
| 양자리            | 3   | **49.8%**     |

완전히 규칙적인 배치일수록 안전하다. 오망성은 우연히 나오지 않는다. 실제 별자리는 불규칙해서 무작위 산포와 잘 갈리지 않으며, 양자리 같은 완만한 삼각형은 절반이 오발동한다.

> 방법 검증: 오망성 단독 0.2%는 `game-combat.js`의 `FIGURE` 주석에 기록된 기존 실측치("random scatter 0.2%")와 일치한다.

### 2-3. 실제 플레이는 무작위와 구별되지 않는다

무한 훈련장(별지기 4 + 유성, 기믹 없음, 거상 중앙)에서 **무작위 조준 795샷**을 실제 물리로 굴렸다.

정산 시점 꼭짓점 개수 분포:

| 점   | 1   | 2   | 3   | 4   | 5   |
| ---- | --- | --- | --- | --- | --- |
| 비중 | 26% | 22% | 19% | 17% | 15% |

1점·2점은 도형이 성립하지 않으므로 **샷의 48%는 후보조차 되지 않는다.**

판정값별 발동률이다. 템플릿은 3점 정삼각형 1개, 4점 정사각형+정형화살 2개, 5점 오망성+카시오페이아+백조 3개다.

| 판정값 | 한 샷당 | 3점 (무작위) | 4점 (무작위) | 5점 (무작위) |
| ------ | ------- | ------------ | ------------ | ------------ |
| 0.19   | 4%      | 8% (8%)      | 8% (6%)      | 8% (10%)     |
| 0.24   | 10%     | 14% (14%)    | 17% (15%)    | 29% (33%)    |
| 0.28   | 17%     | 19% (19%)    | 30% (27%)    | 57% (61%)    |
| 0.32   | 26%     | 30% (26%)    | 46% (43%)    | 80% (80%)    |
| 0.36   | 33%     | 36% (34%)    | 69% (65%)    | 92% (92%)    |
| 0.42   | 40%     | 46% (48%)    | 94% (92%)    | 99% (99%)    |

**모든 판정값에서 실제 발동률이 무작위 오발동률과 같거나 오히려 낮다.** 물리로 굴러가 멈춘 배치는 균일 난수 산포와 통계적으로 구별되지 않는다. 벽과 모서리로 몰리기 때문에 균일 난수보다 도형에서 더 먼 경우도 있다.

따라서 **"잘 그린 것"과 "우연"을 가르는 판정값은 존재하지 않는다.** 조이면 둘 다 0이 되고 풀면 둘 다 발동한다.

400샷 기준의 별도 측정에서, 4점과 5점은 **전체 샷 중 가장 근접했던 배치조차** 각각 0.174 / 0.153으로 필요 기준 0.15를 넘거나 겨우 걸쳤다.

### 2-4. 플레이어가 조절할 수 있는 것은 꼭짓점 개수뿐이다

도형 모양은 노릴 수 없지만, **별지기를 몇 명 깨우는지는 조절 가능하다.** `gate 0.32` 기준:

| 깨운 결과 | 1~2점          | 3점 | 4점 | 5점 |
| --------- | -------------- | --- | --- | --- |
| 발동률    | 0% (도형 불가) | 30% | 46% | 80% |

즉 도형 자체는 추첨이지만 **"많이 깨울수록 추첨권이 늘어난다"**는 형태로 실력이 남는다.

### 2-5. 한계

- 조준이 균일 무작위인 봇 기준이다. 사람이 도형을 노렸을 때 얼마나 올라가는지는 **측정하지 않았다.**
- 다만 2-3의 결과는 "의도 없이 쳤을 때의 바닥값"이며, 우연으로는 사실상 나오지 않는다는 뜻이기도 하다.
- 프로젝트 오너의 판단으로 **현재는 조준으로 도형을 만드는 것이 사실상 불가능하다고 보고, 실제 결과를 우연과 동일하게 가정하고 튜닝하기로 했다.** 그 가정 아래에서는 위 표의 "실제"와 "무작위"가 같은 숫자이므로, 판정값은 오탐 방지가 아니라 **원하는 발동 빈도**를 기준으로 고르면 된다.

## 2-6. 샷이 길었던 원인은 유성의 감속 꼬리였다 (2026-08-13 추가)

별자리 연출을 붙인 뒤 "느리다"는 지적이 나와 원인을 나눠 쟀다.

- 실루엣 렌더 비용: 프레임당 **0.05ms vs 0.06ms**(있을 때 / 없을 때). 측정 불가 수준이며 원인이 아니다.
- 물리 정착 시간: **평균 7.18초, 최대 14.97초.** 그리고 그 전부가 유성이 멈추기까지다. 별지기는 마찰 0.974로 금방 선다.

유성 마찰이 프레임당 `0.9915`라 초속 1700에서 정지 임계값까지 400프레임이 넘게 걸렸다. 그 대부분은 **이미 아무것도 못 맞히는 속도로 미끄러지는 시간**이다.

### 균일하게 마찰을 올리면 게임이 망가진다

| 설정                                                     | 정착 평균 | 최대  | 3점+ 발동 | 5점    |
| -------------------------------------------------------- | --------- | ----- | --------- | ------ |
| 기존 `0.9915` 고정                                       | 7.18초    | 14.97 | 50%       | 12%    |
| `0.986` 고정 + 임계 150/110                              | 2.93      | 5.25  | **27%**   | **1%** |
| `0.990` 고정 + 임계 150/110                              | 4.43      | 11.03 | 44%       | 9%     |
| **속도 연동** `>500 → 0.9915, 이하 0.972` + 임계 150/110 | **2.97**  | 7.65  | **40%**   | **8%** |

시간만 보면 `0.986` 고정이 좋아 보이지만, 그 값은 **아직 일할 구간이 남은 유성까지 죽인다.** 5점 도형이 12%에서 1%로 사라져 카시오페이아·백조·오망성을 사실상 못 보게 된다.

속도 연동은 같은 정착 시간(2.97초)에 3점+ 발동 40%, 5점 8%를 지킨다. **빠른 구간은 원래 감속 그대로 두고 죽은 꼬리만 자르는 것**이 답이었다. 설정당 120샷 측정.

### 이 절 이전에 기록했던 발동률 수치는 오염돼 있었다

2-3절과 2-4절의 발동률은 측정 루프가 `figureFx`를 샷마다 초기화하지 않아, 앞 샷의 도형이 다음 샷 것으로 집계된 값이다. 정지 시간과 무작위 오발동률은 영향이 없지만 **발동률만 실제보다 높게 잡혔다.** 초기화한 뒤 같은 조건에서 다시 재면 기존 물리 기준 3점+ 발동은 50%다.

## 3. 측정 신뢰도에 대한 기록

- 초기 스윕에서 같은 조건의 수치가 실행마다 크게 흔들렸다(예: 3점 무작위 오발동 31.6% ↔ 55%). 원인은 하니스가 쓰던 선형 합동 난수(LCG)의 스트림 상관이었다. 구조적 표본을 뽑는 데 부적합했다.
- `Math.random`으로 교체하고 서로 다른 시드 3개로 재현을 확인한 뒤의 값만 이 문서에 남겼다(3점 55/54/57%, 4점 23/24/23%, 5점 10/10/10%).
- 1절의 1샷 클리어는 난수와 무관한 결정론적 입력이며 5회 반복 동일 결과다.
