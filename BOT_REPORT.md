# BOT REPORT — 헤드리스 하니스 측정 기록

> 기준일: 2026-08-13 (계기 교체 2026-08-15, 0절 참조)
>
> 브라우저에 로드된 실제 런타임 함수(`update` · `updateSpecial` · `updateFeedback` · `figureMatch`)를 고정 dt로 직접 구동해 얻은 수치다. 물리를 따로 재구현한 `bot/prism-sim.mjs`와는 다른 경로이며, 그쪽 수치와 섞지 않는다.
>
> 측정 방법과 한계는 각 절의 **측정 조건**과 **한계**를 반드시 함께 읽는다.
>
> **어느 절을 읽어야 하는가**: 난이도를 알고 싶으면 [0-3절](#0-3-계기-교체--무기믹-기준선에서-실제-캠페인으로-2026-08-15)만 본다. 0-1 ~ 0-1-c는 무기믹 기준선의 이력이며, 그 스윕은 지금 포화돼 난이도 신호가 아니다.

## 0. 하니스 구성

- 헤드리스 창에서는 `document.visibilityState`가 `hidden`이라 `requestAnimationFrame`이 스로틀된다. 프레임 루프에 의존하지 않고 `update(1/60)` → `updateSpecial(1/60)` → `updateFeedback(1/60)`를 동기 루프로 직접 호출했다.
- 이 방식은 **판정 결과**(클리어 여부, 피해량, 도형 인식)는 실런타임과 동일하지만, **체감 속도·연출 타이밍·프레임 안정성은 측정하지 않는다.** 그 항목은 실기 플레이로만 확인 가능하다.
- 정산 슬로모션(`0.46 → 0.62 → 0.82`) 시간 배율은 적용하지 않았다. 판정 결과에는 영향이 없고 경과 시간에만 영향을 준다.

## 0-1. 현재 런타임 기믹 없는 기준선 (2026-08-13)

`bot/runtime-harness.mjs`는 별도 물리 근사가 아니라 현재 ordered classic scripts를 VM에서 순서대로 읽고, 1/60초 고정 dt로 전투 갱신 함수를 호출한다. 이번 기준선은 전용 빈 전장, 강화 없음, 5발, 보스 체력 180·260·340·420, 고정 파티 2명(샛별·미리내) / 3명(+윤슬) / 4명(+별하), 시드 4개, 보스 직행(`direct`)과 접점 패링 우선(`contact`)의 96건이다. 임의 시점의 좌·우 궤도 전환은 기본 점수에 넣지 않았다.

- 별지기 미경유 보스 직격은 0.6배, 그 샷의 첫 충돌이 보스인 직격은 추가 0.5배(기본 합산 0.3배)로 낮췄다. 패링·각성·별자리·유닛 능력에는 적용하지 않는다.
- 변경 뒤 전체 클리어율은 34.4%, 평균 잔여 체력은 107.8, 평균 패링은 1.9회였다. 원본은 `bot/latest-report.json`에 남긴다.
- `direct`는 260에서 모든 고정 파티가 실패하고, `contact`는 260에서 모든 고정 파티가 4/4 클리어한다. 기믹 없는 260은 이제 “별지기 경유·패링을 해야 안정적으로 이기는 초반 기준선” 역할을 한다.
- 2인 고정 파티는 `contact`에서 420까지 4/4 클리어하지만, 3인은 340에서 33 체력을 남기고 0/4, 4인은 340에서 38 체력을 남기고 0/4였다. 인원을 늘린 결과가 단조롭게 강해지지 않았다.
- 이는 인원 수 자체의 강약이 아니라 추가 유닛의 능력·시작 슬롯·충돌 경로가 함께 바뀐 결과다. 이 96건만으로 “2인/3인/4인이 더 세다” 또는 순차 해금 단계를 확정해서는 안 된다.
- 다음 스윕은 같은 파티 크기의 조합·슬롯·발사 경로를 넓혀 중앙값과 하위 분위수를 비교한다. 그 뒤에 2→3→4 해금과 무기믹 보스 체력대를 정한다.

좌·우 궤도 전환도 하니스의 `probeSteerDirection()`으로 별도 확인했다. 오른쪽으로 진행하는 유성은 좌클릭 후 위쪽(진행 좌측), 우클릭 후 아래쪽(진행 우측) 속도를 얻으며, 같은 샷에서 두 번째 전환은 거부된다. 이는 입력 방향·1회 제한의 코드 판정 검증이고, 손맛과 최적 전환 시점은 브라우저 실기 범위다.

## 0-1-a. 재측정 (2026-08-14) — 이 스윕은 더 이상 밸런스 신호가 아니다

0-1의 96건을 같은 조건으로 다시 돌렸다. **위 0-1의 수치는 그날의 기록으로 두고, 아래가 현재 값이다.**

| 지표         | 2026-08-13 | 2026-08-14 |
| ------------ | ---------: | ---------: |
| 클리어율     |      34.4% |  **17.7%** |
| 평균 잔여 HP |      107.8 |      134.8 |
| 평균 패링    |        1.9 |        2.0 |

보스 체력대별로 보면 무너진 자리가 분명하다.

| 보스 HP | 2026-08-13 | 2026-08-14 |
| ------- | ---------: | ---------: |
| 180     |      13/24 |      13/24 |
| 260     |      12/24 |   **4/24** |
| 340     |       4/24 |   **0/24** |
| 420     |       4/24 |   **0/24** |

**저체력대는 그대로고 고체력대만 0이 됐다.** 패링 횟수는 1.9→2.0으로 사실상 같으므로, 파장 자체가 줄어든 것이 아니라 **한 판에 넣을 수 있는 총 피해가 줄었다.**

### 원인 — 봇이 별자리를 아예 못 쓴다

96건 전체를 집계하면 이렇다.

- **별자리 발동: 0회** (96건 합계)
- **한 샷 최대 노드: 2개** — 발동 최소치는 `FIGURE_PARRY.minNodes = 3`
- 안내별 보유 케이스: 0건 (빈 전장 시나리오에는 관측 잔광이 없다)

즉 이 스윕이 재는 것은 **당구 접촉 피해와 유닛 지원뿐**이고, 이 게임의 간판 기믹인 별자리는 한 번도 타지 않는다. 그 사이 게임이 별자리 쪽으로 피해 예산을 옮겼다면, 고체력대에서만 격차가 누적돼 무너지는 지금 모양이 정확히 나온다.

**따라서 17.7%를 “게임이 어려워졌다”로 읽으면 안 된다.** 이 수치는 “별자리를 못 쓰는 플레이어의 상한”이다.

### 격리 검증

같은 날 승리 판정을 벽시계 `setTimeout`에서 프레임 시계로 옮겼다(`scheduleWin` → `afterFeedbackUpdate`). 그 변경 **직전 커밋의 `game-session.js`로 되돌려 다시 돌린 결과도 17.7%로 동일**했다. 이번 하락은 그 변경과 무관하며, `d542727` 이후 누적된 변경의 결과다.

### 대응 — `chain` 정책 추가 (같은 날)

봇이 접점 3개를 잇도록 조준 정책 `chain`을 넣었다. 물리를 다시 풀지 않고, 발사 각도 96개를 훑어 **직선 경로가 별지기를 가장 많이 스치는 방향**을 고른다. 벽 반사 뒤 경로는 세지 않으므로 상한이 아니라 하한이다.

스윕을 3정책 144건으로 넓힌 결과다.

| 정책      | 케이스 | 클리어 |  클리어율 | 별자리 발동 |
| --------- | -----: | -----: | --------: | ----------: |
| `direct`  |     48 |      1 |      2.1% |           0 |
| `contact` |     48 |     16 |     33.3% |           0 |
| `chain`   |     48 |     44 | **91.7%** |      **44** |

`direct` 1건 + `contact` 16건 = 17건으로, 위 96건 재측정치(17.7%)와 정확히 일치한다. 즉 정책 두 개는 그대로고 **`chain`만 새로 열린 경로**다.

**별자리를 쓰느냐 마느냐가 클리어율을 33%에서 92%로 바꾼다.** 고체력대 붕괴의 정체가 이것이었다 — 게임이 어려워진 게 아니라, 측정이 게임의 절반만 보고 있었다.

세부에서 눈에 띄는 것:

- `chain` 4인 / 420 HP는 별자리를 8회나 발동하고 평균 패링 9.0인데도 0/4다. 인원이 늘어도 단조롭게 강해지지 않는다는 0-1의 관찰이 별자리를 쓰는 조건에서도 유지된다.
- `chain` 2인 / 420은 4/4다. 2인 파티가 고체력대에서 오히려 안정적인 이유는 아직 설명되지 않았다.

### 인용 규칙

`bot/latest-report.json`의 `summary.clearRate`는 세 정책을 뭉친 값(현재 47.9%)이라 **그대로 인용하면 안 된다.** 별자리를 쓰는 판과 못 쓰는 판이 섞여 있다. 리포트의 `summary.byPolicy`를 정책별로 인용한다.

### 남은 것

`d542727`과 지금 사이 어느 변경이 `contact`의 고체력대를 무너뜨렸는지는 아직 이분 탐색하지 않았다. 다만 `chain`이 같은 체력대를 클리어하므로, 그 변경은 **총 피해량을 줄인 게 아니라 별자리 경로로 옮긴 것**일 가능성이 높다.

## 0-1-b. 하니스가 매 판 마지막 발을 버리고 있었다 (2026-08-14 재측정)

위 표를 포함해 **이 문서의 모든 스윕 수치는 5발이 아니라 4발로 측정된 것**이었다.

`__botRun`의 루프 조건이 `shot < config.shots`였다. 마지막 발을 쏘면 그 안에서 `shot`이 5가 되고, 유성이 날아가기도 전에 다음 회차 조건이 거짓이 되어 루프가 끝난다. 5번째 발은 발사 직후 한 프레임만 돌고 버려졌다 — 피해도 별자리도 정산되지 않고, `shotDuration`에도 기록되지 않는다. 35스테이지를 기믹째로 훑을 때 `shotsUsed: 5`인데 기록된 체공 시간이 4개뿐인 런이 140건 중 64건 나와서 드러났다.

발사 조건을 루프 본문으로 옮겨 **마지막 유성이 멈춘 뒤에** 끝나게 고쳤다. 같은 스윕 144건을 다시 돌린 결과다.

| 정책      | 케이스 | 4발(종전) | 5발(수정) |  별자리 |
| --------- | -----: | --------: | --------: | ------: |
| `direct`  |     48 |      2.1% |      2.1% |       0 |
| `contact` |     48 |     33.3% |     41.7% |       0 |
| `chain`   |     48 |     91.7% |  **100%** | 44 → 44 |

평균 잔여 체력 90.6 → 77.8, 평균 패링 2.8 → 3.0, 뭉친 클리어율 42.4% → 47.9%다.

읽을 때 주의할 것 둘.

- **`chain`이 48/48이 되었다.** 별자리를 쓰는 경로는 이 기준선에서 더 이상 실패하지 않으므로, 이 스윕은 `chain`의 난이도를 구분하지 못한다. 고체력대를 다시 신호로 쓰려면 체력대를 올리거나 기믹을 넣어야 한다.
- 0-1·0-1-a의 서술(`chain` 4인/420이 0/4 등)은 4발 기준이라 **지금 리포트와 대조하면 어긋난다.** 그 절들은 당시 측정 기록으로 남겨 두었고, 현재 수치는 이 절과 `bot/latest-report.json`이 기준이다.

## 0-1-c. 지원 사격 피해가 한 번도 들어간 적이 없었다 (2026-08-15 재측정)

위 수치들도 절반의 게임으로 잰 것이었다. `queueUnitAssist`가 큐에 넣는 지원 사격(검기·저격·각성 피니셔)은 `delay`가 배정된 뒤 `resolveAssist`에서 피해가 정산되는데, **`delay`를 배정하는 `afterUnitAssistQueued` 훅이 `detonateShockwave` 한 곳에서만 발화되고 있었다.** 나머지 경로의 어시스트는 delay가 `undefined`인 채 큐에 들어가 정리 루프의 두 분기(`> 0`도 `<= 0`도) 모두에서 거짓이 되어 — 영원히 제거되지 않고, 피해도 팝업도 임팩트도 없이, 빔 장식만 보스에 박제됐다. 각성 피니셔가 불멸이 되면 `finisherFocus`가 풀리지 않아 **첫 각성 정산 뒤 전투 전체가 0.82배속으로 굳는** 부작용도 함께였다.

훅 발화를 `queueUnitAssist` 안으로 옮기고 delay를 숫자로 초기화한 뒤 같은 144건을 다시 돌린 결과다.

| 정책      | 어시스트 미적용(종전) | 어시스트 정상(수정) |
| --------- | --------------------: | ------------------: |
| `direct`  |                  2.1% |                2.1% |
| `contact` |                 41.7% |           **91.7%** |
| `chain`   |                  100% |                100% |

`contact`가 두 배가 됐다 — **패링 뒤의 지원 사격이 이 게임 피해 예산의 절반**이었다는 뜻이다. `chain`의 별자리 발동은 44→4로 급감했는데, 회귀가 아니라 평균 1.08발(종전 2.31발)에 전투가 끝나 별자리 정산까지 갈 일이 없어진 결과다(노드 3개 도달 자체는 48건 중 32건 유지).

**이 기준선은 이제 포화됐다.** 무기믹 180~420에서 `contact`·`chain` 모두 사실상 만점이라 난이도 신호가 없다. 다음 스윕 전에 보스 체력대 상향 또는 기믹 도입이 선행돼야 한다 — 이는 밸런스 판단이라 측정만 남긴다.

## 0-3. 계기 교체 — 무기믹 기준선에서 실제 캠페인으로 (2026-08-15)

0-1-c에서 지원 사격이 살아나며 무기믹 기준선이 포화됐다(`contact` 91.7% / `chain` 100%). 그런데 같은 날 **실제 35스테이지를 그대로 돌려 보니 신호가 살아 있었다** — 포화된 것은 게임이 아니라 계기였다. 기준선은 전용 빈 전장에 체력 네 단만 쓰는 인공 시나리오이고, 실제 스테이지는 저마다 다른 체력·배치·기믹을 가진다.

그래서 난이도 계기를 캠페인 스윕으로 옮겼다. `bot/run-bot.mjs`는 이제 둘을 함께 낸다.

| 스윕       | 역할                  | 상태                                                                |
| ---------- | --------------------- | ------------------------------------------------------------------- |
| `plain`    | 전투 규칙 회귀 기준선 | **포화 — 난이도 신호 아님** (`summary.plain.saturated`가 자동 판정) |
| `campaign` | **난이도 계기**       | 35스테이지 실측                                                     |

### 시드 대신 조준 오차를 축으로 쓴다

기존 스윕의 시드 축은 사실상 죽어 있었다. 런타임에서 `Math.random()`이 닿는 곳은 크리티컬 확률 하나뿐이라, 35스테이지 중 33개가 4개 시드에서 **완전히 같은 결과**였다.

그 자리를 **조준 오차**로 바꿨다. 발사 각도에 매 발 시드된 난수로 라디안 오차를 더한다 — 서투른 플레이어를 모사하는 축이고, 시드마다 다른 손떨림이 되므로 시드가 비로소 표본이 된다.

| 등급      | σ (rad) | 뜻        |
| --------- | ------: | --------- |
| `precise` |       0 | 오차 없음 |
| `steady`  |    0.04 | 숙련      |
| `loose`   |    0.10 | 보통      |
| `wild`    |    0.22 | 서투름    |

### 결과 (35스테이지 × 2정책 × 4등급 × 6시드 = 1,680건)

| 정책      | precise | steady | loose |      wild |
| --------- | ------: | -----: | ----: | --------: |
| `contact` |   82.9% |  68.6% |   80% |     77.6% |
| `chain`   |   85.7% |  79.5% | 73.3% | **61.9%** |

**`chain`이 단조 감소한다 — 계기가 살아 있다는 뜻이다.** 전체 클리어율은 76.2%.

### 읽을 때의 한계 둘 (반드시 함께 읽는다)

- **`precise`는 표본이 아니다.** σ=0이면 6개 시드가 전부 같은 한 줄을 재생한다. 「통하는 경로가 하나라도 있는가」를 묻는 결정론적 프로브 1회지, 6회 시행의 비율이 아니다. 아래 스테이지 표에서 `precise` 0%가 여럿 보이는 것은 그 스테이지가 불가능해서가 아니라 **봇이 고른 그 한 줄이 실패**해서다.
- **`contact`는 단조가 아니다**(82.9 → 68.6 → 80 → 77.6). 오차가 크면 첫 별지기를 지나쳐 더 멀리 굴러 더 많은 접점을 스치는 일이 생기기 때문이다. 난이도 곡선은 `chain`으로 읽는다.

### 가장 어려운 여덟 스테이지

`chain` 곡선은 precise / steady / loose / wild 순이다.

| 스테이지 | 보스 HP |   전체 | `chain` 곡선             |
| -------- | ------: | -----: | ------------------------ |
| `8-1`    |    1600 | **0%** | 0 / 0 / 0 / 0            |
| `7-1`    |     350 |  47.9% | 0 / 83.3 / 50 / 50       |
| `5-5`    |     320 |    50% | 0 / 83.3 / 50 / 66.7     |
| `4-3`    |     275 |  54.2% | 0 / 83.3 / 50 / 66.7     |
| `2-4`    |     220 |  60.4% | 0 / 100 / 66.7 / 66.7    |
| `7-6`    |     375 |  62.5% | 100 / 66.7 / 66.7 / 33.3 |
| `7-2`    |     355 |  64.6% | 100 / 66.7 / 66.7 / 66.7 |
| `6-5`    |     345 |  66.7% | 100 / 83.3 / 83.3 / 33.3 |

전 등급 전승은 `1-3`·`2-1` 둘뿐이다. **8-1은 어느 등급에서도 0%** — 1600 HP는 이 봇의 사거리 밖이며, 사람이 별자리를 반복 발동시키는 플레이를 봇이 아직 재현하지 못한다는 뜻이기도 하다. 봇 실패를 곧바로 「사람도 못 깬다」로 읽지 않는다.

### 남은 것

- `contact`의 비단조를 설명하려면 접점 수와 이동 거리를 케이스별로 남겨야 한다. 지금 리포트에는 `shotsUsed`만 있다.
- 봇이 별자리를 의도적으로 반복 발동시키는 정책이 없다. 8-1처럼 체력이 큰 판은 그 정책 없이는 영원히 0%다.

## 0-2. 7월드 캠페인 초벌 실행 (2026-08-13)

활성 캠페인은 양자리 3·화살자리 4·까마귀자리 4·카시오페이아 5·백조자리 5·오리온자리 6·북두칠성 7, 총 34스테이지다. HP는 1-1의 120에서 7-7의 380까지 오른다. 양자리 `1-2`·`1-3`은 관측 잔광 1회를 가지며, 첫 성공 패링에 안내별 둘을 더하고 별자리 정산 때만 소진한다. 나머지 활성 스테이지의 `bumpers`와 `gimmicks`는 비어 있다.

- `runCampaignStage()`로 고정 3인(샛별·미리내·윤슬), 접점 패링 정책, 시드 11을 한 번 통과시킨 결과는 **19/34 클리어(55.9%)**였다.
- 이 결과는 출시 난이도 판정이 아니다. 봇은 별지기를 순서대로 향하는 단순 정책이며, 각 스테이지의 시작 배치·보스 위치가 경로 품질에 미치는 영향을 드러내는 초벌 확인이다.
- 따라서 다음 작업은 체력만 재조정하는 것이 아니라, 월드마다 여러 파티·시드·조향 시점을 스윕해 “같은 HP에서 배치가 불공정하게 어려운 곳”과 “높은 HP여도 경로가 너무 쉬운 곳”을 분리하는 일이다.

## 1. 발사석 직상 1발로 캠페인이 뚫린다

### 결과

발사석에서 **위쪽으로 최대 파워 한 발**만 쏘면 캠페인 11개 스테이지(1-1 수업 제외) 중 **10개가 1샷 클리어**된다.

| 스테이지 | 1-2 | 1-3 | 1-4 | 1-5 | 1-6 | 1-7 | 2-1   | 2-2 | 2-3 | 2-4 | 2-5 |
| -------- | --- | --- | --- | --- | --- | --- | ----- | --- | --- | --- | --- |
| 1샷 처치 | ○   | ○   | ○   | ○   | ○   | ○   | **×** | ○   | ○   | ○   | ○   |

2-1만 버티며 260 중 108만 깎였다. 페이즈 밀어내기가 유성을 보스에게서 떼어내기 때문이다. 즉 **설계로 막은 것이 아니라 기믹 하나가 우연히 막고 있다.**

1-2에서 같은 입력을 5회 반복한 결과는 5회 모두 260/260으로 동일했다. 재현성 있는 구조적 현상이며 확률 문제가 아니다.

### 원인

세 값이 겹친다.

1. `startShot()`의 발사석은 `x = W/2 = 360`이고, **캠페인 12개 스테이지 전부 보스 `x`가 360**이다. 조준이 필요 없다.
2. 보스 `y`는 190~214, 충돌 반경 66, 천장은 `y = 13`이다. 보스 위쪽에 약 110px 복도가 생긴다.
3. `boss.hitCooldown`은 0.22초인데 그 복도 왕복 시간이 더 짧다. 유성이 천장과 보스 사이에 갇혀 쿨다운마다 계속 타격한다.

보스 체력만 올리면 왕복 횟수만 늘어난다. 발사석·보스 x 분산, 보스 위 공간 제거, 별지기를 거치지 않은 직격의 배율 상한 중에서 골라야 한다.

### 측정 조건

정리 전 HEAD, 파티 샛별·미리내·모루, 강화 미선택, 보스 체력 260.

## 2. 별자리 도형 인식기

`resolveFigure()`는 깨어난 별지기와 유성을 꼭짓점으로 삼아 점 구름을 만들고, `figureMatch()`로 템플릿과의 거리를 잰다. 거리가 `FIGURE.reject` 이하이면 발동한다.

### 2-1. 인식기의 구조적 제약

- **연결 순서를 보지 않는다.** 점 위치만 비교하므로 오각형과 오망성은 원리적으로 구분이 불가능하다. 코드 주석에도 이미 기록돼 있다.
- **회전을 정규화한다.** 그래서 45° 돌리면 겹치는 도형은 같은 도형이다. 실측한 **정사각형 ↔ 정십자 거리는 정확히 `0.000`**이었다. 둘을 함께 넣으면 정십자 정답률이 19%로 붕괴한다.
- **좌우 반전은 시도하지 않는다.** 뒤집힌 W는 W와 매칭되지 않는다.
- 결론: **정규 도형은 점 개수당 사실상 하나뿐이고, 두 번째 템플릿부터는 비대칭이어야 한다.**

### 2-2. 도형의 규칙성이 오발동률을 결정한다

균일 무작위로 점을 흩뿌렸을 때 발동하는 비율이다. 각 도형 **단독**, `gate 0.19`, N=4000.

| 도형              | 점  | 무작위 오발동 |
| ----------------- | --- | ------------- |
| 오망성 (정오각형) | 5   | **0.2%**      |
| 카시오페이아      | 5   | 4.6%          |
| 백조자리 (북십자) | 5   | 5.2%          |
| 남십자자리        | 4   | 13.1%         |
| 오리온 삼태성     | 3   | 18.0%         |
| 양자리            | 3   | **49.8%**     |

완전히 규칙적인 배치일수록 안전하다. 오망성은 우연히 나오지 않는다. 실제 별자리는 불규칙해서 무작위 산포와 잘 갈리지 않으며, 양자리 같은 완만한 삼각형은 절반이 오발동한다.

> 방법 검증: 오망성 단독 0.2%는 `game-combat.js`의 `FIGURE` 주석에 기록된 기존 실측치("random scatter 0.2%")와 일치한다.

### 2-3. 실제 플레이는 무작위와 구별되지 않는다

무한 훈련장(별지기 4 + 유성, 기믹 없음, 거상 중앙)에서 **무작위 조준 795샷**을 실제 물리로 굴렸다.

정산 시점 꼭짓점 개수 분포:

| 점   | 1   | 2   | 3   | 4   | 5   |
| ---- | --- | --- | --- | --- | --- |
| 비중 | 26% | 22% | 19% | 17% | 15% |

1점·2점은 도형이 성립하지 않으므로 **샷의 48%는 후보조차 되지 않는다.**

판정값별 발동률이다. 템플릿은 3점 정삼각형 1개, 4점 정사각형+정형화살 2개, 5점 오망성+카시오페이아+백조 3개다.

| 판정값 | 한 샷당 | 3점 (무작위) | 4점 (무작위) | 5점 (무작위) |
| ------ | ------- | ------------ | ------------ | ------------ |
| 0.19   | 4%      | 8% (8%)      | 8% (6%)      | 8% (10%)     |
| 0.24   | 10%     | 14% (14%)    | 17% (15%)    | 29% (33%)    |
| 0.28   | 17%     | 19% (19%)    | 30% (27%)    | 57% (61%)    |
| 0.32   | 26%     | 30% (26%)    | 46% (43%)    | 80% (80%)    |
| 0.36   | 33%     | 36% (34%)    | 69% (65%)    | 92% (92%)    |
| 0.42   | 40%     | 46% (48%)    | 94% (92%)    | 99% (99%)    |

**모든 판정값에서 실제 발동률이 무작위 오발동률과 같거나 오히려 낮다.** 물리로 굴러가 멈춘 배치는 균일 난수 산포와 통계적으로 구별되지 않는다. 벽과 모서리로 몰리기 때문에 균일 난수보다 도형에서 더 먼 경우도 있다.

따라서 **"잘 그린 것"과 "우연"을 가르는 판정값은 존재하지 않는다.** 조이면 둘 다 0이 되고 풀면 둘 다 발동한다.

400샷 기준의 별도 측정에서, 4점과 5점은 **전체 샷 중 가장 근접했던 배치조차** 각각 0.174 / 0.153으로 필요 기준 0.15를 넘거나 겨우 걸쳤다.

### 2-4. 플레이어가 조절할 수 있는 것은 꼭짓점 개수뿐이다

도형 모양은 노릴 수 없지만, **별지기를 몇 명 깨우는지는 조절 가능하다.** `gate 0.32` 기준:

| 깨운 결과 | 1~2점          | 3점 | 4점 | 5점 |
| --------- | -------------- | --- | --- | --- |
| 발동률    | 0% (도형 불가) | 30% | 46% | 80% |

즉 도형 자체는 추첨이지만 **"많이 깨울수록 추첨권이 늘어난다"**는 형태로 실력이 남는다.

### 2-5. 한계

- 조준이 균일 무작위인 봇 기준이다. 사람이 도형을 노렸을 때 얼마나 올라가는지는 **측정하지 않았다.**
- 다만 2-3의 결과는 "의도 없이 쳤을 때의 바닥값"이며, 우연으로는 사실상 나오지 않는다는 뜻이기도 하다.
- 프로젝트 오너의 판단으로 **현재는 조준으로 도형을 만드는 것이 사실상 불가능하다고 보고, 실제 결과를 우연과 동일하게 가정하고 튜닝하기로 했다.** 그 가정 아래에서는 위 표의 "실제"와 "무작위"가 같은 숫자이므로, 판정값은 오탐 방지가 아니라 **원하는 발동 빈도**를 기준으로 고르면 된다.

## 2-6. 샷이 길었던 원인은 유성의 감속 꼬리였다 (2026-08-13 추가)

별자리 연출을 붙인 뒤 "느리다"는 지적이 나와 원인을 나눠 쟀다.

- 실루엣 렌더 비용: 프레임당 **0.05ms vs 0.06ms**(있을 때 / 없을 때). 측정 불가 수준이며 원인이 아니다.
- 물리 정착 시간: **평균 7.18초, 최대 14.97초.** 그리고 그 전부가 유성이 멈추기까지다. 별지기는 마찰 0.974로 금방 선다.

유성 마찰이 프레임당 `0.9915`라 초속 1700에서 정지 임계값까지 400프레임이 넘게 걸렸다. 그 대부분은 **이미 아무것도 못 맞히는 속도로 미끄러지는 시간**이다.

### 균일하게 마찰을 올리면 게임이 망가진다

| 설정                                                     | 정착 평균 | 최대  | 3점+ 발동 | 5점    |
| -------------------------------------------------------- | --------- | ----- | --------- | ------ |
| 기존 `0.9915` 고정                                       | 7.18초    | 14.97 | 50%       | 12%    |
| `0.986` 고정 + 임계 150/110                              | 2.93      | 5.25  | **27%**   | **1%** |
| `0.990` 고정 + 임계 150/110                              | 4.43      | 11.03 | 44%       | 9%     |
| **속도 연동** `>500 → 0.9915, 이하 0.972` + 임계 150/110 | **2.97**  | 7.65  | **40%**   | **8%** |

시간만 보면 `0.986` 고정이 좋아 보이지만, 그 값은 **아직 일할 구간이 남은 유성까지 죽인다.** 5점 도형이 12%에서 1%로 사라져 카시오페이아·백조·오망성을 사실상 못 보게 된다.

속도 연동은 같은 정착 시간(2.97초)에 3점+ 발동 40%, 5점 8%를 지킨다. **빠른 구간은 원래 감속 그대로 두고 죽은 꼬리만 자르는 것**이 답이었다. 설정당 120샷 측정.

### 이 절 이전에 기록했던 발동률 수치는 오염돼 있었다

2-3절과 2-4절의 발동률은 측정 루프가 `figureFx`를 샷마다 초기화하지 않아, 앞 샷의 도형이 다음 샷 것으로 집계된 값이다. 정지 시간과 무작위 오발동률은 영향이 없지만 **발동률만 실제보다 높게 잡혔다.** 초기화한 뒤 같은 조건에서 다시 재면 기존 물리 기준 3점+ 발동은 50%다.

## 3. 측정 신뢰도에 대한 기록

- 초기 스윕에서 같은 조건의 수치가 실행마다 크게 흔들렸다(예: 3점 무작위 오발동 31.6% ↔ 55%). 원인은 하니스가 쓰던 선형 합동 난수(LCG)의 스트림 상관이었다. 구조적 표본을 뽑는 데 부적합했다.
- `Math.random`으로 교체하고 서로 다른 시드 3개로 재현을 확인한 뒤의 값만 이 문서에 남겼다(3점 55/54/57%, 4점 23/24/23%, 5점 10/10/10%).
- 1절의 1샷 클리어는 난수와 무관한 결정론적 입력이며 5회 반복 동일 결과다.
