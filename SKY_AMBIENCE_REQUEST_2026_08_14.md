# 여백 하늘 · 이벤트 연출 확장 — 디자인 세션 요청서

> 작성일: 2026-08-14
> 대상: 관측창 양옆 여백(`#dawn-sky`)의 소품·별·연출, 그리고 게임 이벤트에 반응하는 배경 연출
> 방법: 데스크톱 1280×720 / 1440×820 실기, 여백 면적·소품 경계 상자 실측, 런타임 훅 정적 집계
> 상태: **요청서다. 여기서 새 래스터·SFX를 만들지 않는다.** 반입은 [ASSET_BACKLOG.md](ASSET_BACKLOG.md)의 승인 절차를 따른다.

## 0. 이 문서의 위치

세 번째 요청서다. [UI_REVIEW_2026_08_14.md](UI_REVIEW_2026_08_14.md)는 화면 구성, [META_UI_REQUEST_2026_08_14.md](META_UI_REQUEST_2026_08_14.md)는 허브와 별자리 지도를 다뤘다. 이 문서는 **그 화면들을 둘러싼 여백**과 **게임에서 벌어지는 일이 화면에 남기는 흔적**을 다룬다.

같은 규칙이다 — **측정값이나 화면 근거가 없는 항목은 넣지 않는다.** 등급도 같다(A / B / C).

[OUTER_OBSERVER_INTRO_SPEC.md](OUTER_OBSERVER_INTRO_SPEC.md) 6절이 이미 배경 반응 3종을 규정했고 그중 **2종이 미구현**이다. 이 요청은 그 두 칸을 채우는 것에서 출발한다.

### 먼저 — 여백의 값이 방금 두 배가 됐다

같은 날 화면 폭을 통일했다. 그전에는 프로필·소환·상점·편성·업적·도서관이 뷰포트 전체를 덮어 **여백이 6개 화면에서 아예 보이지 않았다.** 지금은 모든 화면이 관측소 컬럼 안에 들어간다.

|            |        여백이 보이는 화면 | 가려지는 화면 |
| ---------- | ------------------------: | ------------: |
| 폭 통일 전 | 4 (타이틀·허브·지도·설정) |             6 |
| **지금**   |             **10 (전부)** |         **0** |

여백에 무엇을 넣든 이제 **세션 내내** 보인다. 이 문서의 요청이 지금 의미가 커진 이유이고, 동시에 **지금 비어 있다는 사실도 세션 내내 보인다**는 뜻이다.

---

## 1. 여백의 현재 상태

### 1-1. [A] 여백의 90%가 비어 있다

1280×720 실측이다.

| 항목             | 값          |
| ---------------- | ----------- |
| 관측소 컬럼      | 506px       |
| 좌우 여백        | **387px씩** |
| 여백 총면적      | 557,280px²  |
| 여백을 채운 비율 | **9.7%**    |
| **빈 비율**      | **90.3%**   |

여백에 있는 것 전부다. 소품 7개와 2×2px 별점 36개가 전부다.

| 좌측 (387px)                                             | 우측 (387px)                                    |
| -------------------------------------------------------- | ----------------------------------------------- |
| 달 150×150, 달토끼 68×51, 우주비행사 72×72, 나침반 48×39 | 고리 행성 160×87, 붉은 행성 72×72, 망원경 84×63 |
| 별점 15개                                                | 별점 21개                                       |

화면 폭의 **60%가 여백**인데(387×2 = 774px vs 컬럼 506px) 그 안에 든 것은 소품 7개다.

**요청**: 여백을 채우는 계층 설계. 판단이 필요한 것은 "무엇을 더 넣을까"보다 **"몇 겹으로 넣을까"**다. 지금은 소품이 전부 같은 평면에 있어 원근이 없다. 예컨대 먼 성운 / 중간 천체 / 가까운 소품 3계층으로 나누면 같은 개수로도 깊이가 생긴다.

### 1-2. [B] 움직이는 것이 사실상 유성 하나뿐이고, 그마저 20%의 시간만 보인다

여백에서 스스로 움직이는 것은 셋이다.

| 요소       | 주기                                      | 화면에 보이는 시간 |
| ---------- | ----------------------------------------- | ------------------ |
| 유성       | 9초마다 1회 (`setInterval(meteor, 9000)`) | 1.8초 → **20%**    |
| 우주비행사 | 상시 부유 (`dawnFloat 6s`)                | 100% (미세)        |
| 나침반     | 상시 부유 (`dawnFloat 8s`)                | 100% (미세)        |

나머지 소품 — 달, 달토끼, 두 행성, 망원경 — 은 **완전 정지**다. 즉 여백의 80% 시간 동안 화면에서 변하는 것은 두 소품의 미세한 상하 부유뿐이다.

**요청**: 자율 연출의 주기 설계. 유성 하나에 9초를 걸지 말고, **서로 다른 주기의 이벤트를 겹쳐** 화면이 죽는 구간을 없애는 안. 짧은 것(별 반짝임 1~2초), 중간(유성·구름 5~10초), 긴 것(달 위상·행성 이동 수 분) 세 층을 제안한다.

### 1-3. [C] 소품 상호작용이 이미 있는데 서로 무관한 장난감으로 흩어져 있다

여백 소품은 이미 반응한다. 어휘가 없는 게 아니라 **묶여 있지 않다.**

| 소품       | 입력        | 반응                         |
| ---------- | ----------- | ---------------------------- |
| 달토끼     | 클릭        | 8프레임 떡방아               |
| 우주비행사 | 마우스 오버 | 5프레임 손 흔들기            |
| 망원경     | 드래그      | 위치 이동                    |
| 나침반     | 드래그      | 위치 이동                    |
| 유성       | 클릭        | 반짝임 + 「소원을 빌었다 ✦」 |
| 빈 하늘    | 클릭        | 반짝임                       |

**요청**: 이 여섯을 하나의 관측소 설정으로 묶는 안. 지금은 각자 다른 게임에서 온 것처럼 보인다. 예컨대 망원경을 끌어 특정 천체에 겹치면 그 천체가 반응하는 식으로 **소품 사이에 관계**를 만드는 방향.

---

## 2. 이벤트 연출 — 게임에서 벌어지는 일이 여백에 하나도 닿지 않는다

### 2-1. [A] 런타임 이벤트 25종이 발화하는데 하늘은 0종을 구독한다

런타임은 이미 촘촘한 이벤트 체계를 갖고 있다. `runRuntimeHooks(...)`로 **25종**이 발화한다.

```
afterArenaDraw        afterAssistsDraw      afterBattleSetup      afterBattleWin
afterBlazeEarned      afterBossDamage       afterBossHitRegistered afterDraw
afterFeedbackUpdate   afterFieldFxDraw      afterFigureShot       afterMeteorLaunch
afterMeteorSteer      afterMobilePairCollision afterParryContact  afterParryRequest
afterPartySettle      afterRosterShown      afterShotEnd          afterShotStart
afterSpecialDraw      afterTableWall        afterUnitAssistQueued beforePartySettle
beforeShotStart
```

이 중 **하늘·인트로 계층이 구독하는 것은 0개**다. `stella-ball-dawn.js`와 `outer-observer.js` 어디에도 `registerRuntimeHook` 호출이 없다(`outer-observer.js`는 주석으로 "런타임 훅은 걸지 않는다"고 명시했다 — 인트로에는 맞는 판단이었지만, 그 결과 **여백 전체가 게임과 절연됐다**).

그래서 지금 이런 일이 벌어져도 여백은 아무 반응이 없다.

- 5점 별자리를 완성해도 (`afterFigureShot`)
- 최종 보스를 잡아도 (`afterBattleWin`)
- 새 월드에 처음 들어가도
- 별지기가 각성해도 (`afterBlazeEarned`)

**요청**: 이벤트 → 배경 반응 대응표. **어떤 이벤트가 여백에 나타날 자격이 있는지**를 정하는 것이 핵심이다. 25개를 다 쓰면 여백이 시끄러워져 전투 가독성을 해친다(스펙 6절의 금지 조항). 3~5개만 고르는 편이 맞다고 본다.

### 2-2. [A] 스펙 6절이 규정한 배경 반응 3종 중 2종이 미구현이다

[OUTER_OBSERVER_INTRO_SPEC.md](OUTER_OBSERVER_INTRO_SPEC.md) 6절 표를 그대로 옮긴다.

| 트리거                      | 규정된 반응                                              | 상태       |
| --------------------------- | -------------------------------------------------------- | ---------- |
| 첫 실행의 인트로            | 존재 일부의 1회 통과                                     | **구현됨** |
| 5점 이상 별자리 현현 완료   | 양옆 별이 존재의 윤곽을 피해 흐르거나 눈금 고리 1회 수축 | **미구현** |
| 월드 첫 진입 / 첫 보스 처치 | 관측창 프레임의 작은 이상 (별 하나 역행, 성운 굴절)      | **미구현** |

두 미구현 항목은 **설계가 이미 끝나 있고 붙일 훅도 이미 있다.** 5점 별자리는 `afterFigureShot`, 보스 처치는 `afterBattleWin`이다.

**요청**: 이 두 반응의 구체 시안. 스펙이 문장으로만 규정한 것을 프레임 수·궤적·대비값으로 옮겨 주면 된다. 스펙 6절의 금지 조항 넷(모든 패링에 반응 금지, 컷인 금지, 중앙 타이밍 방해 금지, 존재를 보상·퀘스트·공포로 쓰기 금지)은 그대로 지킨다.

### 2-3. [B] 화면에 남는 "이 세션에서 무슨 일이 있었는지"가 없다

인트로는 소품을 흩뜨려 **"방금 무슨 일이 있었다"**를 남긴다(스펙 5절). 그 어휘가 인트로에만 있고 이후에는 쓰이지 않는다.

지금은 관측소가 세션 내내 같은 모습이다. 첫 스테이지를 깬 직후와 34스테이지를 깬 뒤의 여백이 **완전히 동일**하다.

**요청**: 진행이 여백에 누적되는 안. 되찾은 별자리가 여백 하늘에 하나씩 걸리는 방향이 이 게임의 제목·설정과 가장 잘 맞는다고 본다. 다만 이건 [META_UI_REQUEST](META_UI_REQUEST_2026_08_14.md) 1-2의 「노드 상태 4종」과 같은 데이터를 쓰므로 **두 요청을 같이 설계해야 한다.**

---

## 3. 디자인 세션에 요청하는 산출물

우선순위 순이다.

1. **이벤트 → 배경 반응 대응표** — 25개 훅 중 여백에 나타날 3~5개 선정과, 각각의 반응·지속 시간·대비 상한. 2-1과 2-2를 함께 푼다.
2. **스펙 6절 미구현 2종의 구체 시안** — 5점 별자리 현현, 월드 첫 진입·첫 보스 처치.
3. **여백 계층 설계** — 먼 배경 / 중간 천체 / 가까운 소품의 3계층 규약과 각 계층의 시차·대비.
4. **자율 연출 주기표** — 짧은·중간·긴 주기 이벤트를 겹쳐 정지 구간을 없애는 안.
5. **소품 상호작용 통합안** — 이미 있는 여섯 반응을 하나의 관측소 설정으로 묶기.

색·프레임 수·원본 규격은 **제안값으로만** 적는다. 실제 반입은 `ASSET_PLAN.md`·`assets/ASSET_MANIFEST.json` 기록과 승인 뒤다.

**공통 제약 셋** — 어떤 안이든 이건 지킨다.

- 여백 연출은 **중앙 관측창을 절대 가리지 않는다.** 인트로 비트 5의 발톱 하나가 유일한 예외이고 그것도 `pointer-events: none`이다.
- 대비는 전장의 패링 노드·보스 체력·별자리 실루엣보다 **항상 낮다.**
- `prefers-reduced-motion`에서 정지 한 장으로 떨어진다. (단 [UI_REVIEW](UI_REVIEW_2026_08_14.md) 3-3이 지적한 대로 현재 모션 감소 규약이 `#dawn-sky *` 선택자에 묶여 있어 **그 규약부터 정해져야 한다.**)

---

## 4. 이 요청서가 보지 않은 것

“문제 없음”이 아니라 **“미확인”**이다.

- **모바일** — 좁은 화면에는 양옆 여백이 아예 없다. `OUTER_OBSERVER_INTRO_SPEC`도 이 문제를 **보류**로 남겼다. 이 문서의 모든 요청은 데스크톱 전제이며, **여백이 없는 비율에서 무엇을 대신 할지는 정해진 바가 없다.**
- **실제 움직임** — 미리보기 창이 가려지면 `requestAnimationFrame`과 CSS 트랜지션이 멈춘다. 소품의 애니메이션 프레임 수와 주기는 코드에서 읽었고, 정지 구도와 경계 상자만 실측했다. **흐르는 모습은 사람이 직접 봐야 한다.**
- **성능** — 여백 연출을 늘렸을 때의 프레임 영향은 재지 않았다. 다만 현재 전투 갱신 19µs·`draw()` 141µs로 프레임 예산의 약 1%뿐이라(`PROJECT_CONTEXT.md`) 여유는 있다.
- **소리** — 이벤트에 붙는 SFX는 범위 밖이다.
- **명암비 수치** — 소품·별의 실제 대비율은 계산하지 않았다.

---

## 5. 참고 — 이 문제를 이미 푼 방식

남의 에셋을 가져오자는 뜻이 아니라, 같은 문제의 알려진 해법을 적어 둔다.

- **넓은 여백**: 세로 컬럼 게임은 보통 여백을 **시차(parallax) 계층**으로 쓴다. 한 평면에 소품을 늘리면 산만해지고, 계층을 나누면 개수를 늘리지 않고도 깊이가 생긴다 — 1-1의 요청이 "몇 개"가 아니라 "몇 겹"인 이유다.
- **이벤트 반응**: 배경이 게임에 반응하는 작품은 대개 **드물게, 크게** 반응한다. 매 타격에 반응하면 배경이 아니라 HUD가 된다. 스펙 6절이 이미 같은 결론을 문장으로 적어 두었다.
- **진행 누적**: 거점 화면에 진행이 쌓이는 구조는 플레이어가 "얼마나 왔는지"를 숫자가 아니라 풍경으로 읽게 한다. 이 게임은 되찾은 별자리라는 소재가 이미 있어 별도 장치를 발명할 필요가 없다.
