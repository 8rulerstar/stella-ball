# 디자인 요구서 — 정산 명중이 «거미줄»이다

> 작성 2026-08-24 · 오너 지시 「타격 피격 이펙트도 요구할래. 지금 뭔가
> 거미줄 같아서 안 어울림」
>
> 재현: 전투에서 각성한 별지기의 정산 공격이 거상에 꽂히는 순간.
> 강제로 보려면 `finisherImpacts.push({ sourceId: "gaon", col: "#f2c56b",
finisher: true, t: 0.26, d: 0.58 })`.

## 1. 한 줄

**동심원 세 겹에 방사선 열둘이 걸려 있어서 문자 그대로 거미줄이다.** 그리고
그 자리에서 «누가 때렸는가»를 말하는 겹이 셋이나 겹쳐 있는데, 가장 시끄러운
것이 하필 아무 뜻도 없는 그 거미줄이다.

## 2. 거미줄의 정체 — 실측

`game-feedback.js`의 `drawFinisherImpacts`가 명중 한 번에 그리는 것이다.

```
동심원 3겹   radius × (0.38, 0.60, 0.82)   #fff4c9, lineWidth 5×fade+1
방사선 12개  안쪽 24+p×20  →  바깥 radius × (0.72 ~ 0.92)
radius = 34 + p × 180        →  0.58초 동안 34px 에서 214px 로 퍼진다
```

**동심원 + 방사 스포크**는 거미줄의 정의 그 자체다. 오너의 표현이 인상이
아니라 기하학적으로 정확하다.

일반 충돌은 이렇지 않다 — `areaBursts`는 퍼지는 원 두 개뿐이라 깨끗하다.
**문제는 정산 명중 한 곳이다.**

## 3. 진짜 문제 — 같은 말을 하는 겹이 셋이다

명중 한 번에 그려지는 순서다.

| 순  | 겹                        | 무엇                                  | 크기        | 성격              |
| --- | ------------------------- | ------------------------------------- | ----------- | ----------------- |
| 1   | `drawFinisherHitSheet`    | **별지기 전용 타격 시트**(2026-08-24) | 150→220px   | 래스터 · 8명 전부 |
| 2   | 색 원 + 흰 코어           | 고유색 원, 흰 사각                    | ~118px      | 벡터 · 공통       |
| 3   | **동심원 3 + 방사선 12**  | ← **거미줄**                          | 34→214px    | 벡터 · 공통       |
| 4   | `drawFinisherImpactMotif` | **능력별 벡터 모티프**                | radius 비례 | 벡터 · 6명분      |

**1번과 4번이 같은 일을 한다** — 둘 다 「이 별지기의 이 능력이 때렸다」를
말한다. 게다가 4번은 여섯 개뿐이라(`slash`·`longshot`·`split`·`seek`·`turn`·
`shockwave`) 윤슬·그믐은 그 겹이 아예 비어 있다.

그리고 **3번이 가장 크고 가장 밝다**. `#fff4c9` 크림색이 `screen` 합성으로
얹히므로, 고유색으로 그린 1·4번 위에서 하얗게 튄다. 결과적으로 여덟 명이
각자 다른 그림을 갖게 만들어 둔 자리인데 화면에서는 **전부 같은 흰 거미줄로
보인다.**

## 4. 요청 — 셋

### 4-1. 거미줄을 대체할 «명중»의 어휘 (최우선)

동심원과 방사선을 버리고, 「꽂혔다」가 읽히는 다른 어휘를 달라. 지금 것은
퍼지는 그물이라 **타격이 아니라 «감싸기»로 읽힌다.**

정하지 않은 방향들 — 파열(중심에서 파편이 튀는), 관통(맞은 방향으로 뚫고
나가는 자국), 함몰(맞은 면이 눌리는), 섬광 한 번 + 짧은 잔해. 어느 쪽이든
**방향이 있어야** 한다: 지금은 완전한 방사 대칭이라 어디서 날아와 맞았는지
그림이 말하지 않는다. 그 정보는 코드가 이미 갖고 있다(때린 별지기의 좌표).

### 4-2. 겹을 줄여 달라 — 어느 겹이 «누가»를 맡을지 정해 달라

1번(별지기 전용 래스터 시트)과 4번(능력별 벡터 모티프)은 같은 말을 두 번
한다. **둘 중 하나로 정해 달라.** 시트 쪽으로 정하면 4번은 코드에서 걷고
(윤슬·그믐이 비어 있는 문제도 함께 사라진다), 모티프 쪽으로 정하면 시트의
`*-impact.png` 여덟 장을 다시 그려야 한다.

권하는 쪽은 시트다 — 여덟 명 전부 있고, 이미 반입돼 있고, 성능도 쟀다.
다만 이건 **디자인 판단이라 정해 주지 않으면 코드가 못 움직인다.**

### 4-3. 세기가 그림에 나오게

지금 크기(`radius = 34 + p × 180`)는 **시간에만 반응한다.** 큰 정산이든 작은
정산이든 같은 크기로 퍼진다. 각성 요구서 §4-1 이 지적한 것과 같은 병이
여기에도 있다 — 눈금이 없다. 피해량·굴린 성적 같은 값이 크기나 파편 수에
실리면 좋겠다. 코드가 그 값을 이미 들고 있다.

## 5. 같은 길을 다시 걷지 않기 위해

**2026-08-24 시전 시트에서 한 번 헛짚었다.** 여덟 장을 새로 받아 반입했는데
오너가 「가온 이펙트가 똑같은 것 같다」고 했고, 확인해 보니 배선은 정상인데
**금색 초승달을 금색 초승달로 바꾼 것**이었다 — 여덟 장 중 일곱이 기존
per-kind 시트와 같은 모티프였다. 칠한 비율은 오히려 +27% 컸는데도 「바뀌었다」가
읽히지 않았다.

그래서 이 요구서의 검수 기준은 **크기나 밝기가 아니라 «앞의 것과 다른
어휘인가»**다. 동심원+방사선을 조금 다르게 그린 동심원+방사선은 답이 아니다.

## 6. 지켜야 하는 것

- **판단색 불가침(§1-4).** 별지기 고유색과 유성·바닥의 판정색은 연출이 덮지
  않는다. 지금 거미줄의 `#fff4c9`가 정확히 그 선을 밟고 있다 — 고유색으로
  그린 아래 겹을 하얗게 덮는다.
- **예약색 금지.** 살구 램프(오리온)·안내별 금·CTA 금·경고 적색은 쓰지 않는다.
- **래스터를 낼 거면 기존 규격을 따른다**: 768×192, 가로 4프레임(192×192),
  셀 8px(논리 24×24), 알파 0/255. 그러면 기존 준비 검사
  (`naturalWidth === naturalHeight * 4`)와 그리기 코드를 그대로 쓴다.
- **성능.** 지금 명중 한 번이 프레임에 얹는 것은 원 1 + 사각 1 + 호 3 +
  선 12 + 사각 12 + 모티프다. 늘릴 거면 `scripts/probe-hero-fx.mjs` 방식으로
  rAF p95 를 재고 커밋에 붙일 것. 현재 기준선은 **p95 9.1ms, 20ms 초과 0**이다.
- **히트스톱은 건드리지 말 것.** `impactStop > 0`인 프레임은 `update()`가
  통째로 건너뛰어 판이 실제로 언다. rAF 는 계속 돌므로 프레임 지표에 안 보인다 —
  «측정을 통과하는 렉»이고 이 저장소가 이미 한 번 겪었다.

## 7. 검수

- [ ] 동심원+방사선이 아닌 다른 어휘인가 (§5 의 재발 방지)
- [ ] 어디서 날아와 맞았는지 그림이 말하는가
- [ ] 여덟 별지기의 명중이 서로 구분되는가 — 특히 hue 가 1.2° 차이인
      달무리·그믐
- [ ] 그 아래 별지기 고유색과 거상의 판정색이 그대로 읽히는가
- [ ] 큰 정산과 작은 정산이 다른 크기로 보이는가 (§4-3)
- [ ] rAF p95 첨부 — 기준선 9.1ms

## 8. 코드 연결 지점

- `prototypes/js/game-feedback.js`
  - `drawFinisherImpacts` — 거미줄(동심원 3 + 방사선 12)이 여기 한 곳에 있다
  - `drawFinisherHitSheet` — 별지기 전용 래스터 시트(겹 1)
  - `drawFinisherImpactMotif` — 능력별 벡터 모티프(겹 4, 여섯 개뿐)
- `prototypes/js/game-data.js` — `heroFxSheets` 의 `*Hit` 여덟 항목
- 되돌리기: `heroFxSheets` 를 빈 객체로 두면 시트 겹이 준비 검사에서 떨어져
  이전 그림으로 돌아간다. 파일을 지울 필요가 없다.
