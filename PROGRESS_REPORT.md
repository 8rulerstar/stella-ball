# Stella Ball 진행 보고 · 세션 인수인계

> 기준일: 2026-08-12
> 이 문서는 보고용 요약이자 다음 Codex 세션의 시작점이다. 문서별 역할은 `DOCUMENTATION_INDEX.md`, 상세한 방향은 `GAME_DIRECTION.md`, 제출용 협업 서사는 `CODEX_COLLABORATION.md`, 날짜별 판단은 `DEVLOG.md`를 따른다.

## 현재 한 줄

**유성으로 3명의 별지기를 실제 당구공처럼 굴리고, 멈춘 위치에서 터지는 각성 공격과 별자리 배율로 공허 거상을 공략하는 웹 액션 전략 프로토타입**이다.

실행 파일은 `prototypes/prism-breakers.html`이며, 공개 주소는 <https://8rulerstar.github.io/prism-breakers/prototypes/prism-breakers.html>이다.

## 이번 기준선에서 완료한 것

- 첫 실행은 프롤로그 → `1-1 별빛의 첫 충돌` 온보딩 전장으로 이어진다. 루나가 6개의 카드로 유성 직격, 비연의 정지 후 원거리 각성, 리아의 이동 속도 비례 회전 칼날과 무정산 규칙을 실제 전장에서 보여 준다. 카드는 게임 이벤트로 자동 전환되지 않으며, 언제나 플레이어가 버튼을 눌러야 다음으로 넘어간다. 실습 중에는 카드가 물러나고 조준 가이드만 남으며, 결과 카드는 테이블이 멈춘 뒤에만 돌아온다. 같은 목표를 두 번 놓치면 다음 단계로 넘어갈 수 있어 수업에 갇히지 않는다.
- 마지막 6단계는 설명이 아니라 실전이다. 거상의 불멸이 해제되고 체력 `RULES.tutorialCoreHp`(120)로 내려오며, 유성은 무제한이라 실패로 막히지 않는다. 직접 처치하면 일반 전투 결과 화면 대신 업적 「첫 관측자의 증명」으로 이어져 세 번째 파티 슬롯이 열리고, 메인 화면에서 다시 볼 수 있다.
- 전투 중 `ESC` 키 또는 전장 좌측 상단의 정지 버튼으로 일시정지할 수 있다. 전장은 화면에 남은 채 얼어붙고, `계속하기 · 설정 · 관측소로 나가기`를 제공한다. 정지 상태에서 연 설정은 허브가 아니라 정지 메뉴로 돌아오므로 진행 중인 전투를 잃지 않는다.
- 온보딩 설명은 전장 하단의 짧은 대화 배너로 표시한다. 문장은 순서대로 나타나며, 실습 버튼을 누르면 배너를 숨겨 전장을 비우고 유성 출발점·목표 고리·손가락 포인터·점선만으로 조작을 유도한다.
- 메타 허브는 별자리 지도를 화면 본문으로 두고, 메뉴를 하단 탭바(프로필·소환·관측·상점·설정)로 내렸다. 지도 위 월드 바의 좌우 화살표로 별자리를 넘긴다. 지도의 별을 누르면 그 스테이지가 선택되고 하단 임무 바의 이름·규칙·편성과 CTA가 함께 바뀐다. 잠긴 별은 비활성이며, `1-1`을 고르면 CTA가 「수업 다시 보기」로 바뀐다. 지도 노드 목록은 `constellationMapStages()` 하나를 전체 지도 화면과 공유한다.
- 캠페인은 별자리 월드 구조다. `북두칠성` 7 스테이지(두베~알카이드)와 `카시오페이아` 5 스테이지(카프~세긴)이며 스테이지 이름은 실제 별 이름이다. 노드 위치는 실제 별자리 도형에서 나온다. 진행은 `progress.clears` 하나로 판정하므로 저장 데이터 마이그레이션이 없다.
- 세로 핀볼 시안을 탑다운 당구 전장으로 전환했다. 중력·드레인·플리퍼는 현재 코어 규칙이 아니다.
- 정지 위치 이어 쏘기를 재도입했다 (2026-08-12, 사람 결정). 일반·훈련 전투의 다음 유성은 이전 유성이 멈춘 자리에서 출발하며, 루나의 1-1 수업만 안내 문구가 위치를 전제하므로 하단 발사석을 유지한다. 원래도 인게임 문구는 “멈춘 자리에서 다음 샷”이었고, 화면 코드의 래퍼가 정지점 인자를 삼키며 고정 발사석이 되어 있던 것을 바로잡았다.
- 공과 유닛은 충돌 후 모두 움직이는 물리 오브젝트다. 움직인 유닛은 테이블이 멈춘 뒤 현재 위치에서 각성 공격을 한다.
- 전체 8인 로스터와 3인 파티 편성: 시작은 가온·비연·리아 3명이며, 루미·하루·세라·태오·닉스는 `별빛 소환`에서 해금한다. 3번째 자리는 1-1을 클리어해야 열리고, 그전까지 파티는 2명이다.
- 편성과 배치는 한 화면이다. 위쪽 전장 미니맵(실제 720×900 비율)의 자리에 별지기가 서 있고, 아래 트레이에 보유 별지기가 모두 나열된다. 트레이에서 자리로 끌어 놓거나 눌러서 자리를 지정하며, 이미 배치된 별지기끼리는 자리를 맞바꾼다. 자리 순서가 그대로 전투의 `s.slots` 좌표와 `ZONE_RULES` 발동 조건이 된다.
- 8개 능력의 전투 이펙트와 고유 규칙을 구현했다. 루미의 분열체도 유닛 충돌·능력 발동·운동량을 가진다.
- 몸체 피해와 고보상 약점 피해, 충돌 즉시 피해, 별지기 각성 공격, 콤보·히트스톱·화면 흔들림·별자리 배율을 연결했다.
- 충돌 물리는 최대 3개의 90Hz 서브스텝만 사용하고, 지속 이펙트 갱신은 렌더 프레임당 한 번만 수행한다. 정적 전장 레이어 캐시, 60Hz 궤적 샘플링, 변화 기반 HUD 갱신, 혼잡 시 장식만 줄이는 VFX 예산으로 충돌 스파이크를 낮췄다.
- 유닛의 보스 직격 히트스톱은 28ms로 제한하고, 정산은 0.46→0.62→0.82 단계형 슬로모션과 캐릭터별 지연 VFX 생성으로 유지한다. 정산 명중은 전체 HUD 대신 보스 체력만 갱신한다.
- 정산은 캐릭터별 확대·집중선·조준 표식·별자리 노드와 충전/해방/명중 SFX를 순차 재생해 서로 겹치지 않는다. 이동·충돌·각성·승패 SFX도 샘플과 저음 절차음을 계층화했다.
- 단일 HTML/JS/CSS를 유지보수 영역별 CSS 7개와 ordered classic JS 13개 모듈로 분리했다. 마지막 CSS 2개(`stella-ball-theme.css`·`stella-ball-dawn.css`)와 마지막 JS 2개(`stella-ball-pixel-ui.js`·`stella-ball-dawn.js`)는 표현 전용 패스다. 파일 책임과 로드 순서는 `prototypes/ARCHITECTURE.md`에 고정했다.
- 공허 거상 보스의 idle/hit 애니메이션, 전장 타일, 픽셀 UI, Galmuri 글꼴, 처치 연출, 메타 허브/튜토리얼/설정/업적 화면을 반입했다.
- `무한 훈련장`을 추가했다. 보스 체력은 무한이며 유성 5개를 모두 쓰면 자동 보충된다. 기록 제출용 스테이지가 아니라 물리·배율·능력 QA용이다.
- 기믹은 스테이지 데이터의 `gimmicks`에서 생성된다. 공명 범퍼·반사 벽·가속 발판·공허 잔재에 더해 굳은 껍질(피해 n회 무시), 흐린 발판(배율 하락), 도는 방벽(체력을 갖고 보스를 공전), 페이즈 전환(모서리 밀어내기 / 재수면)이 있다. 보스 피해는 전부 `applyBossHit()` 한 곳을 지난다.
- 비전투 화면과 숨겨진 탭에서는 전투 시뮬레이션·Canvas 렌더를 건너뛰고, HUD·SFX·짧은 이펙트 배열·조준 계산의 반복 할당을 줄였다.
- 골드는 클리어 100(보상함 적립 후 수령), 업적 일회성 보상, 출석 보상(하루 한 번, 연속 60~220)에서 나온다. 쓰는 곳은 `별빛 소환`(100), 유성 도색(500), 별지기 도색(320)이며 전부 외형 전용이다. 유료 재화·확률형 중복 뽑기는 만들지 않았다.
- 업적 좌측에 `도서관` 탭이 있다. 별지기 8명 도감과 별자리 배율 조건표를 보여 준다.

## 실제 전투 규칙

- 일반 스테이지: 보스 체력 260, 기본 5발. 강화 `예비 탄창`은 발사 수를 +1 한다.
- 첫 발사: 하단 발사석의 유성을 아래로 드래그해, 반대 방향으로 발사한다.
- 다음 발사: 이전 유성이 멈춘 자리에서 같은 방식으로 발사한다. 좋은 정지 위치를 남기는 것이 다음 샷의 전략이다. (1-1 수업만 하단 발사석 고정)
- 약점: 몸체도 피해를 받지만, 빛나는 약점은 기본적으로 더 높은 피해 배율을 받는다.
- 배율: 유성의 보스 직격, 모든 별지기 각성, 별지기의 보스 충돌, 벽 반사 등이 `CONSTELLATION`을 올린다.
- 훈련장: 전투 종료/기록 제출 없이 계속 재장전된다. `R` 키로 편성 화면으로 돌아간다.

## 로스터와 역할

| 유닛 | 규칙                                                                        |
| ---- | --------------------------------------------------------------------------- |
| 가온 | 정지 후 가까운 보스를 근접 베기                                             |
| 비연 | 정지 후 보스와 멀수록 강해지는 화살 저격                                    |
| 루미 | 첫 충돌 시 이번 샷의 유성을 하나 더 분열                                    |
| 하루 | 충돌 시 가장 가까운 다른 유닛으로 강제 중계                                 |
| 리아 | 이동 중 보스를 관통하며 속도에 비례해 주변을 베는 회전 칼날, 정산 공격 없음 |
| 세라 | 충돌 뒤 클릭 1회로 유성을 90° 전환하고 에너지 부여                          |
| 태오 | 모든 충돌 수에 비례한 범위 충격파                                           |
| 닉스 | 마지막으로 충돌한 아군의 능력을 복제                                        |

## 최근 커밋 흐름

- `e0d2847` ~ `f21d3dc`: 유닛 이펙트, 충돌 운동량, 전체 속도 기준선 정비
- `4e29e4d` ~ `6625f7f`: 처치 연출, 스테이지 선택, 메타 허브·튜토리얼·설정/업적
- `f34d455` ~ `80b22e0`: 능력 이펙트 강화, 유닛 충돌 피해, 공허 거상, 분열 룬볼 물리
- `b67e635` ~ `93d4476`: 배율 UI, 전장 타일, Galmuri 게임 글꼴
- `87c38d4`: 무한 체력 훈련장
- `019b936`: 스테이지 데이터 기반 반사 벽·가속 발판·잡몹
- `2e22fda` · `e6391e9`: 화면 수명주기·공용 UI 분리와 런타임 최적화

## 다음 세션 인수인계 — 2026-08-12 기준

이번 세션은 사용자가 지적한 12가지를 A(코드 교정) / B(새 시스템) / C(아트) 세 단계로 나눠 **A와 B를 끝냈다.** C는 시작하지 않았다.

### 끝낸 것

- **A단계** — 일시정지 버튼 아이콘(초승달 실루엣 제거), `RIPOSTE` → `연타`, 토스트 큐(한 충돌에 여러 메시지가 순차 재생), 리아 칼날이 멈추기 직전까지 유지, 온보딩 카드를 화면 가운데로 + 실습 중에는 전장 밖 상단 바로, 1-3 기믹을 반사 벽만으로, 허브 칩 `업적 N / M`.
- **B단계** — 별자리 월드 구조(북두칠성 7 + 카시오페이아 5, 캠페인 12스테이지), 새 기믹 5종, 도서관 탭, 출석 보상, 상점의 별지기 색 도색.

### 남은 작업

1. **C단계 · 리아 모션 — 완료 (2026-08-12)** — `ria-roll.png`(idle 90도 무손실 텀블)·`ria-attack.png`(자체 idle + 칼날 플러리 오버레이)를 반입하고 `heroAnimArt`에 리아를 등록했다. `scripts/generate_unit_action_sheets.py`에 리아는 자체 idle 기반 특수 항목으로 추가되어 재생성 가능하다. 실전에서 구르기 시트 재생·속도 비례 칼날·관통 피해를 확인했다. attack 시트는 현재 규칙상 정산에 쓰이지 않지만 데이터 등록 일관성과 참조 무결성을 위해 함께 반입했다.
2. **C단계 · 태오 스프라이트 — 완료 (2026-08-12)** — 사람이 ① Orc 리스타일 안을 선택했다. Tiny RPG Orc를 4배 확대 + 고정 윈도우(발 y=136 정렬) + 색상 규칙(녹색 피부→황동 브론즈, 강철 유지, 참격→잔불 크림)으로 리스타일해 `taeo-orc-idle.png`(6프레임)·`taeo-roll/attack`·정지 토큰을 반입했다. 재생성은 `scripts/generate_taeo_orc_restyle.py`, 데이터는 `heroes.taeo`(frames 6)·`cuteUnitArt`·`combatUnitSize`(118)만 변경. 구 Pawn 시트는 롤백 대비 보존. `npm run verify`·`smoke` 통과, 실전 캔버스에서 구르기·공격 시트 표시 확인.
3. **새 콘텐츠 밸런스 미측정** — 캠페인 12개 스테이지의 난이도 곡선과 새 기믹 5종(굳은 껍질·흐린 발판·도는 방벽·모서리 밀어내기·재수면)은 수치를 재지 않았다. 봇 하니스가 현재 물리로 갱신된 뒤의 작업이다.
4. **전투 캔버스 톤** — 전장 바닥은 여전히 보라색으로, 주변 UI의 청록–살구와 어긋난다(`game-core-render.js`의 스테이지 아트 캐시 색). 판정 가독성 검증 후 별도 패스.
5. **`showStageSelect()` — 확인 완료 (2026-08-12)** — 화면 자체는 정상 동작한다(월드 바·7노드·잠금 상태·뒤로→허브 복귀 확인). 실제 도달 경로는 온보딩 완료 화면의 「별자리 지도」·「다음 관측」 두 곳뿐이다. 허브의 `#hubStageSelect` 핸들러는 대체된 구버전 `showMeta` 오버라이드 안의 죽은 코드로, 최종 허브는 지도를 본문에 내장하므로 문제 없다. 추후 훅 마이그레이션 때 구버전 허브 두 벌을 정리하면 된다.
6. **아트 표 공유** — 스테이지 13개가 아트 세트 5개를 `stageArtFor()` 폴백으로 나눠 쓴다. 두 월드가 같은 지형으로 보이므로 월드별 지형 세트는 `ASSET_BACKLOG.md` 항목이다.
7. **미사용 픽셀 스프라이트 — 완료 (2026-08-12)** — `stella-ball-dawn.js`가 킷의 `coin`·`emblem`·`rank`·`mail` 스프라이트를 `--px-*` CSS 변수로 1회 등록하고, `stella-ball-dawn.css`가 허브·결과 화면의 자리 표시용 CSS 원형 코인과 프로필 세 패널(기록/순위/우편함) 제목 글리프를 이 스프라이트로 바꾼다. 화면 코드는 무변경(표현 전용 레이어에서만 처리), 킷이 없으면 기존 CSS 도형으로 폴백. 같은 커밋에서 전투 중 별지기 도색 미적용 버그도 수정했다 — `game-meta.js`가 `drawFrame` 래퍼 대신 `drawFrameRaw`를 재정의하도록 바꿔 `heroSkinFilter` 색조 체인을 복원(전투에서 장착 도색 hue-rotate 적용 확인).

### 검수 환경에서 알아 둘 것

- 헤드리스 브라우저 창에서는 `document.visibilityState`가 `hidden`이라 렌더 루프가 대기한다. **캔버스가 검게 나오는 것은 버그가 아니며**, `draw()`를 직접 부르면 정상 렌더된다. `getBoundingClientRect()`가 전부 0을 반환하는 순간도 같은 원인이므로 치수 판단은 스크린샷으로 한다.
- 새 4파일이 `prettier --check`에 걸리지만 HEAD에서도 `stella-ball-theme.css`가 이미 걸려 있었고 CI는 포맷을 검사하지 않는다.
- 보스 피해는 전부 `applyBossHit()` 한 곳을 지난다. 새 피해 경로를 추가할 때 `boss.hp`를 직접 깎으면 굳은 껍질과 페이즈 전환이 새므로 반드시 이 함수를 쓴다.

## 확인된 주의점과 다음 작업

1. **전투 밸런스** — 현재 충돌 최소 운동량·별자리 배율·각성 공격이 한 발에 겹치면 원샷 또는 과도한 체공이 될 수 있다. 먼저 봇 하니스를 현재 당구 물리로 갱신하고, 목표 클리어율 60~70%·원샷 희귀·짧은 샷 지속시간을 함께 스윕한다. 체력만 올리지 않는다.
2. **자동 검증 갱신** — 기존 헤드리스 봇 기록은 과거 규칙 기준이다. 현재 이동 별지기·분열체·8인·5발·1-2 범퍼를 반영한 뒤에만 새 수치를 제출 자료로 사용한다.
3. **스테이지 확장 규칙** — 새 기믹을 소개하는 스테이지에는 그 기믹만 둔다. 그 외에도 한 스테이지에 최대 두 종류까지다. 무한 훈련장만 전부를 동시에 켠다.
4. **검증 스크립트 기준선** — 공허 거상·전용 약점 에셋 기준으로 갱신했다. 이후 보스/핵심 에셋을 바꾸면 `scripts/verify-evidence.mjs`와 `assets/ASSET_MANIFEST.json`도 같은 커밋에서 맞춘다.
5. **Hive 실통신** — 클라이언트 인터페이스는 연결돼 있으나 Console AppID/서버 어댑터를 통한 로그인·저장·리더보드 왕복은 아직 완료가 아니다.
6. **외부 플레이 기록** — 다른 기기에서 첫 샷·다음 샷·유닛 각성·훈련장 재장전을 확인하고, 짧은 영상/스크린샷을 남긴다.
7. **크로스플랫폼 작업** — `.gitattributes`, `scripts/check-portability.mjs`, Windows CI를 추가했다. 새 에셋/코드를 반입한 뒤에는 `node scripts/verify-evidence.mjs`를 실행하고, 필요한 파일이 반드시 Git 추적 상태인지 확인한다.

## 내일 macOS에서 시작

Node.js 20 이상이 설치된 터미널에서 저장소 루트로 이동한 뒤 실행한다. 외부 npm 의존성이 없어 `npm install`은 필요하지 않다.

```sh
git pull --ff-only
npm run verify
npm run smoke
npm run serve
```

`http://127.0.0.1:4173/`에서 메인 CTA, 온보딩 모달의 입력 잠금/실습 전환, `1-2` 공명 범퍼, 한 번의 정산 연출과 SFX를 순서대로 확인한다. 수정 후에는 `git status --short`로 대소문자 차이와 새 에셋 추적 상태를 확인한다. `.claude/`와 `node_modules/`는 커밋하지 않는다.

## 다음 세션의 작업 규칙

- 현재 코어는 당구형이다. 사용자의 명시적 결정 없이는 핀볼/슬링샷/빙의 시안으로 되돌리지 않는다.
- 전체 8인 로스터, 시작 3인(가온·비연·리아), 기본 5발을 문서·UI·검증에서 같은 기준으로 쓴다.
- `무한 훈련장`은 QA 전용이며 순위/밸런스 통계에 섞지 않는다.
- Hive의 실제 왕복 전에는 “연동 완료”라고 쓰지 않는다.
- 새 기능 하나마다 `DEVLOG.md`에 Codex 작업, 막힌 점, 사람의 판단, 커밋을 남긴다.
