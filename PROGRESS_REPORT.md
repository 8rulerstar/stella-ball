# Stella Ball 진행 보고 · 세션 인수인계

> 기준일: 2026-08-23
> 이 문서는 보고용 요약이자 다음 Codex 세션의 시작점이다. 문서별 역할은 `DOCUMENTATION_INDEX.md`, 상세한 방향은 `GAME_DIRECTION.md`, 제출용 협업 서사는 `CODEX_COLLABORATION.md`, 날짜별 판단은 `DEVLOG.md`를 따른다.

## 현재 한 줄

**유성으로 3명의 별지기를 실제 당구공처럼 굴리고, 멈춘 위치에서 터지는 각성 공격과 별자리 배율로 공허 거상을 공략하는 웹 액션 전략 프로토타입**이다.

실행 파일은 `prototypes/prism-breakers.html`이며, 공개 주소는 <https://8rulerstar.github.io/prism-breakers/prototypes/prism-breakers.html>이다.

## 이번 기준선에서 완료한 것

- 첫 실행은 프롤로그 → `1-1 하말 · 첫 관측` 온보딩 전장으로 이어진다. **수업은 캠페인과 같은 자동 공명·별빛 선택·각성·별자리 규칙으로 돈다.** 루나는 12장의 카드로 `끌어서 발사 → 별빛 세 곳 선택 → 각성 사전 설명 → 각성 실습과 확인 → 남긴 별빛으로 별자리 → 실전 전체 순서`를 가르친다. 각 규칙은 처음 발동하기 전에 먼저 설명하며, 각성 실습은 실제 `awakenedHero`가 기록되어야 통과한다. 0단계만 루나가 발사 각도를 보스로 고정하고, 별자리 단계는 보스 둘레에 북두칠성 뼈대의 안내별 일곱을 미리 놓아 `bigdipper` 판정 한 번을 보장한다. 실습 화면은 **암전 + 손가락 시연**이며, 카드는 플레이어가 버튼을 눌러야만 넘어간다.
- 마지막 8번째 카드는 `별빛 선택 → 발사 → 공명과 각성 준비 → 모두 멈춘 뒤 각성 공격 → 다음 발사 전 별자리`의 전체 순서를 다시 보여 준다. 그 뒤 거상의 불멸이 해제되고 체력 `RULES.tutorialCoreHp`(120)로 내려오며, 유성은 무제한이라 실패로 막히지 않는다. 직접 처치하면 일반 전투 결과 화면 대신 업적 「첫 관측자의 증명」으로 이어져 세 번째 파티 슬롯이 열리고, 메인 화면에서 다시 볼 수 있다.
- 전투 중 `ESC` 키 또는 전장 좌측 상단의 정지 버튼으로 일시정지할 수 있다. 전장은 화면에 남은 채 얼어붙고, `계속하기 · 설정 · 관측소로 나가기`를 제공한다. 정지 상태에서 연 설정은 허브가 아니라 정지 메뉴로 돌아오므로 진행 중인 전투를 잃지 않는다.
- 온보딩 설명 카드는 전장 가운데에서 읽고, 실습 버튼을 누르면 사라져 유성 출발점·목표 고리·손가락 포인터·점선만 남긴다. 일반 전투의 현재 안내와 루나 대사는 하단 오버레이가 아니라 **보스 체력표와 전장 사이의 고정 안내 띠**를 공유한다. 대사가 나와도 전장이 움직이거나 안내 띠 높이가 바뀌지 않는다.
- 메타 허브는 별자리 지도를 화면 본문으로 두고, 메뉴를 하단 탭바(프로필·소환·관측·상점·설정)로 내렸다. 지도 위 월드 바의 좌우 화살표로 별자리를 넘긴다. 지도의 별을 누르면 그 스테이지가 선택되고 하단 임무 바의 이름·규칙·편성과 CTA가 함께 바뀐다. 잠긴 별은 비활성이며, `1-1`을 고르면 CTA가 「수업 다시 보기」로 바뀐다. 지도 노드 목록은 `constellationMapStages()` 하나를 전체 지도 화면과 공유한다.
- 캠페인은 별자리 월드 구조다. **7월드 34스테이지**(양자리 3 · 화살자리 4 · 까마귀자리 4 · 카시오페이아 5 · 백조자리 5 · 오리온자리 6 · 북두칠성 7)에 최종 `8-1`이 하나 붙는다. 스테이지 이름은 실제 별 이름이다. (2026-08-23 정정 — 이 줄은 「북두칠성 7 · 카시오페이아 5」 둘만 적고 있었다. `DOCUMENTATION_INDEX.md`가 이미 「은퇴한 `LEGACY_GIMMICK_STAGES` 표를 현재 캠페인으로 착각한 서술」이라고 못 박은 그 문장이 여기 남아 있었다. 런타임 실측으로 확인했다.) 노드 위치는 실제 별자리 도형에서 나온다. 진행은 `progress.clears` 하나로 판정하므로 저장 데이터 마이그레이션이 없다.
- 세로 핀볼 시안을 탑다운 당구 전장으로 전환했다. 중력·드레인·플리퍼는 현재 코어 규칙이 아니다.
- 정지 위치 이어 쏘기를 재도입했다 (2026-08-12, 사람 결정). 일반·훈련 전투의 다음 유성은 이전 유성이 멈춘 자리에서 출발하며, 루나의 1-1 수업만 안내 문구가 위치를 전제하므로 하단 발사석을 유지한다. 원래도 인게임 문구는 “멈춘 자리에서 다음 샷”이었고, 화면 코드의 래퍼가 정지점 인자를 삼키며 고정 발사석이 되어 있던 것을 바로잡았다.
- 공과 유닛은 충돌 후 모두 움직이는 물리 오브젝트다. **각성과 공명은 서로 다른 두 계통이다(2026-08-16 되돌림).** 유성에 밀리거나 쿠션에 튀어 실제로 굴러간 별지기는 깨어나 멈춘 자리에서 정산 공격을 하고, 별지기 충돌이 자동으로 일으키는 공명은 별빛 노드·배율·접촉 능력을 만든다. 한 샷이 둘 다 낼 수 있다.
- 전체 8인 로스터와 3인 파티 편성: 시작은 샛별·미리내·윤슬 3명이며, 별하·살별·달무리·모루·그믐은 `별빛 소환`에서 해금한다. 파티 자리는 처음부터 3개다(2026-08-23 오너 지시로 「1-1 클리어가 세 번째 자리를 연다」는 관문과 무료 소환권 보상을 걷었다).
- 편성과 배치는 한 화면이다. 위쪽 전장 미니맵(실제 720×900 비율)의 자리에 별지기가 서 있고, 아래 트레이에 보유 별지기가 모두 나열된다. 트레이에서 자리로 끌어 놓거나 눌러서 자리를 지정하며, 이미 배치된 별지기끼리는 자리를 맞바꾼다. 자리 순서가 그대로 전투의 `s.slots` 좌표가 된다. 자리 이름은 거상과의 실제 거리에서 계산한다 — `slotRole()`이 300·390 두 문턱으로 근접/중거리/원거리 자리를 나눈다. 예전의 경유·공명·마무리 이름은 `triggerZone`이 사라진 뒤로 발동하지 않는 조건을 설명하고 있어서 폐기했다.
- 7개 능력은 전투 이펙트와 고유 규칙을 구현했다. 별하의 분열체도 유닛 충돌·능력 발동·운동량을 가진다. 달무리의 기존 클릭 90° 전환은 제거했고, 새 충돌 발동 조건은 패링 실험 뒤에 별도로 정한다.
- 몸체 피해와 고보상 약점 피해, 각성 정산 공격, 콤보·히트스톱·화면 흔들림·별자리 배율을 연결했다.
- 충돌 물리는 최대 3개의 90Hz 서브스텝만 사용하고, 지속 이펙트 갱신은 렌더 프레임당 한 번만 수행한다. 정적 전장 레이어 캐시, 60Hz 궤적 샘플링, 변화 기반 HUD 갱신, 혼잡 시 장식만 줄이는 VFX 예산으로 충돌 스파이크를 낮췄다.
- 공명은 작은 각성 링·짧은 히트스톱·접점 VFX만 쓴다. 샷 종료 각성 정산은 별지기 한 명씩 초점을 잡되(0.52초 뒤 시작, 1.62초 간격), 별자리가 뜬 샷에서는 현현이 끝난 1.36초 뒤부터 줄을 서 둘이 겹치지 않는다. 별자리 명중은 전체 HUD 대신 보스 체력만 갱신한다.
- 런타임은 소유자별 ordered classic script와 표현 전용 레이어로 분리돼 있다. `game-runtime.js`가 동결된 공개 API와 검증된 수명주기 훅을 제공하며, 새 전역 함수 덮어쓰기는 스모크 검사에서 금지한다. 2026-08-23 `npm run verify`는 HTML·CSS·게임플레이 JS를 포함한 **25개 런타임 파일**을 검증했다. 정확한 파일 책임과 로드 순서는 `prototypes/ARCHITECTURE.md`를 단일 기준으로 삼는다.
- 전장 바닥은 절차 생성 Observatory Ground 패스로 교체했다. 스테이지마다 바닥 슬래브·천문 각인·보스 균열·양극 조명·벽을 한 장의 캔버스에 캐시하며, `game-arena-carve.js`는 `render` 모듈에 전장 렌더러를 설치하는 표현 전용 레이어다. 물리·충돌·밸런스는 바꾸지 않는다.
- 공허 거상 보스의 idle/hit 애니메이션, 전장 타일, 픽셀 UI, Galmuri 글꼴, 처치 연출, 메타 허브/튜토리얼/설정/업적 화면을 반입했다.
- `무한 훈련장`을 추가했다. 보스 체력은 무한이며 유성 5개를 모두 쓰면 자동 보충된다. 기록 제출용 스테이지가 아니라 물리·배율·능력 QA용이다. 허브 지도 아래 `무한 훈련장` 버튼으로 진입하고, 이곳만 파티가 4명이며 거상이 판 정중앙에 선다. 기믹은 하나도 두지 않는다.
- 모든 전투에는 별자리 도형 연출이 있다. **별지기와 부딪히면 자동으로 공명이 일어나** 그 접점이 별빛 노드가 된다(2026-08-19). 점 개수가 정확히 맞는 표에서 3점 양자리, 4점 화살자리·까마귀자리, 5점 카시오페이아·백조자리·오망성, 6점 오리온자리, 7점 북두칠성 중 하나로 발동하며 8점 이상은 표가 없어 이름 없는 도형이 된다. 실루엣 7종은 접점 선이 뼈대로 교정된 뒤 나타나고 오망성은 코드 전용 연출을 쓴다. **8종의 능력은 모두 갈라져 있다** — 포위 피해·관통·약점 표식·껍질 파괴·비행·전원 각성·삼연격·유성 +1. 캐스트 시네마틱은 `game-figure-cinematics.js`가 그린다.
- 기믹은 스테이지 데이터의 `gimmicks`에서 생성된다. 공명 범퍼·반사 벽·가속 발판·공허 잔재에 더해 굳은 껍질(피해 n회 무시), 흐린 발판(배율 하락), 도는 방벽(체력을 갖고 보스를 공전), 페이즈 전환(모서리 밀어내기 / 재수면)이 있다. 보스 피해는 전부 `applyBossHit()` 한 곳을 지난다.
- 비전투 화면과 숨겨진 탭에서는 전투 시뮬레이션·Canvas 렌더를 건너뛰고, HUD·SFX·짧은 이펙트 배열·조준 계산의 반복 할당을 줄였다.
- 골드는 클리어 100(보상함 적립 후 수령), 업적 일회성 보상, 출석 보상(하루 한 번, 연속 60~220)에서 나온다. 쓰는 곳은 `별빛 소환`(100), 유성 도색(500), 별지기 도색(320)이며 전부 외형 전용이다. 유료 재화·확률형 중복 뽑기는 만들지 않았다.
- 업적 좌측에 `도서관` 탭이 있다. 별지기 8명 도감과 별자리 배율 조건표를 보여 준다.

## 2026-08-14에 더해진 것

- **최종 스테이지 8-1 `관측되지 않은 점`.** 일곱 별자리 34스테이지 뒤에 여덟 번째 월드 `outside`가 스테이지 하나로 붙는다. 1600 HP·4인 파티·별자리 없음(`shape: []`). 체력 70/40/15%에서 **형태만** 4단으로 바뀌며(다리 손실·기울기·동공 확장) 전투 판정은 그대로다. 기존 순차 해금이 그대로 7-7 클리어를 조건으로 만들어 저장 형식은 안 바뀌었다.
- **타이틀 인트로 6비트 9.8초.** 정적 → 관측 이상 → 통과 → 눈이 멈춤 → **관측창을 붙잡고 흔듦** → 점등. 발톱 하나만 `main` 앞으로 오고 흔들리는 것은 중앙 관측창뿐이다. `게임 시작!`은 첫 프레임부터 눌린다. 모션 감소에서는 붙잡는 비트를 통째로 뺀다.
- 인트로의 개체와 8-1 보스는 **같은 몸**이고, `prototypes/boss-art.js`가 절차적 픽셀로 둘 다 그린다. 래스터 반입 없음.
- **승리 판정이 프레임 시계로 옮겨졌다.** 예전에는 `setTimeout(2550)`이 판정을 쥐고 있어 승리 컷 도중 일시정지해도 타이머가 흘렀고, macOS 헤드리스에서는 그 타이머가 아예 죽어 E2E가 멈췄다. 이제 `battle.victory.t`가 `d`에 닿을 때 `afterFeedbackUpdate`에서 판정한다.
- **하니스에 `chain` 조준 정책.** 기존 정책은 한 샷에 접점 2개를 못 넘어 별자리(최소 3개)를 한 번도 발동하지 못했다. `contact` 33.3% / `chain` 91.7%다. `bot/latest-report.json`의 `summary.clearRate`는 세 정책을 뭉친 값이라 **그대로 인용하면 안 되고** `summary.byPolicy`를 쓴다.
- **성능 기준선 실측**: 게임플레이 갱신 19µs, `draw()` 141µs(1-1)·100µs(8-1). 프레임 예산의 약 1%라 최적화할 문제가 없다.
- 죽은 선언 5개(65줄) 제거.

### 당시 알려진 미해결 (2026-08-14 스냅샷)

> 아래는 8월 14일 시점의 이력이다. 보스 아트 배정과 8-1 엔딩은 이후 해결됐고, 현재 미해결 목록은 아래 `2026-08-23 현재 남은 일`을 따른다.

- **34스테이지가 전부 같은 보스로 보인다.** `bossArtFor(slug)`는 완성돼 있는데 호출부가 없어 언제나 `boss2/void-colossus.png`를 그린다. 월드→슬러그 배정은 아트 결정이라 미착수. 상세는 [UI_REVIEW_2026_08_14.md](UI_REVIEW_2026_08_14.md) 2-4-a.
- **픽셀 아트가 거의 전부 비정수 배율.** 전장 캔버스가 720×900을 0.689배로 표시한다. 같은 문서 1절.
- 8-1 클리어 보상인 엔딩 화면, 별자리 고점 배경 반응은 미구현.
- 모바일 비율은 이번 검토에서 전혀 보지 않았다.

## 2026-08-15~16에 더해진 것

- **8-1 엔딩 화면.** 최종 스테이지 클리어가 일반 승리 카드 대신 전용 카드를 띄운다. 존재는 끝까지 이름이 없고 좌표만 남으며, 유일한 따뜻한 색은 「돌아온 별의 수」에 쓰는 살구빛이다. 재도전 시에는 다른 문구가 나온다.
- **디자인 세션 §2~§11 반입.** 월드 팔레트와 스테이지 지도 아트, 타격의 크기·방향, 화자 넷을 채널 넷으로 가른 대사 체계(`game-speech.js`), 거상의 포효·밀어내기·등장·죽음, 여백을 가로지르는 로켓·UFO·외계인, 안내별 구분, 별 하나를 남기는 인트로. 각성 연출은 `game-awaken-fx.js`로 분리했다.
- **여백 하늘 앰비언스** `sky-ambience.js`.
- **렉의 원인은 캔버스가 아니라 합성이었다.** 온보딩 화면의 합성 레이어 117개 → **44개**(`#dawn-sky` 자식 244 → 13). 별밭을 요소 160개에서 정적 캔버스 한 장으로 바꾼 결과다. 반짝임은 부분 갱신으로 되찾았다.
- **조작**: 조준의 5도 스냅 제거, 샷 위력 범위 부여, 수업 패링 샷의 축 잠김 해소(최대 |vx|/speed 0.0000 → 0.9314).
- **난이도 계측기**: `runCampaignStage`의 `bossHpOverride`로 판의 «역량»을 잰다. 34스테이지에서 체력은 봇 역량 중앙값의 0.22~0.77배이고 8-1만 2.43배다. 역량이 7스테이지 주기로 반복되는데 체력은 선형 상승이라 두 곡선이 서로를 모른다. **체력 값은 바꾸지 않았다** — 표와 결정 사항 셋은 `BOT_REPORT` 0-4.
- 수명주기 누수 아홉 건, 별자리 정산의 `battleComplete` 가드와 보상 대상 유성 오류, 수령 골드 파괴·세이브 복구.
- 소리: 반입 후 한 번도 울린 적 없던 `ui-01`~`ui-05`를 실제 사건에 배정했다. 새 자산은 만들지 않았다.

### 새로 생긴 계측 도구

미리보기 창은 `document.hidden`이 true라 `requestAnimationFrame`이 안 돌고 뷰포트가 0×0으로 읽힌다. 그래서 화면·프레임 판단은 아래 프로브로 한다. 어느 것도 게이트에 물려 있지 않은 수동 도구다.

| 스크립트                        | 재는 것                                        |
| ------------------------------- | ---------------------------------------------- |
| `scripts/profile-frames.mjs`    | 실제 Chromium의 프레임 시간과 합성 레이어 구조 |
| `scripts/probe-settle-cost.mjs` | 정산 구간의 프레임 비용                        |
| `scripts/probe-aim-polygon.mjs` | 조준 화면·정산 프레임의 그리기 비용            |
| `scripts/probe-sky-guests.mjs`  | 여백 손님의 배치 좌표                          |

## 2026-08-19에 바뀐 것 — 전투 시스템 실험

> 이 절은 실험 당시의 변경 이력이다. `WALL_STARS` 공급 문제와 정지 화면만 본 검증 한계는 이후 해결됐으며, 현재 규칙은 바로 아래 절을 따른다.

**세 상수로 켜고 끈다.** `AUTO_PARRY`·`NODE_ECONOMY`(`game-figure-recognition.js`), `AIM_MODE`·`WALL_STARS`(`game-combat.js`). 셋 다 끄면 이전 전투가 그대로 돌아온다. 상세와 근거는 `PROJECT_CONTEXT.md`, 측정은 `BOT_REPORT.md` 0-11·0-12절.

- **공명이 자동이 됐다.** `Space` 타이밍 시험을 없앴다 — 실측으로 한 판이 주는 기회가 15회인데 필요한 노드는 3개였고, 연속 접점의 47%가 한 번 누름으로 덮였다. 가속은 그대로 남겨 판이 죽지 않는다(공명을 아예 없애면 피해 용량 482→297).
- **별빛이 자원이 됐다.** 샷이 끝나도 별자리가 자동으로 안 터진다. 모인 별빛이 판에 남아 조준과 별자리가 그것을 두고 경쟁한다.
- **조준이 노드 찍기가 됐다 — 별지기 셋도 노드다(2026-08-20, `AIM_STAR.minPick`·`unitNodes`).** 좌클릭으로 별지기·별빛을 **셋 이상** 고르고 `Space`로 쏜다. 방향은 무게중심, 세기는 퍼진 정도. 하한 3은 1픽 확정 레이(보스행 공짜 저격)를 구조적으로 없애기 위한 것이고, 별지기 3명이 노드라 «셋이 안 돼 못 쏘는 판»이 없다. 별지기 노드는 별자리로 타지 않는다. 검증은 `scripts/probe-aim-nodes.mjs` — 0~2픽 발사 차단, 방향·위력 오차 0, 별지기 3+별빛 4 판에서 조합 15→99.
- **약점 타격이 보스 둘레에 별빛 둘을 떨군다.** 난수 없이 황금각으로 흩뿌려 재현된다.
- **별자리 캐스트 시네마틱을 반입했다** (`game-figure-cinematics.js`, 993줄). 판정·피해는 기존 `FIGURE_ABILITIES` 그대로다.
- **물리 하나: 접점 되감기.** 충돌 법선을 «겹친 자리»가 아니라 «닿은 자리»에서 잡는다. 튕김이 뒤로 갈수록 어긋나던 것이 6접촉에서 62.7°→20.7°로 잡혔고, 캠페인 클리어율은 83→83.2%로 움직이지 않았다.

**지금 막혀 있는 것:** `WALL_STARS`가 꺼져 있어 별빛 풀이 중앙 1개다. 그 상태에서는 실측상 **별자리가 판당 0.00회** — 위 별자리 8종과 시네마틱 전체가 게임에서 볼 일이 거의 없다. 공급을 어디서 채울지가 다음 결정이다.

**아직 못 한 검증:** 이 세션의 화면 확인은 전부 `draw()`를 직접 부른 정지 화면이다. 미리보기 탭이 `document.hidden`이라 rAF가 멈춰 있어, 맥동·시네마틱·드래그 같은 «움직이는» 것은 사람이 직접 쳐 봐야 한다.

## 실제 전투 규칙

- 일반 캠페인은 기본 5발이며 보스 체력은 1-1의 120에서 7-7의 380까지 스테이지 데이터가 정한다. 8-1은 1600이고, `RULES.coreHp` 260은 스테이지가 값을 주지 않을 때의 기본값이다. 강화 `예비 탄창`은 발사 수를 +1 한다.
- 유성이 멈추면 배치된 별지기와 판에 남은 별빛 노드 중 **3개 이상**을 고른다. 좌클릭은 선택·해제, 우클릭은 전체 해제, 빈 곳 좌클릭은 반대편 모드 토글이며 `Space`가 발사다.
- 발사 방향은 고른 노드의 무게중심, 세기는 중심에서 노드까지의 평균 거리다. 별지기 노드는 발사 선택지이지만 별자리 재료로 타지 않는다. 선택지가 3개 미만인 판만 드래그 발사로 폴백한다.
- 이동 중에는 발사당 합산 1회만 좌클릭으로 진행 좌측, 우클릭으로 진행 우측에 운동량을 더한다. 이 조향은 발사를 대신하지 않는다.
- 별지기와 부딪히면 자동으로 공명해 접점에 별빛 노드를 남긴다. 예전의 `Space` 타이밍 패링은 없으며, 공명으로 남은 별빛은 조준과 3~7점 별자리 재료로 같이 쓴다.
- 약점은 몸체보다 높은 피해 배율을 받고 보스 둘레에 별빛 둘을 떨군다. 별지기를 한 번도 건드리지 않은 유성의 보스 직격은 감쇠하며, 각성·별자리·유닛 능력에는 그 감쇠를 적용하지 않는다.
- 배율: 유성의 보스 직격, 자동 공명, 벽 반사 등이 `CONSTELLATION`을 올린다. 배율은 별자리 계통의 값이라 각성만으로는 오르지 않는다.
- 훈련장: 전투 종료/기록 제출 없이 계속 재장전된다. `R` 키는 훈련장 전용 단축키가 아니라 어디서든 편성 화면을 여는 키다.
- 별자리: 공명 접점에서 선이 1.4초 그려지고 0.8초 동안 뼈대에 맞춰진다. 0.35초 뒤 3~7점 실루엣이 나타나며, 현현 시작 0.3초 뒤(발동 약 2.85초)에 포위 피해가 적용된다. 연출은 총 5.6초지만 다음 발사는 막지 않는다.

## 로스터와 역할

| 유닛   | 규칙                                                                       |
| ------ | -------------------------------------------------------------------------- |
| 샛별   | 각성해 멈춘 자리에서 보스까지 검기를 발사하며 가까울수록 강해짐            |
| 미리내 | 각성해 멈춘 자리에서 보스와 멀수록 강해지는 화살 저격                      |
| 별하   | 첫 충돌 시 이번 샷의 유성을 하나 더 분열                                   |
| 살별   | 충돌 시 가장 가까운 다른 유닛으로 강제 중계                                |
| 윤슬   | 정산 공격이 없고, 각성하면 보스·별지기를 관통하며 속도 비례 회전 칼날을 켬 |
| 달무리 | 유성에 닿으면 기본 공명으로 함께 가속하며 별도의 고유 공격은 없음          |
| 모루   | 각성해 멈춘 자리에서 이번 샷의 충돌 수에 비례한 범위 충격파                |
| 그믐   | 마지막으로 충돌한 아군의 능력을 복제                                       |

## 최근 커밋 흐름

- `e0d2847` ~ `f21d3dc`: 유닛 이펙트, 충돌 운동량, 전체 속도 기준선 정비
- `4e29e4d` ~ `6625f7f`: 처치 연출, 스테이지 선택, 메타 허브·튜토리얼·설정/업적
- `f34d455` ~ `80b22e0`: 능력 이펙트 강화, 유닛 충돌 피해, 공허 거상, 분열 룬볼 물리
- `b67e635` ~ `93d4476`: 배율 UI, 전장 타일, Galmuri 게임 글꼴
- `87c38d4`: 무한 체력 훈련장
- `019b936`: 스테이지 데이터 기반 반사 벽·가속 발판·잡몹
- `2e22fda` · `e6391e9`: 화면 수명주기·공용 UI 분리와 런타임 최적화

## 2026-08-23 현재 남은 일

> 8월 19일에 남겼던 별빛 공급·온보딩 재작성·캠페인 기믹 배치는 이후 해결됐다. 현재 현황은 `PROJECT_CONTEXT.md`의 실측 기준선과 8월 23일 출시 준비 패스를 합쳐 정리했다.

1. **오너 결정 2건.** 다중 탭 저장 충돌(`MULTI_TAB_SAVE_2026_08_23.md`)과 폭 920px 이하 편성 트레이의 가로 스크롤을 플레이어가 알아채게 할지가 열려 있다. 도는 방벽은 2026-08-23 오너 승인으로 최대 내구도 부활과 재점화 신호를 채택했다.
2. **밸런스 기준을 정한다.** 최악의 손으로도 캠페인 34판은 전부 클리어되고 8-1만 실패한다(`CLEAR_FLOOR_2026_08_22.md`). 체력만 올리지 말고 목표 클리어율·원샷 희귀도·샷 길이를 먼저 확정해야 한다.
3. **봇과 사람의 조준 계약을 맞춘다.** 현재 봇은 임의 각도를 쓰지만 사람은 노드 무게중심으로만 쏠 수 있다. 지금 `BOT_REPORT.md`의 이전 수치는 탐색 이력이며 제출용 난이도 수치로 인용하지 않는다.
4. **외부 플레이와 실기 화면을 남긴다.** CDP 프로브로 프레임·정산·기믹·키보드 경로를 재했지만, 다른 실기기의 가독성·체감·성능 기록은 아직 남아 있다.
5. **Hive 실통신을 완료한다.** 클라이언트 인터페이스는 있지만 Console AppID·서버 어댑터를 통한 로그인, 저장, 리더보드 왕복은 완료되지 않았다.

### 검수 환경에서 알아 둘 것

- 헤드리스·숨은 탭에서는 `document.visibilityState`가 `hidden`이라 렌더 루프가 대기한다. **캔버스가 검게 나오는 것은 버그가 아니며**, `draw()`를 직접 부르면 정상 렌더된다. `getBoundingClientRect()`가 전부 0을 반환하는 것도 같은 원인이라 **합성 포인터 이벤트로 조작을 몰 수 없다** — 좌표 변환이 `null`을 돌려준다.
- 보스 피해는 전부 `applyBossHit()` 한 곳을 지난다. 새 피해 경로에서 `boss.hp`를 직접 깎으면 굳은 껍질과 페이즈 전환이 새므로 반드시 이 함수를 쓴다.
- 하니스에 새 런타임 파일을 넣기 전에 `Math.random`을 grep한다. 시드 재현성이 「난수를 한 곳에서만 쓴다」는 전제 위에 있다.

## 확인된 주의점과 다음 작업

1. **전투 밸런스** — 현재 충돌 최소 운동량·별자리 배율·각성 공격이 한 발에 겹치면 원샷 또는 과도한 체공이 될 수 있다. 현재 당구 물리·패링·별자리·즉시 능력을 직접 구동하는 기믹 없는 하니스 기준선을 만들었으니, 다음에는 파티 조합·배치·경로를 넓혀 목표 클리어율 60~70%·원샷 희귀·짧은 샷 지속시간을 함께 스윕한다. 체력만 올리지 않는다.
2. **자동 검증 갱신** — `bot/run-bot.mjs`는 2·3·4인 고정 파티, 5발, 보스 체력 180·260·340·420, 직접/패링 경로를 96건 실행해 `bot/latest-report.json`을 쓴다. 아직 캠페인 기믹·파티 조합 전수·브라우저 포인터 체감은 범위 밖이다.
3. **스테이지 확장 규칙** — 양자리 `1-2`·`1-3`의 관측 잔광은 첫 성공 패링을 3점 별자리 체험으로 연결하는 1회 보조 기믹이다. 이후 기믹을 더할 때는 새 기믹을 소개하는 스테이지에 그 기믹만 두고, 그 외에도 최대 두 종류까지만 둔다. 무한 훈련장에는 기믹을 두지 않는다.
4. **검증 스크립트 기준선** — 공허 거상·전용 약점·별자리 실루엣·전장 바닥 패스 기준으로 갱신했다. `scripts/verify-evidence.mjs`는 이제 `game-figure.js`, `game-arena-carve.js`, 실루엣 5종을 런타임·매니페스트 검사에 포함한다. 이후 보스/핵심 에셋/런타임 패스를 바꾸면 이 스크립트와 `assets/ASSET_MANIFEST.json`도 같은 커밋에서 맞춘다.
5. **Hive 실통신** — 클라이언트 인터페이스는 연결돼 있으나 Console AppID/서버 어댑터를 통한 로그인·저장·리더보드 왕복은 아직 완료가 아니다.
6. **외부 플레이 기록** — 다른 기기에서 첫 샷·다음 샷·유닛 각성·훈련장 재장전을 확인하고, 짧은 영상/스크린샷을 남긴다.
7. **크로스플랫폼 작업** — `.gitattributes`, `scripts/check-portability.mjs`, Windows CI를 추가했다. 새 에셋/코드를 반입한 뒤에는 `node scripts/verify-evidence.mjs`를 실행하고, 필요한 파일이 반드시 Git 추적 상태인지 확인한다.

## 다음 세션 시작

Node.js 20 이상이 설치된 터미널에서 저장소 루트로 이동한 뒤 실행한다. 외부 npm 의존성이 없어 `npm install`은 필요하지 않다.

```sh
npm run verify
npm run smoke
npm run serve
```

`http://127.0.0.1:4173/`에서 메인 CTA, 1-1 수업의 네 단계, 노드 3개 이상 선택 후 `Space` 발사, 이동 중 좌·우 1회 조향, `1-2`·`1-3` 안내별 소모, 캠페인 기믹을 순서대로 확인한다. 화면·동작 질문은 `AGENTS.md`에 정리된 CDP 프로브를 같이 선택한다. 수정 후에는 `git status --short`로 새 에셋과 사용자 변경을 확인한다.

## 2026-08-23 출시 준비 패스에서 고친 것

한 줄 요약: **출시 직전 관점으로 훑어 데이터 손실 둘과 화면 결함 둘을 고쳤고, 계측 도구 셋의 거짓말을 바로잡았다.** 자세한 근거는 각 커밋 본문에 있다.

- **잘 친 판이 최고 기록을 지웠다.** 되돌림 능력이 죽이는 샷에 붙으면 `shotsUsed`가 0이 되고 로더가 그 값을 거부해 `bestShots`가 99(기록 없음)로 돌아갔다. 쓰기에 하한 1을 걸었다(`scripts/probe-best-shots.mjs`).
- **관측 잔광이 표시는 ×1, 동작은 ×∞였다.** 차감 코드가 노드 경제 도입 뒤 도달 불능이 돼 있었다. 값을 치르는 시점을 「밝힌 순간」으로 옮겼다.
- **전투 안내 줄(`#tip`)이 타이틀·허브에 떠 있었다.** 허브에서는 주 버튼 「관측」 아래를 덮었다. `body.game-mode`로 한정했다.
- **도서관이 낮은 창에서 108px 넘쳤다.** 접는 조건이 창 너비만 보는데 실제 좁힘의 원인은 높이였다(`80vh` 기반). 1366×768 노트북에서도 났다.
- **지도에서 고른 칸의 번호가 반 잘렸다.** 고른 칸만 별이 커지는데 칸 높이는 고정이었다.
- **수업 재시청이 주지도 않은 소환권을 약속했다.** 「1분 튜토리얼」로 들어가면 프롤로그 표식이 안 남아 나중에 수업이 1/6부터 되돌아가기도 했다. 둘 다 고쳤다.
- **「첫 실행 상태로」가 인트로 표식을 반만 지웠다**(sessionStorage만, localStorage 사본이 남음).
- **거상 시트 20장이 매핑만 되고 안 그려졌다.** 포효에 `attack`, 퇴장에 `death`를 이었다.
- 계측 쪽 — clearability 프로브가 회차마다 다른 답을 냈고(기동 경합 + 정책 특성), 배치 프로브가 가로 넘침을 전혀 안 봤으며, perfwatch가 잰 값을 매 프레임 버렸다. 셋 다 고치고 회귀 프로브를 남겼다.

**2026-08-23 UI 전수 확인에서 추가로 고친 것.** 낮고 넓은 창에서 `main`의
폭을 `80dvh`로 제한하던 규칙 때문에 920×512 화면의 실제 콘텐츠 폭이 약
340px까지 줄어들었다. 그 결과 허브·스테이지 선택·편성·배치·업적·도서관·
설정 일곱 화면이 가로로 넘치거나 버튼을 화면 밖으로 밀었다. 높이 620px
이하의 가로 창에서는 메타 화면이 가용 뷰포트 폭을 쓰게 바꾸고, 고정 비율
캔버스 대신 스크롤 가능한 메타 스테이지가 높이를 소유하게 했다.

- 편성 트레이는 초상을 더 줄이지 않고 8명을 모두 도달 가능한 폭에 둔다.
- 허브 지도 노드는 Bayer명과 번호만 남겨 끝 노드의 이름 잘림을 없앴다.
- 업적과 도서관은 안쪽·바깥쪽 스크롤이 겹치지 않도록 한 축으로 정리했다.
- 승리 카드의 업적 알림은 결과 흐름 안의 작은 상태 바로 바꾸고, 전투 대사와
  조작 힌트가 결과 화면에 남지 않게 했다.

**같은 날 실화면 연출 재검수에서 추가로 고친 것.** 정지 화면 취향이 아니라
`probe-summon`·`probe-gimmick-stages`·`probe-settlement`이 만든 실제 Chromium
캡처를 기준으로 소환·월드·프로필·업적·승패 화면을 다시 비교했다.

- 소환 보상 초상에 별 아이콘용 `:first-child` 규칙이 겹쳐 32px 빈 칸처럼
  보이던 충돌을 고쳤다. 거의 정지해 보이던 10초 의식은 같은 5비트를 6.8초로
  압축하고 현현 플래시를 보강했다. 보유 인원 증가와 100골드 차감도 함께 잰다.
- 전장 바닥은 일곱 월드 색상환을 직접 사용한다. 슬래브·천문 각인·먼지와
  고정 계기판 테두리가 같은 월드 톤을 받아, 보스와 기믹뿐 아니라 장소 자체가
  바뀐다. 스테이지 선택 지도도 기존 별자리 실루엣의 대비를 올렸다.
- 프로필의 기록·순위·우편함과 업적 카드는 새 자산 없이 기존 절차 아이콘과
  황동·청록·장미색 표면을 나눠 반복감을 줄였다. 좁은 열의 표를 두 칸으로
  압축하는 시도는 1280×760 실측에서 한글이 쪼개져 되돌렸다.
- 일시정지·승패가 열리면 이전 조작 힌트, 전투 토스트, 루나 대사가 함께
  숨는다. 패배는 절차적 교차 셔터와 균열선으로 승리와 다른 종결 구도를 갖는다.

최종 실측은 `probe-meta-screens --sizes 1280,900 1280,760 920,512`의
**12화면 × 3크기 모두 배치 문제 0건**, `probe-settlement`의 승리·패배
**2결과 × 2크기 모두 배치 문제 0건**이다. `npm run check`도 검증·런타임
스모크·1-1 온보딩 E2E까지 통과했다.

**같은 날 전투 직관성 패스에서 추가로 고친 것.** 판 안에 설명을 더 쌓지 않고
좌측 전투 계기판에 `별빛 선택 → 발사 → 공명 → 각성 공격` 네 단계를 고정했다.
현재 단계만 금빛 마름모로 켜지고, 한 줄 안내는 선택 개수·조준 세기·진행 중인
정산을 실제 상태에서 계산한다. 조준선의 위력도 숫자만 보여 주던 것을
`약함/보통/강함 + 백분율`로 바꾸고, 캔버스 하단의 두 줄 설명은 현재 행동 한
줄로 줄였다.

- 반사 벽은 조준선이 닿을 때 충돌점과 점선 반사 경로를, 가속 발판은 통과 뒤
  방향 갈매기표를 미리 그린다. 도는 방벽은 0.55초 뒤 위치를 낮은 알파의
  잔상으로 보여 주고, 페이즈 임계 체력 10% 전에는 거상 둘레에 경고 고리를
  켠다. 굳은 껍질은 기존 남은 겹 링과 파편 표현을 유지한다.
- 공명은 청록 원형 파동, 각성은 별지기 색의 네 갈래 마름모, 별자리 배율은
  금빛 오각 연결선, 약점은 금빛 다중 링과 뾰족한 핵으로 갈라 같은 크기의
  원형 섬광처럼 보이지 않게 했다.
- 전투별 약점 명중·벽/쿠션·도는 방벽·가속 발판·노드 조준 위력을 기록한다.
  패배 카드의 `다음 관측`은 이 기록에서 우선순위를 골라 약점, 방벽 타이밍,
  노드 벌림, 반사 경로 중 하나를 구체적으로 제안한다.
- 920×512 낮은 가로 화면에서 전투 열이 위로 39px 잘리던 것을 실측해 이
  구간의 캔버스 폭을 보정했다. 루나 대사는 넓은 화면에서는 전장 왼쪽의 빈
  공간으로 옮기고, 좁은 화면에서는 뷰포트 안쪽에 고정해 문장 하단이 잘리지
  않게 했다.

검증은 `probe-aim-nodes`의 조준 규칙·방향/위력 오차 0, `probe-gimmick-stages`의
기믹 8개 스테이지와 콘솔 오류 0, `probe-settlement`의 승패 2결과 × 2크기 배치
정상으로 확인했다. 전용 래스터·아이콘·SFX를 만들지 않았으므로 이번 패스에는
새 자산 반입이나 `ASSET_BACKLOG.md` 상태 변경이 없다.

**같은 날 방벽·하늘 반응 패스에서 추가로 고친 것.** 도는 방벽은 격파 후
1.4초가 지나면 최대 내구도로 재점화하며 팝업·토스트로 복귀를 알린다.
하늘은 5점 이상 별자리 현현과 세션 첫 보스 처치에 전용 투명 픽셀
오버레이로 반응한다. 중앙 게임 컬럼은 비워 조작과 읽기를 가리지 않는다.

**결정 대기 하나** — 코드로 밀지 않았다: [MULTI_TAB_SAVE_2026_08_23.md](MULTI_TAB_SAVE_2026_08_23.md)(탭 두 개가 서로의 진행을 지운다). 도는 방벽은 권고안 A를 채택해 부활 시 최대 HP를 복구하고 재점화 팝업·토스트를 낸다.

## 다음 세션의 작업 규칙

- 현재 코어는 당구형이다. 사용자의 명시적 결정 없이는 핀볼/슬링샷/빙의 시안으로 되돌리지 않는다.
- 전체 8인 로스터, 시작 3인(샛별·미리내·윤슬), 기본 5발을 문서·UI·검증에서 같은 기준으로 쓴다.
- `무한 훈련장`은 QA 전용이며 순위/밸런스 통계에 섞지 않는다.
- Hive의 실제 왕복 전에는 “연동 완료”라고 쓰지 않는다.
- 새 기능 하나마다 `DEVLOG.md`에 Codex 작업, 막힌 점, 사람의 판단, 커밋을 남긴다.
