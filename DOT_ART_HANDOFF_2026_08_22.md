# 도트 작화 지시서 — 판 위에서 그림이 아예 없는 자리 여섯

> 작성일 2026-08-22 · 「도트를 더 찍자」 지시에 대한 **작화 세션용 인계서**.
>
> 이 문서를 읽는 쪽이 그림을 그린다. 이 저장소는 그 결과를 받아서 붙인다.
> 그래서 여기 적힌 것은 이 저장소의 할 일 목록이 «아니다» — 무엇을 어떤
> 격자·크기·프레임·색으로 그려 주시면 되는지, 그리고 그것이 어떤 모양으로
> 돌아와야 저장소에 그대로 들어가는지를 적은 것이다.
>
> 관련: [VISUAL_UPGRADE](VISUAL_UPGRADE_2026_08_22.md) (이 문서가 따라 나온
> 요구서) · [BOARD_TEXT](BOARD_TEXT_2026_08_22.md) ·
> [DESIGN_REQUEST](DESIGN_REQUEST_2026_08_22.md) · [UI_KIT_DAWN](UI_KIT_DAWN.md) ·
> [ASSET_BACKLOG](ASSET_BACKLOG.md) · [AGENTS](AGENTS.md)

## 1. 이 문서는 무엇인가

### 1-1. 오가는 방식

같은 방식으로 패치가 이미 두 번 돌아왔다.

| 무엇                 | 커밋      | 돌아온 모양                            |
| -------------------- | --------- | -------------------------------------- |
| 기믹 도트 리스킨     | `d8b0c27` | 번호 매긴 「이대로 적용」 쪽지 + 파일  |
| 시각 업그레이드 일괄 | `5d07beb` | 같은 형식 (「코드만」 — 새 래스터 0장) |

형식이 이미 정해져 있다는 뜻이다. **번호 매긴 적용 쪽지 + 파일**로 주시면
된다. 쪽지의 각 항목은 「어느 파일의 어디에 무엇을 넣는다」 한 줄이면 충분하다.

### 1-2. 이미 출고된 것 — 다시 그리지 않으셔도 된다

- **기믹 범례 아이콘 여덟 장.** 16×16 PNG, 셀 2px, 논리 8×8.
  `scripts/generate_dot_icons.py` 가 굽는다(손으로 찍은 파일이 아니다).
  출력 `assets/library/icons/gimmick/{wall,boost,drag,add,orbit,shield,roar,sleep}.png`,
  매니페스트 `assets/ASSET_MANIFEST.json:538-554`,
  소비 `prototypes/js/game-combat-physics.js:1197-1216`.
  아직 스프라이트가 안 왔을 때를 위한 벡터 폴백이 `:1217` 이하에 남아 있다.
- **도트 기믹 리스킨.** `prototypes/stella-ball-dot-gimmicks.js` 한 파일.
  반사 벽 `:69-188` · 가속 발판 `:191-255` · 흐린 발판 `:258-320` ·
  공명 범퍼 `:323-379` · 도는 방벽 `:382-466` · 굳은 껍질 `:472-555` ·
  관측점 표석 `:558-619`(소비자 없음, `bumpers` 복원 대기).
  소비자는 `prototypes/js/game-combat.js:223-260` 의 위임 한 줄씩이다.
- **UI 프레임 재사용.** `library/ui/panel-frame.png` · `hpbar-frame.png` +
  `hpbar-fill.png` 를 9-slice 로 붙였다. `prototypes/stella-ball-upgrade.css`.
  새 래스터 0장이라 일괄 제작 승인 대상이 아니었다
  (`ASSET_BACKLOG.md:46-58`, 상태 `반입 완료`).
- **격자 「한 줄」 수정 둘.** 거상 배율 `0.48 → 0.5`(`game-data.js:1629`),
  유성 표시 `31 → 32`(`game-core-render.js:1479`). §2가 셀 2px을 못 박을 수
  있게 된 근거가 이 둘이다.

이 넷은 손대지 않으셔도 된다. 다만 §3의 집 규칙과 팔레트는 전부 이 넷에서
나온 값이라 그대로 따라 주시면 된다.

## 2. 먼저 못 박은 것 — 격자

### 2-1. 규칙

> **판 위 도트 셀 = 2px.**
> 새 그림의 한 논리 픽셀은 판 좌표에서 2×2 정사각형이다.

### 2-2. 근거

- 도트 킷이 `const CELL = 2`(`stella-ball-dot-gimmicks.js:16`)이고, 물리에서
  온 실수 좌표를 전부 `snap(v) = Math.round(v / CELL) * CELL`(`:34`)로 끊는다.
  회전 변환을 쓰지 않고 곡면은 호를 셀로 직접 쌓는다.
- **거상이 같은 셀에 선다.** 보스 팩 시트는 384×384 가로 4프레임에 셀 4px
  (`ASSET_MANIFEST.json` boss10 `note`: 「셀 4px, 논리 격자 48유닛, 중심
  x=24 접지선 y=44」). 표시 배율이 `scale: 0.5`(`game-data.js:1629`)이므로
  **원본 4px 블록이 판 위에서 정확히 2px**로 앉는다. 판 위에서 가장 큰
  그림과 가장 작은 그림이 지금 같은 셀 위에 있다.
- 범례 아이콘 PNG도 셀 2(`generate_dot_icons.py:23`)이고 정수 스냅으로
  그린다(`game-combat-physics.js:1214`).
- 유성 꼬리도 2px 격자로 스냅한다
  (`game-core-render.js:1178-1181`, `Math.round(ball.trail[i].x / 2) * 2`).

### 2-3. 불편한 사실 — 그래도 화면에서는 정수로 안 앉는다

숨기지 않고 적는다. [BOARD_TEXT](BOARD_TEXT_2026_08_22.md) §3·§5의 실측이다.

| 창        | 판 CSS 크기 | 백버퍼 720×900 대비 |
| --------- | ----------- | ------------------- |
| 1024×680  | 389×486     | **0.54배**          |
| 1280×900  | 565×706     | **0.78배**          |
| 1600×1050 | 685×856     | 0.95배              |
| 1920×1200 | 716×895     | 0.99배              |
| 2560×1440 | 716×895     | 0.99배 (상한)       |

- 백버퍼는 720×900 고정이고 리사이즈가 없다(`prototypes/prism-breakers.html:84-90`).
- 이 기기의 `devicePixelRatio` 는 1.5다. 1280×900에서 720 백버퍼가 847
  기기 픽셀이 되어 **1.18배 비정수 확대**를 한 번 더 통과한다. DPR을
  처리하는 코드는 없다(텔레메트리 한 줄뿐, `game-perfwatch.js:500`).
- 즉 **어떤 창에서도 2px 셀이 기기 픽셀 정수에 앉지 않는다.** 0.54배에서
  셀 하나는 화면 1.08px이다.

### 2-4. 그래서 작화 세션이 하실 일

- **논리 격자로만 그려 주시면 된다.** 판 좌표 1px = 설계 1px로 두고, 한 셀은
  2×2. 축소를 미리 보정하지 마시고, 홀수 두께나 1px 디테일을 쓰지 마신다.
- 최소 알아볼 수 있는 특징은 **셀 두 칸(판 4px)** 로 잡아 주시면 안전하다.
  0.54배를 곱하면 2.16 화면 px이다. 셀 한 칸짜리 특징은 1.08px이 되어
  행이 통째로 버려진다(`UI_REVIEW_2026_08_14.md:38` — 「축소는 확대보다
  나쁘다」).
- **판 배율·백버퍼·DPR 결정은 작화 세션 몫이 «아니다».**
  [VISUAL_UPGRADE](VISUAL_UPGRADE_2026_08_22.md) §8-1(목표 픽셀 스케일)과
  §8-5(백버퍼와 DPR)가 열려 있고, 그것은 오너 결정이다. §6에서 「손그림보다
  생성 스크립트」를 권하는 이유가 정확히 이것이다 — 그 결정이 바뀌어도
  스크립트는 `CELL` 한 줄이고 손그림은 다시 그리는 일이다.

## 3. 집 규칙

### 3-1. 도트 규칙 여덟 (킷 헤더 원문, `stella-ball-dot-gimmicks.js:9-12`)

> 셀 2px · 1셀 외곽선 · 상단 하이라이트 · 5단 밴드 + 체커 디더 · 하드 섀도 ·
> 셀 스냅 애니메이션(서브픽셀 이동 없음) · 곡면은 호를 셀로 직접 쌓는다
> (회전 변환 없음) · 캔버스 판정색과 `heroes[].col` 은 건드리지 않는다.

부연 넷.

- **하드 섀도**는 블러가 아니다. `#04080a` 를 알파 0.5로 4px 내린 사각형
  하나다(`:35-41`). DOM 쪽도 같은 색으로 `drop-shadow(0 4px 0 #04080a)`.
- **5단 밴드**란 «외곽선 1 + 톤 5»다. 반사 벽 평시 팔레트가 그 원형이다
  (`:101-108`): 외곽선 `#0e2028`, 그 안에 `#f2fbf9` → `#cfe6e8` → `#9dc2cf`
  → `#6f98a4` → `#446e7a`. 칠하는 규칙은 상단 3행 = 최상단 톤, 대각
  `diag > 0.22` = 2단, `> -0.05` = 3단, 그 아래 4단, 하단 3행 = 5단 체커.
  **밝은 정점 1 + 본색 3 + 그늘 1** 순서를 그대로 복제해 주시면 된다.
  16px 아이콘처럼 셀이 모자라면 4색(`b/h/d/o`)으로 줄인 선례가 이미 있다
  (`generate_dot_icons.py:28-37`).
- **난수 금지.** 킷 헤더 넷째 줄이 「난수 없음 — 애니메이션은 전부
  `frameClock` 에서 나온다(봇 하니스 결정론 유지)」다. `MAINTENANCE.md` 도
  같은 말을 한다: 하니스는 「난수는 `aimSigma` 한 곳」이라는 전제 위에 있고,
  그래서 `game-figure-cinematics.js` 가 하니스에서 빠져 있고
  `CINE.orionKnock` 이 꺼진 채 출고됐다.
- **글로우가 없다.** `combatFxBlur()` 가 무조건 0을 반환한다
  (`game-data.js:2016-2018`). 전투 패스의 모든 `shadowBlur` 호출이 실질적으로
  0이다. **빛은 그림 자기 픽셀 안에 담아 주셔야 한다** — 캔버스가 얹어 주지
  않는다.

### 3-2. 격자가 셋이다 — 어디에 속하는지 먼저 정한다

| 킷                                  | 단위     | 어디                 |
| ----------------------------------- | -------- | -------------------- |
| `stella-ball-dot-gimmicks.js:16`    | `CELL=2` | 캔버스(판) 위 전부   |
| `stella-ball-pixel-ui.js:9`         | `U=3`    | DOM 버튼 실루엣      |
| `stella-ball-pixel-ui.js` 의 `px()` | `s=4`    | 배경 데코 스프라이트 |

이 지시서가 부탁하는 것은 §4-1~4-4가 **`CELL=2`**, §4-5·4-6이 **DOM/캔버스
양쪽에 쓰이는 아이콘**이다. 아이콘은 `CELL=2` 로 굽고 표시 크기를 셀의
정수배로만 쓰는 것이 이미 출고된 방식이다.

### 3-3. 절대 재사용하면 안 되는 색 (LOAD-BEARING)

이 색들은 장식이 아니라 **판정**이다. 같은 색이 다른 자리에 또 나오면 규칙이
읽히지 않는다.

1. **별지기 고유색 여덟** — `#f2c56b` 샛별 · `#ef718d` 미리내 · `#70dce1` 별하
   · `#9ee477` 살별 · `#5fe0cf` 윤슬 · `#bca7ff` 달무리 · `#ffac67` 모루 ·
   `#9f83ff` 그믐 (`game-data.js:227,241,255,269,283,297,311,328`).
   변경 금지가 `UI_KIT_DAWN.md:12` 와 VISUAL_UPGRADE §3-1에 명문화돼 있다.
   근사색도 피해 주신다. (예: 윤슬 `#5fe0cf` 와 방벽 보석 `#4db8b3` 이 이미
   가깝다.)
2. **약점 살구 램프** `#b06a3d` / `#eea56f` / `#ffd2a0` + 무리 `#ffd2a044`.
   `game-core-render.js:1446` 위의 주석이 「이 화면에서 살구빛은 노릴 가치가
   있는 단 하나에 예약」이라고 선언한다(`:1444`).
3. **별빛 출처색 넷** `#ffb0d8`(약점) / `#9fd8ff`(쿠션) / `#ffd27f`(안내별) /
   `#ffd98e`(기본). 「이 별빛이 어디서 나왔나」를 색으로만 구분한다.
4. **고른 것 / 안 고른 것** `#ffffff` · `#ffffff88` · 별자리 후보 무리
   `#ffd27f55`.
5. **피해 등급** `#ffe59a`(약점) 대 `#e6f7ef`(몸체) — 팝업 색이 곧 배수
   1.7배 여부다(`game-combat.js:160`).
6. **남은 내구** 잔재 HP 핍 `#b578e8` / `#2b1a40`, 방벽 HP 핍 `#9adfc9` /
   `#1d3a40`.
7. **유성의 `moving` / `rest` 쌍** (`#d3e7cf`/`#e0b45a` 등) — 굴러가는
   중인지 멈췄는지.
8. **기믹 점등(hot) 대 평시** — 벽 `#fffbe8`, 발판 `#d7ffb4`, 범퍼 `#ffe9a8`,
   방벽 warm 램프. 「방금 나를 밀었다」가 그 색 변화 하나로만 읽힌다.

자유롭게 쓰셔도 되는 쪽(DECORATIVE): 기믹 몸통 밴드 전부, 하드 섀도
`#04080a`, 판 바닥·각인·글로우, 거상 `PAL` 6단, 표석 전체, UI 패널·탭 밴드.
명도 구조만 지키면 교체 가능하다.

### 3-4. 이미 알고 있는 충돌 셋 (되풀이하지 마시라는 뜻으로 적는다)

- `generate_dot_icons.py:35` 의 `roar` 가족이 `b=#ffd2a0` 을 쓴다. 위 2번의
  예약색이다. 벡터 폴백은 같은 아이콘에 `#ff9d6e` 를 썼는데
  (`game-combat-physics.js:1282`) PNG로 넘어오면서 예약색을 밟았다. **새 포효
  계열 그림은 `#ff9d6e` 쪽으로 잡아 주시면 된다.**
- `stella-ball-dot-gimmicks.js:615` 의 표석 점등 보석이 약점 램프와 «완전히
  동일»하다. 표석은 지금 소비자가 없어 화면에 안 나온다.
- `#9adfc9` 를 굳은 껍질(`SHIELD_L[1]`)과 도는 방벽 HP 핍·스터드가 공유한다.
  둘이 한 판에 같이 나오면 「남은 내구」가 구분되지 않는다. **새 궤도 계열
  그림은 `#9adfc9` 를 피하고 `#7cc6bb` 계열로.**

### 3-5. 한글 10px 규칙 — 그림에 글자를 넣지 않는다

> **Korean text below 10px is unreadable in this project.** Galmuri11 is drawn
> for 11px, so 8px labels turn to mush. — `prototypes/MAINTENANCE.md:106`

그런데 판은 이 규칙을 **어느 창에서도 지키지 못한다**. 소스의 10px이
1280×900에서 7.8px, 1024×680에서 5.4px로 그려진다
([BOARD_TEXT](BOARD_TEXT_2026_08_22.md) §4). 그래서:

- **새 스프라이트에 한글을 넣지 마신다.** 도트로 찍은 글자도 같은 축소를
  통과한다.
- 이름을 말해야 하는 자리는 **글자 대신 형태**로 간다. 기믹 아이콘이 그렇게
  풀렸다 — 「판에 실제로 그려지는 모양 그대로를 16px로 줄인 것」이라 이름을
  못 읽어도 짝이 지어진다(`DESIGN_REQUEST_2026_08_22.md:30-35`).
- 판 위 글자 크기 자체의 하한은 열린 결정이다(§9).

## 4. 찍어 주셨으면 하는 것

가치 순이다. 순서의 근거는 **「얼마나 자주 보이나 × 사건의 무게와 지금
그림의 격차」** 다. 각 항목에서 «실측»은 코드에서 읽은 값이고 «제안»은 이
문서가 계산해 붙인 값이다 — 제안은 바꾸셔도 된다. 실측은 코드가 그렇게
돼 있다는 뜻이다.

### 4-1. 공허 잔재의 죽음 — 지금은 그림이 «한 장도» 없다

- **무엇**: 잔재가 죽는 순간의 4프레임 시트 한 장.
- **지금은 어떻게 그려지나**: hp가 0이 되면 `a.down = 1.6` 이 서고
  (`game-session.js:1102`), 그 프레임부터 그리기 루프가
  `if (a.down > 0) continue;`(`game-core-render.js:1226`)로 통째로 건너뛴다.
  **잔재가 한 프레임에 사라진다.** 남는 것은 원 테두리 하나뿐이다 —
  `areaBursts` 에 `r:44, d:0.42` 를 밀어 넣고(`game-session.js:1104`)
  반지름 18→62px로 커지며 옅어진다(`game-core-render.js:1246-1257`).
  사체·조각·먼지·낙하 어느 것도 없다. 1.6초 뒤 같은 자리에 hp가 복구된 채로
  다시 나타나고 「공허 잔재 재생성」 토스트만 뜬다(`:1672-1683`).
- **판 위 크기(실측)**: 살아 있는 잔재는 **96×96 board px**로 그려진다
  (`drawAnimated(..., 96, frame)`, `game-core-render.js:1233`). 원본 시트는
  `assets/enemies/void-wisp-idle.png` / `-hit.png` 둘 다 **1024×256**
  (= 256px 셀 4프레임). 그림자 타원 24×7 at `(a.x, a.y+30)`. HP 3칸 바는
  칸 13×6 바탕에 11×4 채움, `a.y-44`. 충돌 반지름은 `r = 23`
  (`game-data.js:1570`) — 그림 96px과 판정 46px이 2배 넘게 어긋난다(§9).
- **프레임 수와 길이(실측)**: 살아 있을 때 4프레임 루프, 평시 155ms/프레임,
  피격 중 55ms/프레임(`game-core-render.js:1232`). 사망 이펙트는 링 하나가
  0.42초. `-death` 시트는 **없다**(`assets/enemies/` 에 `void-wisp.png`,
  `-idle.png`, `-hit.png` 셋뿐).
- **색**: 잔재 몸의 정본은 지금 스프라이트다. 폴백 원이 `#a65d57` / 코어
  `#3c2529`, HP 핍이 `#b578e8` / `#2b1a40`, 그림자 `#00000055`.
  범례 아이콘 `add` 가족은 `b #c86bd8 · h #f0c8f8 · d #7a3a85 · o #1f0a24`
  (`generate_dot_icons.py:33`)로 **판 위 그림과 다르다**. 사망 시트를 그리실
  때 정하는 5단 밴드가 곧 이 가족의 정본이 되면 좋겠다 — 그러면 아이콘
  네 색도 거기서 뽑아 다시 구우면 짝이 맞는다.
- **왜 스프라이트가 될 수 있나**: 크기가 96px 하나로 고정이고, 상태가
  「죽었다」 하나이며, 재생 길이도 코드가 정한 상수다. 잔재는 34판 중 여러
  판에 나오고 반복해서 죽는데 지금 그 순간의 그림이 0장이다.
- **제안 규격**: **768×192 가로 4프레임 시트**(셀 192px). 표시 96px이므로
  배율 0.5 — **원본 4px 블록이 판 위 2px**로 앉아 §2의 셀과 정확히 맞는다.
  `drawAnimated` 가 프레임 폭을 `naturalWidth / 4` 로 뽑으므로
  (`game-core-render.js:850`) 헬퍼 코드는 손댈 필요가 없다.
  파일명 `assets/enemies/void-wisp-death.png`, 런타임 키 `wispDeath`
  (`game-data.js:1725-1731` 의 `staticArt` 에 한 줄 추가하면 `:1893` 의
  `Object.values(staticArt)` 가 프리로드까지 해 준다).
  기존 `-idle`/`-hit` 과 같은 1024×256(셀 256)으로 주셔도 돌아가지만 표시
  배율이 0.375라 격자가 깨진다. **192 셀 쪽을 권한다.**
  그리는 쪽(“`down > 0` 이면 건너뛴다”를 “죽는 동안은 사망 시트를 재생한다”로)
  은 이 저장소가 고친다.

### 4-2. 도는 방벽의 «틈» — 플레이어가 노리는 것인데 그림이 없다

- **무엇**: 궤도 위에서 방벽과 방벽 사이의 «열린 자리»를 알리는 작은 표지.
- **지금은 어떻게 그려지나**: 방벽 «막대»만 그린다
  (`stella-ball-dot-gimmicks.js:382-466`). **틈을 나타내는 그림은 코드 어디에도
  없다** — 궤도 원도, 점선 경로도, 틈 표시도 없다. `orbitals` 를 참조하는 곳은
  저장소 전체에 네 곳이고(`game-combat-physics.js:352·668`,
  `game-combat.js:244`, `game-core-render.js:1724`) 그중 그리기는
  `game-combat.js:244` 의 위임 한 줄뿐이다.
- **판 위 크기(실측)**: 궤도 반지름은 스테이지 데이터값이고 아홉 가지다 —
  118 (`6-1`), 122 (`5-x`), 124 (`6-2`), 126 (`7-6`), 128 (`2-x`·`6-5`),
  132 (`6-3`), 134 (`6-6`), 136 (`1-7`), 138 (`6-4`)
  (`game-data.js:707-710, 804, 1343, 1368-1402, 1415`). 코드 기본값 130
  (`:1595`). 궤도 «차선»은 `rIn = radius-12` ~ `rOut = radius+12`, 띠 두께
  24px(실제로는 2px 셀 13줄 = 26px). 방벽 한 장은 약 **97 × 26 board px**.
  틈의 실제 크기는 방벽 개수로 갈린다 — 방벽 1개 스테이지는 틈 하나가
  654~716px 호, 2개(180° 대칭)는 302~335px 짜리 둘, 3개(120°)는 191~199px
  짜리 셋. 궤도 둘레는 741px(r=118) ~ 867px(r=138).
- **프레임 수와 길이(실측)**: 방벽에는 프레임 애니메이션이 없다.
  `frameClock` 을 읽지 않고, 변화는 각도 적분(`orbit.a += speed * d`,
  `game-combat-physics.js:674`)과 피격 0.22초 팔레트 교체 둘뿐이다.
  회전 속도 0.9~1.5 rad/s → 한 바퀴 4.19~6.98초. 조준 중에도 돈다
  (`game-core-render.js:1724`).
- **색**: 궤도 가족색 `#7cc6bb` 계열. 평시 방벽이 tooth `#b7e6de` / hi
  `#7cc6bb` / body `#2a4d57` / body2 `#1c3944` / inner `#0e2028` / cap
  `#d8f2ea` / stud `#9adfc9`. **`#9adfc9` 는 피해 주신다** — §3-4의 충돌.
- **왜 스프라이트가 될 수 있나**: 표지 자체는 크기가 고정이고, 위치는 방벽
  양 끝(런타임 좌표)에 셀 스냅으로 찍으면 된다. **다만 회전 변환이 금지라
  방향성 있는 모양은 쓸 수 없다** — 어느 각도에 놓여도 같아 보이는 모양
  (마름모·원·십자·2×2 스터드 같은 것)이어야 한다. 궤도 «차선» 자체(점선 원)는
  스프라이트가 될 수 없다(§5).
- **제안 규격**: 논리 8×8 → **16×16 PNG**, 평시·점등 2벌.
  `assets/library/icons/gimmick/` 이 아니라 판 위 물건이므로
  `assets/library/gimmick/orbit-gate.png` 같은 새 자리를 제안한다(분류 이름은
  맡긴다). 필요하면 12×12(논리 6×6)까지 줄여도 되지만 셀 두 칸 규칙(§2-4)
  때문에 그 아래로는 권하지 않는다.

### 4-3. 포효가 도착하는 모서리 — 좌표가 «네 곳으로 고정»이다

- **무엇**: 포효로 밀려난 것이 모서리에 닿는 순간의 각인. 4프레임.
- **지금은 어떻게 그려지나**: `drawBossRoar()`(`game-core-render.js:1546-1587`)
  가 세 박자를 낸다. (a) 0~0.12초 판 전체를 `#03070a` 로 알파 0.34까지 덮어
  「빨아들인다」. (b) 0.12초부터 파형 링 3겹이 0.09초씩 어긋나 밖으로.
  (c) 0.46~0.78초 유성이 도착한 자리에서 안쪽으로 부챗살 5줄.
  거기에 모서리마다 `areaBursts` 링(`r:52, d:0.5, #f6c48e`)이 하나씩 얹히고
  `screenShake = 26`(이 게임 최대값, 강타 24보다 한 단계 위)이 들어간다.
  **거상의 «몸»에는 아무 연출도 없다** — 다만 이건 그림이 없어서가 아니다(§5-6).
- **판 위 크기(실측)**: 목표 모서리는 **네 곳으로 고정**이다 —
  `(96,176)`, `(624,176)`, `(96,724)`, `(624,724)`
  (`game-combat-physics.js:257-262`, `W-96` · `H-176`).
  모서리 링은 반지름 18 → 70px(지름 36→140px), `lineWidth 3`.
  균열 쐐기는 5줄, 각도 `inward ± 0.44rad`(0.22 간격), 길이 26 → 80px
  (가운데) ~ 96px(바깥 줄), `lineWidth 2`.
  파형 링은 반지름 40 → 660px으로 **판 밖으로 나간다**(§5).
- **프레임 수와 길이(실측)**: 밀려나는 구간은 트리거 후 120ms 대기 → 340ms
  이동 → 총 460ms(`roarTo.at = frameClock+120`, `until = frameClock+460`,
  `game-combat-physics.js:225-249`). 감속 도착(`ease = 1-(1-t)²`).
  시각은 0 → 0.95초에 스스로 꺼진다(`game-core-render.js:1549-1552`).
  실제 이동 거리는 200~380 board px(판 정중앙에서 최대 380.3px,
  발사 위치 유성 265.1px). 발동은 보스 체력 비율이 `stagePhases.at` 을 지날
  때마다 1회 — `7-1` 은 0.6에서 1번, `7-2`·`7-5` 는 2번, `7-4`·`7-7` 은
  0.75/0.5/0.25에서 3번(`game-data.js:1405-1421`).
- **색**: 암전 `#03070a`(최대 알파 0.34), 파형 첫 겹 `#fff1c7`, 둘째·셋째
  `#f6c48e`, 모서리 링 `#f6c48e`, 균열 쐐기 `#fff1c7`.
  **`#ffd2a0` 는 쓰지 마신다**(§3-3의 2번, §3-4의 첫 줄). 포효 계열은
  `#ff9d6e` 쪽으로.
- **왜 스프라이트가 될 수 있나**: 도착 좌표가 네 곳으로 고정이고, 사건이
  한 종류이며, 길이가 상수다. 이 게임에서 화면을 가장 크게 흔드는 사건인데
  도착 자리에 남는 것이 원 테두리 하나다.
- **제안 규격**: 논리 16×16 → **32×32 PNG × 4프레임 가로 시트(128×32)**.
  네 모서리 각각 안쪽을 향해야 하므로 방향이 필요한데 회전이 금지다 —
  **네 방향을 각각 그려 주시거나(4방향 × 4프레임 = 512×32), 방향 없는
  대칭 모양 한 벌**로 주시면 된다. 어느 쪽이든 좋다. 어느 쪽인지만
  적용 쪽지에 적어 주시면 배치는 이 저장소가 한다.

### 4-4. 굳은 껍질이 깨질 때의 «파편»

- **무엇**: 껍질이 통째로 걷히는 400ms 동안 흩어지는 조각. 작은 4프레임 시트.
- **지금은 어떻게 그려지나**: `shieldShatter`
  (`stella-ball-dot-gimmicks.js:536-556`)가 남은 겹 수만큼 픽셀 호 조각을
  바깥으로 밀어내며 알파를 1→0으로 뺀다. 조각 하나는 `pixelArc`
  (`:474-488`) 4겹(반지름 r, r-2, r-4, r-6)이라 **두께 8px의 띠 하나뿐이고
  그 안에 결·금·파편 같은 형태 정보가 전혀 없다.** 호출부는
  `game-combat.js:251-258`.
- **판 위 크기(실측)**: 반지름 78 → 170 board px
  (`78 + snap(age*92)`, 2px 단위 스냅). 바깥 지름 156 → 340px, 판 너비의 47%.
  띠 두께는 항상 8px. 조각 하나의 호 길이는 슬롯 수에 따라 —
  `slots=2` → 각 폭 2.9816rad, r=78에서 약 233px; `slots=3` → 1.9344rad,
  약 151px; `slots=4` → 1.4108rad, 약 110px. 조각 사이 틈은 고정 `gap=0.16rad`.
  평시 링은 반지름 78 고정이라 **192px 거상 몸통 «안쪽»**에 붙어 있다.
- **프레임 수와 길이(실측)**: 프레임 시트 없이 매 프레임 절차 생성.
  수명 정확히 400ms(`age = (frameClock - shattered.at) / 400`,
  `game-combat.js:252`). 60fps면 약 24프레임. 반지름이 2px씩 끊기므로 실제로
  구분되는 위치는 46단(78→170).
- **색**: 흩어지는 조각은 `SHIELD_L_HOT` 4겹 고정 — 바깥→안 `#ffffff`,
  `#fff1d6`, `#ffd9a4`, `#c98d55`(`:473`). 평시 링은 `SHIELD_L` —
  `#eafaf4`, `#9adfc9`, `#5fa397`, `#2f5d59`(`:472`). 동반 링·팝업 `#9adfc9`.
- **왜 스프라이트가 될 수 있나**: **띠(호)는 스프라이트가 될 수 없다** —
  반지름이 런타임이고 슬롯이 2·3·4로 갈린다(`bossShield.max` 는 스테이지
  데이터상 2·3·4 셋뿐: `game-data.js:658, 834, 1334-1342, 1401, 1419`).
  스프라이트가 되는 것은 **띠에서 떨어져 나가는 자유 파편**이다. 크기가
  작고 고정이며, 코드가 셀 스냅 좌표에 뿌리면 된다.
- **제안 규격**: 논리 8×8 → **16×16 × 4프레임(64×16)**, 파편 모양 2~3종.
  400ms는 코드가 고정한 값이니 프레임 간격은 이 저장소가 100ms로 나눈다.
- **먼저 아셔야 할 것**: 이 400ms 연출은 **거의 안 보인다.** `shattered` 를
  세우는 곳은 `game-figure.js:224`(카시오페이아 「껍질 파괴」) 단 한 곳이다.
  평범하게 껍질을 다 소모하는 경로(`game-combat-physics.js:183-196`)는
  `hits` 를 1씩 줄이고 `flash` 만 켤 뿐 `shattered` 를 세우지 않는다.
  즉 **5점 별자리 카시오페이아를 성립시킨 판에서만 나온다.** 그래서 이
  항목이 네 번째다.

### 4-5. 판 위에 남은 폰트 글리프 셋

- **무엇**: 판(캔버스)에 폰트로 그려지는 기호 셋을 도트로. `×`(개수), `↶`/`↷`
  (회전·전환), `☝`(수업용 손가락).
- **지금은 어떻게 그려지나 · 판 위 크기(실측)**:

| 글리프   | 자리                                                                | 캔버스 폰트                              |
| -------- | ------------------------------------------------------------------- | ---------------------------------------- |
| `×` 개수 | 기믹 범례 「[아이콘 16px] 이름 ×N」 (`game-combat-physics.js:1320`) | `11px Galmuri11, ui-monospace` (`:1315`) |
| `↶` `↷`  | 비행 중 「좌클릭 ↶ · 우클릭 ↷ · 1회 전환」 (`:1086`)                | `bold 10px ui-monospace` (`:1082`)       |
| `↷`      | 「↷ 빈 곳 클릭 = 반대편」 (`:1566`)                                 | `700 11px Galmuri11` (`:1563`)           |
| `☝`     | 수업 「여기를 누르세요」 (`game-onboarding.js:1202-1204`)           | **`28px sans-serif`** (`:1198`)          |

- **우선순위**: `×` 개수가 가장 높다. **바로 옆에 16px 도트 아이콘이 붙어
  있어 어긋남이 제일 잘 보인다.** `☝` 는 판 위에서 유일하게 시스템 산세리프로
  그려지는 글자다 — 픽셀 격자가 없을 뿐 아니라 서체 자체가 다르다.
- **프레임 수와 길이(실측)**: `×` 와 `☝` 의 몸통은 정지.
  `↷`(`:1566`)만 연속 깜빡임 — `globalAlpha = 0.35 + 0.45·(0.5 + 0.5·sin(frameClock/250))`,
  주기 약 1.57초. `☝` 는 상하 진동·파문·알파 셋을 매 프레임 계산한다(파문 링
  5→24px). **깜빡임·진동은 코드가 계속 한다** — 그림은 정지 한 장이면 된다.
- **색**: `×` 는 범례 글자색 `#cfdad7`(상자 바탕 `#04080ac9`, 테 `#24363a`).
  `↶ ↷` 는 `#fff2c6`(전환 전) / `#e8f7df`(전환 완료). `☝` 는 외곽선 `#15131c`
  (`lineWidth 4`) + 채움 `#fff4cc`, 파문 링 `#ffe6a1`.
- **왜 스프라이트가 될 수 있나**: 전부 고정 크기 장식이고 상태가 없다.
  기믹 아이콘 여덟 장이 이미 같은 길로 갔다.
- **제안 규격**: 논리 8×8 → **16×16 PNG**, `generate_dot_icons.py` 를 확장해서
  같은 자리에 굽는 것이 가장 싸다(§6). `☝` 만 지금 28px이라 논리 16×16 →
  32×32가 맞다.
- **주의 하나**: `↷` 는 **같은 그림이 두 뜻**이다 — `:1086` 에서는 「우클릭 =
  시계 회전」, `:1566` 에서는 「조준선 뒤집기」다. 하나로 합치지 마시고 두
  장으로 주시는 편이 안전하다. 같은 이유로 `×` 도 **배율(`×1.0`)과
  개수(`×3`)를 합치면 안 된다** — 배율 쪽은 `prism-breakers.html:66`,
  `game-combat.js:1343·1369·1394`, `game-onboarding.js:1122` 네 자리이고
  값의 단위다.

### 4-6. `✦` 별·스파클 — 한 소스에서 정수 배율 사다리

- **무엇**: 프로젝트에서 가장 많이 흩어진 글리프 하나를 도트 한 소스로.
- **지금은 어떻게 그려지나**: `✦ ✧ ★` 가 **11군데 15회** 나온다 —
  `game-meta.js:361·907(×4)·1101·1188`,
  `game-onboarding.js:592·894·967·1407`, `game-combat-physics.js:1410`,
  `prism-breakers-polish.css:145`(CSS `content` 라 JS 검색에 안 잡힌다),
  `stella-ball-dawn.js:180·448`, `sky-ambience.js:273`.
- **크기(실측)**: **8px** (`.stage-cleared-mark`, `theme.css:2572-2584`)부터
  **37px** (`.gacha-reveal`, `theme.css:749-752`)까지 **열한 단계**로 흩어져
  있다. 그중 판 위에 그려지는 것은 하나 — 「남은 ✦ N → 별자리」가
  `700 11px Galmuri11`(`game-combat-physics.js:1408-1410`).
- **확인 못 함**: `.stage-drawer-mark`(`game-meta.js:1188`),
  프로필 아이콘 폴백 `<i>`(`game-meta.js:361`), 허브 아바타 폴백 `◆`
  (`game-onboarding.js:837`), 상점 주인 폴백 `☄`(`game-meta.js:664`),
  스테이지 기믹 라벨의 `×`(`game-data.js:1526-1532`), 엔딩 `<b>∅</b>`
  — 이 여섯은 **전용 `font-size` 규칙이 CSS 어디에도 없어 상속값이다.
  확인 못 함.** 추정하지 않았다. 그리기 전에 **작화 세션이 게임을 띄워
  계산된 크기를 재 주시면 좋겠다**(§8의 `probe-meta-screens.mjs` 로 그 화면을
  띄울 수 있다). 띄우기 어려우면 이 저장소에 요청해 주시면 잰다.
- **프레임 수와 길이(실측)**: 글리프 자체는 전부 정지다. 움직임은 부모가
  준다 — 소환 궤도는 `gachaOrbit 13s linear infinite`(`theme.css:864-868`),
  결과 컷 별띠는 `outcomeStar 1.9s` × 5개(delay 0/0.18/0.36/0.54/0.72s,
  `polish.css:271-286`), 새벽 킷 팝은 `dawnPop .9s`(`dawn.css:106-115`).
  **그림은 정지 한 장이면 된다.**
- **색**: 자리마다 다르다 — `#ffedd2`(소환 대형) · `#ffe2b6`(궤도) ·
  `#f7c493`(결과 컷, `theme.css:379-382` 가 polish 기본값을 덮는다) ·
  `#fff7d5`(타이틀 CTA) · `#ffd2a0`(새벽 킷). **한 색으로 굽고 소비처가
  칠하는 편**이 낫다고 보지만, 도트에는 CSS 색이 안 먹으므로 결국 몇 벌을
  구울지는 §9의 팔레트 결정과 묶인다. **일단 한 벌(밝은 크림 계열)만
  주시면 충분하다.**
- **왜 스프라이트가 될 수 있나**: 모양 하나가 열한 크기로 흩어져 있고 전부
  고정 크기 장식이다. `✧` 는 항상 `✦` 옆에 나란히 서는 «흐린 형제»라
  별도 스프라이트가 아니라 **같은 셰이프의 저휘도 변형**이면 된다.
- **제안 규격**: 논리 8×8 하나에서 셀 1·2·3·4로 구워
  **8 / 16 / 24 / 32px 네 장**. CSS를 그 네 값으로 몰아 넣는 것은 이
  저장소가 한다. 지금 15px·17px·18px·22px·31px·37px 로 흩어진 것을 그대로
  두면 정수배가 아니라 도트가 살아남지 못한다.

## 5. 찍지 «말아» 주셨으면 하는 것

시간을 아끼시라고 적는다. 아래는 전부 **고정 스프라이트가 될 수 없거나
이미 있는 것**이다.

1. **반사 벽 · 발판.** 스테이지마다 크기가 다르다 — 확인된 벽 치수만
   104×18, 108×18, 116×20, 118×18, 132×20, 140×20, 148×20 일곱 가지,
   발판은 118×28 ~ 190×40. 도트 킷이 판 3장 분절로 늘리는 이유가 그것이다
   (`stella-ball-dot-gimmicks.js:74-78`).
2. **공명 범퍼 · 도는 방벽 본체 · 굳은 껍질 링.** 반지름이 스테이지
   데이터값이고(궤도 118~138, 범퍼 r=26~27, 껍질 78) 각도가 매 프레임 바뀐다.
   회전 변환이 금지라 회전된 스프라이트를 붙일 수도 없다.
3. **궤도 «차선»(점선 원) 자체.** 둘레가 741~867px이고 곡률이 아홉 가지다.
   직선 타일을 곡선에 붙이려면 회전이 필요한데 금지다. 셀로 쌓아야 한다 —
   이 저장소가 한다. **§4-2가 부탁하는 것은 차선이 아니라 «표지»다.**
4. **포효 파형 링.** 반지름 40 → 660px으로 커진다(`game-core-render.js:1568`).
   판이 720 너비이므로 **링이 도중에 화면 밖으로 나간다.** 시트로 못 굽는다.
5. **판 전체 암전.** 720×900 전면 채우기다.
6. **거상의 포효 «몸».** 그림이 없어서가 아니라 **이미 있는데 코드가 안
   부른다.** 보스 팩은 슬러그마다 네 상태 시트를 갖고 있고
   (`game-data.js:1717-1722` 가 `idle · hit · attack · death` 를 전부 매핑),
   `assets/library/anim/boss10/` 에 **40장이 실재한다**(10종 × 4상태,
   384×384 4프레임). 그런데 그리기는 `"hit" : "idle"` 둘만 고른다
   (`game-core-render.js:1405-1411`). 매니페스트도 「attack/death 상태 재생과
   weakCount 감산 규칙은 아직 미구현」이라고 적어 두었다. **포효에 `attack`
   시트를 재생하는 것은 코드 일이지 작화 일이 아니다.**
7. **별자리 실루엣 일곱 장.** 시트는 전부 384×384로 이미 있다
   (`assets/library/constellations/`: aries, bigdipper, cassiopeia, corvus,
   cygnus, orion, sagitta). **화면 표시 크기는 고정값이 아니다** —
   `384 × (fit.scale / 138)` 이고(`game-figure.js:967`,
   `game-figure-recognition.js:851-852`) `fit.scale` 은 그 판에서 별지기가
   어디 섰는지에서 나온다. **확인 못 함 — 런타임 값이다.**
   그리고 아트 규격(384px, 골격 1단위 = 46셀 ×3 = 138px, 원점 정중앙)을
   바꾸면 **일곱 장을 전부 다시 구워야** 정렬이 맞는다.
8. **별자리 성립 연출의 선·노드.** 매 판 다른 도형이고, 선 두께 13px(오망성
   17px) 라운드캡 스트로크가 도형 자체다. 별빛 노드도 반지름이 매 프레임
   바뀐다(안 고름 9 · 고름 11에 탄생 `grow` 최대 2.8배, 호흡 최대 1.22배).
9. **판 바닥 · 아레나.** 720×900을 스테이지마다 다른 씨앗으로 굽는다
   (`game-arena-carve.js`).
10. **피해 숫자 · 팝업.** 글자다. 하드 2px 오프셋 그림자를 이미 쓰고 있어
    화풍은 맞다.
11. **약점 보석 · 궤도 각인.** 궤도 반지름 84px은 고정이지만 각도가 `boss.a`
    에서 나오는 런타임 좌표이고, 색이 §3-3의 예약색이다.
12. **허브 탭 아이콘 다섯.** 이미 런타임에 8×8 도트로 교체된다 —
    `stella-ball-upgrade.js:172-197` 의 `tabIcons()` 가 `span.textContent` 를
    비우고 img로 갈아 끼운다(16px, `★` 만 20px).
    `game-onboarding.js:967` 의 `◎ ☄ ★ ◈ ⚙` 는 킷이 없을 때의 폴백으로만
    남아 있다.
13. **패널 · 카드 · 표 · HP 미터 프레임.** `library/ui/` 에 이미 있고 이미
    붙였다(§1-2).

## 6. 어떻게 만들어 주시면 좋은가

### 6-1. 손그림보다 «굽는 스크립트»를 권한다

이유 하나뿐이다. **목표 픽셀 스케일이 아직 열린 결정이다**
([VISUAL_UPGRADE](VISUAL_UPGRADE_2026_08_22.md) §8-1, §8-5). 그 결정이
바뀌면 —

- 스크립트로 오면 `CELL` 한 줄 고치고 **다시 돌리는 일**이다.
- 손으로 찍혀 오면 **다시 그리는 일**이다.

이미 출고된 `scripts/generate_dot_icons.py` 가 그 이유를 파일 첫 문단에
적어 두었고, 실제로 그렇게 만들어졌다.

### 6-2. 따라 주시면 되는 네 요소

`scripts/generate_dot_icons.py` 가 작동하는 예다. 네 가지만 지키면 형태는
자유다.

1. **모듈 최상단 상수** — `CELL = 2`(`:23`), `LOGICAL = 8`(`:24`),
   `OUT = Path("assets/library/icons/gimmick")`(`:25`).
2. **이름 → ASCII 도안 dict** (`ART`, `:40-129`). 한 글자 = 한 논리 픽셀,
   `.` 는 투명, `o` 외곽선 · `b` 본색 · `h` 하이라이트 · `d` 그늘.
   **이 dict가 곧 원화다** — 사람이 읽고 고칠 수 있다.
3. **이름 → 색 튜플 dict** (`PALETTE`, `:28-37`). `(b, h, d, o)` 순.
4. **행 길이를 `assert` 하는 `bake()`** (`:132-147`):
   `assert len(line) == LOGICAL, f"{name} {row}행이 {len(line)}칸"`.

`main()` 에 `assert len(ART) == len(PALETTE) == 8` 이 있다(`:151`) —
짝 맞춤을 강제하는 장치다. **아홉 장째를 넣으면 이 숫자도 같이 고쳐
주셔야 한다.**

확장(같은 규격이면 이 파일에 키를 더한다)이든 병렬(다른 규격이면 새 파일을
뜬다)이든 좋다. 병렬 선례가 이미 있다 —
`scripts/generate_boss_pack_10.mjs` + `scripts/boss-pack-core.js`(384×384
4프레임 시트), `scripts/generate_constellation_art_6_7.mjs`(384×384 1프레임),
`scripts/generate_ability_bursts.py`. 파이썬이든 `.mjs` 든 상관없다.
이 기기에는 Python + Pillow 가 있다.

### 6-3. 손으로 찍어 주셔도 된다

**괜찮다.** 다음만 지켜 주시면 그대로 들어간다.

- 모든 채움이 **2×2 정수 블록**. 1px 디테일·홀수 두께 없음.
- 이미지 크기가 셀의 정수배(16 / 32 / 64 / 128 / 192 / 384 …).
- 알파는 **0 아니면 255**. 반투명 가장자리를 쓰지 마신다 — 축소가 행을
  통째로 버리므로 반투명 테두리는 프레임마다 다르게 사라진다.
  (출고된 베이커도 투명은 건너뛰고 나머지는 알파 255로만 찍는다,
  `generate_dot_icons.py:137-146`.)
- 시트는 **가로**, 프레임 폭 = 프레임 높이, 프레임 수는 4를 기본으로.
  (`drawAnimated` 가 `naturalWidth / 4` 로 프레임 폭을 뽑는다,
  `game-core-render.js:850`.)
- 파일명 = 런타임 키. 대문자·공백·마침표 끝맺음 금지(§7의 게이트가 막는다).

### 6-4. 어느 쪽이든 함께 주셔야 하는 것

- 번호 매긴 **적용 쪽지** — 파일마다 「어디에 넣는다」 한 줄.
- **팔레트 표** — 새로 쓴 색 전부와, 그것이 §3-3의 예약색을 밟지 않는다는
  확인 한 줄.
- 프레임 시트면 **프레임 수와 의도한 재생 길이**(ms). 코드가 정한 길이가
  이미 있으면(예: 껍질 400ms) 그쪽에 맞춘다.

## 7. 반입 절차 — 이대로면 재작업이 없다

### 7-1. 순서

1. **`ASSET_BACKLOG.md` 에 항목을 먼저 적는다.** `AGENTS.md:152` 가
   「기능 작업 중에 화면이 좋아질 것 같다는 이유로 새 래스터·시트·아이콘·SFX를
   만들거나 반입하지 않는다. 필요를 먼저 `ASSET_BACKLOG.md` 에 적는다」이다.
   양식은 `ASSET_BACKLOG.md:31-42` 의 여덟 줄(화면/트리거 · 플레이 목적 ·
   필요한 형태 · 희망 규격·프레임 · 화풍·색 규칙 · 기존 후보·재사용 경로 ·
   코드 연결 지점 · 결정·검수 메모). **모르는 값은 추정하지 않고 `미정`** 이다
   (규칙 4).
2. **상태 라벨.** 새 항목은 `명세 필요` 또는 `후보`로 들어가고, **오너가
   `제작 준비` 로 올린 것만 제작 자격이 있다**(`ASSET_BACKLOG.md` 규칙 5,
   `AGENTS.md:155`). **지금 `제작 준비` 항목은 0개다.**
   즉 이 지시서는 작화 지시인 동시에 **그 승인 신청**이다 — §4의 여섯 항목을
   장부에 올리고 오너가 `제작 준비` 로 표시한 뒤에 그리는 것이 규칙대로다.
3. 굽는다 / 그린다. 출력은 `assets/library/<분류>/<런타임키>.png` 또는
   §4가 지정한 자리.
4. **`git add` 로 인덱스에 올린다.** 디스크에 두기만 하면 게이트가 막는다(7-2).
5. 런타임 참조를 **`prototypes/js/game-*.js`** 에 `../assets/...` 상대경로로
   적는다(7-3의 사각지대 주의).
6. `assets/ASSET_MANIFEST.json` 에 섹션을 추가한다(7-4).
7. `ASSET_PLAN.md` 에 출처·라이선스·경로·런타임 용도를 적는다.
8. `npm run format` → `npm run check`.
9. §8의 프로브로 실기 스크린샷.
10. 장부를 `반입 완료` 로 옮긴다.

### 7-2. 커밋을 막는 게이트 — 인덱스에 없는 에셋

`scripts/check-portability.mjs:57-67` 원문이다. 문제의 문장은 65행이다.

```js
for (const match of contents.matchAll(assetReference)) {
  const reference = match[0];
  const path = relative(root, resolve(referenceBase, reference))
    .split(sep)
    .join("/");
  if (!path.startsWith("assets/") || !existsSync(resolve(root, path))) {
    failures.push(`${file}: 에셋 경로가 유효하지 않습니다 (${reference}).`);
  } else if (!trackedSet.has(path)) {
    failures.push(`${file}: Git에 없는 에셋을 참조합니다 (${path}).`);
  }
}
```

`trackedSet` 의 출처는 같은 파일 `:7-11` 의 `git ls-files -z` 다. 즉 **「디스크에
있나」가 아니라 「인덱스에 있나」를 묻는다.** PNG를 굽고 코드에 경로를 적은 뒤
`git add` 를 안 하면 반드시 이 문장이 뜨고 `:97-99` 에서 throw 한다.
스크립트 · PNG · 매니페스트 · 코드 참조를 **한 커밋에 함께** 넣으면 된다.

같은 파일의 나머지 셋도 걸린다.

- **운영체제 절대 경로 금지**(`:42`, `:54-56`) — `/Users/`, `/Volumes/`,
  `file://`, `C:\` 형태가 런타임 파일에 있으면 죽는다. 맥에서 만든 경로를
  그대로 붙여 넣지 마신다.
- **Windows 대소문자 충돌**(`:70-86`) — `Wall.png` 와 `wall.png` 를 같이
  두면 걸린다. `CON`/`PRN`/`AUX`/`NUL`/`COM1-9`/`LPT1-9` 이름과 마침표·공백
  으로 끝나는 이름도 금지.
- **런처 두 개**(`:88-95`) — `PLAY_WINDOWS.cmd`, `RUN_STELLA_BALL.command` 가
  인덱스에 있어야 한다.

### 7-3. 게이트 사각지대 — 어디에 경로를 적느냐가 중요하다

`check-portability.mjs:14-39` 의 `runtimeFiles` 는
`prototypes/js/game-*.js` 와 `prism-breakers-*.css` 까지만 열거한다.
**`stella-ball-*.js` · `boss-art.js` · `outer-observer.js` 는 목록에 없다** —
거기 적힌 에셋 경로는 게이트가 전혀 검사하지 않는다. 실제로 지금 게이트
밖에 있는 살아 있는 참조가 셋 있다(`stella-ball-upgrade.css` 의
`panel-frame.png`·`hpbar-frame.png`, `outer-observer.js` 의
`observer-eye-sheet.png`). **새 스프라이트 경로는 `prototypes/js/game-*.js`
안에 적어 주시는 편이 안전하다** — §4-1의 `staticArt`(`game-data.js:1725`)와
§4-5의 아이콘 맵(`game-combat-physics.js:1197`)이 이미 그 안이다.

### 7-4. 매니페스트 항목의 모양

절차 생성 도트 세트는 `gimmickIcons`(`ASSET_MANIFEST.json:538-554`)를 그대로
본뜨면 된다.

```json
"gimmickIcons": {
  "note": "기믹 범례 아이콘 여덟. scripts/generate_dot_icons.py 가 굽는다 — 손으로 찍지 않아 목표 픽셀 스케일이 바뀌면 다시 돌리면 된다.",
  "cell": 2,
  "logical": 8,
  "size": 16,
  "source": "scripts/generate_dot_icons.py",
  "files": { "wall": "library/icons/gimmick/wall.png", "…": "…" }
}
```

`files` 값은 `assets/` 를 뺀 상대경로다. 스프라이트 시트 계열은 대신
`frameWidth` / `frameHeight` / `frames` 를 쓰고, 절차 생성물이면 `regen` 에
재생성 명령을 적는 것이 관례다(`boss10.entries[]`, `:345-535`).

**한 가지 보강.** `AGENTS.md:155` 는 `source, license, path, runtime use` 넷을
요구하는데 `gimmickIcons` 에는 `license` 와 `runtimeUse` 가 빠져 있다.
`constellations`(`:324-331`)에는 둘 다 있다. **새 섹션에는 넷을 다 적어
주시면 좋겠다** — 빠진 둘은 이 저장소가 `gimmickIcons` 쪽에도 채워 넣는다.

또 하나. `verify-evidence.mjs:124-134` 의 실재 검사 목록에 `gimmickIcons` 가
없다 — 새 섹션도 실재 검사를 받게 하려면 그 배열에
`...Object.values(manifest.<새섹션>?.files ?? {})` 한 줄을 더해야 한다.
이건 이 저장소가 한다.

### 7-5. 포맷은 반드시 `npm run format`

`npx prettier` 를 맨손으로 돌리지 마신다.

> **Format only with the pinned Prettier.** `package.json` pins 3.5.3; a bare
> `npx prettier --write .` runs 3.9.6 — `prototypes/MAINTENANCE.md:108`

`package.json:11-12` 가 `npx --yes prettier@3.5.3 --write .` 로 버전을 박아
두었다. 고정을 빼면 최신판이 `10~11px` 같은 한 줄 안의 물결표 둘을 GFM
취소선으로 «해석»해 `10~~11px` 로 파일에 굳혀 버린다. 실제로 문서 일곱 개
24곳이 한 명령에 상했고 표 안에서는 사이에 낀 굵게까지 깨졌다.
`verify-evidence.mjs:169-210` 의 정규식이 추적 중인 모든 `.md` 를 훑어 이
자국을 잡고 「범위 표기가 취소선이 됐습니다」로 throw 한다.

### 7-6. 게이트를 돌리는 명령

```bash
npm run verify   # verify-evidence.mjs (안에서 check-portability.mjs 재실행)
npm run smoke    # smoke-runtime.mjs — 스크립트·스타일시트 «순서»까지 대조
npm run check    # 위 둘 + 온보딩 E2E
```

`.git/hooks` 에 훅은 없다. 커밋 전에 손으로 돌리는 규약이고, CI는
`.github/workflows/evidence.yml` 이 ubuntu·windows 양쪽에서 돌린다.

## 8. 어떻게 확인하는가

새 프로브는 필요 없다. 전부 이미 있다.

```bash
node scripts/probe-gimmick-stages.mjs      # → artifacts/gimmicks/<스테이지>.png
node scripts/probe-meta-screens.mjs        # → artifacts/meta-screens/<화면>-<크기>.png
node scripts/probe-settlement.mjs          # → artifacts/settlement/<win|lose>-<크기>.png
node scripts/probe-summon.mjs              # → artifacts/summon/{call,observe,answer,manifest,intro,done}.png
node scripts/probe-text-size.mjs           # 판 위 글자의 «화면» px + 그린 횟수
node scripts/probe-window-scale.mjs        # 창을 키우면 무엇이 커지나
node bot/run-bot.mjs                       # 회귀 감시 — 아트만 바꿨으면 diff 0이어야 정상
```

| 이 지시서의 항목                                      | 보는 도구                                                                           |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------- |
| §4-1 잔재 사망 · §4-2 궤도 틈 · §4-3 포효 · §4-4 껍질 | `probe-gimmick-stages.mjs` (기본 8판: 1-3 2-2 3-3 4-3 5-4 6-4 7-4 8-1, 창 1280×900) |
| §4-5 판 위 글리프                                     | `probe-gimmick-stages.mjs` + `probe-text-size.mjs`                                  |
| §4-6 `✦` 사다리                                       | `probe-meta-screens.mjs` (12화면 × 2크기)                                           |
| 격자가 화면에서 살아남나                              | `probe-window-scale.mjs`, `--sizes 1280,900 1024,680`                               |

공통 함정 둘. **외부 관측자 인트로를 안 끄면 전부 새까맣게 나온다**
(`window.StellaIntroObserver?.stop()`), **입장 시네마 4.2초와 `.cin-veil`
0.45초가 걷힌 뒤에 찍어야 한다.** 하니스(`scripts/lib/probe-harness.mjs`)가
크롬을 못 찾으면 `STELLA_BROWSER_PATH` 를 세우라고 죽는다.
프로브는 **색을 판정하지 않는다** — 색 대조는 스크린샷을 눈으로 본다.

## 9. 정해지지 않은 것 — 가정하지 마시라는 뜻으로 적는다

이 지시서가 «풀지 않은» 결정들이다. 전부 오너 몫이고, 여기 적힌 것 중
어느 것도 작화 세션이 정하실 필요가 없다.

1. **목표 픽셀 스케일** ([VISUAL_UPGRADE](VISUAL_UPGRADE_2026_08_22.md) §8-1).
   A 정수 배율만 허용 / B 논리 셀 하나로 통일(2px 후보) / C 지금대로 두고
   축소만 부드럽게. `UI_REVIEW_2026_08_14.md:46` 이 「다른 항목보다 먼저
   답이 필요하다」고 적은 지 8일째다. §2의 「셀 2px」은 **이 결정을 앞질러
   정한 것이 아니라, 지금 판 위에서 실제로 성립하는 값**이다.
2. **백버퍼와 devicePixelRatio** (BOARD_TEXT §6). 백버퍼를 화면에 맞출지,
   판의 최소 크기를 정할지, 판 위 글자를 키울지, 규칙의 적용 범위를 고칠지.
3. **판 위 한글의 하한.** §4에는 별지기 이름표 배지가 «없다» — 그것은
   VISUAL_UPGRADE §5-1이 「§8-6 선행 결정」이라고 못박은 항목이라 일부러
   뺐다.
4. **팔레트가 청록인가 보라인가** (VISUAL_UPGRADE §8-4). 문서
   (`UI_KIT_DAWN.md:14-21`)는 청록, 출고본(`stella-ball-theme.css:7-42`)은
   보이드 보라, 가장 최근 아트 파일(도트 킷)은 청록이다. **당장은 이렇게
   가 주시면 된다 — 판 위 새 아트는 도트 킷 값(청록)을, DOM 새 아트는
   `theme.css` 의 `:root`(보라)를 진실로 삼는다.** 문서 쪽이 갱신 대상이다.
5. **살구 예약색이 이미 깨져 있다.** 유성 꼬리가 `#ffd2a0` 을 매 프레임
   화면 가로질러 그린다(`game-core-render.js:1182`) — 그 62줄 아래에서
   「살구빛은 노릴 가치가 있는 단 하나에 예약」이라고 적어 놓은 그 색이다.
   규칙을 「살구는 플레이어 것(유성·약점·보상)」으로 다시 적을지, 꼬리를
   스킨 색으로 옮길지는 오너 판단이다. **그때까지 새 그림에는 살구를 쓰지
   마신다**(§3-3).
6. **`roar` 아이콘 팔레트**(`generate_dot_icons.py:35`)를 `#ff9d6e` 계열로
   되돌릴지. 되돌린다면 §4-3의 색도 함께 정해진다.
7. **그림과 판정이 어긋난 자리 둘.** 방벽은 그림 약 97×26px에 판정이
   반지름 26의 원 하나(`game-data.js:1598`, `game-combat-physics.js:354`),
   잔재는 96px 스프라이트에 판정 반지름 23(`game-data.js:1570`).
   새 도트를 판정 크기에 맞추면 화면이 훨씬 작아 보이고, 지금 크기를
   유지하면 「맞았는데 안 맞았다」가 남는다. **§4-1의 제안은 지금 크기
   96px을 유지하는 쪽이다** — 판정을 바꾸는 것은 게임 규칙 변경이라
   아트 패스에서 결정할 일이 아니다.
8. **부서진 방벽이 hp 0인 채로 돌아온다.** 잔재는 재생성 때 hp를 `maxHp`
   로 되돌리는데(`game-core-render.js:1676-1681`) 방벽은 `down` 만 줄이고
   hp를 복구하지 않는다(`game-combat-physics.js:669-673`). 부순 방벽이
   1.4초 뒤 hp 0으로 다시 나타나 다음 접촉에 즉시 또 부서진다.
   **「부서진 방벽」 상태를 그리실 계획이라면 이 반복 깜빡임을 감안해
   주셔야 한다.** 코드 쪽 결정이다.
9. **확인 못 함으로 남긴 크기들.** §4-6의 여섯 자리(전용 `font-size`
   규칙이 CSS에 없어 상속값), 결과 메달·메트릭 6장의 실제 표시 크기
   (VISUAL_UPGRADE §5-3), `library/props` 6장의 원본 격자.
   **추정하지 않았고, 추정하지 마시라.** 재는 방법은 §8이고, 재기 어려우면
   이 저장소에 요청해 주시면 잰다.
