# 메타 UI · 별자리 지도 개선 — 디자인 세션 요청서

> 작성일: 2026-08-14
> 대상: 허브(메타) 화면 전체, 스테이지 별자리 지도(허브 인라인 + 전체화면)
> 방법: 데스크톱 1024×700 / 1280×720 / 1440×820 실기, DOM·계산 스타일·경계 상자 실측
> 상태: **요청서다. 여기서 새 래스터·SFX를 만들지 않는다.** 반입은 [ASSET_BACKLOG.md](ASSET_BACKLOG.md)의 승인 절차를 따른다.

## 0. 이 문서의 위치

[UI_REVIEW_2026_08_14.md](UI_REVIEW_2026_08_14.md)의 후속이다. 그 검토는 편성·소환·상점·기록을 다뤘고 허브·지도는 `2-5`에서 **화살표 크기 한 줄([C])**로만 지나갔다. 이 문서는 그 자리를 실측으로 채운다.

같은 규칙을 지킨다 — **측정값이나 화면 근거가 없는 항목은 넣지 않는다.** 등급도 같다(A: 화풍·구조의 근간, B: 해당 화면의 완성도, C: 여유가 있을 때).

중복 제기하지 않는 것: 픽셀 정수 배율 정책(그 문서 1절), 화면 전환 어휘(3-1), 빈 상태 표현(3-2), 모션 감소 규약(3-3). **1절의 픽셀 배율 답이 여기에도 선행한다** — 노드 크기를 정하려면 UI 배율 정책이 먼저 필요하다.

### 이 문서를 쓰는 동안 코드에서 이미 고친 것 두 가지

아래 수치는 **고친 뒤 상태**다. 디자인 세션이 지금 빌드를 열면 이렇게 보인다.

- **화면 폭이 튀던 문제.** 허브·지도는 502px 관측소 컬럼인데 프로필·소환·상점·편성·업적은 `position: fixed`라 뷰포트 전체(1280px)를 먹었다. 허브에서 아무 탭이나 열면 폭이 2.55배로 뛰었다. 502px에서 다섯 화면 모두 넘침이 0이라 전부 컬럼으로 통일했다 — 이렇게 두면 새벽 하늘과 인트로가 흩뜨린 여백 소품이 모든 화면에서 계속 보인다.
- **전체화면 지도의 바닥 버튼이 잘리던 문제.** `.constellation-map`의 `min-height: 500px`가 하드 바닥이라 머리·바닥·패딩까지 678px를 요구하는데 컬럼은 628px였다. 오버레이가 `overflow: hidden`이라 「뒤로 · 무한 훈련장 · 튜토리얼 다시보기」가 스크롤도 못 하고 잘렸다. 바닥값을 240px로 낮춰 `flex: 1`이 실제 남는 공간을 쓰게 했다.

**둘 다 배치 버그를 없앤 것이지 디자인을 정한 것이 아니다.** 아래 요청은 그대로 유효하다.

---

## 1. 별자리 지도

### 1-1. [A] 같은 별자리가 두 곳에서 서로 다른 비율로 그려지고, 좁은 쪽에서 노드가 겹친다

지도는 두 번 그려진다. 허브 안의 작은 지도(`.hub-map`)와 전체화면 지도(`.constellation-map`)다. **둘은 같은 퍼센트 좌표를 쓰는데 상자 비율이 거의 두 배 차이 난다.**

| 지도        | 상자 (1280×720) | 가로세로비 | 노드 크기 |
| ----------- | --------------- | ---------: | --------- |
| 허브 인라인 | 470×267         |   **1.76** | 83×88     |
| 전체화면    | 462×450         |   **1.03** | 68×61     |

퍼센트 좌표를 비율이 다른 상자에 얹으면 별자리 모양 자체가 달라진다. 카시오페이아의 W는 허브에서 납작해지고 전체화면에서 서면서, **같은 별자리가 두 화면에서 다른 도형으로 보인다.**

그리고 짧은 쪽(허브)에서는 노드가 실제로 겹친다. 노드는 83×88인데 세로 공간이 267px뿐이다.

**허브 지도 노드 겹침 (1280×720 실측)**

| 월드           | 스테이지 | 겹침 | 상세                                                      |
| -------------- | -------: | ---: | --------------------------------------------------------- |
| 양자리         |        3 |    0 | —                                                         |
| 화살자리       |        4 |    0 | —                                                         |
| 까마귀자리     |        4 |    0 | —                                                         |
| 카시오페이아   |        5 |    0 | —                                                         |
| 백조자리       |        5 |    0 | —                                                         |
| **오리온자리** |        6 |    2 | `6-3∩6-4` 12×83px, `6-4∩6-5` 3×30px                       |
| **북두칠성**   |        7 |    3 | `7-3∩7-4` 3×67px, **`7-4∩7-5` 69×19px**, `7-6∩7-7` 69×9px |

`7-4∩7-5`의 69px는 노드 폭(83px)의 **83%**다. 두 스테이지 버튼이 거의 포개져 있다.

**중요한 것은 이 표의 순서다.** 겹침은 스테이지가 많은 월드에서만 생기고, 스테이지 수는 캠페인 진행에 따라 늘어난다(3→4→4→5→5→6→7). **플레이어가 오래 할수록 지도가 나빠진다.**

전체화면 지도도 창이 좁아지면 같은 문제가 생긴다.

| 뷰포트   | 상자    | 가로세로비 | 오리온 `6-3`↔`6-4` |
| -------- | ------- | ---------: | ------------------- |
| 1440×820 | 542×570 |       0.95 | 간격 8px            |
| 1280×720 | 462×450 |       1.03 | **겹침 3×61px**     |
| 1024×700 | 446×430 |       1.04 | **겹침 6×61px**     |

또한 상자 비율이 창 크기에 따라 0.95 → 1.03 → 1.04로 변하므로, **창을 줄이면 별자리 모양이 같이 찌그러진다.** 경로 SVG는 `viewBox="0 0 100 100"`에 `preserveAspectRatio="none"`이라 선도 같이 늘어난다.

**요청**: 별자리 배치의 좌표계 규약. 판단이 필요한 것은 셋이다.

- 지도 상자를 **고정 비율**로 둘 것인가(별자리 모양이 항상 같지만 여백이 생긴다), 아니면 좌표를 상자에 맞춰 재계산할 것인가?
- 노드 크기를 **스테이지 수에 따라 줄일 것인가**(7개 월드에서 작아진다), 아니면 상자를 늘릴 것인가?
- 허브 인라인 지도와 전체화면 지도는 **같은 그림이어야 하는가?** 아니라면 허브 쪽은 무엇을 보여주는 것이 맞는가(1-4 참조).

### 1-2. [A] 클리어한 스테이지가 아직 안 한 스테이지와 똑같이 보인다

노드의 시각 상태는 **두 가지뿐**이다. CSS에도 `.constellation-node.active`와 `.constellation-node.locked`만 있고 클리어 상태를 위한 클래스가 없다.

| 상태            |  opacity | 글자색             |
| --------------- | -------: | ------------------ |
| `active` (현재) |        1 | `rgb(237,242,255)` |
| `locked` (잠김) | **0.42** | `rgb(135,148,185)` |
| **클리어**      |        — | **없음**           |

`progress.clears = 3`으로 두고 화살자리 지도를 열어 노드 4개를 확인했다. 이미 깬 스테이지와 지금 갈 스테이지가 **같은 표현**이다.

진행도를 보여주는 것은 노드가 아니라 배경의 경로선 하나뿐이다(`constellationRoute()` — 열린 곳까지 실선, 그 뒤는 점선). 즉 **"별자리를 되찾는다"는 이 게임의 핵심 서사가 지도에서 선 하나로만 표현된다.** 별이 하나씩 켜지는 그림이 아니다.

`ASSET_BACKLOG.md`에 「클리어 스테이지 점등·별자리 진행」이 이미 있다. 이 요청은 그 항목에 **측정된 근거와 상태 개수**를 붙이는 것이다.

**요청**: 노드 상태 세트. 최소 네 가지가 필요하다 — 잠김 / 열림(미클리어) / **클리어** / 현재 출격. 각 상태의 밝기·테두리·별 기호와, 클리어 순간의 점등 연출.

### 1-3. [B] 일곱 별자리가 모두 같은 상자에 정규화돼 개성이 사라진다

각 월드의 `shape` 좌표를 전부 뽑았다. **일곱 월드가 전부 x 12~90, y 20~66 범위에 들어간다.**

| 월드         | 좌표                                                    |
| ------------ | ------------------------------------------------------- |
| 양자리       | (13,62) (48,27) (87,55)                                 |
| 화살자리     | (12,55) (35,55) (66,24) (88,55)                         |
| 까마귀자리   | (12,61) (35,28) (68,37) (89,62)                         |
| 카시오페이아 | (12,34) (30,64) (50,30) (70,64) (88,34)                 |
| 백조자리     | (12,54) (35,54) (52,22) (68,54) (90,54)                 |
| 오리온자리   | (12,62) (28,27) (46,48) (60,48) (76,28) (89,62)         |
| 북두칠성     | (13,20) (31,30) (48,36) (64,42) (66,66) (84,58) (86,30) |

실제 하늘에서 북두칠성은 길고 가늘며, 화살자리는 아주 작고, 카시오페이아는 넓은 W다. 지금은 **전부 같은 크기의 상자를 채우도록 늘려져** 있어서 월드를 넘겨도 "다른 별자리에 왔다"는 인상이 약하다. 카시오페이아의 W만 원형에 가깝고, 백조자리는 `y=54`에 네 점이 일렬로 있어 십자가 아니라 T로 읽힌다.

**요청**: 별자리마다 **고유 비율을 유지**하는 배치안. 상자를 채우는 대신 실제 별자리 비율로 놓고 남는 여백을 성운·눈금·주변 별로 채우는 방향이 이 게임의 관측소 설정에도 맞는다.

### 1-4. [C] 같은 지도가 두 화면에 있는데 역할이 나뉘어 있지 않다

허브에 인라인 지도가 있고, 전체화면 지도가 따로 있다. 둘 다 같은 노드를 같은 좌표로 그린다. 플레이어는 **같은 그림을 두 번** 본다.

**요청**: 둘의 역할 분리안. 예컨대 허브는 "지금 어디쯤 왔는가"(월드 전체 진행), 전체화면은 "다음에 어디로 갈 것인가"(스테이지 선택)로 나누는 안.

---

## 2. 메타(허브) 화면

### 2-1. [B] 화면의 주요 컨트롤이 권장 터치 타깃보다 작다

1280×720 실측이다. 44px는 일반적인 최소 터치 타깃 권장치다.

| 컨트롤                    | 크기       | 비고                      |
| ------------------------- | ---------- | ------------------------- |
| `worldPrev` / `worldNext` | **34×34**  | 월드 이동. 가장 자주 누름 |
| `hubTraining`             | **62×40**  |                           |
| `hubStartBattle`          | **136×43** | **이 화면의 주 CTA**      |
| `hubProfile` 등 탭 5종    | 85×53      | 충족                      |
| `hubRecordChip`           | 68×59      | 충족                      |

주 행동 버튼(`게임 시작!`)이 43px로 1px 모자라고, 월드 이동 화살표는 34px로 가장 작다. 기존 검토 2-5가 화살표를 [C]로 잡았는데, **주 CTA까지 걸리므로 [B]로 올린다.**

### 2-2. [B] 정보 밀도가 낮고 빈 값이 그대로 노출된다

허브는 502×628 안에 다섯 구역이 세로로 쌓인다.

| 구역              | 크기    |
| ----------------- | ------- |
| `hub-topbar`      | 470×69  |
| `world-bar`       | 470×66  |
| `hub-map`         | 470×267 |
| `hub-mission-bar` | 470×78  |
| `hub-tabbar`      | 470×75  |

상단바 실제 내용: `OBSERVATORY ID / PLAYER 01 / 오늘의 밤하늘 관측 중 / 업적 2 / 5 / 수령 2 / 골드 0 / 최단 기록 —`

**「최단 기록 —」이 빈 값 그대로 나온다.** `small` 글자는 9px, `tag`는 12px로 상단바가 작은 글자로 빽빽한 반면, 지도(267px)가 세로의 43%를 차지하면서 그 안에서는 노드가 겹친다. **면적 배분과 정보 밀도가 반대로 가 있다.**

### 2-3. [C] 스테이지 이름 자리에 여유가 8px밖에 없다

미션바의 이름 칸은 **221px 고정**이다. 여덟 월드 첫 스테이지 이름을 모두 재봤다.

| 이름                       | 폭    | 여유    |
| -------------------------- | ----- | ------- |
| 관측되지 않은 점 · 성도 밖 | 213px | **8px** |
| 베텔게우스 · 어깨의 불꽃   | 198px | 23px    |
| 메사르팀 · 세 점의 고리    | 188px | 33px    |
| 샴 · 첫 화살               | 96px  | 125px   |

지금은 넘치지 않지만 여유가 8px이고, 마크업이 `<b class="marquee">`라 넘치면 흐르게 돼 있다. **이름 길이 상한(현재 폰트 기준 213px)을 규격으로 못 박거나, 칸을 넓히는 판단이 필요하다.** 영어 표기를 넣을 계획이 있으면 지금 정해야 한다.

### 2-4. [B·판단 필요] 지도가 스테이지를 구분해 보여줄 재료가 지금은 없다

이건 시각 문제가 아니라 **데이터 문제**라 정직하게 적는다. 35개 스테이지의 기믹을 전부 뽑았다.

| 기믹                   |      스테이지 수 |
| ---------------------- | ---------------: |
| **(없음)**             |           **32** |
| 관측 잔광 ×1           | 2 (`1-2`, `1-3`) |
| 페이즈 · 형태 변화 3단 |        1 (`8-1`) |

미션바가 32개 스테이지에서 **똑같이 「무기믹 전장」**을 띄운다. 스테이지 사이에 실제로 다른 값은 거상 HP(120~1600)와 별 이름뿐이다.

**따라서 지도를 예쁘게만 만들면 "예쁜데 전부 똑같은 34칸"이 된다.** 디자인 세션에 묻고 싶은 것은 이것이다 — **지도가 스테이지를 무엇으로 구별해 보여줘야 하는가?** 후보는 셋이다.

1. 거상 HP·난이도를 노드 크기·밝기로 (지금 있는 유일한 변수)
2. 월드별 보스 초상을 노드에 (보스 10종은 이미 반입·연결돼 있다)
3. 기믹을 실제로 채우는 것이 먼저다 — 그렇다면 이 항목은 아트가 아니라 기획으로 넘어간다

---

## 3. 디자인 세션에 요청하는 산출물

우선순위 순이다. **1번이 나머지의 규격을 정한다.**

1. **별자리 좌표계 규약 한 장** — 지도 상자의 고정 비율, 노드 크기 결정 규칙(스테이지 수 3~7), 노드 사이 최소 간격. 북두칠성 7개와 양자리 3개가 같은 규칙으로 성립해야 한다.
2. **노드 상태 세트 4종** — 잠김 / 열림 / 클리어 / 현재 출격. 각 상태의 밝기·테두리·별 기호와 클리어 점등 연출.
3. **일곱 별자리 배치 시안** — 실제 별자리 비율을 유지한 좌표. 남는 여백의 처리 방식 포함.
4. **허브 면적 재배분 시안** — 502×628 안에서 지도·상단바·미션바의 비중. 빈 값(`최단 기록 —`)의 표현 포함.
5. **2-4의 답** — 지도가 스테이지를 무엇으로 구별할 것인지.

색·프레임 수·원본 규격은 **제안값으로만** 적는다. `ASSET_PLAN.md`·`assets/ASSET_MANIFEST.json` 기록은 실제 반입 승인 뒤다.

---

## 4. 이 요청서가 보지 않은 것

“문제 없음”이 아니라 **“미확인”**이다.

- **모바일·태블릿** — 데스크톱 세 해상도만 봤다. 허브가 502px 고정 폭이라 좁은 화면에서 어떻게 되는지 전혀 확인하지 않았다. 1-1의 겹침은 **좁을수록 심해지므로 모바일에서 더 나쁠 가능성이 높다.**
- **명암비 수치** — 노드의 `opacity`와 색상값은 쟀지만, 배경이 그라데이션·별 이미지라 실제 대비율은 계산하지 않았다. `locked`의 0.42가 읽히는 밝기인지는 사람이 봐야 한다.
- **실제 움직임** — 미리보기 창이 가려지면 `requestAnimationFrame`과 CSS 트랜지션이 멈춘다. 정지 구도와 계산된 값만 봤고, 호버·전환·점등이 흐르는 모습은 확인하지 못했다.
- **소리** — 범위 밖이다.
- **설정·프로필·도서관 화면** — 열어서 오류만 없는지 봤고, 구성 품질은 검토하지 않았다.

---

## 5. 참고 — 이 문제를 이미 푼 방식

남의 에셋을 가져오자는 뜻이 아니라, 같은 문제의 알려진 해법을 적어 둔다.

- **노드 겹침**: 스테이지 수가 가변인 맵은 보통 노드 크기를 고정하고 **상자를 스크롤·확대**하거나, 좌표를 그래프로 두고 **최소 간격 제약으로 자동 배치**한다. 지금처럼 고정 상자에 고정 크기 노드를 퍼센트로 놓는 방식은 개수가 늘면 반드시 겹친다.
- **진행 표시**: 별자리를 소재로 쓰는 맵은 대체로 **별 하나 = 클리어 하나**로 두고, 선은 이미 이어진 곳만 그린다. 지금은 선이 진행을 나타내고 별은 상태가 없어 역할이 반대다.
- **비율 유지**: 별자리를 상자에 맞추는 대신 **긴 축을 기준으로 맞추고 짧은 축에 여백**을 두면 모양이 보존된다. 그 여백은 관측소 UI(눈금·좌표·성운)로 채운다.
