# 연출·이펙트 개선 요구서 — 2026-08-13

> **2026-08-16 main 반입.** 이 문서는 `worktree-vfx-polish` 브랜치에만 있었고 main에 한 번도 들어온 적이 없다.
> 워크트리를 정리하면서 통째로 사라질 뻔해 옮겨 왔다.
>
> 작성 이후 08-15·08-16에 55커밋 분량의 디자인 세션 작업이 들어갔으므로, **아래 네 건 중 무엇이 이미 처리됐는지는 다시 대조해야 한다.**
> 최소한 4번(별빛 노드)은 `edc351c`가 성능 쪽만 손댔을 뿐 가시성은 그대로이고, 2번(소환 연출)은
> [UI_REVIEW_2026_08_14.md](UI_REVIEW_2026_08_14.md) 2-2가 여전히 「연출 공간이 비어 있다」로 지적하고 있다.
> §6의 발주자 결정은 그대로 유효하다.

작업 4건의 요구 문서다. 전부 **시각 표현**이고 게임 규칙·수치는 건드리지 않는다.

발주자가 실물을 보고 지적한 문제는 다음 네 가지다.

1. 타이틀 화면이 별로다. 특히 캐릭터 3명 나오는 부분. **더 단순하되 밋밋하지 않게**, 효과와 애니메이션을 넣어 예쁘게.
2. 소환 연출이 밋밋하다. **10초짜리 연출**로 멋있게.
3. 패링 이펙트가 심심하고, **실패했는지조차 모르겠다.**
4. 별빛 노드가 잘 안 보인다. **게임을 방해하지 않으면서 한눈에** 보이게.

---

## 0. 네 건 모두에 걸리는 불변 조건

**어긴 채로 올라오면 반려 대상이다.** 순서대로 읽고 시작한다.

### 0.1 기존 화풍 유지 — 발주자가 명시한 최우선 조건

현재 테마는 Dawn Observatory v3다. 근거 문서는 [UI_KIT_DAWN.md](UI_KIT_DAWN.md)이고, 팔레트는 `prototypes/stella-ball-theme.css` 최상단에 있다.

```
--night-0 #0a0f12   --night-1 #101a1e   --night-2 #16242a   --void #223438
--moon    #cfdad7   --star    #ffd2a0   --gold  #c97a45
--mist    #8ba39f   --line    #34494d
```

- **새 색을 발명하지 않는다.** 위 토큰과 `--sb-*` 유닛 색 안에서 푼다. 값이 모자라면 기존 토큰의 알파·명도 변형으로 만들고, 새 토큰을 추가할 때는 왜 기존 걸로 안 되는지 커밋 메시지에 쓴다.
- 픽셀 아트다. `imageSmoothingEnabled = false`가 이미 걸려 있다. **캔버스에 부드러운 그라디언트 원·블러 덩어리를 쓰지 않는다.** 빛은 `shadowBlur`와 알파 레이어로 낸다 — 지금 코드가 쓰는 방식이 그것이다.
- `heroes[].col`은 캔버스·게임플레이 판독용이라 **바꾸지 않는다.** UI 표면에만 `--sb-*`를 쓴다.
- 3D, 사실적 라이팅, 파티클 스프라이트 남발, 화면 전체를 덮는 플래시는 이 화풍이 아니다.

### 0.2 새 이미지 파일을 만들지 않는다

`CLAUDE.md`의 지연 아트 반입 규칙이 그대로 적용된다.

- 네 건 전부 **코드·CSS·기존 에셋만으로** 낸다. PNG·SVG를 새로 그려 넣지 않는다.
- 새 그림이 정말 필요하다고 판단되면 그 자리에서 만들지 말고 [ASSET_BACKLOG.md](ASSET_BACKLOG.md)에 범위를 좁힌 항목으로 접수하고, 그 부분은 코드 표현으로 임시 처리한 뒤 넘어간다.
- 기존에 쓸 수 있는 것: `assets/library/constellations/*.png`(실루엣 7종), `assets/original/prism-orb.svg`·`rune-glyphs.svg`, `assets/library/fx/`, `staticArt`·`metaArt`가 이미 가리키는 것들.

### 0.3 로드 순서와 소유 경계

- `stella-ball-dawn.css`는 **마지막 스타일시트**여야 하고 바로 앞이 `stella-ball-theme.css`다. `scripts/smoke-runtime.mjs`가 정확한 순서를 고정하고 있으므로, 순서를 바꾸면 같은 커밋에서 그 목록도 고쳐야 한다.
- `stella-ball-pixel-ui.js`·`stella-ball-dawn.js`는 표현 전용이다. **게임 상태를 넣지 않는다.**
- 캔버스 그리기를 추가할 때는 `registerRuntimeHook("afterDraw" | "afterSpecialDraw" | "afterFeedbackUpdate")`를 쓴다. `draw`·`drawSpecial`·`updateFeedback`을 직접 갈아끼우지 않는다.
- 부득이하게 함수를 감쌀 때는 **선행 정의를 반드시 잡아서 호출한다**(`const baseX = x;`). 안 잡으면 `npm run smoke`가 잡아낸다.

### 0.4 성능 예산

`PROJECT_CONTEXT.md`의 기준선을 깨지 않는다.

- 지속 이펙트 갱신은 **렌더 프레임당 1회**. 물리 서브스텝 안에서 갱신하지 않는다.
- HUD DOM은 **값이 바뀔 때만** 만진다.
- 새 파티클 배열을 만들지 않는다. 짧은 이펙트는 기존 `fieldFx`·`areaBursts`·`popups`를 쓰고 제자리에서 정리한다.
- `fieldFx`는 12개에서 잘린다(`updateExpanded`). 한 이벤트가 그 예산을 혼자 먹지 않게 한다.

> **함정 하나.** `fieldFx`는 `simulatePhysics` 안에서만 시간이 흐르고, `modernUpdate`는 `ball.moving`이 false면 거기 도달하기 전에 빠져나온다([game-core-render.js](prototypes/js/game-core-render.js)). 즉 **유성이 멈춘 뒤에 `fieldFx`에 넣은 이펙트는 다음 발사까지 화면에 그대로 박혀 있는다.** 정산 이후에 뜨는 연출은 `fieldFx`가 아니라 자체 시계를 쓰는 곳(`figureFx` 같은)에 얹어야 한다. 3·4번 작업이 이 경계에 걸린다.

### 0.5 완료 판정

- `npm run verify`와 `npm run smoke` 통과.
- 실제 브라우저에서 확인한다. **rAF는 탭이 백그라운드면 멈추므로**, 헤드리스로 볼 때는 프레임을 직접 돌려야 한다.
- 접근성: 애니메이션을 추가하면 `prefers-reduced-motion` 축약 경로를 함께 넣는다.

---

## 1. 타이틀 화면

### 현재 상태

`renderTitlePresentation()` — [prototypes/js/game-session.js:110](prototypes/js/game-session.js:110). DOM + CSS + 인라인 SVG다. CSS는 `prism-breakers-story.css`, `prism-breakers-polish.css`, `stella-ball-theme.css` 세 군데에 흩어져 있다.

한 화면에 지금 들어 있는 것:

| 요소                     | 내용                                                                   |
| ------------------------ | ---------------------------------------------------------------------- |
| `.title-stars`           | CSS 별 24개                                                            |
| `.title-dawn`            | 새벽 그라디언트                                                        |
| `.title-sky-map`         | SVG 별자리 — 선 3벌, 노드 6개                                          |
| `.title-shooting-star`   | 유성 1개                                                               |
| `.title-orb`             | 오브 이미지                                                            |
| `.title-hope`            | "별은 다시 이어진다"                                                   |
| **`.title-guardian` ×3** | **미리내·윤슬·샛별 초상 + 이름**                                       |
| `.title-route`           | 관측 항로 1-1 / 1-2 / 1-3                                              |
| `.title-copy`            | 워드마크, 키커, 오늘의 관측 카드, 3단계 루프, 시작 버튼, 튜토리얼 버튼 |

### 문제

**한 화면이 열두 가지를 동시에 말한다.** 캐릭터 3명은 그중에서도 가장 자리를 많이 먹으면서, 타이틀에서 해야 할 일(이 게임이 무엇인지 한 번에 보여 주기)에는 기여가 적다. 발주자가 콕 집은 부분이 이것이다.

### 요구

**거의 다 빼고, 남은 것에 연출을 몰아준다.** 발주자 결정으로 삭제 범위가 확정됐다.

**삭제 확정 — 넷 다 뺀다.**

| 뺄 것                           | 비고                                              |
| ------------------------------- | ------------------------------------------------- |
| `.title-guardian` ×3            | 실루엣으로도 남기지 않는다                        |
| `.title-route` (관측 항로)      | 진행도는 허브가 이미 보여 준다                    |
| `.title-mission` (오늘의 관측)  |                                                   |
| `.title-play-loop` (3단계 루프) | `처음인가요? 1분 튜토리얼` 버튼이 역할을 대신한다 |

**남는 것은 이게 전부다.** 하늘·별·새벽 그라디언트, 별자리 SVG, 유성, 오브, `별은 다시 이어진다`, 워드마크·키커, `게임 시작!`, `1분 튜토리얼`.

> **그래서 연출이 화면의 전부가 된다.** 정보를 이만큼 걷어내면 남은 요소가 스스로 화면을 지탱해야 한다. 아래 2~4번이 이 작업의 본체이고, 여기서 밋밋하면 그냥 빈 화면이 된다.

1. **별자리 SVG를 화면의 중심 사건으로 올린다.** 지금은 배경 장식인데, 이제 타이틀에서 게임의 정체(별을 잇는다)를 말하는 **유일한** 요소다. 선이 **그려지는 과정**을 보여 준다 — `stroke-dasharray` 애니메이션으로 노드에서 노드로 이어지고, 이어질 때마다 노드가 짧게 밝아진다. 캐릭터가 빠진 자리를 이게 채운다.
2. 진입 시퀀스에 순서를 준다. 지금은 `.title-guardian`의 `--arrival`만 계단식인데 그게 사라지므로, 순서를 새로 짜야 한다. **어두운 하늘 → 별 → 별자리 연결 → 워드마크 → CTA** 순으로 2초 안에 끝낸다.
3. 정지 상태에서도 죽지 않게 **느린 상시 움직임**을 남긴다. 별 반짝임(이미 있음), 유성(이미 있음), 별자리 선의 아주 느린 호흡. 화면이 비었으므로 이 상시 움직임의 몫이 전보다 크다.
4. 남은 요소의 **크기와 여백을 다시 잡는다.** 넷을 들어낸 자리를 그대로 비워 두면 위쪽으로 쏠린 레이아웃이 된다.

### 하지 말 것

- 워드마크와 `게임 시작!` 버튼의 위치·크기·역할은 유지한다. 진입 동선은 건드리지 않는다.
- `#enterHub`·`#titleHelp`의 id와 핸들러를 유지한다. `game-onboarding.js`가 CTA 동작을 덮어쓰므로 id가 바뀌면 첫 실행 흐름이 끊긴다.
- 1080ms 뒤 `enter.classList.add("ready")` 타이밍은 유지하거나, 바꾼다면 새 시퀀스 길이에 맞춰 같이 옮긴다.

### 검수

- 첫 진입에서 CTA가 읽히기까지 2초를 넘지 않는다.
- 1440×900과 1280×720에서 잘리는 요소가 없고, **넷을 들어낸 뒤에도 화면이 비어 보이지 않는다.**
- `prefers-reduced-motion: reduce`에서 시퀀스가 즉시 완료 상태로 나타난다.
- 삭제한 네 블록의 CSS가 세 파일(`story`·`polish`·`theme`)에 남아 죽은 규칙이 되지 않았는지 확인한다.

---

## 2. 소환 연출

### 현재 상태

`showGacha()` — [prototypes/js/game-meta.js](prototypes/js/game-meta.js) 약 1071줄. CSS는 `stella-ball-theme.css`.

지금 연출의 전부:

```js
reveal.classList.add("rolling");
drawButton.disabled = true;
setTimeout(() => {
  /* 초상화로 교체 */
}, 920);
```

`.gacha-orbit`에 유니코드 `✦ ✧ ✦` 세 글자가 돌고, **920밀리초** 뒤 결과 초상으로 갈아끼운다. 그게 끝이다.

### 문제

이 게임에서 새 별지기를 만나는 건 몇 안 되는 수집 순간인데, 연출 길이가 1초가 안 되고 내용도 글자 세 개다. 발주자 표현으로 "밋밋하다".

### 요구

**약 10초짜리 연출.** 단계를 나눠서 기대가 쌓이게 한다. 아래는 시간 배분 제안이며, 합이 10초 근처면 조정해도 된다.

| 구간     | 대략       | 내용                                                                                               |
| -------- | ---------- | -------------------------------------------------------------------------------------------------- |
| 1 · 부름 | 0 – 2.0s   | 화면이 가라앉고 관측 원이 열린다. 골드가 빠져나가는 것이 보인다                                    |
| 2 · 관측 | 2.0 – 5.5s | 별빛이 모인다. 점점 빨라지고 밝아진다                                                              |
| 3 · 응답 | 5.5 – 7.0s | 한 점이 응답한다. **여기서 등급·희귀도를 암시하지 않는다** — 현재 소환은 확정 해금이라 등급이 없다 |
| 4 · 현현 | 7.0 – 8.5s | 별자리 선이 그려지며 실루엣이 잡히고, 초상이 나타난다                                              |
| 5 · 소개 | 8.5 – 10s  | 이름·능력·한 줄 소개가 자리를 잡는다                                                               |

추가 요구:

- **스킵은 첫 소환만 잠근다**(확정). 최초 1회는 10초를 온전히 보여 주고, 두 번째부터는 아무 입력이나 받으면 4단계(현현)로 점프한다. "처음인지"의 판정은 이미 있는 진행 저장(`progress`)에 플래그 하나를 얹어 해결하고, 저장 실패 시에는 **스킵 가능** 쪽으로 넘어간다 — 못 건너뛰는 쪽이 더 나쁜 실패다.
- 기존 `pullGachaHero()`의 결과 판정·골드 차감·저장은 **그대로 둔다.** 연출은 결과가 정해진 뒤의 표현일 뿐이다.
- 연출 도중 화면을 나가도(`showMeta()` 등) 상태가 깨지지 않아야 한다. 지금 코드가 `document.body.contains(reveal)`로 확인하는 이유가 그것이다. 같은 방어를 유지한다.
- 실패 경로(`reason === "gold"`, `"complete"`)는 연출 없이 지금처럼 즉시 처리한다.
- SFX는 기존 `playSfx("unlock")` 계열에서 고른다. **새 음원을 만들지 않는다.** 단계별로 구분이 필요하면 `ASSET_BACKLOG.md`에 접수한다.

### 하지 말 것

- 확률 연출·등급 연출을 넣지 않는다. 현재 소환은 100골드 확정 1명이고, `PROJECT_CONTEXT.md`의 범위 원칙이 확률형 중복 뽑기를 금지한다. **없는 시스템을 연출로 암시하면 안 된다.**
- 10초를 채우려고 같은 동작을 반복하지 않는다. 비어 있으면 시간을 줄인다.

### 검수

- 실제 소환에서 시작부터 이름이 읽히기까지 10초 ±1.5초.
- 스킵이 어느 구간에서 눌려도 결과가 동일하고, 중복 실행되지 않는다.
- 연속 3회 소환 후에도 DOM에 잔여 노드·타이머가 남지 않는다.
- `prefers-reduced-motion: reduce`에서 2초 이내로 축약된다.

---

## 3. 패링 이펙트

### 현재 상태

전부 [prototypes/js/game-figure.js](prototypes/js/game-figure.js)에 있다.

타이밍 상수:

```js
parryWindow: 0.4,  contactMemory: 0.18,  nearMissWindow: 0.14,
falseStartCooldown: 0.3,  missCooldown: 0.72
```

지금 나오는 것:

| 상황          | 현재 표현                                                                                          |
| ------------- | -------------------------------------------------------------------------------------------------- |
| 창 열림       | 유성 둘레 링. `flash`와 **같은 그리기 경로**, `pulse`만 1.0                                        |
| **성공**      | `flash = 0.44` → 같은 링이 조금 밝게 · `runeBurst 0.92` · `relay` fieldFx 0.48초 · 팝업 `별빛 n/7` |
| **늦은 입력** | `flash = 0.2` → **같은 링이 조금 어둡게** · 회색 `relay` 0.26초 · 작은 팝업 `공명 놓침` · 토스트   |
| **창 헛돔**   | 토스트 `공명 비움 · 다음 접점을 기다리세요`. **그림 없음**                                         |
| **노드 소실** | 토스트 `패링 실패 · 모은 별빛이 흩어졌습니다`. **그림 없음**                                       |

### 문제

두 가지가 겹쳐 있다.

1. **성공과 실패가 같은 그림이다.** `drawFigureShot`의 링 하나가 창 열림·성공·늦음을 전부 그리고, 차이는 밝기뿐이다. 혼전 중에 밝기 차이를 읽을 수 없다.
2. **가장 아픈 실패에 그림이 아예 없다.** 모아둔 별빛이 통째로 날아가는 순간이 토스트 한 줄이다. 발주자가 "실패한지도 모르겠다"고 한 게 이 지점이다.

### 요구

**세 결과가 서로 다른 모양이어야 한다.** 색만 바꾸는 것으로는 부족하다 — 모양·방향·움직임이 달라야 혼전에서 읽힌다.

1. **창 열림**은 "지금 누를 수 있다"는 예고다. 유성에 붙어 **모이는** 방향의 움직임. 0.4초 안에 닫히는 게 보여야 한다 — 남은 시간이 링의 길이나 두께로 읽히면 좋다.
2. **성공**은 접점에서 **터져 나가는** 방향. 그 자리에 별빛 노드가 생기므로, 이펙트가 노드로 수렴해 끝나면 인과가 읽힌다. 유닛 색(`g.col`)을 쓴다.
3. **실패**는 성공의 반대 방향·반대 리듬이다. 흩어짐, 꺼짐, 끊김. `--mist #8ba39f`나 `--line #34494d` 쪽 무채색으로 빠진다.
4. **노드 소실은 별도로 크게 낸다.** 지금까지 모은 노드들이 **각자 자기 자리에서** 흩어지는 것을 보여 준다. 노드 위치를 이미 알고 있으므로 새 자료구조가 필요 없다. 이 게임에서 가장 비싼 실패이므로 가장 큰 그림이어야 한다.
5. **화면 정지(히트스톱)는 이번 범위에서 제외한다**(확정, §6-5). 기존 `impactStop`도 이 목적으로 새로 건드리지 않는다. 실패에 흔들림을 넣지 않는 것은 그대로 유지한다 — 벌처럼 느껴지면 재시도를 막는다.

### 하지 말 것

- **타이밍 상수 5개를 바꾸지 않는다.** 이번 작업은 표현만이다. 창이 짧아 보이는 게 진짜 문제라면 그건 별도 밸런스 판단이고, 여기서 조용히 바꾸면 안 된다.
- `consumeTrainingParry`·`requestTrainingParry`·`finishFigureShot`의 판정 순서와 반환값을 바꾸지 않는다. 이펙트 호출만 얹는다.
- 노드 소실 연출은 **정산 이후에도 뜰 수 있으므로** `fieldFx`에 담지 않는다(§0.4 함정).

### 검수

- 소리를 끄고 텍스트를 가린 상태에서 성공/늦음/소실 세 가지를 구분할 수 있다.
- 세 개가 연속으로 일어나도 서로 지워지지 않는다.
- 유성이 멈춘 뒤에 뜬 이펙트가 다음 발사 전에 사라진다.
- `fieldFx` 12개 상한을 혼자 채우지 않는다.

---

## 4. 별빛 노드 가시성

### 현재 상태

`drawFigureShot` — [prototypes/js/game-figure.js:902](prototypes/js/game-figure.js:902).

```js
x.globalAlpha = 0.4; // 943줄까지 유지된다
for (const node of nodes) {
  x.fillStyle = node.col || "#dff3ea";
  x.arc(node.x, node.y, 5, 0, Math.PI * 2); // 반지름 5px
  x.fill();
}
```

- 노드 점: **반지름 5px, 불투명도 0.4**, 유닛 색
- 간선: 파선 `[5,6]`, 불투명도 0.4, `#9adfc9`
- 이름표: 12px 모노, 불투명도 0.9, 유성 위

### 문제 — 실측

원인은 **크기와 대비 두 가지가 겹친 것**이고, 색 하나의 문제가 아니다. 아래는 1280×720 뷰포트에서 실제로 잰 값이다.

**크기.** 캔버스 버퍼는 720×900인데 화면에는 496×620으로 나온다. 배율 **0.689**다.

|             | 캔버스 좌표 | 실제 화면      |
| ----------- | ----------- | -------------- |
| 노드 반지름 | 5px         | **3.4 CSS px** |
| 노드 지름   | 10px        | **6.9 CSS px** |

**대비.** 바닥을 실측하면 `[24,24,44]`, 상대 휘도 25.4다. 여기에 지금 코드 그대로(불투명도 0.40, 반지름 5) 각 유닛 색을 합성한 결과:

| 유닛       | 색        | 합성 후        | 대비비   |
| ---------- | --------- | -------------- | -------- |
| 살별       | `#9ee477` | `[78,106,74]`  | 3.37     |
| 샛별       | `#f2c56b` | `[111,93,69]`  | 3.29     |
| 별하       | `#70dce1` | `[59,102,116]` | 3.25     |
| 윤슬       | `#5fe0cf` | `[52,104,109]` | 3.23     |
| 모루       | `#ffac67` | `[116,83,68]`  | 3.09     |
| 달무리     | `#bca7ff` | `[90,81,128]`  | 3.00     |
| **그믐**   | `#9f83ff` | `[78,67,128]`  | **2.59** |
| **미리내** | `#ef718d` | `[110,60,83]`  | **2.54** |

**여덟 색 전부가 2.5~3.4 구간에 몰려 있다.** 특정 유닛만 나쁜 게 아니라 전체가 고르게 낮고, 불투명도 0.40이 그 원인이다. 가장 나쁜 둘은 보라(그믐)와 분홍(미리내)이다.

정리하면, 3.4 CSS px짜리 점을 대비비 2.5~3.4로 그리고 있다. **크기를 키우면 판을 가리므로, 불투명도와 대비 쪽에서 푸는 게 맞다.**

### 요구

**한눈에 들어오되 판을 가리지 않는다.** 두 조건이 반대 방향이므로 크기가 아니라 **대비**로 푼다.

1. **불투명도부터 걷는다. 이게 가장 큰 원인이다.** 지금 `globalAlpha = 0.4`가 간선용으로 설정된 뒤 점 그리기까지 그대로 흘러들어 간다(930줄에서 켜고 948줄까지 유지). 점과 선의 불투명도를 분리하고, 점은 거의 불투명하게 그린다.
2. 노드에 **밝은 코어 + 유닛색 후광** 2단 구조를 준다. 코어를 유닛과 무관한 밝은 고정색(`--moon #cfdad7`·`--star #ffd2a0` 계열)으로 두면 여덟 색이 전부 같은 바닥 대비를 얻고, 후광이 누구인지를 말한다. 유닛 색만으로 대비를 만들면 미리내(2.54)·그믐(2.59)이 계속 뒤처진다.
3. 크기는 크게 늘리지 않는다. 반지름 5 → 6~7 정도가 상한이고, 그 이상은 유성·유닛과 겹쳐 판독을 방해한다. **화면 배율이 0.689이므로 캔버스 반지름 7이 화면 4.8px이다** — 크기로 푸는 데는 한계가 있다.
4. **새로 생긴 노드는 잠깐 더 밝게** 한다. 방금 무슨 일이 있었는지가 위치와 함께 읽힌다. 0.3초 안에 평상 밝기로 내려온다.
5. 간선은 지금처럼 **노드보다 조용하게** 유지한다. 주인공은 점이다.
6. **이름표 `별빛 n/7 · 3점부터 발동`은 삭제한다**(확정). 유성을 따라다녀 이동 중 읽히지도 않고, 노드가 제대로 보이면 개수는 점을 세면 된다. 한 프레임에 24회씩 나가는 캔버스 텍스트도 그만큼 줄어든다.
   - **대신 잃는 것이 있다.** "3개부터 발동"이라는 규칙을 첫 플레이어가 배울 곳이 사라진다. 이 안내는 온보딩이나 훈련장 최초 진입 쪽에서 한 번 해 주는 것으로 옮기고, 이번 작업 범위 밖이라면 `ASSET_BACKLOG.md`가 아니라 후속 과제로 적어 둔다.
   - 3개에 도달하는 순간을 **노드 표현 자체로** 알린다(예: 3번째 노드가 생길 때 간선이 처음으로 이어져 닫힌 도형이 되는 것이 보이게). 글자를 없애는 대신 그림이 그 일을 해야 한다.

### 하지 말 것

- 노드가 유성·유닛·보스 위에 그려져 조작 판단을 가리면 안 된다. 그리기 순서를 확인한다.
- `node.col`을 버리지 않는다. 누가 만든 노드인지는 유지해야 할 정보다.
- 노드 좌표·개수·판정에 손대지 않는다. 표현만이다.

### 검수

- 여덟 유닛 색 **전부**의 대비비를 위 표와 같은 방법으로 다시 재고, **최저값이 현재 2.54보다 확실히 올라간다.** 특히 미리내 `#ef718d`와 그믐 `#9f83ff`를 확인한다.
- 유성이 최고 속도로 지나갈 때 노드가 궤적에 묻히지 않는다.
- 노드 7개가 동시에 있어도 화면이 어수선해지지 않는다.
- 대비 근거를 **픽셀 실측으로** 남긴다. 눈대중 판단은 근거로 인정하지 않는다. 측정 코드는 위 표를 만든 것과 같은 방식이면 된다 — 바닥 한 점을 찍고, 노드를 그리고, 같은 점을 다시 찍어 상대 휘도로 비교한다.

---

## 5. 작업 순서 제안

독립적이라 순서를 바꿔도 되지만, 이렇게 하면 확인이 쉽다.

1. **4번 (노드 가시성)** — 가장 작고, 고칠 지점이 명확하고, 3번의 배경이 된다.
2. **3번 (패링)** — 4번에서 만든 노드 표현을 성공 연출의 도착점으로 쓴다.
3. **2번 (소환)** — 독립적이고 분량이 가장 크다.
4. **1번 (타이틀)** — 삭제 범위가 확정됐으므로 되돌아올 일은 없지만, 넷을 들어낸 뒤 남은 화면을 다시 잡는 데 시간이 가장 많이 든다.

3·4번은 `game-figure.js` 한 파일에 몰려 있다. **다른 세션이 같은 파일을 자주 만지므로**(최근 `524fafb`도 이 파일이다) 시작 전에 `git status`로 확인하고, 겹치면 워크트리를 따로 쓴다.

1번은 `game-session.js` + CSS 세 파일, 2번은 `game-meta.js` + `stella-ball-theme.css`라 서로 겹치지 않는다. **1·2번과 3·4번은 병렬로 진행해도 안전하다.**

---

## 6. 발주자 결정 — 2026-08-13 확정

다섯 건 모두 확정됐다. 아래가 최종이며, 1~4절의 요구는 이 결정에 맞춰 이미 고쳐져 있다.

| #   | 항목                                  | 확정                                                        |
| --- | ------------------------------------- | ----------------------------------------------------------- |
| 1   | 타이틀의 캐릭터 3명                   | **완전히 뺀다.** 실루엣으로도 남기지 않는다                 |
| 2   | 타이틀의 안내 3종                     | **셋 다 뺀다.** `관측 항로`·`오늘의 관측`·`3단계 루프` 전부 |
| 3   | 소환 스킵                             | **첫 소환만 잠금.** 두 번째부터는 아무 입력으로 건너뛴다    |
| 4   | 노드 이름표 `별빛 n/7 · 3점부터 발동` | **없앤다.** 개수는 노드 자체로 읽히게 한다                  |
| 5   | 패링 히트스톱                         | **이번 범위에서 제외.** 아래 메모를 읽고 넘어간다           |

### 5번에 대한 메모 — 사실 확인

건너뛰기로 한 이유가 "다른 세션이 슬로우를 넣은 것 같다"였는데, **확인해 보니 새로 들어온 슬로우는 없다.** 최신 커밋(`524fafb Add contact-memory parry timing`)의 `prototypes/js/` 변경분에 `impactStop`·`battle.slow`·`finisherScale` 관련 추가가 하나도 없다.

지금 있는 시간 조작은 전부 이전부터 있던 것이다.

| 기제            | 위치                                           | 용도                         |
| --------------- | ---------------------------------------------- | ---------------------------- |
| `impactStop`    | `game-feedback.js:484`에서 설정, 루프에서 소비 | 타격 순간의 프레임 정지      |
| `battle.slow`   | `game-feedback.js:674`에서 `0.08`              | 보스 회전 감속               |
| `finisherScale` | 루프 안                                        | 마무리 연출의 연속 시간 배율 |

즉 히트스톱 자리는 여전히 비어 있고, 넣는다면 기존 `impactStop`을 그대로 쓰면 된다. **이번에는 안 넣기로 한 결정을 그대로 따르되**, 3번 작업을 하다 성공 순간이 허전하면 이 메모를 근거로 다시 물어본다.

---

## 7. 참고 문서

- [UI_KIT_DAWN.md](UI_KIT_DAWN.md) — 팔레트·버튼 킷·데코 규칙의 단일 기준
- [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md) — 범위 원칙과 성능 기준선
- [prototypes/ARCHITECTURE.md](prototypes/ARCHITECTURE.md) — 파일별 소유 경계와 로드 순서
- [prototypes/MAINTENANCE.md](prototypes/MAINTENANCE.md) — 성능·검증 변경 절차
- [ASSET_BACKLOG.md](ASSET_BACKLOG.md) — 새 그림·SFX가 필요해졌을 때의 접수처
