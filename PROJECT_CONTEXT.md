# Project Context — 지속 인수인계 메모

> 새 세션 또는 컨텍스트 압축 후에도 이 문서를 먼저 읽고 프로젝트를 이어 간다.

## 현재 확정한 방향

게임의 공식 표시명은 **Stella Ball**이다. 세계관상 「잊힌 별의 관측자」에서 관측자는 유성으로 3명의 별지기를 함께 굴리고, 멈춘 위치에서 발동하는 각성 공격과 별자리 배율로 공허 거상을 공략하는 60~90초 액션 전략 게임이다.

한 줄: **당구식 궤적 설계 × 움직이는 별지기 각성 × 수집형 파티 조합 RPG**

현재 데모: `prototypes/prism-breakers.html`

런타임 구조와 파일별 수정 위치: `prototypes/ARCHITECTURE.md`

## 절대 지킬 원칙

1. 유저 0명에서도 완결된 싱글 플레이여야 한다. 온라인 인구가 난이도·콘텐츠·클리어 가능성을 바꾸면 안 된다.
2. Hive는 필수 코어가 아니라 일일 도전, 개인 기록, 리더보드, 이벤트, 글로벌 운영의 확장 계층이다.
3. 캐릭터는 단순 `ATK +n%`가 아니다. 충돌, 분열, 중계, 이동 후 각성 공격 중 하나를 바꿔 조합의 이유를 만들어야 한다.
4. 한 판은 60~90초, 조작과 목표는 30초 안에 읽혀야 한다.
5. 해커톤 MVP에 실제 가챠, 상점·구매형 경제, 과도한 콘텐츠, 실시간 멀티플레이를 넣지 않는다. 골드는 일반 스테이지 클리어당 고정 100을 주는 누적 표시만 구현하며, 소비처는 별도 결정 전까지 만들지 않는다. 8명 중 3명을 고르는 로스터 선택만으로 수집형 확장 가능성을 보인다.
6. 실제 Console 왕복이 끝나지 않은 Hive 기능은 제출 성과처럼 말하지 않는다. 이전 규칙용 헤드리스 봇 결과는 보관하되, 현재 브라우저 전투와 맞는 새 모델을 만들기 전에는 최신 밸런스 근거로 쓰지 않는다.

## Hive 리더보드 원칙

Stella Ball의 Hive 연동은 전투 중이 아니라 **한 판이 끝난 뒤의 결과**에만 작동한다. 데일리 시드는 보스 패턴·보스 체력·강화 카드 묶음을 고정하고, 같은 조건에서 아래 결과를 저장한다.

```text
dailySeed, clearTimeMs, shotsUsed, shotsRemaining,
maxSingleHit, partyIds, upgradeIds, gameVersion
```

- 메인 순위: `클리어 여부 → 남은 발사 수 → 클리어 시간`.
- 별도 기록: 최대 단발 피해, 최소 발사 클리어, 파티/강화별 최고 기록.
- 최대 피해는 메인 순위에 섞지 않는다. 점수 누적 파밍보다 명확한 클리어를 우선하기 위해서다.
- N=1은 로컬 최고 기록, 다수 유저는 같은 표면의 글로벌 경쟁이다. 어떤 경우에도 유저 기록이 게임 난이도를 바꾸지 않는다.

## 현재 구현 기준과 다음 우선순위

상세 현황은 `PROGRESS_REPORT.md`를 먼저 본다. 메인 화면은 아군 비연·리아·루미와 별자리 진행도를 전면에 두고, 현재 임무 `1-2 균열 회랑`, 세 단계 플레이 루프, `별자리 관측 시작` CTA를 한 화면에서 설명한다. 현재 플레이 가능한 캠페인은 첫 실행 수업 `1-1 별빛의 첫 충돌`과 첫 일반 전투 `1-2 균열 회랑`이다. 1-2는 공명 범퍼 두 개만 새 기믹으로 사용하며, 이후 스테이지도 기믹을 하나씩만 추가한다. 1-3 이후 노드는 잠금 상태다.

온보딩 안내는 전장 하단의 짧은 루나 대화 배너다. 문장은 순서대로 나타나고, 표시 중에는 캔버스 포인터 입력과 전투 단축키를 잠근다. 각 실습의 `직접 해보기` 버튼을 누르면 배너를 제거해 전장을 비우고, 유성 출발점·목표 고리·손가락 포인터·점선만 남겨 해당 실습 입력을 유도한다. 충돌·피해·정산으로 다음 설명이 발생하면 배너와 입력 잠금이 함께 복원된다.

일반 스테이지 클리어는 현재 고정 **100 골드**를 로컬 진행 데이터에 누적하고, 메타 상단과 결과 화면에 표시한다. 1-1 수업과 무한 훈련장은 골드를 주지 않으며, 소비처·상점·가챠는 의도적으로 미구현이다.

전투는 핀볼이 아니라 당구형 물리다. 현재 코드에서 각 샷은 하단 발사석에서 시작하며, 샷 안에서 공과 선택한 별지기 3명은 모두 물리 오브젝트다. 대부분의 별지기는 움직인 뒤 모든 공이 멈추면 현재 위치에서 고유 공격을 한다. 리아는 예외로 정산 공격 없이 이동 중 보스의 물리 충돌을 무시해 관통하고, 속도에 비례한 회전 칼날 피해를 준다. 기본 로스터는 가온·비연·루미·하루·리아·세라·태오·닉스의 8명이다.

성능 기준선은 최대 3개의 90Hz 물리 서브스텝, 렌더 프레임당 1회 지속 이펙트 갱신, 60Hz 궤적 샘플링이다. 전장 바닥·균열·장식·프레임은 스테이지별 정적 캔버스 한 장으로 캐시하고, HUD DOM은 값이 변할 때만 갱신한다. 이펙트가 몰리면 `game-feedback.js`의 VFX 예산이 오래된 장식 레이어 수만 줄이며 타격 판정·팝업·최신 피드백은 유지한다. 이 기준을 깨는 새 파티클 배열이나 서브스텝 내부 DOM 갱신을 추가하지 않는다.

타격 연출도 성능 기준에 포함한다. 별지기의 보스 직격은 `contact` 프로필의 28ms 히트스톱만 사용한다. 정산은 캐릭터별 확대·집중선·별자리 노드·충전/해방/명중 SFX를 각자의 차례에만 생성하며, 시간 배율 `0.46 → 0.62 → 0.82`로 복귀한다. 정산 큐는 캐릭터마다 간격을 두어 겹치지 않고, 명중 때는 전체 HUD 대신 보스 체력만 갱신한다.

일반 보스 체력은 260이다. 몸체도 즉시 피해를 받으며, 이동하는 약점은 더 큰 보상을 주는 목표다. 전투를 끊는 방벽/코어 페이즈는 없다. `무한 훈련장`은 보스 체력 무한과 유성 자동 보충으로 물리와 배율을 확인하는 QA 전장이다.

`1-2`의 공명 범퍼는 스테이지 정의의 좌표를 `setupBattle()`이 런타임 오브젝트로 생성한다. 잡몹·롤오버·추가 장애물을 한꺼번에 넣지 않는다. 다음 스테이지는 먼저 플레이 목적을 정한 뒤 새 기믹 하나만 선택한다.

현재 밸런스는 **미확정**이다. 유닛 충돌의 최소 운동량, 별자리 배율, 유닛 각성 공격이 한 발 안에서 중첩되어 원샷 클리어 또는 과도한 체공이 생길 수 있다. 이 문제는 다음 작업에서 현재 물리 하니스로 측정한 뒤에만 수정한다. 이전 4발·8인·가로 반사 모델의 `bot/latest-report.json`은 역사 기록일 뿐 현재 수치 근거가 아니다.

다음 우선순위:

1. `bot/run-bot.mjs`를 현재 당구 물리·이동 별지기·분열체·8인·5발·1-2 범퍼 규칙으로 갱신하고, 클리어율·평균 샷·원샷 비율·평균 샷 지속시간을 출력
2. 하니스에서 목표 클리어율 60~70%, 원샷은 희귀한 보상급 비율인지 확인하며 보스 체력·마찰·충돌 가속·별자리 배율·각성 공격을 함께 스윕
3. `1-3`의 플레이 목적과 단일 신규 기믹을 정하고, 1-2에 추가 기믹을 덧붙이지 않기
4. macOS에서 메인 CTA·온보딩 입력 잠금·1-2 범퍼·정산 연출·SFX를 실제 브라우저로 확인
5. `HIVE_SETUP.md`의 실제 AppID·데이터 스토어·리더보드 왕복을 완료한 뒤 일일 기록 UI를 열기

## 아트 에셋 기준

현재 아트 기준은 **Tiny Swords 계열 픽셀 캐릭터 + 어두운 유적 배경 + 고대비 프리즘 이펙트**다. 기능 작업 중 발견한 새 디자인·에셋·애니메이션·SFX 필요사항은 먼저 `ASSET_BACKLOG.md`에 후보로 기록하고, 사람의 일괄 아트 작업 승인 뒤에만 실제 파일을 만든다. 실제 반입 목록과 프레임 규격은 `ASSET_PLAN.md`, `assets/ASSET_MANIFEST.json`, 향후 사용 후보는 `assets/ASSET_LIBRARY.md`, 출처는 `assets/ATTRIBUTION.md`에서 관리한다.

재사용 가능한 프리즘 에셋 패키지(원본 11개 + 4프레임 애니메이션 시트)는 `/Users/8rulerstar/Assets/Prism-Breakers-Pixel-Asset-Pack/`에 별도 보관한다.

- 별지기: 가온, 비연, 루미, 하루, 리아, 세라, 태오, 닉스 8명. 보스: 공허 거상.
- 유성·별지기·보스 약점은 `assets/original/`의 Prism Breakers 전용 픽셀 SVG다.
- 서로 다른 픽셀 팩을 무분별하게 섞지 않는다. Retro Lines와 Tiny RPG Soldier & Orc는 현재 화면에 사용하지 않는다.
- Canvas 렌더링 때 반드시 `imageSmoothingEnabled = false`를 쓴다.

## Codex 협업 기록 — 반드시 유지

공식 Track 1은 Codex 활용 과정 설명을 선택 가산점으로 받고, 어디에 사용했는지·무엇을 구현했는지·무슨 문제를 해결했는지·사람이 무엇을 결정했는지 설명하도록 한다.

- 제출용 원본: `CODEX_COLLABORATION.md`
- 매일 기록: `DEVLOG.md`
- 구현 후 자동 검증: `bot/run-bot.mjs`와 `bot/latest-report.json`
- 개발 이력 및 검증 운영: `EVIDENCE_PROTOCOL.md` — `main` push → GitHub Actions artifact → GitHub Pages 배포 → DEVLOG의 commit SHA 연결
- Codex 에셋 제작 근거: `assets/ASSET_GENERATION_LOG.md`
- 최신 보고/세션 인수인계: `PROGRESS_REPORT.md`

과정의 핵심 사실: Codex는 다수의 프로토타입, 웹 물리/UI, 향후 자동 테스트를 빠르게 만들고 비교하는 역할이다. 사람은 죽음 누적 구조와 직관성이 낮은 보드형 시안을 폐기하고, Stella Ball의 코어·범위·재미 기준을 결정했다.

개발 이력은 대화 전문이 아니라 작은 커밋, 당일 push, Actions 검증 리포트, Pages 배포, DEVLOG의 판단 기록으로 남긴다. 공개 `main`에서는 amend/rebase/force-push를 피한다.

첫 공개 기준선은 [`76a8a3d`](https://github.com/8rulerstar/prism-breakers/commit/76a8a3da7f6c9f80d8edfb944e8f15804048c5fe)이며, 첫 Actions 검증과 Pages 배포가 2026-08-07에 성공했다. 다음 세션도 이 개발 이력을 작은 작업 단위로 이어 간다.

## 폐기·보존 대상

- 폐기된 Echo Run의 `game.js`, `style.css`와 과거 탐색용 프로토타입은 2026-08-11 정리했다. 루트 `index.html`은 Stella Ball의 공식 진입점이며, `RUN_STELLA_BALL.command`/`.bat`과 `PLAY_WINDOWS.cmd`가 설치 없는 운영체제별 실행 진입점이다.
- `prototypes/`의 이전 시안: 탐색·Codex 협업 기록으로 보존한다. 현재 메인으로 홍보하지 않는다.
- 글로벌 죽음 누적 / 플레이어 데이터로 난이도를 바꾸는 구조: 다시 채택하지 않는다. 대규모 난독화·악용·하향 나선 문제가 있다.
