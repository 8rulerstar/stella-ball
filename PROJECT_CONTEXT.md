# Project Context — 지속 인수인계 메모

> 새 세션 또는 컨텍스트 압축 후에는 [DOCUMENTATION_INDEX.md](DOCUMENTATION_INDEX.md)와 이 문서를 먼저 읽고 프로젝트를 이어 간다. 이 문서는 현재 구현 규칙의 단일 기준선이다.

## 현재 확정한 방향

게임의 공식 표시명은 **Stella Ball**이다. 세계관상 「잊힌 별의 관측자」에서 관측자는 유성으로 3명의 별지기를 함께 굴리고, 굴러가 멈춘 별지기의 각성 공격과 충돌이 자동으로 남기는 별빛이 그리는 별자리로 공허 거상을 공략하는 60~90초 액션 전략 게임이다.

한 줄: **당구식 궤적 설계 × 별지기 각성 정산 × 공명 별자리 배율 × 수집형 파티 조합 RPG**

현재 데모: `prototypes/prism-breakers.html`

런타임 구조와 파일별 수정 위치: `prototypes/ARCHITECTURE.md`

## 절대 지킬 원칙

1. 유저 0명에서도 완결된 싱글 플레이여야 한다. 온라인 인구가 난이도·콘텐츠·클리어 가능성을 바꾸면 안 된다.
2. Hive는 필수 코어가 아니라 일일 도전, 개인 기록, 리더보드, 이벤트, 글로벌 운영의 확장 계층이다.
3. 캐릭터는 단순 `ATK +n%`가 아니다. 충돌, 분열, 중계, 이동 후 각성 공격 중 하나를 바꿔 조합의 이유를 만들어야 한다.
4. 한 판은 60~90초, 조작과 목표는 30초 안에 읽혀야 한다.
5. 해커톤 MVP에 유료 재화·확률형 중복 뽑기·현금 구매형 경제, 과도한 콘텐츠, 실시간 멀티플레이를 넣지 않는다. 상점은 **획득한 골드로만 사는 외형 전용 상점**으로 한정한다. 유성 도색은 색상만 바꾸며 피해·속도·물리에 관여하지 않는다. 골드는 일반 스테이지 클리어당 고정 100을 주며, `별빛 소환`은 100골드로 아직 만나지 못한 별지기 1명을 확정 해금하는 짧은 수집 루프다. 8명 중 3명을 고르는 로스터 선택으로 수집형 확장 가능성을 보인다.
6. 실제 Console 왕복이 끝나지 않은 Hive 기능은 제출 성과처럼 말하지 않는다. 이전 규칙용 근사 봇 결과는 보관만 하고, 최신 수치는 현재 런타임을 직접 구동하는 하니스와 실기 검증을 함께 근거로 쓴다.

## Hive 리더보드 원칙

Stella Ball의 Hive 연동은 전투 중이 아니라 **한 판이 끝난 뒤의 결과**에만 작동한다. 데일리 시드는 보스 패턴·보스 체력·시작 배치를 고정하고, 같은 조건에서 아래 결과를 저장한다.

```text
dailySeed, clearTimeMs, shotsUsed, shotsRemaining,
maxSingleHit, partyIds, gameVersion
```

- 메인 순위: `클리어 여부 → 남은 발사 수 → 클리어 시간`.
- 별도 기록: 최대 단발 피해, 최소 발사 클리어, 파티별 최고 기록.
- 최대 피해는 메인 순위에 섞지 않는다. 점수 누적 파밍보다 명확한 클리어를 우선하기 위해서다.
- N=1은 로컬 최고 기록, 다수 유저는 같은 표면의 글로벌 경쟁이다. 어떤 경우에도 유저 기록이 게임 난이도를 바꾸지 않는다.

## 현재 구현 기준과 다음 우선순위

상세 현황은 `PROGRESS_REPORT.md`를 먼저 본다. 메인 화면은 아군 미리내·윤슬·별하와 별자리 진행도를 전면에 두고, 현재 출격 스테이지와 세 단계 플레이 루프, `별자리 관측 시작` CTA를 한 화면에서 설명한다. 캠페인은 **별자리 하나가 월드, 그 꼭짓점이 스테이지**다. 양자리(3)·화살자리(4)·까마귀자리(4)·카시오페이아(5)·백조자리(5)·오리온자리(6)·북두칠성(7)의 7월드, 총 34스테이지이며 각 월드는 왼쪽 첫 노드부터 오른쪽으로 순차 해금한다. 지도에서는 `1-1` 같은 번호가 가장 크게 보이고, 그 아래에 별 이름·부제가 나온다. 진행은 `progress.clears` 하나로 판정한다 — 캠페인 i번째를 깨면 i+1번째가 열리므로 새 저장 항목이 없다. 메타 하단 탭은 `프로필 · 소환 · 관측 · 상점 · 설정`이며, 프로필 안에 관측자 기록·순위표·우편함이 함께 있다. 순위표는 현재 로컬 기록이며 화면에 그렇게 표기한다.

온보딩 안내는 전장 하단의 짧은 루나 대화 배너다. 문장은 순서대로 나타나고, 표시 중에는 캔버스 포인터 입력과 전투 단축키를 잠근다. 각 실습의 `직접 해보기` 버튼을 누르면 배너를 제거해 전장을 비우고, 유성 출발점·목표 고리·손가락 포인터·점선만 남겨 해당 실습 입력을 유도한다. 충돌·피해·정산으로 다음 설명이 발생하면 배너와 입력 잠금이 함께 복원된다.

1-1 수업은 캠페인과 **같은 규칙**으로 돈다(2026-08-21). 실험 상수 우회(`onboardingRunning()`)는 제거했고, 자동 공명·별빛 경제·노드 조준이 수업에도 그대로 적용된다. 네 단계다 — `0 끌어서 발사 → 1 노드 셋 찍고 Space → 2 남긴 별빛으로 별자리 → 3 3인 실전`. 0단계만 루나가 발사 각도를 거상으로 고정하고(그래서 그 단계만 조향 입력도 막는다), 2단계는 보스 둘레에 안내별 셋을 미리 깔아 양자리 한 번을 확정 현현시킨다 — 플레이어는 실전 전에 별자리 현현·능력·피해까지 한 번은 반드시 본다. 이 보정은 수업 상태에만 존재한다. 옛 조향·`Space` 패링 수업의 잔해(비행 중 정지, 항로 링, 「지금이에요 — Space」 문구)는 같은 날 전부 걷었다.

1-1 수업을 마치면 골드 대신 **무료 소환권 1장**을 주어 세 번째 자리를 바로 채우게 한다. 골드는 자동 지급하지 않는다. 일반 스테이지 클리어는 고정 **100 골드**를 각각의 `관측 보상함` 항목으로 적립하고, 각 업적도 일회성 골드 보상(150·100·200·400·300)을 가진다. 두 종류 모두 **업적 탭에서 항목마다 직접 수령해야** 잔액에 들어간다. 새 업적 달성 토스트는 전투 밖에서 누르면 업적 탭으로 바로 이동한다. 허브 상단 기록 칩은 수령 대기 수를 배지로 보여 준다. 시작 보유 별지기는 **샛별·미리내·윤슬** 3명이며, 나머지 별하·살별·달무리·모루·그믐은 메타의 `별빛 소환`에서 100골드로 한 명씩 확정 해금한다. 1-1 수업과 무한 훈련장은 골드를 주지 않으며, 유료 재화·확률형 중복·천장·현금 결제는 의도적으로 미구현이다. 골드는 `별빛 소환`(100) 외에 관측소 상점의 유성 도색(개당 500)과 별지기 도색(개당 320)에 쓴다. 출석 보상은 하루 한 번, 연속 일수에 따라 60~220 골드를 준다.

전투는 핀볼이 아니라 당구형 물리다. 첫 샷은 하단 발사석에서 시작하고 다음 샷은 이전 유성이 멈춘 자리에서 잇는다(1-1 수업만 발사석 고정). 샷 안에서 공과 선택한 별지기 3명은 모두 물리 오브젝트다. **각성과 공명은 서로 다른 두 계통이다(2026-08-16 되돌림).** 각성은 움직임이 만든다 — 유성에 밀리거나 쿠션에 튀어 실제로 굴러간 별지기는 깨어나고, 멈춘 자리에서 고유 정산 공격을 한다. 공명은 별자리 계통이다 — 별지기와 부딪히면 자동으로 일어나 그 접점이 별빛 노드가 되고, 유성과 별지기를 함께 가속하며 배율을 올린다. 한 샷은 둘 다 낼 수 있고, 별자리가 뜬 샷도 각성 공격을 잃지 않는다(현현이 끝난 뒤 정산이 이어진다). 윤슬만 정산 공격이 없고, 각성 자체가 회전 칼날과 보스·별지기 관통으로 나온다. 전체 로스터는 샛별·미리내·별하·살별·윤슬·달무리·모루·그믐의 8명이고, 시작 로스터는 샛별·미리내·윤슬이다.

**입력은 전투 공통 규칙이다(2026-08-21 현행화).** 유성이 멈췄을 때는 **노드 조준**이다 — 좌클릭으로 별지기·별빛 노드를 셋 이상 찍고(다시 찍으면 무르기, 우클릭은 전부 무르기, 빈 곳 좌클릭은 «반대편» 토글), `Space`로 발사한다. **`Space`는 발사이지 공명이 아니다.** 노드가 셋에 못 미치는 판(1-1 수업의 첫 단계처럼 별지기가 서 있지 않은 판)에서만 예전 좌클릭 드래그 발사로 떨어진다. 이동 중에는 발사당 합산 한 번만 좌클릭으로 진행 방향의 왼쪽, 우클릭으로 오른쪽에 운동량을 더해 궤도를 꺾을 수 있다(`ball.steerUsed` 1회권, 발사를 대신하지 않는다).

**공명은 자동이다(`AUTO_PARRY`).** 별지기와 부딪히면 타이밍 입력 없이 공명이 일어나 그 접점이 별빛 노드가 되고, 추가 운동량·배율과 접촉 능력(별하의 분열, 살별의 중계)을 함께 만든다. 평상시 충돌은 탄성 반동만 남으며, 그 반동에 밀려 굴러가면 각성은 공명과 무관하게 일어난다. 예전의 「`Space` 대기 창 → 접점 잔광 → 승격」과 그 실패 벌칙(별빛 소거·긴 재사용 대기)은 자동 공명이 들어오며 **없어졌다** — 되돌리려면 `AUTO_PARRY`를 false로 두면 그 경로가 그대로 돌아온다. 달무리의 새 충돌 발동 조건과 수치 밸런스는 아직 미정이다.

성능 기준선은 최대 3개의 90Hz 물리 서브스텝, 렌더 프레임당 1회 지속 이펙트 갱신, 60Hz 궤적 샘플링이다. 전장 바닥·균열·장식·프레임은 스테이지별 정적 캔버스 한 장으로 캐시하고, HUD DOM은 값이 변할 때만 갱신한다. 짧은 이펙트 배열은 제자리에서 정리하고, 50종 SFX의 실제 Audio 객체는 해당 소리가 처음 재생될 때만 만든다. 이펙트가 몰리면 `game-feedback.js`의 VFX 예산이 오래된 장식 레이어 수만 줄이며 타격 판정·팝업·최신 피드백은 유지한다. 이 기준을 깨는 새 파티클 배열이나 서브스텝 내부 DOM 갱신을 추가하지 않는다. **실측 기준선(2026-08-15 재측정, 캔버스 백버퍼 720×900)**: 게임플레이 갱신 2µs, `draw()` 약 224µs, `updateFeedback` 3µs — 프레임 예산 16.7ms의 약 1.3%다. 캔버스는 창 해상도가 아니라 720×900 고정이므로 `draw()` 비용은 픽셀 수가 아니라 **드로우 콜 수**가 지배한다. 프레임당 `fillRect` 632회 중 **540회가 `stepRing` 세 개**이고, 그것이 `draw()`의 약 67%다. 이 오버샘플링은 의도적이다 — 세 링 중 둘이 알파(`0x44`·`0x33`)라 같은 칸을 겹쳐 칠하는 것이 그 링을 진하게 만든다. 각도 간격을 넓히면 다른 격자 칸을 칠하고, 구워서 blit하면 링이 통째로 어긋나며(칠해진 픽셀의 78~134% 상이), 한 패스로 묶으면 겹침이 사라진다. 전부 실측했고 전부 외형을 바꾼다. **1.3%를 아끼자고 화풍을 바꾸지 않는다.** 현재 병목은 물리·판정이 아니므로 성능을 이유로 전투 로직을 미세 최적화하지 않는다.

**프레임 제보를 받으면 브라우저의 그래픽 가속부터 확인한다(2026-08-18).** 하루를 들여 코드에서 원인을 찾다가, 여섯 번의 수정이 전부 실패한 뒤에야 확인한 사실이다 — 게임을 돌리던 크롬에서 **그래픽 가속이 꺼져 있었고 소프트웨어 래스터로 그리고 있었다.** `chrome://settings/system`의 「사용 가능한 경우 그래픽 가속 사용」이고, `chrome://gpu`의 Canvas·Compositing이 `Hardware accelerated`인지로 확인한다.

증상은 코드 문제와 구별된다. **JS는 전부 싸게 나오는데 프레임만 길다** — `draw()` 0.5~1.0ms, CPU 70% 유휴, 레이아웃이 초당 10.4ms(1%), 5초 동안 JS의 기하 읽기 53회. 그런데 값을 움직이는 것은 셋뿐이고 전부 「래스터할 픽셀 양」이다: `shadowBlur`(0으로 강제하면 정산 프레임 p95 12.5→4.3ms), 여백 하늘 소품, 그리고 **창 크기 — 제곱으로 커진다**(1280에서 괜찮은 것이 1920에서 16.7ms를 넘긴다). 이 저장소는 같은 성질을 이미 적어 두었다 — 흐림은 GPU 캔버스에서 싸고 CPU 래스터에서 잔혹하다(3연쇄 46.34ms→7.77ms).

그래서 **위 실측 기준선은 전부 가속이 켜진 인스턴스의 값이다.** 소프트웨어 래스터에서는 같은 코드가 전혀 다른 프레임을 낸다. 계기는 `prototypes/js/game-perfwatch.js`에 있다(F9로 켜면 렌더러 이름과 판정이 화면에 뜬다). 설정의 **배경 소품 끄기**는 이 상황에서 실제로 효과가 있으므로 남긴다 — 가속을 켤 수 없는 기기의 탈출구다.

타격 연출도 성능 기준에 포함한다. 공명은 짧은 각성 링과 접점 피드백만 쓴다. 샷 종료의 각성 정산은 별지기 한 명씩 차례로 초점을 잡되(0.52초 뒤 시작, 1.62초 간격), 별자리가 뜬 샷에서는 현현이 끝난 1.36초 뒤부터 줄을 서 둘이 겹치지 않게 한다. 별자리 현현 피해는 전체 HUD 대신 보스 체력만 갱신한다.

일반 보스 체력은 공통 260이 아니라 각 스테이지의 `bossHp`다. 1-1의 120부터 7-7의 380까지 오르며, 월드 안에서는 약 10~15%만 상승한다. 일곱 별자리 뒤에 붙는 최종 스테이지 **8-1 `관측되지 않은 점`만 1600**으로 크게 벗어난다(7-7의 4.2배). 몸체도 즉시 피해를 받으며, 이동하는 약점은 더 큰 보상을 주는 목표다. 별지기를 한 번도 건드리지 않은 유성의 보스 직격은 0.6배이고, 그 샷의 첫 충돌이 보스면 추가 0.5배로 낮아진다. 별지기 접촉 뒤의 직격과 패링·각성·별자리·유닛 능력에는 이 감쇠가 없다. 전투를 끊는 방벽/코어 페이즈는 없다. `무한 훈련장`은 보스 체력 무한과 유성 자동 보충으로 물리와 배율을 확인하는 QA 전장이다. 허브 지도 아래 `무한 훈련장` 버튼으로 들어가며, 캠페인 순서 밖이라 지도 노드로는 닿지 않는다. 파티가 4명인 곳은 여기와 8-1 둘뿐이고, 훈련장은 거상이 판 정중앙(360, 450)에 선다.

모든 전투에는 별자리 도형 연출이 켜진다. **별지기와 부딪히면 자동으로 공명이 일어나** 접점에 **별빛 노드**가 남는다(2026-08-19, `AUTO_PARRY`). 유닛과 유성은 계속 물리적으로 움직인다. 3점 양자리, 4점 화살자리·까마귀자리, 5점 카시오페이아·백조자리·오망성, 6점 오리온자리, 7점 북두칠성 중 점 개수가 정확히 맞는 표에서 가장 가까운 도형으로 발동한다. **8점 이상은 표가 없어 이름 없는 도형이 되고 포위 피해만 들어간다.** 실루엣 7종은 모두 `assets/library/constellations/`에 있다 — 6·7점은 [FIGURE_ART_SPEC_6_7.md](FIGURE_ART_SPEC_6_7.md) 기준으로 반입 완료이며, `scripts/generate_constellation_art_6_7.mjs`로 재생성한다. 접점 선은 뼈대 자리로 교정되고 이름표를 보여 준다. **고유 능력은 8종 전부 갈라져 나왔다** — 양자리 포위 피해, 화살자리 관통(`axis: [3, 0]` 자루 끝→촉), 까마귀자리 약점 표식, 카시오페이아 껍질 파괴, 백조자리 비행, 오망성 전원 각성, 오리온자리 삼연격, 북두칠성 유성 +1 · 항로 · 전원 각성이다. 별자리는 각성 정산을 대신하지 않고 그 위에 얹힌다. 샛별의 검기는 사거리 밖이어도 도달하되 가까울수록 강하다. 캐스트 시네마틱은 `prototypes/js/game-figure-cinematics.js`가 그리며 판정·피해에는 손대지 않는다.

**별빛 경제와 별빛 조준 (2026-08-19 실험).** 세 상수로 켜고 끈다 — `AUTO_PARRY`·`NODE_ECONOMY`(`game-figure-recognition.js`), `AIM_MODE`·`WALL_STARS`(`game-combat.js`). 셋 다 끄면 이전 전투가 그대로 돌아온다.

- **공명이 자동이다.** Space 타이밍 시험을 없앴다. 근거는 실측이다 — 한 판이 주는 공명 기회가 중앙값 15회인데 별자리에 필요한 노드는 3개고, 연속 접점 간격의 47%가 무장창(0.4초) 안에 들어와 한 번 누르면 다음 것까지 덮였다. 타이밍 시험이 아니라 「누르는 것을 잊었는가」 시험이었다. 가속(`resolveMeteorParryContact`)은 그대로 두었다 — 공명을 아예 없애면 피해 용량이 482→297로 38% 사라지는데 그 대부분이 노드가 아니라 이 가속이다.
- **별빛이 자원이다.** 샷이 끝나도 별자리가 자동으로 터지지 않는다. 모인 별빛이 판에 남아 두 곳에서 경쟁한다 — 조준(고르면 그것으로 쏜다)과 별자리(고르지 않은 나머지가 태워진다). 한 번의 선택이 두 결과를 낸다.
- **조준은 노드를 찍어서 한다 — 배치된 별지기 셋도 노드다(2026-08-20, `AIM_STAR.minPick`·`unitNodes`).** 좌클릭으로 별지기·별빛을 **셋 이상** 고르고(다시 찍으면 무름, 상한 없음), 우클릭으로 전부 무르고, `Space`로 쏜다. 방향은 유성에서 고른 노드들의 **무게중심**으로, 세기는 그 가운데에서 노드까지의 **평균 거리**다 — 넓게 벌릴수록 세다. 하한을 3으로 올린 이유: 1픽을 열어 두면 «유성→그 별빛» 확정 레이가 한 클릭 거리에 있고, 약점 별빛이 보스 링에 깔려 보스행 저격이 공짜였다(위력 페널티는 방향만 필요한 순간에 아무것도 물지 않는다 — 최저 위력도 주행 약 2000px). 셋부터는 방향이 언제나 «만든 점»이다. 별지기가 노드라서 하한이 성립한다 — 파티 3명이 바닥 공급을 보장하고, 별지기 위치가 매 샷 물리로 재배치되므로 이번 샷의 자리 잡기가 다음 샷의 조준 메뉴가 된다. 별지기 노드는 별자리로 타지 않는다(재료는 안 찍은 별빛만). 첫 샷은 파티 배치가 정하는 한 가지 조합뿐이라 당구의 브레이크 샷처럼 스테이지가 오프닝을 정한다. 노드가 셋 미만인 판(수업·온보딩, `nodeEconomyOn()` 꺼짐)만 드래그로 떨어진다.
- **약점을 때리면 보스 둘레에 별빛 둘이 떨어진다.** 황금각으로 흩뿌리되 난수를 쓰지 않아 재현된다. 별자리는 둘러싼 것을 때리므로 보스 둘레의 별빛이 곧 다음 별자리의 자리가 된다.
- **온보딩에서는 실험이 꺼진다.** 루나의 수업이 「Space로 공명」과 「샷이 끝나면 별자리가 열린다」를 그대로 가르치고 E2E도 그 흐름을 따른다. 전투가 확정되면 수업을 다시 짠다.

미결: `WALL_STARS`가 꺼져 있어 별빛 풀이 얕고, 별지기 노드가 조준의 바닥은 보장하지만 **별자리 재료**(별빛)의 공급은 여전히 약점 별빛뿐이다. 공급을 어디서 더 채울지가 다음 결정이다. 근거 수치는 [BOT_REPORT.md](BOT_REPORT.md) 0-11·0-12절과 `scripts/probe-aim-nodes.mjs`에 있다(별지기 3+별빛 4 판에서 조합 15→99, 방향 칸 3~4→4~5, 위력 폭 28~52→36~100%).
**최종 스테이지 8-1 `관측되지 않은 점` (2026-08-14).** 일곱 별자리 34스테이지 뒤에 여덟 번째 월드 `outside`가 스테이지 하나로 붙는다. 별자리가 아니라 성도 밖의 점 하나라 `shape`가 비어 있고, 지도·전장 아스트롤라베는 이을 선 없이 그린다. 기존 순차 해금(`progress.clears`)이 그대로 7-7 클리어를 조건으로 만들므로 저장 형식은 바뀌지 않았다. 보스는 1600 HP·4인 파티이며, 체력 70/40/15%에서 **형태만** 4단으로 바뀐다 — `gimmicks.phases.effect`가 `runStagePhase`가 모르는 `form`이라 전투 판정에 아무 효과도 주지 않고 다리 손실·기울기·동공 확장으로만 진행도를 보여 준다. 이 개체는 타이틀 인트로에서 관측창 밖을 지나가는 것과 **같은 몸**이고, 여백과 전장을 `prototypes/boss-art.js`가 절차적 픽셀로 함께 그려 래스터 반입이 없다. 이름은 끝까지 주지 않고 좌표(`관측되지 않은 점`, `∅`)만 쓴다. 6절의 별자리 고점 배경 반응은 **아직 미구현**이다. 클리어 엔딩 화면은 2026-08-15에 구현했다 — `win()`이 최종 스테이지에서 일반 승리 카드 대신 전용 엔딩을 그리고(`endingCard`), 이야기 훅이 그 위를 덮지 않는다. 스펙의 금지선을 지켜 이름은 끝까지 주지 않고 좌표 `∅`만 남기며, 살구빛은 되찾은 별 수에만 쓰고 존재 쪽은 청록-잿빛으로 둔다. 완료는 `stella-ball.campaign-clear.v1`에 남아 재관측 시 문구가 바뀐다. 상세는 [OUTER_OBSERVER_INTRO_SPEC.md](OUTER_OBSERVER_INTRO_SPEC.md) 11절.

편성 자리 이름은 하드코딩이 아니라 그 자리와 거상 사이의 실제 거리에서 나온다(`slotRole()`, 문턱 300·390). 캠페인 자리는 204~417 범위에 흩어져 있다.

기믹 런타임은 스테이지 정의의 `gimmicks` 설정을 `setupStageGimmicks()`가 런타임 오브젝트로 만든다. 지원 종류는 공명 범퍼, 반사 벽, 가속 발판, 공허 잔재, **흐린 발판**, **굳은 껍질**, **도는 방벽**, **페이즈 전환**이다. 양자리의 `1-2`·`1-3`은 첫 성공 패링 때 안내별 둘을 더하는 `guideStarCharges: 1`을 사용한다. 안내별은 충돌·물리 판정이 없고, 해당 샷이 3점 이상 별자리로 정산될 때만 소진된다. 나머지 32개 캠페인과 무한 훈련장은 기믹을 비워 둔 HP·시작 배치·유성 경로 기준선이다.

현재 밸런스는 **미확정**이다. 별지기 충돌의 최소 운동량, 별자리 배율, 각성 공격이 한 발 안에서 중첩되어 원샷 클리어 또는 과도한 체공이 생길 수 있다. 이전 4발·8인·가로 반사 근사 모델의 `prism-sim.mjs`와 옛 보고서는 역사 기록이다.

**난이도 계기는 2026-08-15에 교체됐다.** 무기믹 기준선(`sweepPlainArena`)은 지원 사격 전달이 고쳐지며 포화돼(`contact` 91.7% / `chain` 100%) 더 이상 난이도를 구분하지 못한다 — 전투 규칙 회귀 감시용으로만 쓰고, 리포트의 `summary.plain.saturated`가 이를 자동으로 표시한다. 난이도는 `sweepCampaign`으로 읽는다: 35개 실제 스테이지를 진짜 체력·배치·기믹 그대로 돌리고, **시드가 아니라 조준 오차**(σ 0 / 0.04 / 0.10 / 0.22 rad)를 축으로 쓴다. 시드는 런타임에서 크리티컬 확률에만 닿아 35스테이지 중 33개가 시드 전체에서 같은 결과를 냈다. 1,680건에서 `chain`은 85.7 → 79.5 → 73.3 → 61.9%로 단조 감소한다. 읽을 때의 한계 둘은 [BOT_REPORT.md](BOT_REPORT.md) 0-3절에 있다 — `precise`(σ=0)는 표본이 아니라 결정론적 1회 프로브이고, `contact`는 비단조라 곡선은 `chain`으로 읽는다.

**직격 우회 경로 조치 (2026-08-13)**: 이전에는 발사석과 보스가 같은 x축이라 첫 충돌 보스 왕복만으로 캠페인 11개 중 10개를 1샷 클리어할 수 있었다. 이제 별지기 미경유 직격은 0.6배, 그 샷 첫 충돌이 보스면 합산 0.3배로 낮춘다. 기믹 없는 96건에서 260 HP 직접 경로는 전부 실패하고 접점 패링 경로는 전부 클리어한다. 실제 12개 캠페인의 재측정은 아직 남아 있으므로, 완전 해결로 표현하지 않는다. 상세는 [BOT_REPORT.md](BOT_REPORT.md) 0-1절과 1절을 함께 본다.

다음 우선순위:

1. 봇에 **별자리를 반복 발동시키는 정책**을 넣는다. 지금 `chain`은 한 번 이어 붙이는 데서 끝난다. 8-1은 모든 조준 등급에서 0%지만 불가능해서가 아니다 — 80런 분포에서 최고 1328/1600(83%), 상위 10 평균 943, 중앙값 365다. 좋은 판은 이미 8할을 깎으므로 남은 것은 편차를 줄이는 정책이다.
2. 조준 사다리 곡선으로 스테이지별 체력을 조정한다. 현재 전 등급 전승은 `1-3`·`2-1` 둘, 최난은 `7-1`(47.9%)·`5-5`(50%)·`4-3`(54.2%)이며 체력 순서와 난이도 순서가 어긋난다.
3. `contact`의 비단조(82.9 → 68.6 → 80 → 77.6)를 설명한다. 케이스별 접점 수·이동 거리를 리포트에 남겨야 원인을 가릴 수 있다.
4. 모든 전투의 접점 기억 패링과 발사당 1회 궤도 전환을 실기 검증한다. 혼전에서 충돌 직후 `Space · 공명` 잔광이 읽히는지, 선입력·순간입력·늦은입력이 모두 한 번의 공명으로 끝나는지, 좌·우 전환이 의도한 경로 선택으로 읽히는지, 실패 비용이 납득되는지를 정한다.
5. macOS에서 메인 CTA·온보딩 입력 잠금·1-2 범퍼·별자리 연출·SFX를 실제 브라우저로 확인한다. 일반 전투와 훈련장에서 비공명 탄성 충돌만으로도 각성이 일어나는지, 샷 종료의 3~~7점 발동, 0~~2점 소멸, 실패 후 재사용 대기시간, 별자리가 뜬 샷에서 현현 뒤에 이어지는 각성 정산의 순서를 함께 확인한다.
6. 별자리 8종의 고유 능력·구분 SFX·좌우 반전 지원을 설계한다
7. `HIVE_SETUP.md`의 실제 AppID·데이터 스토어·리더보드 왕복을 완료한 뒤 일일 기록 UI를 열기

## 아트 에셋 기준

현재 아트 기준은 **Tiny Swords 계열 픽셀 캐릭터 + 어두운 유적 배경 + 고대비 프리즘 이펙트**다. 기능 작업 중 발견한 새 디자인·에셋·애니메이션·SFX 필요사항은 먼저 `ASSET_BACKLOG.md`에 후보로 기록하고, 사람의 일괄 아트 작업 승인 뒤에만 실제 파일을 만든다. 실제 반입 목록과 프레임 규격은 `ASSET_PLAN.md`, `assets/ASSET_MANIFEST.json`, 향후 사용 후보는 `assets/ASSET_LIBRARY.md`, 출처는 `assets/ATTRIBUTION.md`에서 관리한다.

재사용 가능한 프리즘 에셋 패키지(원본 11개 + 4프레임 애니메이션 시트)는 `/Users/8rulerstar/Assets/Prism-Breakers-Pixel-Asset-Pack/`에 별도 보관한다.

- 별지기: 샛별, 미리내, 별하, 살별, 윤슬, 달무리, 모루, 그믐 8명. 보스: 공허 거상.
- 유성·별지기·보스 약점은 `assets/original/`의 Prism Breakers 전용 픽셀 SVG다.
- 전장 바닥은 `game-arena-carve.js`가 스테이지마다 한 장씩 캐시하는 절차 생성 Observatory Ground 레이어다. 새 지형 래스터를 반입하지 않았으며, 바닥·벽·별자리 각인은 표현 전용이다.
- 서로 다른 픽셀 팩을 무분별하게 섞지 않는다. Retro Lines와 Tiny RPG Soldier & Orc는 현재 화면에 사용하지 않는다.
- Canvas 렌더링 때 반드시 `imageSmoothingEnabled = false`를 쓴다.

## Codex 협업 기록 — 반드시 유지

공식 Track 1은 Codex 활용 과정 설명을 선택 가산점으로 받고, 어디에 사용했는지·무엇을 구현했는지·무슨 문제를 해결했는지·사람이 무엇을 결정했는지 설명하도록 한다.

- 제출용 원본: `CODEX_COLLABORATION.md`
- 매일 기록: `DEVLOG.md`
- 구현 후 자동 검증: `bot/run-bot.mjs`와 `bot/latest-report.json`
- 개발 이력 및 검증 운영: `EVIDENCE_PROTOCOL.md` — `main` push → GitHub Actions artifact → GitHub Pages 배포 → DEVLOG의 commit SHA 연결
- Codex 에셋 제작 근거: `assets/ASSET_GENERATION_LOG.md`
- 최신 보고/세션 인수인계: `PROGRESS_REPORT.md`

과정의 핵심 사실: Codex는 다수의 프로토타입, 웹 물리/UI, 향후 자동 테스트를 빠르게 만들고 비교하는 역할이다. 사람은 죽음 누적 구조와 직관성이 낮은 보드형 시안을 폐기하고, Stella Ball의 코어·범위·재미 기준을 결정했다.

개발 이력은 대화 전문이 아니라 작은 커밋, 당일 push, Actions 검증 리포트, Pages 배포, DEVLOG의 판단 기록으로 남긴다. 공개 `main`에서는 amend/rebase/force-push를 피한다.

첫 공개 기준선은 [`76a8a3d`](https://github.com/8rulerstar/prism-breakers/commit/76a8a3da7f6c9f80d8edfb944e8f15804048c5fe)이며, 첫 Actions 검증과 Pages 배포가 2026-08-07에 성공했다. 다음 세션도 이 개발 이력을 작은 작업 단위로 이어 간다.

## 폐기·보존 대상

- 폐기된 Echo Run의 `game.js`, `style.css`와 과거 탐색용 프로토타입은 2026-08-11 정리했다. 루트 `index.html`은 Stella Ball의 공식 진입점이며, `RUN_STELLA_BALL.command`와 `PLAY_WINDOWS.cmd`가 설치 없는 운영체제별 실행 진입점이다.
- `prototypes/`의 이전 시안: 탐색·Codex 협업 기록으로 보존한다. 현재 메인으로 홍보하지 않는다.
- 글로벌 죽음 누적 / 플레이어 데이터로 난이도를 바꾸는 구조: 다시 채택하지 않는다. 대규모 난독화·악용·하향 나선 문제가 있다.
