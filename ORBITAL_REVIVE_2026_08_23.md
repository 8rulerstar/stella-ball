# 도는 방벽 부활 결정 요청 — hp 0으로 되살아나 «1대 컷»이 된 기믹

2026-08-23 전수 조사(자동 발굴 + 항목별 반박 검증)에서 확정한 버그와,
그 수정이 밸런스를 움직이므로 코드로 밀지 않고 여기 적는 결정 재료다.
형식은 INGAME_FX_REQUEST 관례를 따른다 — 실측 근거를 함께 싣는다.

## 1. 증상 — 실측

도는 방벽(`orbitals`)은 죽으면 `down = 1.4`를 받고 1.4초 뒤 궤도로
복귀하는데, **hp를 복구하는 줄이 없다.** `orbit.hp`의 쓰기는 저장소
전체에 두 곳뿐이다: 생성(`game-data.js`의 `hp, maxHp: hp`)과 피격
감산(`game-combat-physics.js:363`). 복구는 0곳.

런타임 재현(실제 스테이지 데이터·실제 보스 체력, 6-3 방벽 hp 60, 시드 1):

| 프레임 | 사건                                                                                |
| ------ | ----------------------------------------------------------------------------------- |
| 539    | 「방벽 -6」 → hp 6→0, down=1.4, 격파 토스트, addDown 효과음                         |
| 624    | down 만료 → 방벽이 궤도로 복귀, **hp 0/60**                                         |
| 767    | 다음 접촉: `hpBeforeHit: 0` → 죽음 분기 재진입, **두 번째** 격파 토스트·효과음·파편 |

같은 모양이 6-1(hp 54)에서도 재현된다(1033프레임 13→0, 1148프레임 0→0).
결과: 54~64로 설계된 방벽이 **첫 격파 이후에는 무엇에 닿아도 1대 컷**이
되고, 격파 연출이 반복 재생되며, 도트 리스킨의 체력 눈금 6칸은
`Math.ceil((0/60)*6) = 0`으로 내내 꺼져 있으면서 방벽은 유성을 막는다.

관련 기록 — [STAGE_GIMMICKS_2026_08_22.md](STAGE_GIMMICKS_2026_08_22.md)
§8-6이 이 결함을 이미 적어 두었다(「되살릴 때 hp를 복구하지 않는다」).
그때는 방벽 없는 제안이었지만, 지금은 `STAGE_GIMMICK_TRIAL`이 6-1~6-6·
7-6에 방벽을 실제로 깔아 두어 **살아 있는 버그**가 됐다.

## 2. 왜 코드로 밀지 않았나 — 수정의 밸런스 영향 실측

수정본(양쪽 down 루프에서 만료 시 `hp = maxHp` 복구, 공용 헬퍼)을
메모리에서 적용해 리포트와 같은 축(시드 6 × 정책 2 × 조준 4)으로 방벽
스테이지 7판 전부를 재실행했다. **336케이스 중 8케이스가 움직였고,
방향이 양쪽이다:**

| 케이스                  | 변화                        |
| ----------------------- | --------------------------- |
| 6-3 contact/loose/s101  | 실패→**클리어** (잔여 13→0) |
| 6-3 chain/loose/s11     | 실패→**클리어** (111→0)     |
| 6-4 chain/wild/s83      | **클리어→실패** (0→24)      |
| 6-6 chain/wild/s47      | **클리어→실패** (0→48)      |
| 6-3 contact/steady/s137 | 잔여 36→101                 |
| 6-6 contact/steady/s83  | 잔여 121→171                |
| 7-6 chain/loose/s11     | 잔여 84→105                 |
| 7-6 chain/wild/s11      | 잔여 43→36                  |

읽는 법: 지금의 «1대 컷 방벽»은 사실상 공짜 장애물이라, 온전한 부활은
6-4·6-6을 더 어렵게 만든다. 6-6은 이미 실패 71%로 표에 올라 있는 판이다
(STAGE_GIMMICKS §2). 즉 이 수정은 **버그 수정이면서 동시에 6월드 난이도
상향**이고, 그 판단은 코드가 아니라 오너의 것이다.

## 3. 선택지

### A. 온전한 부활 (§8-6이 상정한 방향) — 권고

만료 시 `hp = maxHp` 복구. **반드시 함께 갈 것 둘:**

- **공용 헬퍼 하나로.** down 감쇠 루프가 둘이다 —
  `game-combat-physics.js`(비행 중)와 `game-core-render.js`(조준 중,
  방벽만 정지 중에도 도는 규칙 때문). 한쪽만 고치면 만료가 «언제
  일어났느냐»에 따라 복구 여부가 갈리는 시드 의존 동작이 되어 지금의
  일관된 버그보다 나쁘다.
- **부활 신호.** `adds`는 「공허 잔재 재생성」 토스트가 있는데 방벽은
  없다. 신호 없이 눈금만 소리 없이 차면 격파 직후의 플레이어에게 «새
  버그»로 읽힌다. 토스트든 눈금 점화 연출이든 하나는 필요하다.
- 필요하면 같은 변경에서 `STAGE_GIMMICK_TRIAL`의 방벽 hp(54~64)를
  낮춰 6-4·6-6의 하락을 상쇄할 수 있다 — 그 값도 결정 사항이다.

### B. 죽으면 끝 (부활 제거)

`down` 만료 시 복귀 대신 배열에서 제거. 기믹이 «한 번 뚫으면 열리는
자물쇠»가 된다. 지금보다 쉬워지는 방향 — 8케이스와는 다른 케이스들이
움직일 것이고, 별도 실측이 필요하다.

### C. 현상 유지 + 연출만 봉합

물리는 그대로 두고 hp 0 재격파 시 토스트·효과음·파편의 «반복»만
막는다. 리포트가 움직이지 않는 유일한 선택지지만, 「눈금 0인데 막는다」
는 모순은 남는다. 결정을 미룰 때의 임시 봉합으로만 의미가 있다.

## 4. 게이트 주의사항 (어느 선택이든)

- `verify-evidence.mjs`의 기믹 표식 4종은 건드리지 않는다 — 통과.
- `smoke-runtime.mjs`는 함수 이름 중복 정의를 금지한다 — 새 헬퍼
  이름은 prototypes/ 전체에서 유일해야 한다.
- A·B는 `bot/latest-report.json` 재생성이 «정당»하다(물리 변경).
  BOT_REPORT.md에 인용된 clearRate·byStage 수치도 함께 낡는다.

## 5. 같은 조사에서 나온, 오너 몫의 다른 재료

결정을 요구하지는 않지만 장부에 올려 둘 것들:

- **잠자는 에셋 실측치** (VISUAL_UPGRADE §8-2의 «292개»에 해당하는
  덩어리): `assets/characters/expanded/` 86파일 12.6MB(에셋 트리의
  44%, 20유닛 × 4상태 시트 — 런타임 참조 0), `assets/library/enemies/` +`anim/enemies/` 43파일 1.18MB(8적 로스터 완비 — 참조는 제작
  매니페스트뿐), 보스 팩 미도달 슬러그 둘(pentacle-core·erosion-warden,
  12파일 123KB — WORLD_BOSS가 7월드만 매핑).
- **LEGACY_GIMMICK_STAGES 12/13 항목(~297줄)은 도달 불능**이지만,
  저장소에 남은 **유일한 실전 범퍼 배치**가 그 안에 있다(§3-2 표석
  결정의 참조물). §3-2를 결정할 때 함께 소비하거나 지운다.
- AUTO_PARRY·NODE_ECONOMY 스위치의 꼬리 코드가 도달 불능인 것은
  **되돌림 스위치 관례상 의도된 것** — 정리 대상이 아니다.
