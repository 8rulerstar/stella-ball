# 별자리 실루엣 추가 요청 — 6점 오리온자리 · 7점 북두칠성

> 작성일: 2026-08-13
>
> [FIGURE_ART_SPEC.md](FIGURE_ART_SPEC.md)의 **후속 요청서**다. 화풍·규격·연출 시퀀스·검수 기준은 그 문서를 그대로 따르며, 여기서는 **새로 추가된 두 별자리의 뼈대와 개별 요청만** 적는다.
>
> 먼저 읽을 것: 기존 문서의 4절(화풍과 기술 제약), 5절 공통 항목, 6절 확정표.

## 0. 먼저 알아야 할 것 — 이 둘은 아직 화면에 안 나온다

**만들어도 당장은 볼 수 없다.** 그래도 지금 요청하는 이유는 별빛 노드 시스템이 병렬로 작업 중이고, 그것이 들어오는 즉시 붙일 수 있게 하기 위해서다.

현재 꼭짓점 소스는 `figureVertices()` — **각성한 별지기 + 유성**이다. 그래서 상한이 파티 인원으로 고정된다.

| | 별지기 | 최대 꼭짓점 | 도달 가능 계층 |
| --- | --- | --- | --- |
| 캠페인 12스테이지 | 3 | **4점** | 3·4점만 |
| 무한 훈련장 | 4 | **5점** | 3·4·5점 |

6점과 7점은 **어느 전장에서도 도달 불가능**하다. 실제로 6·7점 좌표를 정산에 태워 보면 점이 5개뿐이라 전부 5점 카시오페이아로 떨어진다.

이 천장은 **별빛 노드 시스템이 들어오면 사라진다.** 그 설계에서 꼭짓점은 별지기가 멈춘 자리가 아니라 **패링에 성공한 지점**이라, 유닛 수와 무관하게 쌓인다. 6·7점은 그 시스템의 최상위 보상으로 설계된 것이다.

**그래서 `share`(출현율)를 비워 뒀다.** 도달 가능해진 뒤 실측할 값이고, 지금 적으면 추측이 된다.

## 1. 왜 3·4점을 늘리지 않고 6·7점을 열었나

아트 요청 범위를 좁힌 근거이므로 남긴다. 후보 별자리들을 실제 인식기(`figureMatch`)에 넣어 쌍별 거리를 쟀다. **`reject = 0.19` 미만이면 인식기가 둘을 같은 것으로 본다.**

4점 후보 5종 — 10쌍 중 **6쌍이 구분 불가**였다.

| 쌍 | 거리 | |
| --- | --- | --- |
| 남십자성 ↔ 돌고래자리 | 0.077 | 구분 안 됨 |
| 페가수스사각 ↔ 돌고래자리 | 0.101 | 구분 안 됨 |
| 까마귀자리 ↔ 돌고래자리 | 0.130 | 구분 안 됨 |
| 까마귀자리 ↔ 페가수스사각 | 0.138 | 구분 안 됨 |
| 남십자성 ↔ 까마귀자리 | 0.161 | 구분 안 됨 |
| 남십자성 ↔ 페가수스사각 | 0.169 | 구분 안 됨 |
| 화살자리 ↔ 나머지 전부 | 0.436~0.572 | 구분됨 |

회전과 크기를 정규화하고 나면 **"사각형 비슷한 것"은 전부 한 덩어리**가 된다. 3점은 더 좁아서 각도 두 개로만 정의되고, 5점에서도 오망성 ↔ 마차부자리가 0.059로 겹쳤다.

**점이 늘수록 모양 공간이 넓어진다.** 그래서 옆이 아니라 위로 열었다. 황도 12궁은 이 이유로 채택하지 않았다 — 점 개수가 3~5에 거의 안 들어가고(전갈 9+, 궁수 8, 사자 9), 게자리·물고기자리처럼 형태가 흐릿한 것이 많다.

## 2. 만들 자산 — 2종

| 점 | 별자리 | 그릴 대상 | 도형 | 파일 |
| --- | --- | --- | --- | --- |
| 6 | 오리온자리 | 사냥꾼 | 어깨 2 + 띠 3 + 발 1, 모래시계 | `orion.png` |
| 7 | 북두칠성 | 국자 (생물 아님) | 그릇 4(닫힘) + 손잡이 3(열림) | `bigdipper.png` |

규격은 기존과 동일하다. **384 × 384**, 128 그리드를 ×3, 뼈대 원점이 정중앙, 뼈대 1단위 = 그리드 46칸. 이 두 값이 시트와 뼈대를 묶으므로 **어느 쪽도 바꾸면 정합이 깨진다**(`FIGURE_ART_SIZE`, `FIGURE_ART_UNIT`).

덮는 면적은 뼈대 볼록껍질로 환산한 추정치다. 실측이 아니라 기존 5점 평균을 19%로 놓고 비례한 값이므로 참고용이다.

| 별자리 | 추정 테이블 점유 |
| --- | --- |
| 오리온자리 | 약 11% |
| 북두칠성 | 약 8.6% |
| (참고) 오망성 | 약 34.8% |
| (참고) 카시오페이아 | 약 7.5% |

**둘 다 카시오페이아와 비슷하거나 조금 큰 정도다.** 점이 많다고 넓게 퍼지지 않는다 — 오리온은 세로로 길고 북두칠성은 가늘고 길다.

## 3. 뼈대 좌표 — 코드에 이미 들어간 값

두 별자리 모두 **J2000 적경·적위를 평면 투영**해 넣었다. 기존 5종과 같은 방식이다(`x = -(RA - RA₀)·cos δ₀`, `y = -(δ - δ₀)`, 최장 반지름으로 정규화). 실제 하늘의 배치이므로 **임의로 바꾸면 별자리가 아니게 된다.**

### 오리온자리 (6점)

```
[-0.539, -0.717]   베텔게우스 (왼쪽 어깨, 붉은 초거성)
[ 0.225, -0.610]   벨라트릭스 (오른쪽 어깨)
[-0.173,  0.233]   알니타크 (띠 왼쪽)
[-0.057,  0.158]   알닐람  (띠 가운데)
[ 0.050,  0.066]   민타카  (띠 오른쪽)
[ 0.494,  0.870]   리겔    (오른발, 청백 초거성)
```

간선: `어깨-어깨 → 오른어깨-띠우 → 띠우-띠중 → 띠중-띠좌 → 띠좌-왼어깨 → 띠좌-리겔`

**사이프(왼발)를 뺐다.** 정식 오리온은 7별이지만 7점은 북두칠성이 쓰고, 6점을 맞추면서 실루엣이 오리온으로 읽히려면 뺄 수 있는 별은 먼 쪽 발 하나뿐이다.

### 북두칠성 (7점)

```
[ 0.778, -0.445]   두베    (그릇 앞 위)
[ 0.797, -0.058]   메라크  (그릇 앞 아래)
[ 0.267,  0.136]   페크다  (그릇 뒤 아래)
[ 0.047, -0.105]   메그레즈 (그릇 뒤 위 · 손잡이 시작)
[-0.346, -0.027]   알리오트
[-0.651,  0.047]   미자르
[-0.892,  0.452]   알카이드 (손잡이 끝)
```

간선: 그릇 `두베-메라크-페크다-메그레즈-두베`(닫힘) + 손잡이 `메그레즈-알리오트-미자르-알카이드`(열림)

## 4. 별자리별 개별 요청

### 오리온자리 (6점)

- **띠 세 별이 이 그림의 중심이다.** 밤하늘에서 오리온을 찾는 방법이 띠부터 찾는 것이고, 뼈대에서도 세 점이 몰려 있어 시선이 그리로 간다. 실루엣의 허리가 정확히 그 세 점 위에 놓여야 한다.
- 어깨 두 별이 그림 위쪽 폭을 정하고 리겔이 아래 한쪽 끝이다. **좌우 비대칭**이다 — 발이 한쪽에만 있다. 사이프 자리를 실루엣으로 암시할지는 **미정**이며, 기존 문서 6절 3번대로 **좌우 반전은 미구현**이라 비대칭이 그대로 노출된다는 점을 감안해 주기 바란다.
- 곤봉·방패·활 같은 부속은 뼈대에 대응하는 별이 없다. **넣는다면 순수 장식**이므로, 도형에서 벗어나지 않는 선까지만.
- 6종 중 유일한 인간 전신이다. 카시오페이아가 "인물이라 픽셀로 어렵다"는 이유로 왕좌만 그리기로 확정됐는데(6절 5번), **오리온에도 같은 판단이 필요하다.** 전신이 어려우면 **띠와 어깨선만 강조하는 안**도 검토 가능하다.

### 북두칠성 (7점)

- **생물이 아니다.** 화살자리와 같은 성격이라 실루엣이 담백해도 된다. 그릇 사다리꼴과 손잡이 꺾임이 이미 국자 모양이므로, **손잡이 끝 곡률과 그릇 깊이만 보강**하는 정도로 충분할 듯하다.
- 실제 별자리 그림에서는 큰곰자리의 **엉덩이와 꼬리**에 해당한다. 곰 전체를 그리는 안도 가능하지만, 뼈대 7점이 곰의 일부일 뿐이라 그림이 도형 밖으로 크게 나간다. **국자 쪽을 권한다.**
- **이 게임에서 이름이 이미 쓰이고 있다.** 월드 1이 `북두칠성`이고 12스테이지 중 7개가 이 별들의 이름이다(두베·메라크·페크다·메그레즈·알리오트·미자르·알카이드). 허브 지도에 같은 배치가 이미 그려진다. **지도의 별 배치와 실루엣이 같은 형태로 읽히면** 연결이 생긴다 — 지도를 참고 자료로 쓸 수 있다.
- 7점이라 가장 도달하기 어려운 별자리가 될 가능성이 높다. **격은 오망성 바로 아래**로 잡아 주기 바란다.

## 5. 확정된 값 (기존 문서 6절에서 승계)

새로 정할 필요 없이 그대로 적용되는 것들이다.

| 항목 | 값 |
| --- | --- |
| 실루엣 투명도 | **0.30** (0.13은 실전 전장에서 안 보였음) |
| 교정 시간·이징 | 0.8초, ease-in-out |
| 발동 타이밍 | 현현 + 0.3초 |
| 좌우 반전 | **미구현** |
| 별자리 선 색 | `#9adfc9` (오망성만 `#ffd2a0`) |
| 실루엣 위치 | 선 **아래**. 선과 꼭짓점이 항상 더 밝게 |

## 6. 검수 기준

기존 7절에 더해 두 가지를 추가한다.

- 오리온은 **띠 세 별 위에 허리가 놓였는지**로 판정한다. 그것이 어긋나면 다른 것이 맞아도 오리온으로 안 읽힌다.
- 북두칠성은 **허브 지도의 북두칠성 배치와 같은 형태로 읽히는지** 확인한다.
- 두 별자리가 기존 5종과 **하나의 세트로 보이되**, 7점(북두칠성)이 5점들보다 격이 한 단계 높아야 한다.

## 7. 미정 목록

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | 오리온 전신 vs 띠·어깨만 | **미정** — 카시오페이아가 왕좌만으로 확정된 전례가 있음 |
| 2 | 사이프(왼발) 암시 여부 | **미정** — 뼈대에 없는 별을 그림에만 넣을지 |
| 3 | 북두칠성 국자 vs 큰곰 전체 | 국자 **권장**, 최종 판단 필요 |
| 4 | 6·7점 출현율 | **측정 불가** — 별빛 노드 도입 후 실측 |
| 5 | 각 별자리 고유 능력 | **미정** — 기존 6종도 전부 포위 피해로 동일 |

## 8. 구현 현황 — 2026-08-13

**코드 반입 완료, 아트 대기.** `prototypes/js/game-figure.js`의 `FIGURE_SHAPES`에 6·7 계층이 추가됐다.

검증한 것:

- 8종 전부 자기 자신을 정확히 인식 (거리 0.000, 점수 1.00)
- 같은 계층 내 쌍은 전부 구분 — 최소가 화살자리 ↔ 까마귀자리 0.337로 `reject 0.19`에서 충분히 멀다
- 흐트러뜨린 7점 구름도 북두칠성으로 분류 (점수 0.53)
- 아트 파일이 없는 상태에서 정산·그리기 전 구간 예외 없음
- `art`는 **`null`로 두고 파일 경로를 주석에 적어 뒀다.** 없는 파일을 가리키면 매 로드마다 404가 콘솔에 남고, 그런 소음이 진짜 에러를 묻는다. 시트가 반입되면 `game-figure.js`에서 주석을 풀면 된다
- `npm run verify` / `npm run smoke` 통과

**아직 안 되는 것:** 0절대로 6·7점은 도달 불가다. 별빛 노드 시스템이 들어와야 실물을 볼 수 있다.
